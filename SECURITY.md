# 安全策略（Security Policy）

> 权威文档：D18（`DATA_SECURITY_PRIVACY_GOVERNANCE_RBAC`）、D21（`COMPLIANCE_AUTHORIZATION_AIGC_LABEL_CONTENT_REVIEW`）

---

## 1. 安全报告

如果您发现安全漏洞，请**不要**通过公开 Issue 报告。请通过以下方式私下联系：

- 仓库 Owner 私信
- GitHub Security Advisory（Settings → Security → Advisories）

我们将在 72 小时内确认收到报告，并在 7 天内给出初步评估。

## 2. 安全原则

本项目处理用户上传视频、账号画像、AI 生成内容、积分交易等敏感数据，必须遵循以下安全原则：

| 原则 | 说明 |
|------|------|
| 最小权限 | 每个角色/服务只获得完成任务所需的最小权限 |
| 数据隔离 | 用户数据严格隔离，不可跨用户访问 |
| 密钥管理 | 所有密钥通过环境变量或 Secret Manager 管理，不得硬编码 |
| 审计日志 | 所有敏感操作必须记录审计日志 |
| 传输加密 | 所有外部通信使用 TLS |
| 存储加密 | 敏感数据加密存储 |

## 3. 禁止行为

以下行为在本项目中被严格禁止：

| 禁止行为 | 说明 |
|----------|------|
| 硬编码密钥 | API Key、数据库密码、JWT Secret 等不得出现在代码中 |
| 提交 .env 文件 | 环境变量文件已在 .gitignore 中排除 |
| 越权访问 | 不得绕过 RBAC/ABAC 访问其他用户数据 |
| 裸露敏感字段 | API 响应不得包含密码哈希、内部 ID 等敏感信息 |
| 无审计操作 | 后台操作用户资产必须记录审计日志 |
| 文件上传漏洞 | 上传文件必须经过类型白名单和安全扫描 |

## 4. 已启用的 GitHub 安全功能

| 功能 | 状态 |
|------|------|
| Secret Scanning | 已启用 |
| Secret Push Protection | 已启用 |
| Dependabot Alerts | 已启用 |
| Dependabot Security Updates | 已启用 |
| Branch Protection | 已启用（main） |

## 5. 数据分级

按 D18 定义，本项目数据分为以下级别：

| 级别 | 示例 | 保护要求 |
|------|------|----------|
| L4 机密 | 支付凭证、密钥、用户密码哈希 | 加密存储、最小访问、审计 |
| L3 敏感 | 用户视频、画像、积分流水 | 权限隔离、加密传输、审计 |
| L2 内部 | 模型调用日志、任务状态、报告 | 权限控制、日志 |
| L1 公开 | 产品文档、公开 API 文档 | 无特殊保护 |

## 6. AIcoding Agent 安全约束

所有 AIcoding Agent 必须遵守：

| 约束 | 说明 |
|------|------|
| 不得读取/导出 Secrets | Agent 不得尝试获取 GitHub Secrets 或环境变量中的密钥值 |
| 不得提交测试密钥 | 测试中使用的密钥必须是假值或环境变量占位符 |
| 不得越权操作 | Agent 不得绕过分支保护或权限控制 |
| 不得泄露用户数据 | 日志、错误信息、测试数据中不得包含真实用户信息 |

---

*本文件由 Manus Agent 基于 D18/D21 自动生成。*
