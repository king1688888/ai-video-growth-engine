# 契约权威关系图（Contract Authority Map）

> 版本：V1.0  
> 更新时间：2026-05-26  
> 维护人：Manus Agent（AIcoding 总控）  
> 说明：本文件明确各领域的最终权威文档，当文档间出现冲突时，以本表定义的权威关系裁决。

---

## 1. 最高权威层级

```text
D00（项目根上下文 / 单一事实源）
 ├── 产品方向、版本边界、不做清单 → 最终仲裁
 └── 当任何文档与 D00 冲突时，以 D00 为准
```

---

## 2. 领域权威矩阵

| 领域 | 权威文档 | doc_alias | 权威范围 | 冲突裁决规则 |
|------|----------|-----------|----------|-------------|
| 产品方向与边界 | D00 | — | 定位、不做清单、版本边界 | D00 为最终仲裁 |
| 产品需求总入口 | D04 | — | 总需求、流程、功能、异常 | 与 D00 冲突以 D00 为准 |
| 模块边界与研发顺序 | D05 | — | M00-M14 模块定义、输入输出 | 与 D04 冲突以 D04 为准 |
| 技术架构 | D15 | — | 技术栈、服务边界、部署方案 | 与 D05 冲突以 D05 为准 |
| 数据库 / API 工程契约 | D16 | `DB_API_HARD_CONTRACT` | 表结构、字段、API、错误码、幂等 | **工程实现最终口径** |
| 异步任务 / 状态机 / 资产生命周期 | D17 | `ASYNC_QUEUE_STORAGE_ASSET_LIFECYCLE` | 状态机、队列、文件资产、重试、清理 | **状态机最终口径** |
| 安全 / 隐私 / 权限 | D18 | `DATA_SECURITY_PRIVACY_GOVERNANCE_RBAC` | 数据分级、RBAC/ABAC、审计、删除 | 与 D16 冲突以 D16 字段为准 |
| 商业化 / 计费 / 积分 | D19 | `COMMERCIAL_BILLING_CREDIT_ORDER_COST_SETTLEMENT` | 套餐、积分、订单、成本、毛利、退款 | **计费结算最终口径** |
| 测试 / 发布 / 监控门禁 | D20 | `TEST_ACCEPTANCE_RELEASE_MONITORING_ALERTING` | 测试分层、上线门禁、监控、告警、回滚 | **上线阻断最终标准** |
| 合规 / 授权 / AIGC / 审核 | D21 | `COMPLIANCE_AUTHORIZATION_AIGC_LABEL_CONTENT_REVIEW` | 授权、审核、标识、投诉删除、风险分级 | **合规底线最终口径**（更严格规则优先） |
| 运营后台 / 客服 / 补偿 | D22 | `OPS_SUPPORT_REFUND_MANUAL_REVIEW` | 后台、工单、补偿、人工复核 | 与 D19 退款冲突以 D19 为准 |
| AIcoding Agent 行为规范 | D23 | `AICODING_AGENT_EXECUTION_PROTOCOL` | Agent 规则、任务模板、交付格式 | **Agent 行为最终规范** |
| 最终验收 / 交接 / 上线决策 | D24 | `FINAL_ACCEPTANCE_HANDOFF_VERSION_GOVERNANCE` | 验收门禁、交接清单、上线决策 | **最终验收总控** |

---

## 3. 冲突裁决优先级链

当两个文档对同一事项有不同定义时，按以下优先级链裁决：

```text
D00（方向/边界）
  > D16（数据库/API 工程字段）
    > D17（状态机/队列/资产）
      > D19（计费/积分/结算）
        > D21（合规/授权，更严格规则优先）
          > D20（测试/发布门禁）
            > D23（Agent 行为规范）
              > D24（最终验收）
                > 其他文档
```

**特殊规则：** 当 D21 合规要求与任何其他文档冲突时，**以更严格的合规规则为准**，即使该规则来自较低优先级文档。

---

## 4. 关键权威声明（来自 D24）

D24 在其兼容性声明中明确定义了以下权威关系：

| 权威角色 | 权威文档 | 说明 |
|----------|----------|------|
| acceptance_authority | D24 | 最终验收由 D24 定义 |
| release_gate_authority | D20 | 发布门禁由 D20 定义 |
| db_api_authority | D16 | 数据库/API 由 D16 定义 |
| task_state_authority | D17 | 任务状态由 D17 定义 |
| billing_authority | D19 | 计费结算由 D19 定义 |
| compliance_authority | D21 | 合规底线由 D21 定义 |
| agent_execution_authority | D23 | Agent 行为由 D23 定义 |

---

## 5. AIcoding Agent 使用规则

1. **开始任务前**：先读 D00，再读对应领域权威文档。
2. **涉及数据库/API 变更**：必须以 D16 为准，不得自行定义字段。
3. **涉及任务状态变更**：必须以 D17 状态机为准。
4. **涉及积分/扣费**：必须以 D19 为准。
5. **涉及合规/风控**：必须以 D21 为准，采用更严格规则。
6. **涉及发布/上线**：必须通过 D20 门禁。
7. **交付格式**：必须遵循 D23 交付标准。
8. **最终验收**：必须满足 D24 门禁。

---

## 6. 编号冲突处理规则

| 规则 | 说明 |
|------|------|
| 不得硬编码文档编号 | 代码/数据库/任务模板中使用 `doc_alias` |
| 以文件自身声明为准 | 各文件 `编号兼容说明` 优先于 D00 旧地图 |
| 冲突时以 Owner 确认为准 | 若仍有歧义，提交 open_questions 等待 Owner 决策 |

---

*本文档由 Manus Agent 自动生成，作为 Phase 0 文档理解阶段的核心输出。*
