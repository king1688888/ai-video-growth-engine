# D09｜Creative IR Schema 与原创化重构规则文档

> 文档编号：D09  
> 文档名称：Creative IR Schema 与原创化重构规则文档  
> 文档版本：V1.0  
> 生成日期：2026-05-25  
> 根上下文：D00《AIcoding 研发总控文档 / 项目上下文总索引》  
> 前置文档：D00、D01、D02、D03、D04、D05、D06、D07、D08  
> 后置文档：D10 Prompt 体系、D11 模型路由、D12 AI 评测样本集、D13 质检评分与相似度风控、D15-D17 工程架构/数据库/API/任务队列、D18 数据安全、D19 测试发布监控、D20 商业化系统、D21 合规审核、D22 数据安全与权限、D23 运营后台与人工复核、D24 最终交接  
> 当前主线版本：MVP1「视频拆解 + 原创脚本生成」  
> 文档性质：AI 创意中间层 Schema + 原创化重构规则 + Prompt/工程/风控/商业化契约 + AIcoding 执行任务文档  
> 核心定位：本文件不是“改写文案技巧”，而是把 D07 Content DNA 中可学习的爆款结构转化为模型无关、可校验、可版本化、可风控、可计费、可评测的 Creative IR，并以原创化重构方式生成适合用户行业、人设、产品和素材的原创脚本、标题、封面建议与视频 Prompt。

---

## 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 文档编号 | D09 |
| 文档名称 | Creative IR Schema 与原创化重构规则文档 |
| 所属项目 | AI 短视频爆款增长引擎 |
| 当前研发主线 | MVP1：视频拆解 + 原创脚本生成 |
| 文档级别 | S+，AI 原创化核心资产文档 |
| 上级根文档 | D00《AIcoding 研发总控文档 / 项目上下文总索引》 |
| 直接前置依赖 | D00、D01、D02、D03、D04、D05、D06、D07、D08 |
| 直接后置依赖 | D10、D11、D12、D13、D15、D16、D17、D18、D19、D20、D21、D22、D23、D24 |
| 适用阶段 | MVP1 AI 原创化重构研发、Prompt 工程、输出包生成、相似度风控、积分计费、后台质检、内测上线验收 |
| 适用对象 | 创始人、产品负责人、AI 产品经理、技术产品经理、后端 Agent、AI/Prompt Agent、前端 Agent、测试 Agent、风控合规负责人、运营后台负责人 |
| 文档目标 | 定义 Creative IR 的 Schema、字段、生成流程、原创化重构规则、Prompt、API、数据库、任务队列、风控、计费、测试验收与 AIcoding 执行提示词 |
| 最高约束 | 与 D00 冲突时，以 D00 为准；与 D03 MVP 范围冲突时，按 D03 收敛；与 D07 Content DNA 字段冲突时，以 D07 已确认字段为输入口径；与 D13/D21 风控合规冲突时，以更严格规则为准；与 D16/D17 最终工程契约冲突时，以 D16/D17 为实现口径并回写 D09 |
| 输出形态 | Markdown 文档 + JSON Schema + 字段映射表 + 原创化规则 + Prompt 模板 + API 草案 + 数据库草案 + 状态机 + 测试验收清单 + AIcoding 执行提示词 |
| 维护责任人 | 项目 Owner / AI 产品总控 / AI 产品架构负责人 / 技术产品负责人 |
| 更新触发条件 | Content DNA Schema 变化、原创化规则变化、Prompt 版本变化、输出包变化、相似度阈值变化、模型供应商变化、积分规则变化、合规策略变化、Benchmark 失败、上线验收不通过 |
| 文档状态 | V1.0 初版可执行；后续随 D10-D24 深化后持续回写修订 |

---

## 2. 文档目标

### 2.1 本文档要解决的问题

D09 解决的是 MVP1 中最关键的“从爆款结构学习到原创内容生产”的工程化问题。系统不能从 Content DNA 直接生成脚本或视频模型 Prompt，因为直接生成会导致 Prompt 不稳定、模型切换成本高、相似度风控难插入、版本难回溯、计费难拆分、质量难评测、合规风险难审计。因此必须建立 Creative IR 作为模型无关的创意中间层。

D09 必须解决以下 12 个问题：

1. **Creative IR 是什么**：明确它是承接 Content DNA、用户画像、目标平台、原创化强度和风控约束的创意中间表示，不是最终脚本，也不是视频模型专用 Prompt。
2. **Creative IR 不是什么**：不是复刻指令、洗稿模板、仿脸仿声方案、未授权素材复用方案、规避平台审核方案。
3. **Content DNA 如何映射到 Creative IR**：定义可学习结构、不可复用表达、风险标签、用户画像、行业迁移、产品植入、目标平台的字段映射。
4. **原创化重构如何执行**：定义低强度结构参考、中强度行业改写、高强度原创重构、强原创裂变四档原创化策略，并规定 MVP1 默认使用中高强度原创化。
5. **Creative IR Schema 如何设计**：定义总 Schema、分镜 Schema、脚本意图 Schema、标题策略 Schema、封面策略 Schema、视频 Prompt 蓝图 Schema、风控约束 Schema、版本字段。
6. **AI Prompt 如何工程化**：定义 Creative IR 生成 Prompt、原创化重构 Prompt、IR 到脚本 Prompt、IR 到标题 Prompt、IR 到封面建议 Prompt、IR 到视频 Prompt、IR 质检 Prompt。
7. **工程如何落地**：定义数据库、API、任务队列、日志、权限、事件、幂等、版本管理、部署影响。
8. **商业化如何接入**：定义积分预估、冻结、扣费、失败返还、部分成功扣费、模型成本、后台统计、毛利分析。
9. **合规如何前置**：定义授权、AIGC 标识、风险分级、相似度控制、拦截策略、人工复核、投诉删除。
10. **如何测试和验收**：定义 Schema 合法率、字段完整率、原创化评分、相似度风险召回、输出可用率、Prompt 回归、E2E、计费幂等和后台验收。
11. **如何支持后续文档**：为 D10 Prompt、D11 模型路由、D12 Benchmark、D13 风控、D16 数据库/API、D17 任务队列、D20 商业化、D21 合规提供输入。
12. **AIcoding 如何直接执行**：输出可复制给 Manus / Codex / Claude Code / Hermes Agent 的研发任务拆分和执行提示词。

### 2.2 本文档不解决的问题

| 不在 D09 完整展开的内容 | 负责文档 | D09 只提供 |
|---|---|---|
| 视频上传、抽帧、ASR、OCR、镜头切分 | D08 | 只消费 D08 产生的证据包与 D07 Content DNA，不重新定义视频处理管线 |
| Content DNA 完整字段和拆解报告 | D07 | 只定义可进入 IR 的字段映射与校验要求 |
| Prompt Registry、版本平台、Prompt 测试全集 | D10 | 提供 D09 相关 Prompt 模板、变量、输出 Schema 与评测要求 |
| 模型路由、供应商适配、成本控制 | D11 | 提供 IR 到模型 Prompt 的模型无关结构和成本日志字段 |
| AI Benchmark 样本集全集 | D12 | 提供 Creative IR 的评测维度、样本类型和通过门槛 |
| 相似度风控与合规拦截最终细则 | D13/D21 | 提供 IR 阶段的风险输入、风险输出和拦截接入点 |
| 完整技术架构 | D15 | 提供 Creative IR 服务边界和依赖 |
| 最终数据库 Schema/API/任务队列 | D16/D17 | 提供草案，最终以 D16/D17 为准 |
| 商业化套餐、订单、支付、退款 | D20 | 提供 IR 任务扣费节点、成本字段和失败返还规则 |
| 数据安全、权限治理与隐私保护 | D18/D22 | 提供 Creative IR 数据分级、访问权限、删除审计要求 |
| 页面视觉设计和高保真 UI | D06 | 提供 Creative IR 结果页与后台页 ViewModel 字段 |
| 完整视频生成和成片拼接 | MVP3 / D11/D17 后续 | MVP1 只输出视频生成 Prompt，不直接生成成片 |

### 2.3 本文档的研发落点

D09 最终必须落到以下工程对象：

1. `CreativeIR` JSON Schema。
2. `CreativeIRSegment` / `StoryboardBeat` 分镜级 Schema。
3. `OriginalizationPlan` 原创化重构策略 Schema。
4. `CreativeRiskConstraint` 风控约束 Schema。
5. `DeliverableIntent` 输出意图 Schema。
6. `creative_irs`、`creative_ir_versions`、`creative_ir_generation_jobs`、`creative_ir_quality_checks`、`creative_ir_events` 数据表草案。
7. `POST /api/v1/tasks/{task_id}/creative-ir/generate` 等 API 草案。
8. `creative_ir.generate`、`creative_ir.validate`、`creative_ir.risk_check`、`deliverable.generate` 任务队列草案。
9. `P04_CREATIVE_IR_GENERATION`、`P05_ORIGINALIZATION_REWRITE`、`P06_IR_TO_SCRIPT`、`P07_IR_TO_TITLE`、`P08_IR_TO_COVER`、`P09_IR_TO_VIDEO_PROMPT`、`P10_IR_QUALITY_CHECK` Prompt 模板。
10. JSON Schema 校验、JSON repair、字段补全、低置信度降级、人工复核机制。
11. 前端报告页 `CreativeIRViewModel` 与 `DeliverablePackViewModel`。
12. 后台任务详情页 Creative IR 诊断区、风险区、模型成本区、人工复核区。
13. Prompt 回归测试与 Benchmark 验收规则。
14. AIcoding 研发任务拆分与交付标准。

---

## 3. 与 D00 及其他文档的关系

### 3.1 D00 对 D09 的硬约束

| D00 约束 | D09 落地方式 |
|---|---|
| 产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具 | Creative IR 只迁移底层结构、内容策略、节奏、用户心理触发和转化路径，不复用原视频素材、台词、人物、声音、配乐、Logo、封面和可识别表达 |
| 核心链路为对标内容输入 → 多模态拆解 → Content DNA → Creative IR → 原创输出 → 质检风控 → 数据回填 | D09 定义 Content DNA 与原创输出之间的模型无关中间层 |
| 当前优先 MVP1：视频拆解 + 原创脚本生成 | D09 的 P0 只覆盖单条视频生成原创脚本、标题、封面建议、视频 Prompt，不做完整视频生成与自动发布 |
| 不得自行扩大第一版范围 | 对标账号级 IR、批量模板库、视频片段生成、自动发布、Prompt 进化只做 P1/P2 字段预留 |
| 不得跳过风控、任务日志、成本记录、测试验收 | Creative IR 必须记录 `risk_constraints`、`source_similarity_guardrails`、`model_call_logs`、`quality_check_reports`、`credit_transactions` |
| 不得用 mock 冒充真实能力 | Mock Creative IR 只允许开发环境使用，必须写入 `is_mock=true`，页面和后台显著标记，生产默认关闭 |
| 每个模块必须可独立开发、独立测试、独立验收、独立交接 | D09 拆成 Schema、字段映射、生成服务、Prompt、API、队列、前端、后台、测试、交接任务 |

### 3.2 D07 对 D09 的输入

D07 定义 Content DNA 是视频拆解后的结构化内容资产。D09 必须消费 D07 的以下输入：

| Content DNA 输入 | D09 使用方式 |
|---|---|
| `content_type` | 决定 Creative IR 的内容形态，如口播、案例、避坑、清单、探店、教程 |
| `target_audience` | 映射为 IR 的目标受众与痛点表达 |
| `core_hook` | 抽象为钩子类型，不复制原句 |
| `narrative_structure` | 转为原创脚本结构，不复用原顺序的全部细节 |
| `emotional_curve` | 转为情绪节奏规划，如焦虑 → 解决 → 可信 → 行动 |
| `shot_dna` | 转为分镜意图和视觉节奏，不复制原画面 |
| `viral_factors` | 转为可迁移的增长因子，如反差、冲突、利益点、共鸣 |
| `conversion_path` | 转为安全 CTA，不承诺收益/疗效/保过 |
| `reusable_parts` | 作为可学习结构候选 |
| `non_replicable_parts` | 作为硬性禁用约束，进入 `risk_constraints` |
| `risk_flags` | 决定原创化强度、拦截、重写或人工复核 |
| `confidence_score` | 决定是否允许自动生成、降级生成或转人工复核 |

### 3.3 D08 对 D09 的输入

D08 提供多模态证据包和视频理解结果。D09 不直接读取原始视频内容做复制，而是通过 D07 已结构化的 Content DNA 以及 D08 的证据摘要进行可追溯重构。D09 可以在后台诊断中引用 D08 的 `frame_assets`、`transcript_segments`、`ocr_segments`、`shot_segments` 的证据 ID，但不得将原始画面、原字幕、原台词作为生成内容直接复用。

### 3.4 D09 对 D10 的输出

D09 输出 Prompt 体系必须管理的模板、变量和版本：

1. Creative IR 生成 Prompt。
2. 原创化重构 Prompt。
3. IR 到脚本 Prompt。
4. IR 到标题 Prompt。
5. IR 到封面建议 Prompt。
6. IR 到视频 Prompt Prompt。
7. IR 质量评分与 JSON 修复 Prompt。
8. Prompt 输入变量表、输出 Schema、失败回退、Benchmark 样本要求。

### 3.5 D09 对 D13/D21 的输出

D09 必须把以下内容交给质检风控与合规文档继续细化：

1. 原创化强度等级。
2. 文本相似度、结构相似度、钩子相似度、CTA 风险信号。
3. 可复用/不可复用边界。
4. 高风险行业与高风险承诺。
5. 人工复核触发条件。
6. 投诉删除与版本回滚字段。
7. AIGC 标识与使用提示。

### 3.6 D09 对 D16/D17 的输出

D09 需要把 Creative IR 落成硬工程契约：

1. 数据表：`creative_irs`、`creative_ir_versions`、`creative_ir_generation_jobs`、`creative_ir_quality_checks`、`creative_ir_events`。
2. API：生成、查询、重试、版本对比、导出、生成输出包、风控复查。
3. 任务队列：IR 生成、Schema 校验、JSON repair、风控检查、输出包生成、失败补偿。
4. 状态机：`pending`、`generating`、`validating`、`risk_checking`、`approved`、`needs_rewrite`、`manual_review`、`blocked`、`failed`、`archived`。
5. 日志：Prompt 版本、模型调用、成本、延迟、输入摘要、输出摘要、质量评分、风险结果。

---

## 4. 核心结论

### 4.1 一句话结论

Creative IR 是本产品从“拆解爆款结构”走向“生成原创内容”的核心中间层。它的任务不是复制对标视频，而是把 Content DNA 中可学习的结构、节奏、钩子类型、用户心理触发和转化路径，结合用户自己的行业、人设、产品、素材和平台目标，重构为原创内容方案，并通过 Schema、Prompt、风控、版本、计费和评测机制确保可工程化、可商业化、可合规上线。

### 4.2 D09 的核心产品原则

1. **只迁移结构，不迁移表达**：允许学习内容结构、钩子类别、叙事节奏、情绪曲线和转化路径；禁止复用原台词、原字幕、原人物、原声音、原画面、Logo、配乐、封面、独特剧情和其他可识别表达。
2. **先 IR，后 Prompt**：所有脚本、标题、封面建议、视频 Prompt 必须由 Creative IR 派生，不能直接从 Content DNA 生成终稿。
3. **先原创化，后输出包**：Creative IR 生成后必须完成原创化校验、风险校验、质量评分，再生成脚本/标题/封面/Prompt。
4. **默认中高原创化**：MVP1 默认采用 `level_3_high_original_reconstruction`，不提供用户直接选择“高相似复刻”的能力。
5. **风控不可绕过**：不可复用清单、风险标签、授权状态、AIGC 标识、投诉删除、人工复核必须进入 IR 生命周期。
6. **模型无关设计**：Creative IR 不绑定具体视频生成模型或文案模型，后续由 D10/D11 转换为模型专用 Prompt。
7. **可版本化与可回滚**：每次生成、重写、人工复核、用户编辑都要形成版本，保留输入摘要、输出摘要、Prompt 版本、风险结果和成本。
8. **可计费与可补偿**：Creative IR 生成属于高成本 AI 任务，必须关联积分冻结、成功扣费、失败返还、部分成功扣费、后台成本统计。
9. **可评测与可回归**：没有 Schema 合法率、原创化评分、相似度风险、脚本可用率和 Prompt 回归测试，不允许进入商业化上线。

### 4.3 D09 的 P0/P1/P2 范围

| 范围 | 内容 | 是否进入 MVP1 |
|---|---|---:|
| P0 | 单条视频 Content DNA → Creative IR | 是 |
| P0 | 原创化强度等级与默认高原创化 | 是 |
| P0 | 用户画像、行业、人设、产品、目标用户映射 | 是 |
| P0 | 原创脚本意图、分镜意图、标题策略、封面建议策略、视频 Prompt 蓝图 | 是 |
| P0 | 可复用/不可复用约束继承 | 是 |
| P0 | 风险约束、相似度控制、人工复核触发 | 是 |
| P0 | 5 条原创脚本、10 个标题、3 个封面建议、3 条视频 Prompt 的输出包生成 | 是 |
| P0 | 数据表、API、队列、日志、积分联动、后台查看 | 是 |
| P0 | Prompt、JSON Schema、JSON repair、质量评分、回归测试 | 是 |
| P1 | 多个 Content DNA 对比后生成 IR | 预留 |
| P1 | 对标账号级栏目 IR | 预留 |
| P1 | 用户手动编辑 IR 并二次生成 | 可轻量预留 |
| P1 | Prompt 库与用户专属模板 | 预留 |
| P2 | 视频模型自动生成成片 | 不做，MVP3 后置 |
| P2 | 自动发布与平台分发 | 不做 |
| P2 | 数据回填后自动 Prompt 进化 | 不做，MVP4 后置 |

### 4.4 MVP1 成功标准

MVP1 中 D09 模块被视为可内测，必须达到：

1. Creative IR Schema 合法率 ≥ 95%。
2. 必填字段完整率 ≥ 98%。
3. IR 生成任务成功率 ≥ 85%。
4. 原创化评分 ≥ 80/100 的样本占比 ≥ 80%。
5. 直接复用原句、原字幕、原独特表达的严重问题率 ≤ 2%。
6. 高风险标签继承准确率 ≥ 90%。
7. 生成脚本主观可用率 ≥ 60%，其中 P0 首发行业样本 ≥ 65%。
8. 输出包生成失败不导致积分丢失，失败返还准确率 100%。
9. 后台可查看 IR 输入、版本、Prompt 版本、模型成本、风险结果、失败原因。
10. D10 Prompt 回归测试通过，D13 风控检查可消费 IR 字段。

---

## 5. 详细设计

### 5.1 Creative IR 定义

Creative IR，全称 Creative Intermediate Representation，即创意中间表示。它位于 Content DNA 与最终输出包之间，是一种模型无关、结构化、可风控、可版本化的数据资产。

Creative IR 描述的是：

1. 这次原创内容要服务哪个用户、行业、人设和产品。
2. 从对标视频中只学习哪些底层结构和增长机制。
3. 哪些原始表达、素材、台词、视觉元素和身份特征必须禁止复用。
4. 原创化强度采用哪一档。
5. 新内容的核心观点、脚本结构、钩子类型、情绪推进、CTA 和平台适配策略是什么。
6. 每条原创脚本、标题、封面建议、视频 Prompt 应该围绕哪些创意变量生成。
7. 生成结果需要通过哪些质量标准、相似度限制、行业合规规则和人工复核门槛。

### 5.2 Creative IR 与 Content DNA 的边界

| 对象 | 主要回答的问题 | 是否面向用户展示 | 是否可直接生成终稿 | 风控责任 |
|---|---|---:|---:|---|
| Content DNA | 原视频为什么可能有效，哪些结构可学、哪些不可复制 | 是，作为拆解报告 | 否 | 初步识别风险与不可复用元素 |
| Creative IR | 如何把可学习结构迁移为用户自己的原创内容方案 | 部分展示，可作为高级解释 | 否，不是最终文案 | 承接并强化风险约束、原创化约束 |
| Deliverable Pack | 用户最终拿到的脚本、标题、封面建议、视频 Prompt | 是 | 是 | 必须完成最终质检与风控 |

### 5.3 Creative IR 与 Prompt 的边界

Creative IR 不是 Prompt。Prompt 是给模型执行的自然语言/结构化指令，Creative IR 是系统内部的创意状态和数据资产。

禁止做法：

```text
Content DNA → 直接让模型写脚本 / 视频 Prompt
```

推荐做法：

```text
Content DNA
→ 可复用/不可复用判断
→ 用户画像与目标平台适配
→ 原创化策略选择
→ Creative IR
→ IR Schema 校验
→ 风控检查
→ IR 到具体输出 Prompt
→ 脚本 / 标题 / 封面建议 / 视频 Prompt
→ 质检与相似度风控
```

### 5.4 输入对象设计

#### 5.4.1 必须输入

| 输入对象 | 字段/来源 | 是否必填 | 用途 |
|---|---|---:|---|
| `task_id` | 视频拆解任务 | 是 | 绑定任务状态、积分、日志、输出包 |
| `user_id` | 当前用户 | 是 | 权限与资产归属 |
| `creator_profile_snapshot` | 用户画像快照 | 是 | 行业、人设、产品、目标用户、平台、禁用内容 |
| `content_dna_id` | D07 输出 | 是 | Creative IR 的核心输入 |
| `content_dna_snapshot` | Content DNA 版本快照 | 是 | 防止上游字段变化导致回放不一致 |
| `reusable_parts` | D07 | 是 | 可迁移结构候选 |
| `non_replicable_parts` | D07 | 是 | 硬禁用约束 |
| `risk_flags` | D07/D13 | 是 | 风险分级与拦截依据 |
| `output_package_config` | 输出包配置 | 是 | 生成脚本、标题、封面、视频 Prompt 的数量与类型 |
| `originalization_level` | 系统默认/用户选择 | 是 | 默认高原创化 |
| `authorization_snapshot` | 授权确认记录 | 是 | 合规审计 |
| `credit_reservation_id` | 积分冻结记录 | 是 | 计费和失败返还 |

#### 5.4.2 可选输入

| 输入对象 | 用途 | MVP1 处理方式 |
|---|---|---|
| 用户补充想法 `user_extra_brief` | 用户希望融入的新角度、产品点、活动信息 | P0 支持短文本 |
| 可用素材 `available_assets` | 用户自己的门店、产品、案例、人物、场景素材 | P0 只记录文本描述，P1 可接素材库 |
| 禁用词/禁用承诺 `prohibited_claims` | 控制合规风险 | P0 必须支持 |
| 目标视频时长 `target_duration_sec` | 影响脚本长度和分镜数量 | P0 支持 15/30/60/90 秒 |
| 目标平台 `target_platform` | 影响口吻、标题、封面策略 | P0 支持抖音/视频号/小红书/TikTok 枚举预留 |
| 输出语气 `tone_preference` | 专家型、朋友型、老板型、案例型 | P0 支持 |
| 行业风险配置 `industry_risk_config` | 控制高风险行业输出 | P0 后台可配置 |

### 5.5 输出对象设计

D09 的核心输出不是最终脚本，而是 `CreativeIR`。同时 D09 可以触发下游输出包生成，形成 `DeliverablePack`。

| 输出对象 | 说明 | 是否落库 | 是否展示 |
|---|---|---:|---:|
| `CreativeIR` | 创意中间表示主对象 | 是 | 后台全量，前台摘要 |
| `OriginalizationPlan` | 原创化重构计划 | 是 | 前台展示摘要 |
| `StoryboardPlan` | 分镜/内容节奏计划 | 是 | 前台展示分镜建议 |
| `ScriptIntentPlan` | 脚本生成意图 | 是 | 后台/Prompt 使用 |
| `TitleStrategyPlan` | 标题策略 | 是 | 后台/Prompt 使用 |
| `CoverStrategyPlan` | 封面建议策略 | 是 | 前台摘要 |
| `VideoPromptBlueprint` | 视频 Prompt 蓝图 | 是 | 前台以最终 Prompt 展示 |
| `RiskConstraintSet` | 风控约束集合 | 是 | 前台风险提示 + 后台全量 |
| `CreativeIRQualityReport` | IR 质量评分 | 是 | 后台全量，前台必要提示 |
| `DeliverablePack` | 脚本、标题、封面建议、视频 Prompt | 是 | 前台展示 |

### 5.6 原创化强度模型

Creative IR 必须明确原创化强度，禁止默认低原创化。

| 等级 | 枚举值 | 定义 | MVP1 默认 | 允许输出 |
|---|---|---|---:|---|
| L1 | `level_1_structure_reference` | 只学习结构，不生成高相似表达；用于内部拆解和教学解释 | 否 | 只输出结构分析，不输出终稿 |
| L2 | `level_2_industry_adaptation` | 将结构迁移到用户行业，替换人设、产品、场景和案例 | 可选 | 可输出脚本草稿，但需严格相似度检查 |
| L3 | `level_3_high_original_reconstruction` | 重构主题、角度、案例、表达和情绪路径，只保留抽象结构 | 是 | MVP1 默认输出 |
| L4 | `level_4_original_variation_cluster` | 基于结构生成多个完全不同角度的原创裂变版本 | P1/P2 | 可输出批量选题/脚本，MVP1 可预留 |

默认策略：

```text
若 risk_flags 包含版权/肖像/商标/高相似风险 → 强制 L3 或 L4。
若用户请求“照着写、差不多、同款、复刻、克隆、洗稿” → 拦截或转为 L3 原创重构。
若行业为医疗、金融、法律、招商加盟、教育承诺等高风险行业 → 至少 L3，并启用高风险合规模板。
```

### 5.7 原创化重构五层规则

#### 5.7.1 层一：主题重构

将原视频主题抽象为问题类型，然后换成用户自己的业务问题。

| 原始层 | 可学习 | 必须重构 |
|---|---|---|
| 原视频具体话题 | 痛点类型、冲突结构、利益点位置 | 用户行业的新话题、新案例、新观点 |

示例：

```text
原视频：健身教练讲“为什么你练了三个月还没瘦”
抽象结构：打破用户误区 → 给出原因 → 给出行动建议
用户行业：企业服务老板 IP
原创主题：为什么你投了三个月广告还没稳定获客
```

#### 5.7.2 层二：人设重构

不能模仿原视频创作者的人设、口头禅、身份符号、表演风格。只能抽象人设功能。

| 原人设功能 | 用户人设替换 |
|---|---|
| 专家背书 | 用户自己的资历、案例、经验 |
| 朋友劝告 | 用户目标受众能接受的口吻 |
| 老板视角 | 用户自己的经营场景 |
| 测评者视角 | 用户自有产品/服务体验或合规第三方信息 |

#### 5.7.3 层三：结构重构

允许学习结构，但不能机械复制完整顺序和所有桥段。MVP1 的结构重构规则：

1. 保留抽象结构，例如「痛点 → 误区 → 原因 → 方法 → CTA」。
2. 至少替换 70% 的具体信息点。
3. 至少新增 2 个用户行业专属信息点。
4. 钩子不能复用原句；只能复用钩子类型。
5. CTA 必须替换为用户自己的合规行动路径。

#### 5.7.4 层四：表达重构

表达层必须做到：

1. 禁止连续 8 个以上汉字与原字幕/台词一致，除非是不可替换的通用词汇。
2. 禁止复用原视频独特比喻、口头禅、标题句式和标志性表达。
3. 禁止复用原视频可识别剧情桥段。
4. 脚本必须体现用户自己的行业、产品、案例或观点。
5. 标题必须重新构造，不得在原标题上做同义词替换。

#### 5.7.5 层五：视觉与分镜重构

MVP1 不生成成片，但视频 Prompt 和分镜建议不能复刻原视频画面。

1. 允许学习镜头功能，例如「开场特写吸引注意」「中段展示案例」「结尾 CTA」。
2. 禁止描述与原视频高度相似的具体人物、服装、场景、背景、品牌、构图、Logo。
3. 若原视频包含可识别人物或品牌，IR 必须在 `visual_constraints.forbidden_elements` 标记。
4. 视频 Prompt 必须使用用户自有素材或通用原创场景。

### 5.8 平台适配规则

| 平台 | 脚本倾向 | 标题倾向 | 封面建议 | 风控注意 |
|---|---|---|---|---|
| 抖音 | 强钩子、强节奏、口播/案例转化 | 痛点/反差/利益点 | 大字标题 + 人物/场景冲突 | 禁止夸大承诺、搬运感 |
| 视频号 | 信任感、老板 IP、案例、长期价值 | 稳健、专业、可信 | 人物身份 + 关键词 | 禁止虚假背书 |
| 小红书 | 经验分享、清单、避坑、种草 | 搜索友好、关键词明确 | 生活化、清单感 | 禁止绝对化功效 |
| TikTok | 强视觉、强节奏、短句、跨文化表达 | 简短直接 | 高对比视觉 | 注意跨境合规和版权素材 |

### 5.9 首发用户适配规则

| 用户类型 | IR 重构重点 | 输出偏好 |
|---|---|---|
| 企业 IP / 老板 IP | 观点、案例、信任、转化路径 | 口播脚本、观点型标题、专业封面 |
| 本地生活商家 | 门店场景、服务流程、同城痛点、活动转化 | 探店脚本、活动脚本、门店封面 |
| 知识 IP / 教培 | 知识结构、误区纠正、学习路径 | 干货脚本、清单标题、课程引导谨慎表达 |
| MCN / 内容团队 | 批量模板、栏目化结构、可复用节奏 | 多版本脚本、模板摘要、团队备注 |

### 5.10 产品/服务植入规则

产品植入必须自然、合规、可验证。

| 植入位置 | 允许做法 | 禁止做法 |
|---|---|---|
| 开头 | 以用户痛点开场，不直接硬广 | 一上来夸大产品效果 |
| 中段 | 用案例、流程、对比说明价值 | 虚构客户、虚构数据、虚假背书 |
| 结尾 | 安全 CTA，如“想要清单可以评论/私信” | 保证收益、疗效、通过率、法律结果 |
| 视频 Prompt | 展示自有产品/门店/服务场景 | 使用原视频品牌、Logo、人物、素材 |

### 5.11 风控优先级规则

Creative IR 阶段的风控规则必须优先于用户需求。用户要求与合规冲突时，以风控规则为准。

| 风险等级 | 触发条件 | IR 动作 |
|---|---|---|
| R0 安全 | 无明显合规或相似度风险 | 正常生成 |
| R1 提醒 | 存在轻微营销夸张或低置信度 | 生成但附带风险提示 |
| R2 重写 | 表达相似、标题相似、CTA 过激 | 自动重写后再检查 |
| R3 人工复核 | 涉及高风险行业、公众人物、商标、疑似侵权 | 暂停输出或只输出结构分析，创建复核单 |
| R4 拦截 | 仿脸仿声、搬运、未授权复用、违法违规营销、绕过平台规则 | 阻断任务，返还未消耗积分，记录风险 |

### 5.12 Deliverable Pack 生成规则

MVP1 输出包：

| 输出类型 | 数量 | 必须来源 | 质量要求 |
|---|---:|---|---|
| 原创脚本 | 5 条 | `script_intent_plan` + `storyboard_plan` | 适配行业、人设、产品；不能像原视频洗稿 |
| 标题 | 10 个 | `title_strategy_plan` | 不夸大、不虚假、不直接复刻原标题 |
| 封面建议 | 3 个 | `cover_strategy_plan` | 不复刻原封面，不使用原 Logo/人物/画面 |
| 视频生成 Prompt | 3 条 | `video_prompt_blueprint` | 模型无关，可进一步转模型专用 Prompt；不包含仿脸仿声 |

---

## 6. 数据结构 / API / Prompt / 流程 / 页面 / 状态

## 6.1 Creative IR 总 Schema

以下为 MVP1 Creative IR V1.0 草案，最终以 D16 数据库/API 文档固化。

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-growth-engine.local/schemas/creative-ir.v1.json",
  "title": "CreativeIR",
  "type": "object",
  "required": [
    "creative_ir_id",
    "schema_version",
    "task_id",
    "user_id",
    "content_dna_id",
    "creator_profile_snapshot",
    "originalization_plan",
    "creative_brief",
    "storyboard_plan",
    "deliverable_intents",
    "risk_constraints",
    "quality_targets",
    "status",
    "created_at"
  ],
  "properties": {
    "creative_ir_id": { "type": "string" },
    "schema_version": { "type": "string", "const": "creative_ir_v1.0" },
    "task_id": { "type": "string" },
    "user_id": { "type": "string" },
    "content_dna_id": { "type": "string" },
    "content_dna_version": { "type": "string" },
    "is_mock": { "type": "boolean", "default": false },
    "source_trace": {
      "type": "object",
      "required": ["asset_id", "content_dna_snapshot_hash", "authorization_id"],
      "properties": {
        "asset_id": { "type": "string" },
        "content_dna_snapshot_hash": { "type": "string" },
        "authorization_id": { "type": "string" },
        "evidence_pack_id": { "type": "string" },
        "source_video_risk_level": { "type": "string", "enum": ["R0", "R1", "R2", "R3", "R4"] }
      }
    },
    "creator_profile_snapshot": {
      "type": "object",
      "required": ["industry", "target_platform", "persona", "product_or_service", "target_audience", "content_goal"],
      "properties": {
        "industry": { "type": "string" },
        "sub_industry": { "type": "string" },
        "target_platform": { "type": "string" },
        "persona": { "type": "string" },
        "product_or_service": { "type": "string" },
        "target_audience": { "type": "string" },
        "content_goal": { "type": "string" },
        "available_assets_summary": { "type": "array", "items": { "type": "string" } },
        "prohibited_terms": { "type": "array", "items": { "type": "string" } },
        "prohibited_claims": { "type": "array", "items": { "type": "string" } }
      }
    },
    "originalization_plan": {
      "type": "object",
      "required": ["level", "strategy_summary", "must_transform", "must_not_reuse", "novelty_requirements"],
      "properties": {
        "level": {
          "type": "string",
          "enum": [
            "level_1_structure_reference",
            "level_2_industry_adaptation",
            "level_3_high_original_reconstruction",
            "level_4_original_variation_cluster"
          ]
        },
        "strategy_summary": { "type": "string" },
        "must_transform": { "type": "array", "items": { "type": "string" } },
        "must_not_reuse": { "type": "array", "items": { "type": "string" } },
        "novelty_requirements": { "type": "array", "items": { "type": "string" } },
        "minimum_originality_score": { "type": "number", "minimum": 0, "maximum": 100 }
      }
    },
    "creative_brief": {
      "type": "object",
      "required": ["new_topic", "core_angle", "audience_pain_point", "key_message", "cta_strategy"],
      "properties": {
        "new_topic": { "type": "string" },
        "core_angle": { "type": "string" },
        "audience_pain_point": { "type": "string" },
        "key_message": { "type": "string" },
        "proof_points": { "type": "array", "items": { "type": "string" } },
        "product_integration": { "type": "string" },
        "cta_strategy": { "type": "string" },
        "tone": { "type": "string" },
        "target_duration_sec": { "type": "integer" }
      }
    },
    "structure_transfer": {
      "type": "object",
      "properties": {
        "learned_structure_type": { "type": "string" },
        "abstracted_hook_type": { "type": "string" },
        "abstracted_emotional_curve": { "type": "array", "items": { "type": "string" } },
        "abstracted_conversion_path": { "type": "array", "items": { "type": "string" } },
        "source_elements_excluded": { "type": "array", "items": { "type": "string" } }
      }
    },
    "storyboard_plan": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "object",
        "required": ["beat_id", "time_range_sec", "purpose", "message", "visual_intent", "script_intent", "risk_notes"],
        "properties": {
          "beat_id": { "type": "string" },
          "time_range_sec": { "type": "string" },
          "purpose": { "type": "string" },
          "message": { "type": "string" },
          "visual_intent": { "type": "string" },
          "script_intent": { "type": "string" },
          "transition": { "type": "string" },
          "risk_notes": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "deliverable_intents": {
      "type": "object",
      "required": ["scripts", "titles", "cover_suggestions", "video_prompts"],
      "properties": {
        "scripts": {
          "type": "object",
          "properties": {
            "count": { "type": "integer", "const": 5 },
            "variants": { "type": "array", "items": { "type": "string" } },
            "must_include": { "type": "array", "items": { "type": "string" } },
            "must_avoid": { "type": "array", "items": { "type": "string" } }
          }
        },
        "titles": {
          "type": "object",
          "properties": {
            "count": { "type": "integer", "const": 10 },
            "title_angles": { "type": "array", "items": { "type": "string" } },
            "forbidden_patterns": { "type": "array", "items": { "type": "string" } }
          }
        },
        "cover_suggestions": {
          "type": "object",
          "properties": {
            "count": { "type": "integer", "const": 3 },
            "cover_angles": { "type": "array", "items": { "type": "string" } },
            "visual_constraints": { "type": "array", "items": { "type": "string" } }
          }
        },
        "video_prompts": {
          "type": "object",
          "properties": {
            "count": { "type": "integer", "const": 3 },
            "model_agnostic_blueprints": { "type": "array", "items": { "type": "string" } },
            "forbidden_visual_elements": { "type": "array", "items": { "type": "string" } }
          }
        }
      }
    },
    "risk_constraints": {
      "type": "object",
      "required": ["risk_level", "non_replicable_parts", "forbidden_elements", "required_disclaimers", "review_policy"],
      "properties": {
        "risk_level": { "type": "string", "enum": ["R0", "R1", "R2", "R3", "R4"] },
        "non_replicable_parts": { "type": "array", "items": { "type": "string" } },
        "forbidden_elements": { "type": "array", "items": { "type": "string" } },
        "forbidden_claims": { "type": "array", "items": { "type": "string" } },
        "similarity_guardrails": {
          "type": "object",
          "properties": {
            "max_text_similarity": { "type": "number" },
            "max_title_similarity": { "type": "number" },
            "max_structure_similarity": { "type": "number" },
            "direct_quote_allowed": { "type": "boolean", "const": false }
          }
        },
        "required_disclaimers": { "type": "array", "items": { "type": "string" } },
        "review_policy": { "type": "string", "enum": ["auto_pass", "warn", "rewrite_required", "manual_review", "block"] }
      }
    },
    "quality_targets": {
      "type": "object",
      "properties": {
        "minimum_originality_score": { "type": "number" },
        "minimum_relevance_score": { "type": "number" },
        "minimum_actionability_score": { "type": "number" },
        "minimum_schema_completeness_score": { "type": "number" }
      }
    },
    "status": {
      "type": "string",
      "enum": ["pending", "generating", "validating", "risk_checking", "approved", "needs_rewrite", "manual_review", "blocked", "failed", "archived"]
    },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
```

## 6.2 TypeScript 类型草案

```ts
export type RiskLevel = "R0" | "R1" | "R2" | "R3" | "R4";
export type CreativeIRStatus =
  | "pending"
  | "generating"
  | "validating"
  | "risk_checking"
  | "approved"
  | "needs_rewrite"
  | "manual_review"
  | "blocked"
  | "failed"
  | "archived";

export type OriginalizationLevel =
  | "level_1_structure_reference"
  | "level_2_industry_adaptation"
  | "level_3_high_original_reconstruction"
  | "level_4_original_variation_cluster";

export interface CreativeIR {
  creativeIrId: string;
  schemaVersion: "creative_ir_v1.0";
  taskId: string;
  userId: string;
  contentDnaId: string;
  contentDnaVersion: string;
  isMock: boolean;
  creatorProfileSnapshot: CreatorProfileSnapshot;
  originalizationPlan: OriginalizationPlan;
  creativeBrief: CreativeBrief;
  structureTransfer: StructureTransfer;
  storyboardPlan: StoryboardBeat[];
  deliverableIntents: DeliverableIntents;
  riskConstraints: RiskConstraintSet;
  qualityTargets: QualityTargets;
  status: CreativeIRStatus;
  createdAt: string;
  updatedAt?: string;
}
```

## 6.3 数据库表草案

### 6.3.1 `creative_irs`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | uuid | 是 | Creative IR 主键 |
| `task_id` | uuid | 是 | 关联视频拆解任务 |
| `user_id` | uuid | 是 | 用户归属 |
| `content_dna_id` | uuid | 是 | 上游 Content DNA |
| `content_dna_version` | varchar | 是 | 上游版本 |
| `schema_version` | varchar | 是 | `creative_ir_v1.0` |
| `originalization_level` | varchar | 是 | 原创化强度 |
| `risk_level` | varchar | 是 | R0-R4 |
| `status` | varchar | 是 | IR 状态机 |
| `ir_json` | jsonb | 是 | 完整 IR JSON |
| `quality_score` | numeric | 否 | 综合质量分 |
| `originality_score` | numeric | 否 | 原创化评分 |
| `similarity_risk_score` | numeric | 否 | 相似度风险分 |
| `is_mock` | boolean | 是 | 是否 mock |
| `created_by_model` | varchar | 否 | 模型名称 |
| `prompt_version` | varchar | 否 | Prompt 版本 |
| `model_call_log_id` | uuid | 否 | 模型调用日志 |
| `credit_reservation_id` | uuid | 是 | 积分冻结记录 |
| `created_at` | timestamptz | 是 | 创建时间 |
| `updated_at` | timestamptz | 是 | 更新时间 |
| `deleted_at` | timestamptz | 否 | 删除时间 |

### 6.3.2 `creative_ir_versions`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 版本主键 |
| `creative_ir_id` | uuid | 主 IR |
| `version_no` | integer | 版本号，从 1 开始 |
| `change_type` | varchar | `initial_generate` / `auto_rewrite` / `manual_review_edit` / `user_edit` / `prompt_upgrade` |
| `ir_json` | jsonb | 版本内容 |
| `change_reason` | text | 变更原因 |
| `risk_level` | varchar | 当前风险 |
| `quality_score` | numeric | 当前质量分 |
| `created_by` | varchar | system/model/admin/user |
| `created_at` | timestamptz | 创建时间 |

### 6.3.3 `creative_ir_generation_jobs`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | Job 主键 |
| `task_id` | uuid | 任务 ID |
| `creative_ir_id` | uuid | 可为空，生成后回填 |
| `job_type` | varchar | `generate` / `repair` / `rewrite` / `risk_check` / `deliverable_generate` |
| `status` | varchar | `queued` / `running` / `succeeded` / `failed` / `cancelled` / `manual_review` |
| `input_snapshot` | jsonb | 输入摘要 |
| `error_code` | varchar | 错误码 |
| `error_message` | text | 错误信息 |
| `retry_count` | integer | 重试次数 |
| `idempotency_key` | varchar | 幂等键 |
| `started_at` | timestamptz | 开始时间 |
| `finished_at` | timestamptz | 结束时间 |

### 6.3.4 `creative_ir_quality_checks`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 质检记录 |
| `creative_ir_id` | uuid | IR ID |
| `check_type` | varchar | `schema` / `originality` / `similarity` / `policy` / `actionability` |
| `score` | numeric | 评分 |
| `passed` | boolean | 是否通过 |
| `findings` | jsonb | 发现的问题 |
| `recommended_action` | varchar | `pass` / `rewrite` / `manual_review` / `block` |
| `created_at` | timestamptz | 创建时间 |

### 6.3.5 `creative_ir_events`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 事件 ID |
| `creative_ir_id` | uuid | IR ID |
| `event_type` | varchar | `created` / `validated` / `risk_flagged` / `rewritten` / `approved` / `blocked` / `delivered` |
| `actor_type` | varchar | `system` / `model` / `admin` / `user` |
| `actor_id` | uuid | 操作者 |
| `payload` | jsonb | 事件内容 |
| `created_at` | timestamptz | 创建时间 |

## 6.4 API 草案

### 6.4.1 生成 Creative IR

```http
POST /api/v1/tasks/{task_id}/creative-ir/generate
Authorization: Bearer <token>
Idempotency-Key: <uuid>
Content-Type: application/json
```

请求：

```json
{
  "content_dna_id": "uuid",
  "output_package_id": "standard_mvp1_pack",
  "originalization_level": "level_3_high_original_reconstruction",
  "user_extra_brief": "希望结合我自己的企业咨询服务，偏老板口播风格",
  "target_duration_sec": 60
}
```

响应：

```json
{
  "job_id": "uuid",
  "creative_ir_id": null,
  "status": "queued",
  "estimated_credit_cost": 20,
  "credit_reservation_id": "uuid"
}
```

### 6.4.2 查询 Creative IR

```http
GET /api/v1/creative-irs/{creative_ir_id}
```

响应：

```json
{
  "creative_ir_id": "uuid",
  "status": "approved",
  "risk_level": "R1",
  "originalization_level": "level_3_high_original_reconstruction",
  "creative_brief": {},
  "storyboard_plan": [],
  "deliverable_intents": {},
  "quality_summary": {
    "quality_score": 86,
    "originality_score": 88,
    "similarity_risk_score": 12
  }
}
```

### 6.4.3 重新生成 / 重写 Creative IR

```http
POST /api/v1/creative-irs/{creative_ir_id}/rewrite
```

请求：

```json
{
  "reason": "similarity_risk_high",
  "rewrite_level": "stronger_originality",
  "user_extra_brief": "增加客户案例视角，减少原视频结构相似度"
}
```

### 6.4.4 生成输出包

```http
POST /api/v1/creative-irs/{creative_ir_id}/deliverables/generate
```

请求：

```json
{
  "deliverable_types": ["scripts", "titles", "cover_suggestions", "video_prompts"],
  "count_config": {
    "scripts": 5,
    "titles": 10,
    "cover_suggestions": 3,
    "video_prompts": 3
  }
}
```

### 6.4.5 后台查看 IR 诊断

```http
GET /api/v1/admin/creative-irs/{creative_ir_id}/diagnostics
```

返回：

```json
{
  "input_snapshot": {},
  "prompt_version": "P04_CREATIVE_IR_GENERATION@1.0.0",
  "model_call_logs": [],
  "quality_checks": [],
  "risk_checks": [],
  "credit_transactions": [],
  "events": []
}
```

## 6.5 任务队列与状态机

### 6.5.1 队列名称

| 队列 | 责任 | 是否 P0 |
|---|---|---:|
| `creative_ir.generate` | 根据 Content DNA 生成 Creative IR | 是 |
| `creative_ir.validate` | Schema 校验、字段完整性、JSON repair | 是 |
| `creative_ir.risk_check` | IR 阶段风险与相似度预检 | 是 |
| `creative_ir.rewrite` | 相似度/质量/合规不通过后的自动重写 | 是 |
| `deliverable.generate` | 从 IR 生成脚本/标题/封面/Prompt | 是 |
| `deliverable.quality_check` | 输出包质量与风险检查 | 是 |
| `creative_ir.manual_review` | 创建人工复核单 | 是，最小版 |

### 6.5.2 状态机

```text
pending
→ generating
→ validating
→ risk_checking
→ approved
→ deliverable_generating
→ delivered
```

异常分支：

```text
validating → needs_repair → validating
risk_checking → needs_rewrite → generating
risk_checking → manual_review
risk_checking → blocked
any_state → failed
failed → compensated / retry_queued
```

### 6.5.3 状态说明

| 状态 | 含义 | 用户可见 | 后台动作 |
|---|---|---:|---|
| `pending` | 等待生成 | 是 | 可取消 |
| `generating` | 模型生成 IR | 是 | 记录模型成本 |
| `validating` | Schema 校验 | 是 | 失败可 repair |
| `risk_checking` | 风控检查 | 是 | 高风险进入复核 |
| `approved` | IR 可用于生成输出包 | 是 | 可进入 deliverable |
| `needs_rewrite` | 需要重写 | 用户看到“正在优化原创度” | 自动重写 |
| `manual_review` | 人工复核 | 用户看到“等待复核” | 后台处理 |
| `blocked` | 阻断 | 是 | 返还未消耗积分，提示原因 |
| `failed` | 系统失败 | 是 | 重试或返还 |
| `archived` | 归档 | 否/历史可见 | 不再生成 |

## 6.6 Prompt 模板

### 6.6.1 P04_CREATIVE_IR_GENERATION

用途：从 Content DNA、用户画像和风控约束生成 Creative IR。

System Prompt：

```text
你是“AI 短视频爆款增长引擎”的 Creative IR 生成器。
你的任务不是复刻、搬运、洗稿或模仿原视频，而是把 Content DNA 中可学习的抽象结构，重构为适合用户行业、人设、产品、目标平台和素材条件的原创 Creative IR。

硬性规则：
1. 只学习结构、钩子类型、节奏、情绪曲线、用户心理触发和转化路径。
2. 不得复用原视频台词、字幕、人物、声音、Logo、配乐、封面、品牌元素、独特剧情和可识别表达。
3. 必须继承 non_replicable_parts 和 risk_flags，并写入 risk_constraints。
4. 默认使用 level_3_high_original_reconstruction，除非输入明确要求更高原创化。
5. 若用户要求复刻、克隆、洗稿、仿脸、仿声、绕过平台审核，必须将 review_policy 设置为 block 或 manual_review。
6. 输出必须严格符合 CreativeIR JSON Schema，不输出 Markdown，不输出解释性废话。
```

User Prompt 变量：

```text
【任务信息】
Task ID: {{task_id}}
User ID: {{user_id}}
Content DNA ID: {{content_dna_id}}

【用户画像快照】
{{creator_profile_snapshot_json}}

【Content DNA 摘要】
{{content_dna_summary_json}}

【可复用结构】
{{reusable_parts_json}}

【不可复用内容】
{{non_replicable_parts_json}}

【风险标签】
{{risk_flags_json}}

【输出包配置】
{{output_package_config_json}}

【用户补充想法】
{{user_extra_brief}}

请生成符合 creative_ir_v1.0 Schema 的 JSON。
```

输出要求：严格 JSON，必须包含 `creative_brief`、`originalization_plan`、`storyboard_plan`、`deliverable_intents`、`risk_constraints`、`quality_targets`。

### 6.6.2 P05_ORIGINALIZATION_REWRITE

用途：当 IR 相似度过高、原创化不足或风险较高时进行重写。

```text
你是 Creative IR 原创化重写器。
输入是一份未通过原创度/相似度/合规检查的 Creative IR。
请在不改变用户业务目标的前提下，提高原创化程度，降低与源视频在文字、标题、结构、视觉和 CTA 上的相似度。

必须执行：
1. 替换主题角度、案例、表达方式和分镜视觉意图。
2. 保留用户行业、人设、产品、目标用户和内容目标。
3. 不允许复用 source_non_replicable_parts 中任何内容。
4. 风险等级不得被人为降低，除非已移除触发风险的内容并说明原因。
5. 输出完整 Creative IR JSON。

待重写 IR：
{{creative_ir_json}}

质检问题：
{{quality_findings_json}}
```

### 6.6.3 P06_IR_TO_SCRIPT

用途：从已批准 Creative IR 生成 5 条原创脚本。

```text
你是短视频原创脚本生成器。
你只能基于 Creative IR 生成用户自己的原创脚本，不得复用源视频台词、字幕、独特表达、人物设定、可识别剧情或品牌元素。

生成要求：
1. 输出 5 条脚本，每条必须有：标题、适用场景、时长、开场钩子、正文分段、结尾 CTA、拍摄备注、风险提示。
2. 每条脚本角度要有明显差异，不能只是同义改写。
3. 脚本必须适配用户画像、行业、人设、产品和目标平台。
4. 禁止使用风险约束中的 forbidden_claims、forbidden_elements、non_replicable_parts。
5. 若涉及医疗、金融、法律、教育、招商收益等风险，必须使用谨慎表达，不做结果承诺。
6. 输出 JSON，不输出 Markdown。

Creative IR:
{{creative_ir_json}}
```

### 6.6.4 P07_IR_TO_TITLE

用途：从 Creative IR 生成 10 个标题。

```text
你是短视频标题策略生成器。
请基于 Creative IR 输出 10 个原创标题。

规则：
1. 不得复用原视频标题句式和关键词组合。
2. 标题要覆盖不同角度：痛点、反差、清单、避坑、案例、观点、场景、转化。
3. 不得使用夸大、虚假、绝对化承诺。
4. 每个标题附带：标题类型、适用平台、风险等级、为什么适合。
5. 输出 JSON。

Creative IR:
{{creative_ir_json}}
```

### 6.6.5 P08_IR_TO_COVER

用途：从 Creative IR 生成 3 个封面建议。

```text
你是短视频封面策划器。
请基于 Creative IR 输出 3 个原创封面建议。

规则：
1. 不得复刻源视频封面、构图、人物、Logo、配色和可识别视觉符号。
2. 每个封面建议包含：主视觉、封面大字、辅助元素、构图说明、适用平台、风险提示。
3. 优先使用用户自有素材或通用原创场景。
4. 输出 JSON。

Creative IR:
{{creative_ir_json}}
```

### 6.6.6 P09_IR_TO_VIDEO_PROMPT

用途：从 Creative IR 生成 3 条模型无关视频生成 Prompt 蓝图。

```text
你是模型无关视频 Prompt 蓝图生成器。
请基于 Creative IR 输出 3 条可用于后续视频生成模型适配的原创视频 Prompt。

规则：
1. 不生成仿脸、仿声、仿公众人物、仿特定账号、仿原视频画面风格的内容。
2. 不使用源视频人物、Logo、品牌、具体场景、配乐、字幕句式和可识别元素。
3. 每条 Prompt 包含：场景描述、人物/主体、镜头语言、动作、光线、节奏、字幕建议、负面约束、适用模型注意事项。
4. Prompt 必须模型无关，不写某个供应商专有参数；模型专用转换由 D10/D11 后续处理。
5. 输出 JSON。

Creative IR:
{{creative_ir_json}}
```

### 6.6.7 P10_IR_QUALITY_CHECK

用途：检查 Creative IR 是否可进入输出包生成。

```text
你是 Creative IR 质量与合规检查器。
请对输入的 Creative IR 进行结构完整性、原创化、相关性、可执行性、相似度风险和合规风险检查。

输出必须包含：
1. schema_completeness_score: 0-100
2. originality_score: 0-100
3. relevance_score: 0-100
4. actionability_score: 0-100
5. similarity_risk_score: 0-100，分数越高风险越高
6. compliance_risk_level: R0/R1/R2/R3/R4
7. findings: 问题列表
8. recommended_action: pass/rewrite/manual_review/block
9. rewrite_instructions: 若需要重写，给出具体修改要求

Creative IR:
{{creative_ir_json}}

Source non-replicable parts:
{{non_replicable_parts_json}}

Risk flags:
{{risk_flags_json}}
```

## 6.7 前端页面与 ViewModel

### 6.7.1 用户端结果页 Creative IR 摘要

用户不需要看到完整 IR JSON，但需要看到原创化逻辑。

| 区块 | 展示内容 | 用户价值 |
|---|---|---|
| 原创化策略 | 本次采用的原创化强度、迁移原则 | 让用户知道不是洗稿 |
| 新内容方向 | 新主题、核心角度、目标受众、痛点 | 让用户理解创作方向 |
| 分镜/结构规划 | 开头、中段、结尾的内容功能 | 方便拍摄或交给团队 |
| 风险提示 | 不可复用元素、合规提醒、AIGC 标识 | 降低侵权和平台风险 |
| 输出包 | 脚本、标题、封面建议、视频 Prompt | 用户可直接使用 |

### 6.7.2 后台 Creative IR 诊断页

后台必须展示：

1. 输入 Content DNA 版本。
2. 用户画像快照。
3. Creative IR JSON。
4. Prompt 版本。
5. 模型调用日志。
6. 积分冻结/扣费/返还记录。
7. 质量评分。
8. 风险结果。
9. 版本历史。
10. 人工复核操作：通过、要求重写、阻断、备注、补偿。

## 6.8 示例 Creative IR JSON

```json
{
  "creative_ir_id": "cir_20260525_001",
  "schema_version": "creative_ir_v1.0",
  "task_id": "task_001",
  "user_id": "user_001",
  "content_dna_id": "dna_001",
  "content_dna_version": "content_dna_v1.0",
  "is_mock": false,
  "source_trace": {
    "asset_id": "asset_001",
    "content_dna_snapshot_hash": "sha256:example",
    "authorization_id": "auth_001",
    "evidence_pack_id": "evidence_001",
    "source_video_risk_level": "R1"
  },
  "creator_profile_snapshot": {
    "industry": "企业服务",
    "sub_industry": "B2B 咨询服务",
    "target_platform": "抖音",
    "persona": "老板型专家",
    "product_or_service": "企业短视频获客咨询服务",
    "target_audience": "想通过短视频获客但没有稳定内容体系的中小企业老板",
    "content_goal": "获客线索",
    "available_assets_summary": ["老板口播", "客户案例", "服务流程图"],
    "prohibited_terms": ["保证爆单", "百分百获客"],
    "prohibited_claims": ["保证收益", "保证转化"]
  },
  "originalization_plan": {
    "level": "level_3_high_original_reconstruction",
    "strategy_summary": "仅保留原视频的误区纠正结构，主题、案例、表达、人设和 CTA 全部替换为企业服务老板 IP 场景。",
    "must_transform": ["原视频健身减脂主题", "原创作者个人口头禅", "原视频三段案例"],
    "must_not_reuse": ["原字幕原句", "原人物形象", "原封面大字", "原品牌 Logo"],
    "novelty_requirements": ["加入企业投放失败案例", "加入内容获客漏斗视角", "加入安全 CTA"],
    "minimum_originality_score": 80
  },
  "creative_brief": {
    "new_topic": "为什么你连续做了三个月短视频，还是没有稳定获客",
    "core_angle": "不是播放量不够，而是内容缺少信任路径和转化路径",
    "audience_pain_point": "老板投入内容团队却看不到线索增长",
    "key_message": "短视频获客需要从选题、信任、案例、CTA 四段设计，而不是只追热点",
    "proof_points": ["常见账号只做流量型内容", "客户案例能提升信任", "明确 CTA 才能形成线索"],
    "product_integration": "自然引出企业短视频内容诊断服务",
    "cta_strategy": "评论或私信领取内容诊断清单",
    "tone": "专业但不居高临下",
    "target_duration_sec": 60
  },
  "structure_transfer": {
    "learned_structure_type": "误区纠正型：痛点开场 → 常见误区 → 真实原因 → 行动建议 → CTA",
    "abstracted_hook_type": "反常识痛点钩子",
    "abstracted_emotional_curve": ["焦虑", "被说中", "理解原因", "获得方法", "愿意行动"],
    "abstracted_conversion_path": ["痛点共鸣", "专业解释", "案例信任", "低门槛 CTA"],
    "source_elements_excluded": ["原视频健身话题", "原创作者口头禅", "原封面结构"]
  },
  "storyboard_plan": [
    {
      "beat_id": "beat_01",
      "time_range_sec": "0-3",
      "purpose": "开场钩子",
      "message": "指出老板做短视频没获客的核心焦虑",
      "visual_intent": "老板办公室口播，画面干净，不模仿源视频场景",
      "script_intent": "用反常识句式指出问题不在播放量",
      "transition": "直接进入误区解释",
      "risk_notes": []
    },
    {
      "beat_id": "beat_02",
      "time_range_sec": "4-25",
      "purpose": "误区拆解",
      "message": "拆解只追热点、只堆产量、只看播放量三个误区",
      "visual_intent": "口播 + 简单文字条，不使用源视频字幕样式",
      "script_intent": "给出老板能听懂的业务解释",
      "transition": "从误区转入方法",
      "risk_notes": ["避免保证获客"]
    },
    {
      "beat_id": "beat_03",
      "time_range_sec": "26-50",
      "purpose": "方法提供",
      "message": "给出选题、信任、案例、CTA 四段式",
      "visual_intent": "可展示自有流程图或白板",
      "script_intent": "输出可执行框架",
      "transition": "引导领取清单",
      "risk_notes": []
    },
    {
      "beat_id": "beat_04",
      "time_range_sec": "51-60",
      "purpose": "安全 CTA",
      "message": "评论/私信领取内容诊断清单",
      "visual_intent": "老板正面口播 + 简洁字幕",
      "script_intent": "低门槛行动，不承诺结果",
      "transition": "结束",
      "risk_notes": ["禁止保证转化"]
    }
  ],
  "deliverable_intents": {
    "scripts": {
      "count": 5,
      "variants": ["老板口播版", "案例拆解版", "避坑清单版", "反常识观点版", "服务流程版"],
      "must_include": ["企业服务", "短视频获客", "信任路径", "安全 CTA"],
      "must_avoid": ["保证获客", "复用源视频句子"]
    },
    "titles": {
      "count": 10,
      "title_angles": ["痛点", "反常识", "避坑", "清单", "案例"],
      "forbidden_patterns": ["原视频标题句式", "绝对化承诺"]
    },
    "cover_suggestions": {
      "count": 3,
      "cover_angles": ["老板焦虑", "误区对比", "四段式框架"],
      "visual_constraints": ["不使用原封面构图", "不使用原 Logo", "不使用源视频人物"]
    },
    "video_prompts": {
      "count": 3,
      "model_agnostic_blueprints": ["办公室老板口播", "白板框架讲解", "客户案例场景化说明"],
      "forbidden_visual_elements": ["源视频人物", "源视频场景", "源视频品牌符号"]
    }
  },
  "risk_constraints": {
    "risk_level": "R1",
    "non_replicable_parts": ["原台词", "原封面", "原人物", "原字幕样式"],
    "forbidden_elements": ["仿脸", "仿声", "源视频 Logo", "原场景复刻"],
    "forbidden_claims": ["保证获客", "保证成交", "百分百转化"],
    "similarity_guardrails": {
      "max_text_similarity": 0.25,
      "max_title_similarity": 0.2,
      "max_structure_similarity": 0.65,
      "direct_quote_allowed": false
    },
    "required_disclaimers": ["内容由 AI 辅助生成，请结合实际业务审核后使用"],
    "review_policy": "warn"
  },
  "quality_targets": {
    "minimum_originality_score": 80,
    "minimum_relevance_score": 80,
    "minimum_actionability_score": 75,
    "minimum_schema_completeness_score": 95
  },
  "status": "approved",
  "created_at": "2026-05-25T10:00:00Z",
  "updated_at": "2026-05-25T10:03:00Z"
}
```

---

## 7. 异常与失败处理

### 7.1 输入异常

| 异常 | 触发条件 | 处理策略 | 积分处理 | 用户提示 |
|---|---|---|---|---|
| 缺少 Content DNA | `content_dna_id` 不存在或状态未通过 | 不创建 IR 任务 | 不冻结/返还 | “上游拆解结果未完成，请稍后重试” |
| Content DNA 低置信度 | `confidence_score < 0.6` | 降级生成或转人工复核 | 按实际消耗 | “视频拆解置信度较低，结果可能需要人工确认” |
| 用户画像不足 | 行业/人设/产品/目标用户缺失 | 阻止生成，引导补全 | 不冻结 | “请先补全账号画像” |
| 授权缺失 | 未确认素材来源与使用边界 | 阻止生成 | 不冻结 | “请先确认素材授权与原创化规则” |
| 高风险意图 | 用户要求复刻/克隆/仿脸/仿声/搬运 | R4 拦截 | 不扣费或返还未消耗 | “该请求不符合原创化与合规要求” |

### 7.2 生成异常

| 异常 | 处理策略 | 最大重试 | 降级方案 |
|---|---|---:|---|
| 模型超时 | 换模型/重试/排队 | 2 | 输出结构参考，不生成完整输出包 |
| JSON 解析失败 | 调用 JSON repair Prompt | 2 | 转人工复核或失败返还 |
| Schema 校验失败 | 字段补全 + repair | 2 | 失败返还未消耗积分 |
| 原创化不足 | 调用 P05 重写 | 2 | 转人工复核 |
| 风险过高 | 阻断或人工复核 | 0 | 只展示拆解报告，不输出脚本 |
| 输出包部分失败 | 已成功部分可用，失败部分可重试 | 2 | 按部分成功扣费/返还 |

### 7.3 计费异常

| 异常 | 处理策略 |
|---|---|
| 积分冻结失败 | 不创建任务，提示余额不足或系统繁忙 |
| 模型已消耗但 IR 失败 | 按失败规则返还用户积分，后台记录成本损失 |
| IR 成功但输出包失败 | 只扣 IR 基础成本或按产品策略扣部分积分；输出包部分返还 |
| 重试导致重复扣费 | 使用 `idempotency_key` 和 `credit_reservation_id` 防重复扣费 |
| 人工复核阻断 | 返还未消耗积分，已消耗成本按运营策略处理 |

### 7.4 数据与权限异常

| 异常 | 处理策略 |
|---|---|
| 用户访问他人 IR | 返回 403，记录安全审计日志 |
| 后台人员越权查看 | RBAC 拦截，记录审计 |
| IR 版本丢失 | 从 `creative_ir_versions` 回放最近版本 |
| 删除请求 | 软删除 + 资产生命周期清理 + 审计日志 |
| 投诉侵权 | 冻结相关输出展示，创建人工复核单 |

---

## 8. 风控与合规

### 8.1 合规原则

1. **授权前置**：未确认素材来源、使用边界和原创化规则，不允许生成 Creative IR。
2. **AIGC 标识**：用户端结果页、导出报告和视频 Prompt 输出中必须包含 AI 辅助生成提示。
3. **不可复用继承**：D07 的 `non_replicable_parts` 必须无条件写入 IR 的 `risk_constraints`。
4. **禁止身份克隆**：任何仿脸、仿声、仿公众人物、仿特定创作者身份的请求必须拦截。
5. **禁止未授权素材复用**：IR 不得建议复用原视频画面、Logo、配乐、人物、字幕、封面。
6. **高风险行业谨慎生成**：医疗、金融、法律、教育承诺、招商收益等行业必须启用更严格 CTA 和免责声明。
7. **投诉删除机制**：用户或权利人投诉后，必须支持暂停展示、人工复核、删除输出、记录审计。

### 8.2 风险分级

| 等级 | 示例 | 系统动作 |
|---|---|---|
| R0 | 普通企业服务口播、无侵权信号 | 自动通过 |
| R1 | 普通营销夸张、低置信度、轻微相似结构 | 提醒 + 生成 |
| R2 | 标题/脚本相似度偏高、CTA 偏激 | 自动重写 |
| R3 | 公众人物、商标、医疗金融法律结论、教育承诺、疑似版权元素 | 人工复核 |
| R4 | 仿脸仿声、搬运、克隆、未授权复用、违法违规营销、绕过平台审核 | 阻断 |

### 8.3 相似度控制

MVP1 的相似度控制为“规则 + 模型评估 + 人工抽检”混合策略。

| 维度 | MVP1 阈值建议 | 处理 |
|---|---:|---|
| 连续文本重合 | 不允许连续 8 个以上汉字重合 | R2 重写 |
| 标题相似度 | ≤ 0.20 | 超过则重写 |
| 脚本文本相似度 | ≤ 0.25 | 超过则重写 |
| 结构相似度 | ≤ 0.65 | 超过则增加结构变化 |
| 视觉元素相似 | 不得复用人物/Logo/场景/封面 | R3/R4 |
| CTA 风险 | 不得保证收益、疗效、结果 | R2-R4 |

### 8.4 人工复核机制

触发人工复核的条件：

1. `risk_level = R3`。
2. 内容涉及公众人物、明星、影视、综艺、商标、品牌滥用。
3. 内容涉及医疗、金融、法律结论性建议。
4. 输出相似度连续两次重写仍不通过。
5. 用户投诉或第三方投诉。
6. 后台抽检命中严重问题。

人工复核动作：

| 动作 | 结果 |
|---|---|
| 通过 | 标记 `manual_approved`，允许输出 |
| 要求重写 | 写入重写意见，进入 `creative_ir.rewrite` |
| 阻断 | 不输出，返还未消耗积分，记录原因 |
| 删除 | 删除或隐藏相关输出，记录审计 |
| 补偿 | 根据 D20/D23 策略返还积分或客服处理 |

### 8.5 投诉删除机制

1. 用户端和导出报告必须提供投诉/删除入口。
2. 投诉后相关 `DeliverablePack` 状态设为 `under_review`。
3. 后台创建 `manual_review_ticket`。
4. 复核通过后恢复展示；复核不通过则删除/隐藏并记录审计。
5. 若涉及权利人投诉，保留最小必要证据和处理记录，按数据治理文档执行留存与删除。

---

## 9. 测试与验收标准

### 9.1 Schema 测试

| 测试项 | 标准 |
|---|---:|
| Creative IR JSON Schema 合法率 | ≥ 95% |
| 必填字段完整率 | ≥ 98% |
| 枚举字段合法率 | 100% |
| `non_replicable_parts` 继承率 | 100% |
| `risk_constraints` 存在率 | 100% |
| `is_mock` 标记准确率 | 100% |

### 9.2 AI 输出质量测试

| 指标 | MVP1 内测门槛 | 商业上线门槛 |
|---|---:|---:|
| 原创化评分 ≥ 80 的样本占比 | ≥ 75% | ≥ 85% |
| 用户画像适配评分 ≥ 80 | ≥ 75% | ≥ 85% |
| 脚本可用率 | ≥ 60% | ≥ 70% |
| 标题可用率 | ≥ 65% | ≥ 75% |
| 封面建议可用率 | ≥ 60% | ≥ 70% |
| 视频 Prompt 可执行率 | ≥ 60% | ≥ 75% |
| 高风险标签继承准确率 | ≥ 90% | ≥ 95% |
| 严重洗稿/高相似问题率 | ≤ 3% | ≤ 1% |

### 9.3 风控测试

必须覆盖以下样本：

1. 普通企业 IP 口播。
2. 本地生活门店探店。
3. 知识 IP 干货。
4. 强字幕视频。
5. 高相似标题诱导。
6. 用户要求“照着写”。
7. 用户要求“复刻同款”。
8. 用户要求仿脸/仿声。
9. 含 Logo/品牌/商标画面。
10. 医疗疗效承诺。
11. 金融收益承诺。
12. 教育保过承诺。
13. 招商加盟收益承诺。
14. 公众人物/明星内容。
15. 影视综艺混剪内容。

### 9.4 工程测试

| 测试类型 | 必测内容 |
|---|---|
| 单元测试 | 字段映射、原创化等级选择、风险等级映射、状态机 |
| API 测试 | 生成、查询、重写、输出包生成、后台诊断 |
| 队列测试 | 重试、幂等、失败补偿、取消、人工复核 |
| 数据库测试 | 版本记录、软删除、权限隔离、索引性能 |
| 权限测试 | 用户只能访问自己的 IR，后台按角色查看 |
| 计费测试 | 冻结、扣费、部分扣费、返还、重复请求防重 |
| E2E 测试 | 上传 → Content DNA → Creative IR → 输出包 → 风控 → 扣费/返还 → 导出 |
| 回归测试 | Prompt 升级后固定样本质量不下降 |

### 9.5 上线验收门槛

D09 模块进入 MVP1 内测前必须满足：

1. 主流程 E2E 可跑通。
2. 100 条测试任务中 IR 生成成功率 ≥ 85%。
3. Schema 合法率 ≥ 95%。
4. 风险继承准确率 ≥ 90%。
5. 积分扣费/返还幂等准确率 100%。
6. 后台可查每次 IR 生成的输入、输出、模型、Prompt、成本、状态、风险和版本。
7. 所有 mock 输出生产默认关闭并可识别。
8. 至少 30 条 Prompt 回归样本通过。
9. 人工复核流程可用。
10. 投诉删除入口可用。

---

## 10. AIcoding 研发任务拆分

### 10.1 任务总览

| 任务编号 | 任务名称 | 负责 Agent | 优先级 | 依赖 | 交付物 |
|---|---|---|---:|---|---|
| D09-T01 | Creative IR Schema 与类型定义 | 后端/AI Agent | P0 | D07 | JSON Schema、TS 类型、枚举、校验器 |
| D09-T02 | Content DNA 到 IR 字段映射服务 | 后端/AI Agent | P0 | T01 | 映射函数、单元测试、低置信度处理 |
| D09-T03 | 原创化强度与重构规则引擎 | AI/后端 Agent | P0 | T01/T02 | 规则引擎、风险触发、默认等级策略 |
| D09-T04 | Creative IR 生成 Prompt 与模型调用 | Prompt/后端 Agent | P0 | T01/T03/D10 草案 | P04 Prompt、模型调用、JSON repair |
| D09-T05 | Creative IR 数据库与版本管理 | 后端 Agent | P0 | T01 | migration、repository、版本表 |
| D09-T06 | Creative IR API | 后端 Agent | P0 | T05 | 生成、查询、重写、诊断 API |
| D09-T07 | Creative IR 队列与状态机 | 后端 Agent | P0 | T04/T06/D17 草案 | 队列任务、重试、幂等、失败补偿 |
| D09-T08 | IR 阶段风控与质量检查 | AI/风控/后端 Agent | P0 | T04/D13 草案 | P10 Prompt、质量报告、风险动作 |
| D09-T09 | IR 到输出包生成 | Prompt/后端 Agent | P0 | T08 | 脚本、标题、封面建议、视频 Prompt 生成器 |
| D09-T10 | 积分计费与成本联动 | 后端 Agent | P0 | T07/D20 草案 | 冻结、扣费、返还、成本日志 |
| D09-T11 | 前端结果页 Creative IR 摘要 | 前端 Agent | P0 | T06/T09/D06 | ViewModel、组件、风险提示、版本提示 |
| D09-T12 | 运营后台 Creative IR 诊断页 | 后台/前端 Agent | P0 | T06/T08/T10 | 输入输出、模型成本、风险、复核操作 |
| D09-T13 | 测试与 Benchmark 接入 | 测试 Agent | P0 | 全部 | 单测、API、队列、Prompt 回归、E2E |
| D09-T14 | 部署、监控、审计与交接 | DevOps/后端 Agent | P0 | 全部 | 环境变量、日志、指标、告警、交接说明 |

### 10.2 每个任务的验收口径

#### D09-T01：Creative IR Schema 与类型定义

验收标准：

1. `creative_ir_v1.0.schema.json` 可被标准 JSON Schema 校验器加载。
2. TypeScript 类型与 JSON Schema 字段一致。
3. 枚举值覆盖状态、风险等级、原创化等级、输出类型。
4. 至少 10 个合法样本和 10 个非法样本测试通过。
5. Schema 文件有版本号、变更记录和回滚说明。

#### D09-T02：Content DNA 到 IR 字段映射服务

验收标准：

1. 能读取 Content DNA P0 字段并映射到 IR 输入。
2. `non_replicable_parts` 继承率 100%。
3. 低置信度 Content DNA 触发降级或复核。
4. 字段缺失时返回明确错误码。
5. 单元测试覆盖主要首发行业。

#### D09-T03：原创化强度与重构规则引擎

验收标准：

1. 默认原创化等级为 L3。
2. 高风险输入自动升级原创化强度或拦截。
3. 用户请求复刻/克隆/洗稿时不会生成高相似内容。
4. 规则可配置，后台可查看当前阈值。
5. 风险策略变更有版本记录。

#### D09-T04：Creative IR 生成 Prompt 与模型调用

验收标准：

1. Prompt 模板进入 Prompt Registry。
2. 模型调用记录 Prompt 版本、模型、成本、延迟、输入摘要、输出摘要。
3. 输出 JSON 可通过 Schema 校验。
4. JSON repair 可用。
5. 模型失败有重试和降级。

#### D09-T05：数据库与版本管理

验收标准：

1. migration 可执行并可回滚。
2. IR 每次生成和重写均创建版本。
3. 软删除可用。
4. 用户隔离和后台权限可用。
5. 索引覆盖 `task_id`、`user_id`、`content_dna_id`、`status`。

#### D09-T06：API

验收标准：

1. 生成、查询、重写、输出包生成 API 可用。
2. 幂等键防重复任务。
3. 错误码清晰。
4. 权限校验完整。
5. OpenAPI 文档或接口说明完整。

#### D09-T07：队列与状态机

验收标准：

1. 队列任务可重试、可取消、可失败补偿。
2. 状态机不可非法跳转。
3. 任务卡死有超时处理。
4. 后台可查看每个步骤。
5. 失败不会造成积分丢失。

#### D09-T08：风控与质量检查

验收标准：

1. 输出质量报告。
2. 风险等级可触发 pass/rewrite/manual_review/block。
3. 相似度过高自动重写。
4. R3 创建人工复核单。
5. R4 阻断并返还未消耗积分。

#### D09-T09：IR 到输出包生成

验收标准：

1. 生成 5 条原创脚本、10 个标题、3 个封面建议、3 条视频 Prompt。
2. 输出包包含风险提示和 AIGC 标识。
3. 输出可复制、可导出、可反馈。
4. 不生成仿脸仿声或高相似内容。
5. 部分失败可重试并按规则返还积分。

#### D09-T10：积分计费与成本联动

验收标准：

1. IR 任务创建前完成积分预估和冻结。
2. 成功后扣费，失败后返还。
3. 部分成功按规则扣费。
4. 模型成本写入后台统计。
5. 幂等测试 100% 通过。

#### D09-T11：前端结果页

验收标准：

1. 用户能看到原创化策略摘要。
2. 用户能看到风险提示和 AIGC 标识。
3. 输出包展示清晰，可复制、导出、反馈。
4. 失败、复核、阻断状态有清晰提示。
5. Mock 数据有显著标记。

#### D09-T12：运营后台

验收标准：

1. 后台能查看 IR 输入、输出、版本、风险、成本。
2. 后台能处理人工复核。
3. 后台能查看投诉与删除状态。
4. 后台操作记录审计日志。
5. 敏感字段按权限脱敏。

#### D09-T13：测试与 Benchmark

验收标准：

1. 单元/API/队列/E2E 测试通过。
2. 至少 30 条首发行业样本回归通过。
3. 高风险样本拦截准确率达到门槛。
4. Prompt 升级有回归报告。
5. CI 中可运行核心测试。

#### D09-T14：部署、监控、审计与交接

验收标准：

1. 环境变量、模型配置、队列配置、Feature Flag 完整。
2. 关键指标可监控：生成成功率、Schema 合法率、重写率、复核率、阻断率、成本、耗时。
3. 错误日志可追踪到 task_id / creative_ir_id / model_call_log_id。
4. 交接文档包含改动文件、测试命令、风险、待办。
5. 生产默认关闭 mock。

---

## 11. AIcoding 执行提示词

### 11.1 总执行提示词

```text
你现在是本项目的资深 AIcoding 执行 Agent，负责实现 D09《Creative IR Schema 与原创化重构规则文档》。

必须先读取并遵守：
1. D00《AIcoding 研发总控文档 / 项目上下文总索引》
2. D07《Content DNA Schema 与拆解报告标准文档》
3. D08《视频处理与多模态理解工作流文档》
4. 当前 D09 文档

项目硬约束：
- 本项目是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具。
- MVP1 只做视频拆解 + 原创脚本生成，不做完整视频生成、自动发布、账号全量抓取。
- 所有 AI 输出必须 Schema 化、可测试、可追踪、可风控、可计费。
- 不得用 mock 冒充真实能力；如使用 mock，必须 is_mock=true，页面和后台显著标记，生产默认关闭。
- 不得跳过任务日志、模型成本、积分扣费/返还、风控、测试和交接说明。

你的任务：
按 D09-T01 至 D09-T14 拆分研发。每完成一个任务，必须输出：
1. 已读取文档
2. 本次实现范围
3. 修改/新增文件清单
4. 数据库/API/队列/Prompt/前端/后台影响
5. 测试命令与测试结果
6. 未完成项与风险
7. 是否需要回写 D09/D16/D17/D20/D21

禁止：
- 不得实现复刻、克隆、洗稿、仿脸、仿声、绕过平台审核能力。
- 不得直接从 Content DNA 生成终稿而绕过 Creative IR。
- 不得跳过 risk_constraints。
- 不得跳过积分幂等。
- 不得隐藏失败或吞掉异常。
```

### 11.2 D09-T01 Schema 任务提示词

```text
请实现 D09-T01：Creative IR Schema 与类型定义。

输入文档：D00、D07、D08、D09。

交付要求：
1. 新增 creative_ir_v1.0 JSON Schema。
2. 新增 TypeScript 类型或后端 DTO 类型。
3. 新增枚举：OriginalizationLevel、RiskLevel、CreativeIRStatus、ReviewPolicy、DeliverableType。
4. 新增 Schema 校验工具函数。
5. 新增合法样本和非法样本测试。
6. 不实现业务生成逻辑，只做 Schema 与类型。

验收：
- Schema 合法，可加载。
- 类型与 Schema 字段一致。
- 单元测试通过。
- 输出修改文件清单与测试命令。
```

### 11.3 D09-T04 Prompt 与模型调用任务提示词

```text
请实现 D09-T04：Creative IR 生成 Prompt 与模型调用封装。

必须遵守：
- 不能复刻、搬运、洗稿、仿脸、仿声。
- 必须继承 non_replicable_parts 和 risk_flags。
- 默认使用 level_3_high_original_reconstruction。
- 输出必须符合 creative_ir_v1.0 Schema。

交付要求：
1. 建立 P04_CREATIVE_IR_GENERATION Prompt 模板。
2. 支持输入变量：task、creator_profile_snapshot、content_dna_summary、reusable_parts、non_replicable_parts、risk_flags、output_package_config、user_extra_brief。
3. 接入模型调用封装，记录 model_call_log。
4. 支持 JSON repair。
5. 失败时返回明确错误码，不扣除最终费用。
6. 增加 Prompt 回归样本。

验收：
- 固定样本生成 IR Schema 合法率 ≥ 95%。
- 模型调用日志包含 prompt_version、model、cost、latency、status。
- 错误分支测试通过。
```

### 11.4 D09-T08 风控质检任务提示词

```text
请实现 D09-T08：Creative IR 阶段风控与质量检查。

交付要求：
1. 实现 P10_IR_QUALITY_CHECK Prompt 或规则 + 模型混合检查。
2. 输出 schema_completeness_score、originality_score、relevance_score、actionability_score、similarity_risk_score、compliance_risk_level、findings、recommended_action。
3. recommended_action 必须支持 pass、rewrite、manual_review、block。
4. R2 自动进入 rewrite；R3 创建 manual_review_ticket；R4 阻断并触发积分返还。
5. 所有检查结果写入 creative_ir_quality_checks。

验收：
- 高风险样本召回率 ≥ 90%。
- non_replicable_parts 继承和检查率 100%。
- 风控状态前端和后台可见。
```

### 11.5 D09-T09 输出包生成任务提示词

```text
请实现 D09-T09：从 Creative IR 生成 MVP1 输出包。

输出包必须包含：
1. 5 条原创脚本。
2. 10 个标题。
3. 3 个封面建议。
4. 3 条模型无关视频生成 Prompt。

硬规则：
- 只能从 approved Creative IR 生成。
- 不得复用源视频台词、标题、封面、人物、Logo、场景、配乐。
- 必须显示 AIGC 标识和风险提示。
- 必须记录 Prompt 版本、模型调用、成本、质量检查。
- 部分失败必须可重试，积分按规则处理。

验收：
- 输出数量准确。
- JSON Schema 合法。
- 前端可展示/复制/导出。
- 风控检查可消费输出包。
```

### 11.6 D09-T13 测试任务提示词

```text
请实现 D09-T13：D09 模块测试与 Benchmark 接入。

测试范围：
1. Schema 单元测试。
2. Content DNA 到 IR 映射测试。
3. 原创化等级规则测试。
4. 风控分支测试。
5. API 测试。
6. 队列重试和幂等测试。
7. 积分冻结、扣费、返还测试。
8. E2E：Content DNA → Creative IR → 输出包 → 风控 → 计费。
9. Prompt 回归样本至少 30 条。

验收：
- CI 可运行。
- 失败时输出明确原因。
- 生成测试报告。
- 标记仍未覆盖的风险。
```

---

## 12. 不做事项

D09 MVP1 坚决不做：

1. 不做对标视频 1:1 复刻。
2. 不做洗稿、同义词替换式改写、高相似脚本生成。
3. 不做仿脸、仿声、仿公众人物、仿特定账号身份。
4. 不做未授权素材复用建议。
5. 不做自动下载或批量抓取对标账号作品。
6. 不做自动发布。
7. 不做完整视频生成、视频片段生成、拼接、字幕合成、MP4 导出。
8. 不做绕过平台规则、搬运检测、版权检测规避能力。
9. 不做高风险医疗、金融、法律结论性内容自动生成。
10. 不做招商加盟、教育培训等收益/保过承诺。
11. 不做账号级批量 Creative IR 默认主流程。
12. 不做用户可直接选择“低原创高相似”的产品能力。
13. 不做不可追踪、不可版本化、不可风控的临时 Prompt 输出。
14. 不做无积分记录、无模型成本记录、无任务日志的 AI 生成。
15. 不允许 mock 数据进入生产默认路径。

---

## 13. 待确认问题

| 编号 | 问题 | 影响 | 建议责任人 | 优先级 |
|---|---|---|---|---:|
| Q01 | MVP1 默认目标平台是否只先做抖音/视频号，还是同时支持小红书/TikTok 枚举 | 影响平台适配 Prompt 和测试样本 | 产品 Owner | P0 |
| Q02 | Creative IR 生成一次的积分价格、输出包积分价格、失败返还比例 | 影响 D20 商业化和 API 计费 | 商业化负责人 | P0 |
| Q03 | L2 原创化是否允许用户可见选择，还是后台灰度 | 影响产品页面与合规风险 | 产品 + 合规 | P0 |
| Q04 | 文本相似度和结构相似度的最终阈值 | 影响 D13 风控 | AI/风控负责人 | P0 |
| Q05 | 高风险行业名单和后台配置项 | 影响行业风控 | 合规负责人 | P0 |
| Q06 | 人工复核 SLA 和客服补偿规则 | 影响用户体验和 D23 运营 SOP | 运营负责人 | P1 |
| Q07 | Creative IR 是否允许用户手动编辑 | 影响版本管理和前端复杂度 | 产品 Owner | P1 |
| Q08 | 视频 Prompt 是否需要区分 Veo/Seedance/可灵等模型专用格式 | MVP1 暂不做，影响 D10/D11 | AI 架构负责人 | P1 |
| Q09 | Prompt 回归样本由谁维护、样本版权如何处理 | 影响 D12 Benchmark | AI 产品负责人 | P0 |
| Q10 | 投诉删除后的数据留存周期 | 影响 D18/D21/D22 | 法务/合规 | P0 |
| Q11 | 是否支持企业团队空间共享 IR | MVP1 不做，影响后续团队协作 | 产品 Owner | P2 |
| Q12 | 是否将高表现 IR 沉淀为用户专属模板 | MVP4 相关 | 增长负责人 | P2 |

---

## 14. 最终 Checklist

### 14.1 产品与范围 Checklist

- [ ] D09 明确 Creative IR 是原创化中间层，不是复刻指令。
- [ ] MVP1 只支持单条视频 Content DNA → Creative IR → 原创脚本/标题/封面/视频 Prompt。
- [ ] 默认原创化等级为 L3 高原创重构。
- [ ] 用户不能选择高相似复刻模式。
- [ ] 不做完整视频生成、自动发布、账号级自动抓取。

### 14.2 Schema 与 Prompt Checklist

- [ ] `CreativeIR` JSON Schema 已定义。
- [ ] `OriginalizationPlan` 已定义。
- [ ] `StoryboardPlan` 已定义。
- [ ] `DeliverableIntents` 已定义。
- [ ] `RiskConstraintSet` 已定义。
- [ ] P04-P10 Prompt 模板已定义。
- [ ] Prompt 输出必须 JSON Schema 校验。
- [ ] JSON repair 与失败回退已定义。

### 14.3 工程 Checklist

- [ ] `creative_irs` 表草案已定义。
- [ ] `creative_ir_versions` 表草案已定义。
- [ ] `creative_ir_generation_jobs` 表草案已定义。
- [ ] `creative_ir_quality_checks` 表草案已定义。
- [ ] `creative_ir_events` 表草案已定义。
- [ ] 生成/查询/重写/输出包/后台诊断 API 已定义。
- [ ] 队列和状态机已定义。
- [ ] 幂等键和失败补偿已定义。
- [ ] 权限和审计已定义。

### 14.4 商业化 Checklist

- [ ] IR 任务创建前必须积分预估和冻结。
- [ ] 成功后扣费。
- [ ] 失败后返还未消耗积分。
- [ ] 部分成功扣费规则已定义。
- [ ] 模型成本记录到 `model_call_logs`。
- [ ] 后台可查看成本、毛利、失败成本和补偿记录。

### 14.5 风控合规 Checklist

- [ ] 授权确认是生成前置条件。
- [ ] AIGC 标识进入用户端和导出结果。
- [ ] `non_replicable_parts` 100% 继承。
- [ ] R0-R4 风险分级已定义。
- [ ] pass/rewrite/manual_review/block 动作已定义。
- [ ] 投诉删除机制已定义。
- [ ] 人工复核触发条件已定义。
- [ ] 仿脸、仿声、搬运、洗稿、绕过审核被明确禁止。

### 14.6 测试验收 Checklist

- [ ] Schema 合法率门槛 ≥ 95%。
- [ ] 必填字段完整率门槛 ≥ 98%。
- [ ] 风险继承准确率 ≥ 90%。
- [ ] 原创化评分通过门槛已定义。
- [ ] Prompt 回归样本至少 30 条。
- [ ] 单元/API/队列/E2E/计费/权限/后台测试已定义。
- [ ] Mock 生产默认关闭。
- [ ] 后台可追踪输入、输出、模型、Prompt、成本、风险和版本。

---

## 附录 A：D09 推荐目录结构

```text
/apps
  /web
    /components/creative-ir
      CreativeIRSummaryCard.tsx
      OriginalizationPlanCard.tsx
      RiskConstraintNotice.tsx
      DeliverablePackViewer.tsx
  /admin
    /creative-ir
      CreativeIRDiagnosticsPage.tsx

/packages
  /schemas
    creative_ir_v1.0.schema.json
  /types
    creative-ir.ts
  /ai
    /prompts
      P04_CREATIVE_IR_GENERATION.md
      P05_ORIGINALIZATION_REWRITE.md
      P06_IR_TO_SCRIPT.md
      P07_IR_TO_TITLE.md
      P08_IR_TO_COVER.md
      P09_IR_TO_VIDEO_PROMPT.md
      P10_IR_QUALITY_CHECK.md
    /creative-ir
      generateCreativeIR.ts
      validateCreativeIR.ts
      rewriteCreativeIR.ts
      generateDeliverables.ts
      checkCreativeIRQuality.ts
  /backend
    /modules/creative-ir
      creative-ir.controller.ts
      creative-ir.service.ts
      creative-ir.repository.ts
      creative-ir.queue.ts
      creative-ir.billing.ts
      creative-ir.risk.ts
      creative-ir.events.ts
  /tests
    /creative-ir
      schema.test.ts
      mapping.test.ts
      risk.test.ts
      api.test.ts
      queue.test.ts
      e2e.test.ts
```

## 附录 B：D09 错误码草案

| 错误码 | 含义 | HTTP |
|---|---|---:|
| `CREATIVE_IR_CONTENT_DNA_NOT_READY` | Content DNA 未完成 | 409 |
| `CREATIVE_IR_PROFILE_INCOMPLETE` | 用户画像不完整 | 400 |
| `CREATIVE_IR_AUTH_REQUIRED` | 授权确认缺失 | 403 |
| `CREATIVE_IR_HIGH_RISK_BLOCKED` | 高风险拦截 | 403 |
| `CREATIVE_IR_SCHEMA_INVALID` | IR Schema 校验失败 | 422 |
| `CREATIVE_IR_MODEL_TIMEOUT` | 模型超时 | 504 |
| `CREATIVE_IR_JSON_REPAIR_FAILED` | JSON 修复失败 | 422 |
| `CREATIVE_IR_ORIGINALITY_TOO_LOW` | 原创化不足 | 409 |
| `CREATIVE_IR_MANUAL_REVIEW_REQUIRED` | 需要人工复核 | 202 |
| `CREATIVE_IR_CREDIT_RESERVATION_FAILED` | 积分冻结失败 | 402 |
| `CREATIVE_IR_DUPLICATE_REQUEST` | 重复请求 | 409 |
| `CREATIVE_IR_ACCESS_DENIED` | 无权限 | 403 |

## 附录 C：D09 变更记录模板

| 版本 | 日期 | 更新人 | 更新原因 | 主要变更 | 影响文档 | 是否同步 Agent |
|---|---|---|---|---|---|---|
| V1.0 | 2026-05-25 | AI 产品总控 | 初版生成 | 建立 Creative IR Schema、原创化规则、Prompt、API、任务、风控和验收 | D10/D13/D16/D17/D20/D21 | 是 |

