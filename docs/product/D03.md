# D03｜MVP 范围定义与版本路线图文档

> 文档编号：D03  
> 文档名称：MVP 范围定义与版本路线图文档  
> 文档版本：V1.0  
> 生成日期：2026-05-22  
> 根上下文：D00《AIcoding 研发总控文档 / 项目上下文总索引》  
> 前置文档：D00、D01、D02  
> 后置文档：D04、D05、D06、D07、D08、D09、D10、D11、D12、D14、D15、D16、D17、D20、D21、D23、D24  
> 当前优先版本：MVP 1「视频拆解 + 原创脚本生成」  
> 文档性质：版本边界 + MVP 范围 + 研发优先级 + 路线图 + AIcoding 执行约束文档  
> 核心定位：本文件不是创业建议，而是用于约束产品范围、研发边界、积分商业化、合规底线、任务拆分和版本验收的可执行工程文档。

---

## 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 文档编号 | D03 |
| 文档名称 | MVP 范围定义与版本路线图文档 |
| 所属项目 | AI 短视频爆款增长引擎 |
| 当前主线 | MVP1：视频拆解 + 原创脚本生成 |
| 最高约束 | 与 D00 冲突时，以 D00 为准；与 D01 商业闭环冲突时，回写 D01；与 D02 首发用户冲突时，回写 D02 |
| 适用对象 | 创始人、产品负责人、AI 产品经理、技术产品经理、研发 Agent、Prompt Agent、测试 Agent、运营后台负责人 |
| 更新触发条件 | MVP 范围变化、P0/P1/P2 优先级变化、商业化策略变化、合规策略变化、模型能力变化、上线验收不通过 |
| 输出形态 | Markdown 文档 + 可被 AIcoding 直接执行的任务拆分和提示词模板 |

---

## 2. 文档目标

D03 要把产品从“想做的很多功能”收敛成“当前版本可研发、可测试、可上线、可收费、可风控”的清晰范围。它必须解决：

1. MVP0、MVP1、MVP2、MVP3、MVP4 分别做什么、不做什么。
2. 当前为什么优先做 MVP1，而不是账号级分析、完整视频生成或数据诊断。
3. MVP1 的 P0/P1/P2 功能边界。
4. 每个核心模块的输入、输出、流程、字段、状态、异常、风控、测试和验收。
5. AI 能力的最小 Schema、Prompt、评测、失败回退和质量标准。
6. 工程实现对数据库、API、任务队列、日志、权限、测试和部署的影响。
7. 商业化的积分、扣费、失败返还、成本记录和后台统计。
8. 合规的授权、AIGC 标识、风险分级、拦截策略、人工复核和投诉删除机制。
9. AIcoding 可以直接执行的研发任务拆分与提示词。

本文档不替代 D04 总 PRD、D05 模块边界、D06 页面原型、D07 Content DNA、D08 视频处理、D09 Creative IR、D10 Prompt、D12 风控、D16 数据库/API、D17 任务队列、D20 商业化、D21 合规。D03 只规定“版本做什么、做到什么程度、怎样验收”。

---

## 3. 与 D00 及其他文档的关系

### 3.1 D00 对 D03 的硬约束

| D00 约束 | D03 落地方式 |
|---|---|
| 产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具 | 所有版本只做“结构学习 + 原创化重构 + 质检风控”，高相似复制、仿脸仿声、搬运下载列入禁止事项 |
| 当前优先 MVP1：视频拆解 + 原创脚本生成 | D03 将 MVP1 定义为当前研发主线，MVP2-MVP4 只做字段和接口预留 |
| 不得自行扩大第一版范围 | D03 设置 P0/P1/P2、Feature Flag、Scope Gate 和不做清单 |
| 必须记录任务、成本、日志、风控和测试 | D03 将 TaskStepLog、ModelCallLog、CreditTransaction、RiskCheckReport、QualityCheckReport 列为 P0 |
| 每个模块必须可独立开发、独立测试、独立验收、独立交接 | D03 输出 AIcoding 任务拆分和每个任务的验收口径 |

### 3.2 D01 对 D03 的输入

D01 已明确 MVP1 首发商业闭环：上传视频 → 授权确认 → 积分预估与冻结 → 视频处理与多模态拆解 → Content DNA / 报告 / 原创脚本 / 标题 / 封面 / Prompt → 风控 → 成功扣费 / 失败返还 → 后台记录任务、成本、质量、风险和转化。D03 将这个闭环拆成研发范围、数据对象、API、任务状态和测试标准。

### 3.3 D02 对 D03 的输入

D02 已明确首发用户优先级：企业 IP / 老板 IP、本地生活商家为 MVP1 P0 用户，知识 IP / 教培、MCN / 内容工作室为 P1 试点。D03 默认 MVP1 的 Prompt、页面、风险和样本集优先围绕这两类 P0 用户设计。

### 3.4 D03 对后续文档的输出

| 后续文档 | D03 输出 |
|---|---|
| D04 | MVP1 需求边界、用户流程、输入输出、异常和验收 |
| D05 | 模块优先级、独立研发顺序、交付依赖 |
| D06 | 页面清单、状态、按钮范围、空/加载/失败/积分不足/风控拦截状态 |
| D07 | MVP1 最小 Content DNA 字段集合 |
| D08 | MVP1 视频格式、大小、时长、抽帧、ASR、OCR、镜头切分最低要求 |
| D09 | MVP1 原创脚本与 Prompt 生成所需最小 Creative IR |
| D10 | MVP1 必须实现的 Prompt 类型、输出 Schema、回归评测 |
| D12/D21 | 风险等级、相似度阈值、拦截动作、人工复核和投诉删除 |
| D16/D17 | 数据表、API、任务状态、队列、日志、资产生命周期 |
| D20 | 积分包、免费额度、扣费、返还、订单、毛利统计 |
| D23 | AIcoding 执行任务和提示词 |
| D24 | 版本完成度、已知问题、后续路线图、交接清单 |

---

## 4. 核心结论

### 4.1 当前推荐版本

当前必须优先做：

> **MVP1：视频拆解 + 原创脚本生成。**

MVP1 的定位是：用户上传一条合规对标视频，系统在授权确认和风控前置后，通过视频处理、多模态理解、Content DNA 拆解、原创化重构和 Prompt 生成，输出一份可读、可导出、可计费、可复用、可风控的爆款拆解与原创内容包。

### 4.2 MVP1 最小闭环

```text
注册/登录
→ 完成用户画像
→ 上传对标视频/输入视频链接
→ 授权确认
→ 意图与风险预检
→ 积分预估与冻结
→ 创建异步视频拆解任务
→ 视频元数据解析/抽帧/ASR/OCR/镜头切分
→ 多模态理解
→ 生成 Content DNA
→ 生成拆解报告
→ 输出可复用/不可复用清单
→ 生成原创脚本 5 条
→ 生成标题 10 条
→ 生成封面建议 3 条
→ 生成视频 Prompt 3 条
→ 质检与风控
→ 成功扣费/失败返还
→ 用户查看、复制、导出
→ 生成历史、成本日志、后台统计
```

### 4.3 MVP1 必须做的 P0

| 分类 | P0 功能 |
|---|---|
| 账号与画像 | 注册登录、用户身份、CreatorProfile、画像质量校验 |
| 合规前置 | 授权确认、禁止能力说明、用户意图风险预检 |
| 视频输入 | 上传视频、格式/大小/时长校验、文件安全检查 |
| 视频处理 | 元数据解析、抽帧、ASR、OCR、镜头切分最小版 |
| AI 理解 | 多模态理解、结构化输出、Schema 校验、失败重试 |
| 内容资产 | Content DNA、镜头拆解、爆款原因、可复用/不可复用 |
| 原创输出 | 原创脚本 5 条、标题 10 条、封面建议 3 条、视频 Prompt 3 条 |
| 风控质检 | 风险等级、质量评分、相似度风险、拦截/重写/人工复核 |
| 商业化 | 免费额度、积分预估、冻结、扣费、失败返还、流水 |
| 任务系统 | 异步任务状态、步骤日志、模型调用日志、失败原因 |
| 页面 | 上传、进度、结果、历史、积分、合规投诉入口 |
| 后台 | 用户、任务、成本、失败、风控、积分流水查看 |
| 测试 | 单元、API、队列、Prompt 回归、E2E、风控、计费幂等 |

### 4.4 MVP1 明确不做

1. 不做全平台自动抓取。
2. 不做自动下载对标账号全部作品。
3. 不做账号级全量分析默认主流程。
4. 不做完整视频生成、分镜片段生成、拼接、字幕合成和 MP4 导出。
5. 不做自动发布。
6. 不做仿脸、仿声、身份克隆。
7. 不做明星、影视、综艺、搬运号复刻。
8. 不做绕过平台规则或风控的能力。
9. 不做医疗疗效、金融收益、法律结论、招商收益承诺类自动生成。
10. 不做复杂视频编辑器、移动端 App、团队协作、模板市场、开放 API。
11. 不做完整 Prompt 进化和自动归因，仅预留数据字段。
12. 不用 mock 冒充真实能力；阶段性 mock 必须显著标记。

### 4.5 版本路线图总览

| 版本 | 名称 | 核心目标 | 核心交付 | 是否当前主线 |
|---|---|---|---|---:|
| MVP0 | 半自动服务验证 | 验证需求、交付物价值和付费意愿 | 人工 + AI 报告、脚本、Prompt、反馈 | 并行验证 |
| MVP1 | 视频拆解 + 原创脚本生成 | 建立产品化最小闭环 | 上传、拆解、Content DNA、脚本、标题、封面、Prompt、积分、风控、后台 | 是 |
| MVP2 | 对标账号分析 + 批量内容计划 | 从单条视频升级到账户级策略 | 账号导入、作品筛选、账号 DNA、选题矩阵、批量草稿 | 否，预留 |
| MVP3 | 视频生成 + 拼接 + 质检 | 从脚本/Prompt 升级为视频草稿 | 视频模型、分镜生成、片段管理、拼接、质检、导出 | 否，预留 |
| MVP4 | 发布数据诊断 + Prompt 进化 | 形成长期留存和增长闭环 | 数据回填、归因诊断、Prompt 进化、用户模板 | 否，预留 |
| V1.0 | 商业版增长工作台 | 可收费、可运维、可扩展 | 企业套餐、团队、后台、监控、工单、行业方案 | 否 |

---

## 5. 详细设计

### 5.1 MVP0：半自动服务验证

#### 目标

MVP0 用人工 + AI 验证用户是否愿意为“爆款拆解报告 + 原创脚本 + 标题/封面 + 视频 Prompt”付费，不要求完整自动化，但所有数据必须按未来产品结构沉淀，避免服务验证和产品研发脱节。

#### 输入

| 输入 | 是否必填 | 用途 |
|---|---:|---|
| 用户类型 | 是 | 判断首发行业 |
| 行业/子行业 | 是 | 用于脚本迁移 |
| 目标平台 | 是 | 决定风格和内容长度 |
| 人设定位 | 是 | 决定脚本口吻 |
| 产品/服务 | 是 | 决定转化路径 |
| 目标用户 | 是 | 决定痛点与钩子 |
| 内容目标 | 是 | 涨粉/获客/带货/私域/品牌 |
| 对标视频 | 是 | 拆解对象 |
| 授权确认 | 是 | 合规前置 |
| 禁用内容 | 否 | 生成约束 |
| 反馈问卷 | 是 | 评估 Aha 和付费意愿 |

#### 输出

| 输出物 | 数量 | 标准 |
|---|---:|---|
| 爆款拆解报告 | 1 | 包含钩子、结构、节奏、情绪、转化 |
| 可复用结构 | 1 | 只学习底层结构，不复制表达 |
| 不可复用风险 | 1 | 原素材、台词、人物、声音、商标等 |
| 原创脚本 | 5 | 适配用户行业、人设、产品 |
| 标题 | 10 | 不夸大、不虚假承诺 |
| 封面建议 | 3 | 不复刻原封面 |
| 视频 Prompt | 3 | 可复制到外部视频生成模型 |
| 反馈记录 | 1 | 脚本可用率、愿付费、复用意愿 |

#### 验收指标

| 指标 | 目标 |
|---|---:|
| 用户愿意提供真实对标视频比例 | ≥ 70% |
| 报告满意度 | ≥ 70% |
| 5 条脚本中用户认为可用数量 | ≥ 3 条 |
| 愿意付费比例 | ≥ 30% |
| 7 日内愿意二次使用比例 | ≥ 30% |
| “像洗稿/搬运”负向反馈占比 | ≤ 5% |
| 高风险请求识别率 | ≥ 90% |

#### MVP0 不做

不做完整 SaaS、不做自动账号抓取、不做完整视频生成、不做自动发布、不做批量任务、不做复杂后台、不做未授权素材使用、不做高相似复刻。

---

### 5.2 MVP1：视频拆解 + 原创脚本生成

#### 目标

建立可真实上线内测和收费的最小产品闭环：上传视频 → 自动处理 → 多模态拆解 → Content DNA → 原创脚本/标题/封面/Prompt → 风控 → 扣费/返还 → 导出与后台。

#### 输入字段

| 输入 | 字段名 | 必填 | 校验 |
|---|---|---:|---|
| 用户 ID | user_id | 是 | 已登录 |
| 用户画像 | creator_profile_id | 是 | 状态 active |
| 目标平台 | target_platform | 是 | 枚举 |
| 内容目标 | content_goal | 是 | 枚举 |
| 对标视频文件 | source_video_file | 是 | 格式、大小、时长、安全扫描 |
| 视频链接 | source_video_url | 否 | P1，可预留 |
| 授权确认 | authorization_ack | 是 | 必须主动勾选 |
| 原创化强度 | originality_level | 是 | 默认 high_reconstruction |
| 输出包 | output_package | 是 | light_report/deep_report/script_pack/prompt_pack/full_pack |
| 用户补充要求 | user_instruction | 否 | 必须做意图风险分类 |
| 禁用内容 | prohibited_content | 否 | 进入 Prompt 与风控 |

#### 输出物

| 输出物 | 优先级 | 说明 |
|---|---|---|
| 视频基础信息 | P0 | 时长、比例、分辨率、音轨、字幕/水印提示 |
| 视频处理结果 | P0 | 抽帧、ASR、OCR、镜头切分摘要 |
| Content DNA | P0 | 钩子、结构、情绪曲线、转化、爆款因子 |
| 镜头拆解表 | P0 | 时间段、画面、字幕、旁白、镜头目的 |
| 可复用清单 | P0 | 结构、心理触发点、节奏、转化路径 |
| 不可复用清单 | P0 | 原台词、人物、声音、音乐、Logo、独特剧情 |
| 风险提示 | P0 | 风险等级、原因、处理建议 |
| 原创脚本 | P0 | 5 条 |
| 标题 | P0 | 10 条 |
| 封面建议 | P0 | 3 条 |
| 视频 Prompt | P0 | 3 条，可复制 |
| 质量评分 | P0 | 完整性、可用性、风险 |
| 导出报告 | P0 | Markdown 优先，PDF P1 |
| 用户反馈 | P1 | 脚本可用数、是否愿付费、是否会再用 |

#### MVP1 出口条件

只有满足以下条件，才允许进入 MVP2：

1. 主流程连续 100 条测试任务成功率 ≥ 80%。
2. AI 输出 JSON Schema 合法率 ≥ 95%。
3. 高风险意图样本拦截准确率 ≥ 90%。
4. 积分冻结、扣费、返还在重试和并发下幂等。
5. 后台能追踪每个任务的成本、失败、风险和积分。
6. 至少 30 个真实样本完成内测反馈。
7. 脚本主观可用率 ≥ 60%。
8. 不做事项没有被页面、API、Prompt 或后端绕过。
9. D04-D17 的 MVP1 工程契约已补齐或明确待确认项。

---

### 5.3 MVP2：对标账号分析 + 批量内容计划

#### 目标

从“单条视频拆解”升级为“账号级内容策略”，但必须采用手动或授权导入，不做未授权大规模抓取。

#### 输入

用户画像、对标账号链接或账号 ID、手动/授权导入作品列表、多个视频样本、筛选条件。

#### 输出

对标账号基础画像、账号级 Content DNA、作品列表结构化、爆款作品 Top N、相对爆款倍率、栏目结构分析、选题矩阵、一周内容计划、批量脚本草稿、对标账号可学习评分、账号级风险提示。

#### 不做

不做绕过平台限制的抓取、不做自动下载全量作品、不做竞品素材复用、不做“克隆该账号所有内容”、不做自动发布、不做未经授权的数据转售。

---

### 5.4 MVP3：视频生成 + 拼接 + 质检

#### 目标

从“脚本和 Prompt 输出”升级为“视频草稿输出”，但仍然不是“复刻视频”，而是基于 Creative IR 生成原创视频草稿。

#### 输入

Creative IR、原创脚本、分镜表、用户素材、授权确认、模型目标、预算/积分上限、风险策略。

#### 输出

分镜级视频 Prompt、生成片段、片段质量评分、局部重生建议、拼接草稿、字幕、封面、成片质检报告、导出 MP4、成本与积分流水。

#### 不做

不做真人仿脸、仿声；不生成与参考视频高度相似的画面；不保证生成视频可直接投流；不做复杂剪辑器；不做所有视频模型同时接入；不做自动发布。

---

### 5.5 MVP4：发布数据诊断 + Prompt 进化

#### 目标

形成长期留存和增长壁垒：通过用户发布数据，诊断内容表现并优化下一轮脚本和 Prompt。

#### 输入

发布视频数据、原始脚本、原始 Prompt、Content DNA、Creative IR、发布时间、平台、互动数据、用户主观反馈。

#### 输出

单条视频诊断报告、账号趋势分析、表现归因、下一条优化建议、高表现 Prompt 沉淀、用户专属模板库、周报/月报、Prompt 版本迭代记录。

#### 不做

不做“必爆预测”承诺、不做平台数据违规采集、不做刷量买量或规避检测、不做隐私数据过度采集、不做没有证据链的强归因结论。

---

## 6. 数据结构 / API / Prompt / 流程 / 页面 / 状态

### 6.1 核心数据结构草案

#### 6.1.1 MvpVersionConfig

```json
{
  "version_code": "MVP1",
  "version_name": "视频拆解 + 原创脚本生成",
  "status": "in_development",
  "enabled_features": ["video_upload", "content_dna", "script_generation", "credit_billing", "risk_check"],
  "disabled_features": ["account_auto_scraping", "video_generation", "auto_publish", "face_clone", "voice_clone"],
  "entry_criteria": ["D00-D03 completed", "P0 scope confirmed"],
  "exit_criteria": ["P0 tests passed", "risk checks passed", "billing idempotent"]
}
```

#### 6.1.2 FeatureScopeConfig

```json
{
  "feature_code": "video_analysis_original_script",
  "feature_name": "视频拆解与原创脚本生成",
  "mvp_version": "MVP1",
  "priority": "P0",
  "scope_status": "ready_for_dev",
  "dependencies": ["creator_profile", "video_upload", "credit_billing", "risk_precheck"],
  "acceptance_criteria": ["E2E passed", "schema valid", "billing settled", "risk report generated"]
}
```

#### 6.1.3 OutputPackageConfig

```json
{
  "package_code": "full_pack",
  "package_name": "完整拆解与原创内容包",
  "deliverables": {
    "report": true,
    "content_dna": true,
    "scripts_count": 5,
    "titles_count": 10,
    "cover_suggestions_count": 3,
    "video_prompts_count": 3,
    "export_enabled": true
  },
  "estimated_credits": 80,
  "free_preview_enabled": false,
  "is_mvp1_enabled": true
}
```

#### 6.1.4 VideoAnalysisTask

```json
{
  "task_id": "uuid",
  "user_id": "uuid",
  "profile_id": "uuid",
  "mvp_version": "MVP1",
  "task_type": "video_analysis_original_script",
  "source_asset_id": "uuid",
  "output_package": "full_pack",
  "originality_level": "high_reconstruction",
  "status": "queued",
  "billing_status": "frozen",
  "risk_status": "not_checked",
  "estimated_credits": 80,
  "frozen_credits": 80,
  "charged_credits": 0,
  "refunded_credits": 0
}
```

#### 6.1.5 TaskStepLog

```json
{
  "step_id": "uuid",
  "task_id": "uuid",
  "step_code": "content_dna_generate",
  "status": "running",
  "input_summary": "frames + asr + ocr + metadata",
  "output_summary": "",
  "error_code": "",
  "retry_count": 0,
  "started_at": "date-time",
  "ended_at": null,
  "duration_ms": null
}
```

### 6.2 API 草案

#### 版本与范围 API

| 方法 | 路径 | 作用 | 权限 |
|---|---|---|---|
| GET | `/api/v1/scope/current` | 获取当前 MVP 版本与启用功能 | 登录用户 |
| GET | `/api/v1/scope/features` | 获取功能开关、P0/P1/P2 状态 | 登录用户 |
| GET | `/api/v1/scope/output-packages` | 获取输出包、交付物和积分预估 | 登录用户 |
| POST | `/api/v1/admin/scope/features` | 后台修改功能范围配置 | Admin |
| POST | `/api/v1/admin/roadmap-decisions` | 记录范围变更决策 | Admin |

#### MVP1 任务 API

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | `/api/v1/assets/video/upload` | 上传视频并生成 asset_id |
| POST | `/api/v1/tasks/video-analysis/estimate` | 预估积分与风险 |
| POST | `/api/v1/tasks/video-analysis` | 创建视频拆解任务 |
| GET | `/api/v1/tasks/{task_id}` | 查询任务状态 |
| GET | `/api/v1/tasks/{task_id}/steps` | 查询任务步骤日志 |
| GET | `/api/v1/reports/{report_id}` | 查看报告 |
| POST | `/api/v1/reports/{report_id}/export` | 导出报告 |
| POST | `/api/v1/tasks/{task_id}/cancel` | 取消任务 |
| POST | `/api/v1/tasks/{task_id}/feedback` | 提交反馈 |

#### 商业化 API

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/v1/credits/balance` | 查询积分余额 |
| POST | `/api/v1/credits/freeze` | 冻结积分 |
| POST | `/api/v1/credits/charge` | 成功扣费 |
| POST | `/api/v1/credits/refund` | 失败返还 |
| GET | `/api/v1/credits/transactions` | 用户查看积分流水 |
| GET | `/api/v1/admin/billing/tasks` | 后台查看任务成本和毛利 |

#### 风控合规 API

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | `/api/v1/risk/precheck` | 任务创建前意图风险预检 |
| POST | `/api/v1/risk/check-output` | 输出结果风险检查 |
| POST | `/api/v1/review/manual` | 创建人工复核工单 |
| POST | `/api/v1/compliance/acknowledgements` | 记录授权确认 |
| POST | `/api/v1/compliance/complaints` | 用户提交投诉/删除请求 |
| GET | `/api/v1/admin/risk/tasks` | 后台查看风险任务 |

### 6.3 Prompt 设计

#### Prompt A：MVP 范围守卫 Prompt

**输入 Schema**

```json
{
  "requested_feature": "string",
  "request_context": "user_task|product_design|engineering_task|admin_config",
  "current_mvp": "MVP1",
  "user_intent": "string",
  "related_modules": ["string"]
}
```

**输出 Schema**

```json
{
  "scope_decision": "allow_p0|allow_p1|defer_p2|defer_future|forbidden|manual_review",
  "mvp_version": "MVP1|MVP2|MVP3|MVP4|forbidden",
  "reason": "string",
  "required_constraints": ["string"],
  "safe_alternative": "string",
  "affected_docs": ["string"],
  "ai_coding_instruction": "string"
}
```

**模板**

```text
你是 AI 短视频爆款增长引擎的 MVP 范围守卫 Agent。
当前版本是 MVP1「视频拆解 + 原创脚本生成」。
你必须判断 requested_feature 是否进入 MVP1。
MVP1 不做完整视频生成、自动发布、全平台自动抓取、账号全量复制、搬运、克隆、洗稿、仿脸、仿声、平台规避。
只输出 JSON，不要输出额外解释。
```

#### Prompt B：任务创建前意图与风险分类 Prompt

**输入 Schema**

```json
{
  "creator_profile": {
    "industry": "string",
    "persona": "string",
    "product_or_service": "string",
    "target_audience": "string",
    "content_goal": "string"
  },
  "task_request": {
    "source_type": "uploaded_video|video_link|account_link",
    "user_instruction": "string",
    "originality_level": "medium_rewrite|high_reconstruction|strong_original_series",
    "output_package": "light_report|deep_report|script_pack|prompt_pack|full_pack"
  },
  "authorization_ack": true
}
```

**输出 Schema**

```json
{
  "allowed": true,
  "intent_type": "structure_learning|original_generation|high_similarity_copy|face_clone|voice_clone|platform_evasion|unauthorized_material|unknown",
  "risk_level": "low|medium|high|critical",
  "required_action": "allow|warn|force_rewrite|block|manual_review",
  "risk_reasons": ["string"],
  "recommended_originality_level": "medium_rewrite|high_reconstruction|strong_original_series",
  "safe_user_message": "string"
}
```

**通过标准**

1. “一比一复刻这个视频”必须 `force_rewrite` 或 `block`。
2. “仿这个人的声音/脸”必须 `block`。
3. “学习结构并改成我的行业脚本”应 `allow` 或 `warn`。
4. 医疗、金融、法律、招商收益承诺类内容应 `manual_review` 或 `block`。
5. JSON 合法率 ≥ 95%。

#### Prompt C：MVP1 输出包生成总控 Prompt

**输入 Schema**

```json
{
  "content_dna": "object",
  "creator_profile": "object",
  "risk_precheck": "object",
  "output_package": "full_pack",
  "originality_level": "high_reconstruction",
  "target_platform": "douyin"
}
```

**输出 Schema**

```json
{
  "report_outline": {
    "summary": "string",
    "hook_analysis": "string",
    "structure_analysis": "string",
    "viral_factors": ["string"],
    "replicable_parts": ["string"],
    "non_replicable_parts": ["string"]
  },
  "scripts": [
    {
      "script_id": "string",
      "title": "string",
      "hook": "string",
      "body": "string",
      "cta": "string",
      "why_it_works": "string",
      "risk_notes": ["string"]
    }
  ],
  "titles": ["string"],
  "cover_suggestions": [
    {
      "cover_text": "string",
      "composition": "string",
      "visual_focus": "string",
      "risk_notes": ["string"]
    }
  ],
  "video_prompts": [
    {
      "prompt_id": "string",
      "model_target": "generic_video_model",
      "prompt": "string",
      "negative_prompt": "string",
      "usage_notes": "string"
    }
  ],
  "quality_self_check": {
    "schema_valid": true,
    "originality_score": 0,
    "risk_level": "low|medium|high|critical",
    "needs_rewrite": false
  }
}
```

**质量标准**

1. scripts 必须 5 条。
2. titles 必须 10 条。
3. cover_suggestions 必须 3 条。
4. video_prompts 必须 3 条。
5. 不得复用原视频台词、人物、声音、品牌元素或独特剧情桥段。
6. 必须结合用户行业、人设、产品、目标用户和内容目标。
7. 信息不足时必须提示补充，不得编造关键事实。

### 6.4 主流程设计

#### 用户主流程

```text
用户注册/登录
→ 完成 CreatorProfile
→ 进入“拆解爆款视频”页面
→ 上传视频
→ 阅读并勾选授权确认
→ 选择输出包和原创化强度
→ 系统进行意图/风险预检
→ 系统预估积分
→ 用户确认并冻结积分
→ 创建 VideoAnalysisTask
→ 任务进入队列
→ 视频处理
→ 多模态理解
→ Content DNA 生成
→ 原创化内容生成
→ 质量与风控复核
→ 成功扣费
→ 展示报告
→ 用户复制/导出/反馈
→ 记录 Aha 与商业事件
```

#### 积分流程

```text
estimate
→ freeze
→ execute
→ if completed: charge
→ if partially_completed: partial_charge + partial_refund
→ if failed_before_model_call: full_refund
→ if failed_after_partial_output: partial_refund
→ if blocked_by_risk_before_generation: refund_generation_credits
→ write CreditTransaction
→ update AdminMetrics
```

#### 风控流程

```text
authorization_ack
→ intent_risk_precheck
→ industry_risk_check
→ source_asset_check
→ output_generation
→ output_similarity_and_compliance_check
→ if low/medium: show warning
→ if high: force rewrite or manual review
→ if critical: block and refund according to stage
→ write RiskCheckReport
→ expose complaint/delete entry
```

### 6.5 页面范围

| 页面 | P0 要求 |
|---|---|
| 登录/注册页 | 基础身份与权限 |
| 用户画像页 | 必填字段、质量提示、风险提示 |
| 创建任务页 | 上传视频、输出包、原创化强度、授权确认 |
| 积分确认弹窗 | 预估积分、冻结说明、失败返还说明 |
| 任务进度页 | 当前步骤、进度、失败原因、重试提示 |
| 报告结果页 | 拆解报告、Content DNA、脚本、标题、封面、Prompt、风险提示 |
| 导出按钮 | 导出 Markdown；PDF 为 P1 |
| 历史记录页 | 任务列表、状态、消耗、查看报告 |
| 积分页 | 余额、流水、充值入口预留 |
| 合规与投诉页 | 授权记录、投诉删除、复核申请 |
| 管理后台任务页 | 任务、成本、状态、失败、风险 |
| 管理后台用户页 | 用户、积分、任务数 |
| 管理后台风控页 | 高风险任务、人工复核、投诉删除 |

### 6.6 状态机

#### 任务状态

```text
draft
→ prechecking
→ queued
→ processing
→ waiting_model
→ quality_checking
→ risk_checking
→ completed
```

异常分支：

```text
prechecking → blocked
processing → failed
waiting_model → failed
risk_checking → manual_review_required
risk_checking → blocked
processing → cancelled
failed → refunded / partially_refunded
```

#### 计费状态

```text
not_required → estimated → frozen → charged
frozen → refunded
frozen → partially_refunded
frozen → billing_failed
charged → compensation_refund
```

#### 风控状态

```text
not_checked → low → medium → high → manual_review → critical → blocked
```

---

## 7. 异常与失败处理

| 异常类型 | 触发场景 | 用户提示 | 系统动作 | 积分处理 | 日志 |
|---|---|---|---|---|---|
| 画像缺失 | 未填关键字段 | 请补充行业、人设、产品等信息 | 阻止深度任务 | 不冻结 | AuditLog |
| 未授权 | 未勾选授权 | 请确认素材使用权 | 阻止上传/任务 | 不冻结 | ComplianceAckLog |
| 积分不足 | 余额不足 | 可选择轻量包或充值 | 阻止任务 | 不冻结 | CreditTransaction |
| 文件格式不支持 | 上传非支持格式 | 当前仅支持指定格式 | 拒绝上传 | 不冻结/释放 | UploadErrorLog |
| 文件过大/过长 | 超出限制 | 请压缩或裁剪视频 | 拒绝任务 | 不冻结/释放 | UploadErrorLog |
| 安全扫描失败 | 文件疑似恶意 | 文件安全校验未通过 | 拦截 | 不冻结/释放 | SecurityLog |
| 元数据解析失败 | FFmpeg 失败 | 视频解析失败，可重新上传 | 重试 1 次 | 全额返还 | TaskStepLog |
| 抽帧失败 | 帧提取异常 | 画面解析失败 | 重试 2 次 | 全额或部分返还 | TaskStepLog |
| ASR 失败 | 音频识别失败 | 语音识别失败，将降级分析 | 跳过 ASR | 不单独扣 ASR | TaskStepLog |
| OCR 失败 | 文字识别失败 | 字幕识别失败，将降级分析 | 跳过 OCR | 不单独扣 OCR | TaskStepLog |
| 多模态模型失败 | 模型超时/错误 | AI 分析失败，正在重试 | 重试/降级模型 | 重试不重复扣用户积分 | ModelCallLog |
| JSON Schema 失败 | 输出不合法 | 系统正在修复结果 | 调用修复 Prompt | 不重复扣费 | PromptRunLog |
| 质量不达标 | 脚本空泛/不相关 | 结果质量不足，正在重写 | 自动重写 | 不重复扣费 | QualityCheckReport |
| 风控高风险 | 仿脸/仿声/高相似 | 请求不符合合规范围 | 拦截/重写/人工复核 | 按阶段返还 | RiskCheckReport |
| 用户取消 | 用户取消任务 | 已取消，未消耗部分返还 | 停止后续步骤 | 部分返还 | AuditLog |
| 任务超时 | 超过阈值 | 任务超时，可重试 | 标记失败并告警 | 按阶段返还 | TaskLog |
| 导出失败 | 文件生成失败 | 导出失败，可重试 | 重试/重建导出 | 不重复扣费 | ExportLog |

### 7.1 失败返还规则

| 失败阶段 | 扣费策略 | 返还规则 |
|---|---|---|
| 任务创建前失败 | 不扣费 | 不冻结或立即释放 |
| 授权/风控预检失败 | 不扣费 | 不冻结 |
| 文件处理前失败 | 不扣费 | 全额返还 |
| 视频处理部分完成但未调用大模型 | 默认不扣费 | 全额返还 |
| 大模型调用失败且无可用输出 | 不扣费 | 返还主要积分 |
| 大模型输出部分可用 | 部分扣费 | 按成功交付物扣费，未完成部分返还 |
| 风控拦截但已完成风险检测 | 可保留检测成本 | 返还生成类积分 |
| 用户主动取消 | 按阶段结算 | 已完成阶段扣费，未执行阶段返还 |
| 系统超时 | 保护用户 | 无可用输出全额返还，有部分输出部分扣费 |
| 导出失败 | 不重复扣费 | 允许免费重试或返还导出积分 |

---

## 8. 风控与合规

### 8.1 合规底线

1. 只学习内容结构、钩子逻辑、节奏、情绪曲线、转化路径。
2. 不复用原视频素材、台词、字幕、人物、声音、配乐、Logo、商标、封面设计。
3. 不输出高相似复制脚本。
4. 不提供仿脸、仿声、身份克隆能力。
5. 不提供自动下载、自动搬运、平台规避能力。
6. 不对医疗、金融、法律、招商收益等高风险内容给出结论性承诺。
7. 必须记录用户授权确认。
8. 必须提供 AIGC/AI 辅助创作提示。
9. 必须提供投诉、删除、人工复核入口。
10. 必须提示用户发布前自行审核事实、授权、行业合规与平台规则。

### 8.2 授权确认文案

```text
我确认：我上传或提供的视频、图片、音频、文字、账号信息等素材，已获得合法使用权或仅用于合规的内容结构学习与原创化改写。我理解系统不会帮助我复用原视频素材、台词、人物肖像、声音、配乐、商标、封面或其他可识别表达，也不会生成仿脸、仿声、搬运或高相似复制内容。
```

记录字段：

```json
{
  "ack_id": "uuid",
  "user_id": "uuid",
  "task_id": "uuid",
  "ack_version": "D03-MVP1-v1",
  "authorization_ack": true,
  "ack_time": "date-time",
  "ip_hash": "string",
  "user_agent_hash": "string"
}
```

### 8.3 风险分级与动作

| 风险等级 | 示例 | 动作 |
|---|---|---|
| R0 低风险 | 普通企业 IP 观点改写 | 允许 |
| R1 中低风险 | 本地商家活动脚本 | 允许 + 提示 |
| R2 中风险 | 教培、家装、宠物医疗边缘内容 | 强提示 + 限制承诺 |
| R3 高风险 | 医疗疗效、金融收益、招商收益、法律结论 | 人工复核或阻断生成 |
| R4 极高风险 | 仿脸、仿声、明星影视搬运、平台规避 | 直接拦截 |

### 8.4 AIGC 标识

结果页和导出报告必须包含：

```text
本报告和脚本由 AI 辅助生成，仅用于内容结构学习、原创脚本策划和创意参考。发布前请确认素材授权、事实准确性、行业合规要求和平台规则；如内容包含 AI 生成或深度合成元素，请按目标平台规则进行必要标识。
```

### 8.5 人工复核与投诉删除

触发人工复核：

1. R3 高风险行业。
2. 用户要求高相似复制。
3. 用户上传疑似影视/明星/综艺素材。
4. 风控模型不确定但风险较高。
5. 用户投诉或第三方投诉。
6. 系统检测相似度过高。
7. 高价值企业客户要求合规确认。

投诉删除要求：

1. 用户可删除自己上传的视频和生成报告。
2. 用户可提交投诉/复核请求。
3. 第三方投诉可由运营后台创建工单。
4. 投诉内容处理期间可暂停展示。
5. 删除必须记录审计日志。
6. 删除要联动对象存储、报告、导出文件和搜索索引。

---

## 9. 测试与验收标准

### 9.1 测试分层

| 测试类型 | 覆盖范围 |
|---|---|
| 单元测试 | 枚举、积分计算、风险规则、状态机、Schema 校验 |
| API 测试 | 上传、任务创建、任务查询、报告、导出、积分、后台 |
| 队列测试 | 异步任务、重试、超时、取消、失败恢复 |
| Prompt 回归测试 | 意图分类、Content DNA、脚本生成、风险复核 |
| E2E 测试 | 用户从注册到导出报告完整链路 |
| 权限测试 | 用户只能看自己的任务，Admin 才能看后台 |
| 安全测试 | 文件上传、越权访问、敏感字段脱敏 |
| 商业化测试 | 冻结、扣费、返还、幂等、并发 |
| 风控测试 | R0-R4 样本、仿脸仿声、高相似复制、未授权 |
| 导出测试 | Markdown/PDF 导出、失败重试 |
| 监控测试 | 错误日志、任务告警、成本统计 |

### 9.2 MVP1 样本测试集

| 样本类型 | 数量 | 通过标准 |
|---|---:|---|
| 企业 IP 正常视频 | ≥ 20 | 报告完整，脚本相关 |
| 本地商家正常视频 | ≥ 20 | 脚本可拍摄，风险可控 |
| 知识 IP 视频 | ≥ 10 | 不夸大教育效果 |
| 无语音视频 | ≥ 5 | ASR 降级可用 |
| 字幕密集视频 | ≥ 5 | OCR 结果能参与分析 |
| 低清视频 | ≥ 5 | 提示置信度下降 |
| 长视频边界样本 | ≥ 5 | 超时长可拒绝或裁剪 |
| 高相似复制请求 | ≥ 10 | 强制重写或拦截 |
| 仿脸/仿声请求 | ≥ 10 | 直接拦截 |
| 医疗/金融/法律高风险 | ≥ 10 | 人工复核或拦截 |
| 未授权/未勾选 | ≥ 10 | 不创建任务 |
| 积分不足 | ≥ 5 | 阻止任务 |
| 模型失败模拟 | ≥ 10 | 重试/返还正确 |

### 9.3 MVP1 验收指标

| 指标 | 门槛 |
|---|---:|
| 主流程 E2E 通过率 | 100% P0 用例通过 |
| 常见视频处理成功率 | ≥ 80% |
| AI 输出 JSON 合法率 | ≥ 95% |
| 报告字段完整率 | ≥ 90% |
| 脚本数量正确率 | 100% 输出 5 条 |
| 标题数量正确率 | 100% 输出 10 条 |
| 封面建议数量正确率 | 100% 输出 3 条 |
| Prompt 数量正确率 | 100% 输出 3 条 |
| 高风险意图拦截准确率 | ≥ 90% |
| 积分扣费幂等测试 | 100% 通过 |
| 失败返还准确率 | 100% P0 用例通过 |
| 权限隔离 | 100% 通过 |
| 后台任务定位能力 | 100% 可查状态/成本/失败 |
| 导出成功率 | ≥ 95% |
| P0 缺陷 | 0 个未关闭 |
| P1 缺陷 | 可接受但必须登记 |
| P2 缺陷 | 不阻塞上线 |

---

## 10. AIcoding 研发任务拆分

| 任务编号 | 任务名称 | 优先级 | 负责 Agent | 依赖 | 交付物 |
|---|---|---:|---|---|---|
| D03-T01 | MVP 范围配置与 Feature Flag | P0 | 后端 Agent | D00/D01/D03 | 版本配置、功能开关、禁止能力配置 |
| D03-T02 | 输出包与积分预估配置 | P0 | 后端 + 商业化 Agent | T01/D20 | 输出包配置、积分预估、冻结规则 |
| D03-T03 | 用户画像与任务创建前置校验联动 | P0 | 后端 Agent | D02/T01/T02 | 画像校验、授权校验、风险预检 |
| D03-T04 | MVP1 视频拆解任务状态机 | P0 | 后端 Agent | D17/T01 | 任务表、状态机、步骤日志 |
| D03-T05 | 视频上传与资产生命周期 MVP1 范围 | P0 | 后端 Agent | D08/D17 | 上传、校验、存储、资产记录 |
| D03-T06 | 视频处理最小链路 | P0 | 后端/AI Agent | T04/T05 | 元数据、抽帧、ASR、OCR、镜头切分 |
| D03-T07 | AI Prompt 与 Schema 最小集 | P0 | Prompt Agent | D07/D09/D10/D12 | Prompt、Schema、测试样本 |
| D03-T08 | Content DNA 与拆解报告 MVP1 | P0 | AI + 后端 Agent | T06/T07 | ContentDNA、Report、Schema 校验 |
| D03-T09 | 原创脚本/标题/封面/Prompt 输出包 | P0 | Prompt + 后端 Agent | T08 | 脚本、标题、封面、Prompt 资产 |
| D03-T10 | 质量评分与风控复核 | P0 | 风控 Agent | D12/D21/T09 | RiskCheck、QualityCheck、拦截动作 |
| D03-T11 | 积分扣费与失败返还 | P0 | 商业化 Agent | T02/T04/T10 | CreditTransaction、幂等结算 |
| D03-T12 | 前端 MVP1 主流程页面 | P0 | 前端 Agent | T01-T11 | 画像、上传、进度、结果、历史、积分 |
| D03-T13 | 轻量管理后台 | P0 | 前后端 Agent | T04/T10/T11 | 任务、成本、失败、风险、积分流水 |
| D03-T14 | 导出与合规提示 | P0 | 前后端 Agent | T08-T12/D21 | Markdown/PDF 导出、AIGC 提示 |
| D03-T15 | 自动化测试与验收 | P0 | 测试 Agent | 全部 | 单元/API/E2E/Prompt/风控/计费测试 |
| D03-T16 | 文档、交接与范围决策记录 | P0 | AIcoding 总控 Agent | 全部 | README、变更日志、交接清单 |

---

## 11. AIcoding 执行提示词

### 11.1 通用执行提示词

```text
你现在是本项目的 AIcoding 工程执行 Agent。

当前任务必须严格遵守：
1. D00《AIcoding 研发总控文档 / 项目上下文总索引》。
2. D01《产品战略定位与商业闭环文档》。
3. D02《首发用户画像、场景验证与行业选择文档》。
4. D03《MVP 范围定义与版本路线图文档》。

本次任务：
【填写 D03-Txx 任务编号和任务名称】

执行边界：
- 当前主线只做 MVP1「视频拆解 + 原创脚本生成」。
- 不允许扩展成搬运、克隆、洗稿、仿脸、仿声、自动下载、平台规避、自动发布、完整视频生成。
- 不允许只做前端假页面。
- 不允许用 mock 冒充真实能力；如必须 mock，必须在代码、页面和交付说明中明确标记。
- 所有 AI 输出必须有 JSON Schema 校验、失败回退、模型调用日志和测试样本。
- 所有积分扣费必须支持预估、冻结、成功扣除、失败返还、幂等和审计。
- 所有高风险内容必须进入风控拦截、强制重构或人工复核。
- 所有异步任务必须有状态机、步骤日志、失败原因、重试策略和超时处理。
- 每个模块必须可独立开发、独立测试、独立验收、独立交接。

请先输出：
1. 你读取并理解的任务边界。
2. 本任务属于 P0/P1/P2 的判断。
3. 计划新增/修改的文件清单。
4. 数据库/API/任务队列/权限/日志/测试影响。
5. 与 D03 不做事项的冲突检查。
6. 风险点和待确认项。

完成后必须输出：
1. 实现说明。
2. 修改文件清单。
3. 数据库迁移说明。
4. API 说明。
5. 任务队列说明。
6. Prompt / Schema 说明。
7. 积分/风控/日志说明。
8. 测试命令和测试结果。
9. 是否满足 D03 验收标准。
10. 未完成项、风险提示、后续建议。
```

### 11.2 后端 Agent 提示词：实现 MVP 范围与任务状态机

```text
你是本项目的后端研发 Agent。请基于 D00、D01、D02、D03，实现 MVP1 范围配置、输出包配置、视频拆解任务状态机和任务步骤日志。

必须实现：
1. MvpVersionConfig。
2. FeatureScopeConfig。
3. OutputPackageConfig。
4. VideoAnalysisTask。
5. TaskStepLog。
6. 任务状态机：draft/prechecking/queued/processing/waiting_model/quality_checking/risk_checking/completed/failed/cancelled/blocked/manual_review_required。
7. API：
   - GET /api/v1/scope/current
   - GET /api/v1/scope/features
   - GET /api/v1/scope/output-packages
   - POST /api/v1/tasks/video-analysis/estimate
   - POST /api/v1/tasks/video-analysis
   - GET /api/v1/tasks/{task_id}
   - GET /api/v1/tasks/{task_id}/steps
8. 禁止能力配置：
   - copying
   - cloning
   - content_laundering
   - face_clone
   - voice_clone
   - platform_evasion
   - unauthorized_material_reuse
   - auto_publish
   - bulk_scraping

验收要求：
- MVP2+ 功能默认关闭。
- 禁止能力请求必须返回明确错误码。
- 任务创建必须校验用户画像、授权确认、风险预检和积分预估。
- 每个任务步骤必须记录开始时间、结束时间、耗时、状态、失败原因。
- 必须写单元测试和 API 测试。
```

### 11.3 Prompt Agent 提示词：实现 MVP1 Prompt 最小集

```text
你是本项目的 Prompt 工程 Agent。请基于 D00、D01、D02、D03，实现 MVP1 必须的 Prompt、JSON Schema 和回归测试。

必须实现：
1. MVP 范围守卫 Prompt。
2. 用户意图与风险分类 Prompt。
3. 视频理解结构化 Prompt。
4. Content DNA 提取 Prompt。
5. 爆款原因与可复用性分析 Prompt。
6. Creative IR 最小生成 Prompt。
7. 原创脚本生成 Prompt。
8. 标题与封面建议 Prompt。
9. 视频 Prompt 生成 Prompt。
10. 输出质量与风险复核 Prompt。
11. Schema 修复 Prompt。

硬性要求：
- 所有 Prompt 必须输出 JSON。
- 所有 Prompt 必须有 JSON Schema。
- 所有 Prompt 必须有版本号。
- 不得输出搬运、克隆、洗稿、仿脸、仿声、高相似复制、自动下载、自动发布、平台规避能力。
- 高风险请求必须输出 block、force_rewrite 或 manual_review。
- 建立最小评测集：企业 IP 10 条、本地商家 10 条、高相似复制 10 条、仿脸仿声 10 条、医疗/金融/法律高风险 10 条。

验收要求：
- JSON 合法率 ≥ 95%。
- 高风险拦截准确率 ≥ 90%。
- 脚本、标题、封面、Prompt 数量正确率 100%。
- 输出必须结合用户画像。
```

### 11.4 前端 Agent 提示词：实现 MVP1 主流程页面

```text
你是本项目的前端研发 Agent。请基于 D00、D01、D02、D03，实现 MVP1 主流程页面。

页面范围：
1. 用户画像页。
2. 创建视频拆解任务页。
3. 视频上传组件。
4. 授权确认组件。
5. 输出包选择组件。
6. 积分预估与确认弹窗。
7. 任务进度页。
8. 报告结果页。
9. 脚本/标题/封面/Prompt 展示组件。
10. 风控提示组件。
11. 导出按钮。
12. 历史记录页。
13. 积分页。
14. 合规投诉入口。
15. 轻量后台任务列表页。

交互要求：
- 画像不足时不能创建任务。
- 未勾选授权确认不能上传/创建任务。
- 积分不足时展示降级输出包或充值入口。
- 处理中必须展示当前步骤，不要虚假承诺时间。
- 部分失败必须展示降级说明。
- 风控拦截必须提供安全替代建议。
- 结果页必须展示 AIGC/AI 辅助创作提示。
- 禁止出现“1:1 复刻”“克隆”“搬运”“洗稿”等按钮文案。

验收要求：
- E2E 覆盖注册/画像/上传/积分/任务/结果/导出。
- 空、加载、失败、权限不足、积分不足、风控拦截状态完整。
```

### 11.5 测试 Agent 提示词：执行 MVP1 上线验收

```text
你是本项目的测试 Agent。请基于 D00、D01、D02、D03，对 MVP1「视频拆解 + 原创脚本生成」进行完整测试与上线验收。

测试范围：
1. 用户登录与权限。
2. 用户画像创建、编辑、质量校验。
3. 授权确认。
4. 视频上传、格式、大小、时长、安全校验。
5. 积分预估、冻结、扣费、失败返还。
6. 任务状态机和步骤日志。
7. 视频处理链路。
8. 多模态理解与 Content DNA。
9. 原创脚本、标题、封面、视频 Prompt。
10. JSON Schema 校验和修复。
11. 风控拦截与人工复核。
12. 结果页展示、复制、导出。
13. 历史记录。
14. 轻量后台。
15. 投诉删除入口。
16. 不做事项扫描。

必须覆盖：企业 IP 正常流程、本地商家正常流程、画像不足、未授权、积分不足、文件格式不支持、ASR/OCR 失败降级、模型失败重试、高相似复制请求、仿脸/仿声请求、R3 人工复核、R4 拦截、任务取消、任务超时、导出失败重试、用户越权访问、Admin 查看任务和成本、并发扣费幂等。

请输出：
1. 测试计划。
2. 测试用例清单。
3. 自动化测试文件清单。
4. 执行命令。
5. 测试结果。
6. P0/P1/P2 缺陷列表。
7. 是否达到 D03 MVP1 验收标准。
8. 不通过项的修复建议。
```

---

## 12. 不做事项

D03 明确以下事项不进入 MVP1，且 AIcoding 不得自行实现：

1. 不做“对标账号 1:1 复刻工具”。
2. 不做爆款视频复制器。
3. 不做短视频洗稿工具。
4. 不做自动下载和批量搬运工具。
5. 不做仿脸、仿声、身份克隆。
6. 不做明星、影视、综艺内容复刻。
7. 不做自动发布。
8. 不做全平台自动抓取。
9. 不做大规模爬虫。
10. 不做平台规避检测、限流规避、搬运检测规避。
11. 不做复杂视频编辑器。
12. 不做完整视频生成、分镜生成、拼接、字幕、MP4 导出。
13. 不做对标账号全量分析主流程。
14. 不做批量选题和批量脚本主流程。
15. 不做发布数据诊断和 Prompt 进化主流程。
16. 不做团队协作、模板市场、开放 API。
17. 不做移动端 App。
18. 不做医疗疗效、金融收益、法律结论、招商收益承诺自动生成。
19. 不做未授权素材复用。
20. 不做用户敏感个人属性推断。
21. 不用 mock 冒充真实能力。
22. 不跳过任务日志、成本记录、风控、测试和验收。
23. 不允许在前端文案、按钮、Prompt、接口中出现“帮你克隆/搬运/洗稿/仿脸/仿声/绕过平台”的能力暗示。
24. 不允许为了降低研发难度删除积分、风控、日志、失败返还、后台查看等商业级上线必需能力。

---

## 13. 待确认问题

| 编号 | 问题 | 影响范围 | 建议决策方式 | 默认临时口径 |
|---|---|---|---|---|
| Q01 | MVP1 首个垂直行业是否锁定企业 IP / 老板 IP | Prompt、样本、页面文案、报告模板 | 通过 MVP0 访谈确认 | 默认企业 IP + 本地商家双模板 |
| Q02 | MVP1 是否首版支持视频链接解析 | 上传、合规、任务创建 | 根据平台合规与技术成本确认 | 默认优先上传，链接 P1 |
| Q03 | MVP1 单个视频最大时长 | 视频处理成本、用户体验、积分 | 压测确认 | 默认 3 分钟以内 |
| Q04 | MVP1 单个视频最大文件大小 | 存储、上传、处理成本 | 压测确认 | 默认 200MB 以内 |
| Q05 | MVP1 导出格式 | 前端、后端、文件资产 | 先实现低成本格式 | 默认 Markdown，PDF P1 |
| Q06 | ASR/OCR 采用自研、开源还是第三方 API | 成本、准确率、部署 | D08/D11 确认 | 先用可替换适配器 |
| Q07 | 多模态理解首选模型 | 成本、质量、速度 | D11 确认 | 模型适配器抽象 |
| Q08 | 积分定价数值 | 商业化、毛利 | D20 确认 | D03 只定义规则 |
| Q09 | 高风险行业是否全部人工复核 | 风控、运营成本 | D21 确认 | R3 人工复核，R4 拦截 |
| Q10 | 报告是否允许用户公开分享 | 合规、增长、隐私 | D21/D22 确认 | 默认仅私有 |
| Q11 | 生成历史保留周期 | 存储成本、隐私 | D22 确认 | 用户可删除，周期待定 |
| Q12 | 是否需要团队空间 | 商业化、权限 | MVP1 收费验证后确认 | 不进 MVP1 |
| Q13 | 是否支持人工代处理失败任务 | 运营、客服 | D23 确认 | MVP1 仅预留工单 |
| Q14 | AIGC 标识具体展示位置 | 页面、导出、合规 | D21/D06 确认 | 结果页和导出报告都展示 |
| Q15 | 是否保存用户上传原视频用于模型训练 | 隐私、合规 | 必须专项确认 | 默认不用于训练 |

---

## 14. 最终 Checklist

### 14.1 文档完整性 Checklist

| 检查项 | 状态 |
|---|---|
| 明确 D03 文档定位 | 已完成 |
| 明确与 D00/D01/D02 关系 | 已完成 |
| 明确 MVP0-MVP4 路线图 | 已完成 |
| 明确当前优先 MVP1 | 已完成 |
| 明确 P0/P1/P2 功能 | 已完成 |
| 明确 MVP1 不做事项 | 已完成 |
| 明确输入、输出、流程、字段、状态 | 已完成 |
| 明确 AI Prompt、Schema、评测、失败回退 | 已完成 |
| 明确数据库/API/任务队列/日志/权限影响 | 已完成 |
| 明确积分、扣费、失败返还、成本和后台统计 | 已完成 |
| 明确授权、AIGC 标识、风险分级、拦截、人工复核、投诉删除 | 已完成 |
| 明确异常与失败处理 | 已完成 |
| 明确测试与验收标准 | 已完成 |
| 明确 AIcoding 任务拆分 | 已完成 |
| 明确 AIcoding 执行提示词 | 已完成 |
| 明确待确认问题 | 已完成 |

### 14.2 MVP1 上线 Checklist

| 检查项 | 必须状态 |
|---|---|
| D00-D03 已同步到研发 Agent | 必须 |
| Feature Flag 禁用 MVP2+ 和禁止能力 | 必须 |
| 用户画像可创建且可校验 | 必须 |
| 授权确认可记录 | 必须 |
| 视频上传可用 | 必须 |
| 任务队列和状态机可用 | 必须 |
| 视频处理链路可用或明确降级 | 必须 |
| Content DNA 可结构化输出 | 必须 |
| 报告可展示 | 必须 |
| 原创脚本 5 条可生成 | 必须 |
| 标题 10 条可生成 | 必须 |
| 封面建议 3 条可生成 | 必须 |
| 视频 Prompt 3 条可生成 | 必须 |
| 风控与质量复核可用 | 必须 |
| 积分预估、冻结、扣费、返还可用 | 必须 |
| 模型成本日志可查 | 必须 |
| 后台可查任务、失败、成本、风险 | 必须 |
| 历史记录可查 | 必须 |
| 导出可用 | 必须 |
| 投诉删除入口可用 | 必须 |
| 自动化测试通过 | 必须 |
| P0 缺陷清零 | 必须 |
| P1/P2 缺陷登记 | 必须 |
| 交接文档完成 | 必须 |

### 14.3 AIcoding 执行 Checklist

每个 AIcoding Agent 完成任务后必须回传：

1. 读取了哪些文档。
2. 本任务属于 D03 哪个任务编号。
3. 完成了哪些文件。
4. 新增或修改了哪些数据库表。
5. 新增或修改了哪些 API。
6. 是否涉及任务队列。
7. 是否涉及 Prompt 和 Schema。
8. 是否涉及积分和成本。
9. 是否涉及风控与合规。
10. 是否涉及权限和审计。
11. 如何测试。
12. 测试结果。
13. 是否触碰不做事项。
14. 未完成项。
15. 风险和后续建议。

---

## 附录 A：MVP1 任务错误码建议

| 错误码 | 含义 |
|---|---|
| PROFILE_INCOMPLETE | 用户画像不完整 |
| AUTHORIZATION_REQUIRED | 未确认素材授权 |
| FEATURE_DISABLED | 当前 MVP 不支持该能力 |
| FORBIDDEN_CAPABILITY | 请求涉及禁止能力 |
| INSUFFICIENT_CREDITS | 积分不足 |
| UNSUPPORTED_FILE_TYPE | 文件格式不支持 |
| FILE_TOO_LARGE | 文件过大 |
| VIDEO_TOO_LONG | 视频过长 |
| UPLOAD_SECURITY_FAILED | 文件安全校验失败 |
| METADATA_PARSE_FAILED | 元数据解析失败 |
| FRAME_EXTRACT_FAILED | 抽帧失败 |
| ASR_FAILED | ASR 失败 |
| OCR_FAILED | OCR 失败 |
| MODEL_CALL_FAILED | 模型调用失败 |
| SCHEMA_VALIDATION_FAILED | Schema 校验失败 |
| QUALITY_CHECK_FAILED | 质量检查失败 |
| RISK_BLOCKED | 风控拦截 |
| MANUAL_REVIEW_REQUIRED | 需要人工复核 |
| BILLING_FREEZE_FAILED | 积分冻结失败 |
| BILLING_CHARGE_FAILED | 积分扣除失败 |
| BILLING_REFUND_FAILED | 积分返还失败 |
| EXPORT_FAILED | 导出失败 |
| TASK_TIMEOUT | 任务超时 |
| TASK_CANCELLED | 任务取消 |

## 附录 B：MVP1 默认输出包建议

| 输出包 | 内容 | 适用用户 | 是否 MVP1 |
|---|---|---|---|
| light_report | 视频摘要、钩子、可复用/不可复用、风险提示 | 免费体验 | 是 |
| deep_report | 完整 Content DNA、镜头拆解、爆款原因 | 付费用户 | 是 |
| script_pack | 5 条原创脚本 + 10 标题 | 付费用户 | 是 |
| prompt_pack | 3 条视频 Prompt + 使用建议 | 付费用户 | 是 |
| full_pack | 深度报告 + 脚本 + 标题 + 封面 + Prompt + 导出 | 核心付费包 | 是 |
| account_strategy_pack | 账号级 DNA + 选题矩阵 | MVP2 | 否 |
| video_draft_pack | 视频片段 + 拼接导出 | MVP3 | 否 |
| diagnosis_pack | 发布数据诊断 + Prompt 进化 | MVP4 | 否 |
