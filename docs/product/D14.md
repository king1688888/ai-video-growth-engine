# D14｜数据诊断、归因与提示词进化规则文档

> 文档编号：D14  
> 文档名称：数据诊断、归因与提示词进化规则文档  
> 文档版本：V1.0  
> 生成日期：2026-05-25  
> 根上下文：D00《AIcoding 研发总控文档 / 项目上下文总索引》  
> 当前主线版本：MVP1「视频拆解 + 原创脚本生成」  
> 文档性质：发布数据回填 + 表现诊断 + 证据化归因 + Prompt 进化候选 + 实验与回归门禁 + AIcoding 执行文档  
> 核心定位：本文件不是“玄学复盘建议”或“流量预测鸡汤”，而是把用户发布后的真实表现数据，转化为可校验、可追踪、可计费、可风控、可复核、可进入 Prompt Registry 与 Benchmark 的工程化闭环。

---

# 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 文档编号 | D14 |
| 文档名称 | 数据诊断、归因与提示词进化规则文档 |
| 所属项目 | AI 短视频爆款增长引擎 |
| 当前研发主线 | MVP1：视频拆解 + 原创脚本生成 |
| 文档级别 | A+，长期壁垒与增长闭环核心文档；MVP1 以轻量预留和手动回填为主 |
| 上级根文档 | D00《AIcoding 研发总控文档 / 项目上下文总索引》 |
| 直接前置依赖 | D00、D01、D02、D03、D04、D05、D06、D07、D08、D09、D10、D11、D12、D13 |
| 直接后置依赖 | D15 技术架构、D16 数据库/API、D17 异步任务队列、D18 AIcoding 执行规约、D19 测试发布监控、D20 商业化系统、D21 合规授权与内容审核、D22 数据安全与权限、D23 运营后台/客服/人工复核、D24 最终交接 |
| 适用阶段 | MVP1 数据回填预留、MVP1.5 手动数据诊断、MVP2 账号级数据诊断、MVP3/MVP4 Prompt 进化与增长实验 |
| 适用对象 | 创始人、产品负责人、AI 产品经理、技术产品经理、后端 Agent、前端 Agent、AI/Prompt Agent、数据 Agent、测试 Agent、风控合规负责人、商业化负责人、运营后台负责人 |
| 文档目标 | 定义发布数据回填、指标标准化、表现诊断、归因假设、Prompt 进化候选、实验验证、数据库/API/任务队列/日志/权限/测试/计费/合规与 AIcoding 执行任务 |
| 最高约束 | 与 D00 冲突时，以 D00 为准；与 D10 Prompt 版本治理冲突时，以 D10 的 Prompt Registry 为实现入口；与 D12 Benchmark 冲突时，以 D12 的发布门禁为准；与 D13 风控合规冲突时，以更严格的风险策略为准；与 D16/D17 工程契约冲突时，以 D16/D17 为最终实现口径并回写 D14 |
| 输出形态 | Markdown 文档 + 数据结构草案 + API 草案 + 任务队列草案 + Prompt 模板 + 页面状态 + 指标口径 + 测试验收清单 + AIcoding 研发任务拆分 |
| 维护责任人 | 项目 Owner / AI 产品总控 / AI Prompt 架构负责人 / 数据产品负责人 / 技术产品负责人 |
| 更新触发条件 | 指标口径变化、平台数据字段变化、Prompt 版本变化、模型供应商变化、风控策略变化、Benchmark 失败、线上质量下降、用户反馈显著偏差、商业化扣费规则变化、合规口径变化 |
| 文档状态 | V1.0 初版可执行；MVP1 只实现手动数据回填、单作品诊断、Prompt 进化候选预留；自动数据抓取、自动归因、自动替换线上 Prompt 均不进入 MVP1 |

## 1.1 编号兼容说明

当前项目文档体系存在编号演进：D00 旧索引中曾将「数据诊断、归因与提示词进化规则文档」标为 D13，并将「AI Benchmark」标为 D14；但当前已形成 D12「AI 评测样本集、Benchmark 与回归测试文档」和 D13「质检评分、相似度风控与合规拦截文档」，本轮任务明确要求生成 **D14｜数据诊断、归因与提示词进化规则文档**。

为避免 AIcoding 混乱，工程字段统一采用：

| 字段 | 建议值 |
|---|---|
| `doc_code` | `D14` |
| `doc_alias` | `DATA_DIAGNOSIS_ATTRIBUTION_PROMPT_EVOLUTION` |
| `legacy_doc_code` | `D13_DATA_DIAGNOSIS_PROMPT_EVOLUTION`，仅用于旧索引兼容 |
| `depends_on_prompt_doc` | `D10_PROMPT_REGISTRY` |
| `depends_on_benchmark_doc` | `D12_AI_BENCHMARK_REGRESSION` |
| `depends_on_risk_doc` | `D13_QUALITY_SIMILARITY_COMPLIANCE_GUARDRAIL` |
| `conflict_resolution` | 若旧文档编号与本文件冲突，以本次用户指定 D14 执行；上线前由项目 Owner 回写 D00 文档地图 |

## 1.2 D14 的边界声明

D14 不是承诺“AI 一定能解释爆款原因”的魔法系统。短视频数据受平台分发、账号权重、发布时间、粉丝结构、竞争内容、外部热点、投放预算、评论舆情、剪辑执行、封面标题、素材质量等多因素影响。D14 的工程目标是：

```text
把发布后的表现数据
→ 标准化为可比较指标
→ 与原始 Content DNA / Creative IR / Prompt / 输出包建立可追溯关联
→ 生成带证据、带置信度、带不确定性说明的归因假设
→ 输出下一条可执行优化建议
→ 形成 Prompt 进化候选
→ 经过风控、Benchmark、人工复核和实验验证后再进入 Prompt Registry
```

D14 不做伪因果承诺，不把相关性包装成确定因果，不为了追求数据提升而绕过原创化、合规与风控。

---

# 2. 文档目标

## 2.1 本文档要解决的问题

D14 要解决的是本项目从“生成一次内容”升级为“持续学习用户账号表现、持续优化脚本和 Prompt”的长期增长闭环问题。没有 D14，系统会停留在一次性生成工具，无法形成用户专属资产、复购理由和长期壁垒。

本文档必须解决以下 12 个问题：

1. **发布数据如何回填**：用户如何把已发布视频的数据、截图、链接、CSV、手动指标、定性反馈回传系统。
2. **数据如何标准化**：不同平台、不同截图、不同口径的数据如何被统一成 `PublishMetrics` 与派生指标。
3. **数据如何绑定生成资产**：发布数据如何关联到原始对标视频、Content DNA、Creative IR、脚本、标题、封面建议、视频 Prompt、PromptRun、模型版本、风控报告。
4. **表现如何判断**：如何用账号基线、历史均值、同类型内容、同平台指标和最小样本阈值判断“好 / 一般 / 差 / 数据不足”。
5. **归因如何避免伪因果**：如何输出“归因假设”而不是确定因果，如何表达证据、置信度、可验证动作和不确定性。
6. **Prompt 如何进化**：如何从诊断结果生成 Prompt 进化候选，而不是直接覆盖生产 Prompt。
7. **Prompt 进化如何受控**：任何 Prompt 进化候选必须经过 Schema 校验、D13 风控、D12 Benchmark、人工审批、灰度实验和回滚机制。
8. **AI 能力如何工程化**：定义诊断 Prompt、归因 Prompt、进化 Prompt 的输入 Schema、输出 Schema、评测、失败回退和质量标准。
9. **工程如何落地**：定义数据库、API、任务队列、日志、权限、埋点、后台页面、测试和部署影响。
10. **商业化如何闭环**：定义数据诊断积分、失败返还、Prompt 进化成本、后台成本统计、毛利、用户权益与套餐差异。
11. **合规如何嵌入**：定义数据授权、平台数据使用边界、AIGC 标识、隐私脱敏、风险分级、人工复核、投诉删除机制。
12. **AIcoding 如何执行**：输出可直接交给 Manus / Codex / Claude Code / Hermes Agent 的任务拆分、提示词模板和验收口径。

## 2.2 本文档不解决的问题

| 不在 D14 完整展开的内容 | 负责文档 | D14 只提供 |
|---|---|---|
| 产品战略、价值主张和商业定位 | D01 | 数据闭环对长期壁垒和商业闭环的支撑 |
| 首发用户和行业选择 | D02 | 指标字段、行业基线、用户画像依赖 |
| MVP 范围和版本路线 | D03 | D14 在各版本中的启用边界和 Feature Flag |
| 总 PRD 与用户故事 | D04 | 数据回填、诊断、Prompt 进化的功能需求草案 |
| 模块边界和并行研发拆分 | D05 | 数据诊断模块、Prompt 进化模块的边界 |
| 页面原型与交互细节 | D06 | 数据回填页、诊断报告页、Prompt 进化后台的 ViewModel |
| Content DNA 字段全集 | D07 | 读取内容结构、钩子、节奏、转化路径、风险标签等字段作为归因输入 |
| 视频处理与多模态证据包 | D08 | 不重新定义视频解析，只读取证据包 ID 与生成资产关联 |
| Creative IR Schema 与原创化规则 | D09 | 读取原创化策略、脚本意图、分镜策略、标题/封面策略作为归因输入 |
| Prompt Registry、Prompt 版本治理 | D10 | 生成 Prompt 进化候选，最终注册、灰度、回滚由 D10 实现 |
| 模型路由与成本控制 | D11 | 诊断与进化 Prompt 的模型能力、成本、降级要求 |
| AI Benchmark 与回归测试 | D12 | 进化候选必须进入 Benchmark 发布门禁，本文只定义触发与指标 |
| 质检评分、相似度风控与合规拦截 | D13 | D14 产出的建议和进化候选必须受 D13 拦截 |
| 数据库/API/任务队列最终契约 | D16/D17 | 本文提供草案，最终以 D16/D17 固化 |
| 套餐、支付、退款、毛利 | D20 | 数据诊断、进化实验的计费事件和返还规则 |
| 合规条款、审核 SOP、隐私制度 | D21/D22/D23 | 数据授权、脱敏、删除、人工复核入口，最终以合规专项为准 |

## 2.3 本文档的研发落点

D14 最终必须落到以下工程对象：

1. 发布记录：`ContentPublishRecord`。
2. 指标记录：`PublishMetricsRecord`。
3. 标准化指标：`NormalizedMetrics`。
4. 表现基线：`PerformanceBaseline`。
5. 诊断报告：`DiagnosisReport`。
6. 归因假设：`AttributionHypothesis`。
7. 优化建议：`OptimizationRecommendation`。
8. Prompt 进化候选：`PromptEvolutionCandidate`。
9. Prompt 进化评审：`PromptEvolutionReview`。
10. Prompt 实验：`PromptExperiment`。
11. Prompt 表现聚合：`PromptPerformanceAggregate`。
12. 用户反馈事件：`UserFeedbackEvent`。
13. 数据血缘日志：`DataLineageLog`。
14. 诊断任务队列：`metrics.validate`、`metrics.normalize`、`diagnosis.run`、`attribution.generate`、`prompt_evolution.propose`、`prompt_evolution.evaluate`、`prompt_experiment.aggregate`。
15. 前台页面：数据回填页、指标确认页、诊断报告页、优化建议页、Prompt 进化候选提示页。
16. 后台页面：数据诊断看板、Prompt 表现看板、进化候选评审台、实验看板、成本与失败率统计。
17. 测试体系：指标口径单测、Schema 测试、Prompt 回归、伪因果红队、风控测试、权限测试、计费幂等测试、E2E 测试。
18. AIcoding 研发任务拆分与执行提示词。

---

# 3. 与 D00 及其他文档的关系

## 3.1 D00 对 D14 的硬约束

| D00 约束 | D14 落地方式 |
|---|---|
| 产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具 | 数据诊断只用于优化原创内容表现，不允许把“表现好”归因成“更像原视频”，不允许演化出高相似复制策略 |
| 核心链路包含“数据回填 → 归因诊断 → Prompt 进化” | D14 将该链路固化为数据结构、API、队列、Prompt、评测、风控和后台工作流 |
| 当前优先 MVP1 是“视频拆解 + 原创脚本生成” | D14 在 MVP1 只做手动数据回填、单作品轻量诊断和 Prompt 进化候选预留；账号级自动进化延后 |
| 不得自行扩大第一版范围 | 自动抓取平台数据、自动发布、自动替换生产 Prompt、全量账号级策略优化均通过 Feature Flag 关闭 |
| 不得跳过风控、任务日志、成本记录、测试验收 | 每次诊断、归因和进化候选都必须记录 PromptRun、ModelCallLog、TaskStepLog、RiskCheckReport、BenchmarkResult、CreditTransaction |
| 不得用 mock 冒充真实能力 | Mock 指标、Mock 诊断、Mock Prompt 进化仅允许开发环境，必须 `is_mock=true` 且生产默认关闭 |
| 每个模块必须可独立开发、独立测试、独立验收、独立交接 | D14 拆分为数据回填、指标标准化、诊断归因、进化候选、实验评估、后台看板、测试门禁等独立任务 |

## 3.2 与 D10 Prompt 体系的关系

D14 不直接修改生产 Prompt。D14 只产出 Prompt 进化候选，候选必须进入 D10 的 Prompt Registry 流程。

```text
DiagnosisReport
→ AttributionHypothesis
→ PromptEvolutionCandidate
→ D13 风控
→ D12 Benchmark
→ 人工评审
→ D10 PromptVersion 草稿
→ 灰度实验
→ 发布 / 回滚
```

D14 需要从 D10 读取：

1. `PromptTemplate`。
2. `PromptVersion`。
3. `PromptRun`。
4. `PromptAsset`。
5. `PromptEvaluationResult`。
6. `PromptReleaseRecord`。
7. `PromptExperiment`。

D14 向 D10 回写：

1. Prompt 表现聚合。
2. Prompt 进化候选。
3. Prompt 版本变更建议。
4. Prompt 失败原因统计。
5. Prompt 版本实验结果。

## 3.3 与 D12 Benchmark 的关系

D14 产出的任何 Prompt 进化候选不得直接上线，必须进入 D12 的 Benchmark 回归测试。

| D14 产物 | D12 校验要求 |
|---|---|
| Prompt 进化候选 | 通过相关样本集回归测试，不得造成核心质量指标下降 |
| 新诊断 Prompt | 通过诊断样本 Golden Case、反例样本、伪因果样本、低数据样本 |
| 新归因规则 | 通过“证据不足必须降置信度”的红队样本 |
| 新优化建议规则 | 通过合规、原创度、可执行性、行业适配测试 |
| Prompt 版本实验结论 | 必须包含统计口径、样本量、置信度、负面样本和回滚判断 |

## 3.4 与 D13 风控合规的关系

D13 是 D14 的硬闸门。数据表现再好，也不能推动系统向违规方向进化。

D14 必须调用 D13 的场景：

1. 用户上传发布数据截图或评论截图时，检查隐私和敏感信息。
2. 诊断报告输出前，检查是否包含虚假承诺、诱导违规、平台规避策略。
3. Prompt 进化候选生成后，检查是否降低原创化强度、扩大相似表达、引导仿脸仿声、鼓励搬运或规避审核。
4. 实验结论发布前，检查是否把高风险内容标记为“成功模板”。
5. 用户投诉或权利人投诉后，冻结相关诊断报告、Prompt 候选和实验结论。

## 3.5 与 D20 商业化的关系

D14 直接影响商业化：

1. **免费用户**：可查看基础数据回填和有限诊断摘要。
2. **付费用户**：可运行完整诊断、归因假设、下一条优化建议。
3. **团队/企业用户**：可启用账号级趋势、Prompt 表现看板、批量诊断、人工复核。
4. **高成本任务**：诊断、Prompt 进化候选、Benchmark 回归、实验聚合均需要记录模型成本。
5. **失败返还**：指标不合格或诊断失败不得扣除完整诊断积分。
6. **平台质量成本**：自动重跑、JSON 修复、风控重写成本原则上由平台承担，不重复向用户扣费。

## 3.6 与 D21/D22/D23 的关系

D14 涉及用户发布数据、账号数据、截图、评论、转化数据、可能包含个人信息和商业数据，必须与合规、数据安全、运营后台一致：

```text
D21：授权、AIGC 标识、内容审核、投诉删除最终口径
D22：数据分级、权限、脱敏、留存、删除、审计
D23：客服、退款补偿、人工复核、投诉处理 SOP
D14：数据回填、诊断、归因、Prompt 进化的工程实现
```

---

# 4. 核心结论

## 4.1 一句话结论

D14 是本项目从“一次性生成工具”走向“持续增长系统”的关键闭环，但 MVP1 必须克制：只做手动数据回填、单作品表现诊断、证据化归因假设和 Prompt 进化候选，不做自动抓取、自动发布、自动因果判断和自动替换生产 Prompt。

## 4.2 MVP1 P0 必须实现

| 能力 | 是否 P0 | 说明 |
|---|---:|---|
| 生成资产与发布记录绑定 | 是 | 用户可将某个脚本/标题/封面/Prompt 绑定到已发布视频 |
| 手动指标录入 | 是 | 播放、曝光、完播、点赞、评论、分享、收藏、转粉、转化等核心字段 |
| CSV 导入预留 | 建议 | MVP1 可先支持标准模板 CSV，不做平台直连 |
| 截图上传预留 | 建议 | MVP1 只保存截图，不承诺 OCR 自动解析准确性 |
| 指标标准化与派生指标 | 是 | CTR、完播率、互动率、转粉率、转化率、相对基线 |
| 单作品诊断报告 | 是 | 输出表现结论、证据、归因假设、下一步建议 |
| 低数据量提示 | 是 | 数据不足必须明确显示，不得强行归因 |
| Prompt 进化候选 | 是 | 只生成候选，不自动上线 |
| D13 风控联动 | 是 | 诊断报告和进化候选都必须风控 |
| D12 Benchmark 联动 | 是 | 进化候选必须进入回归门禁 |
| 积分扣费与失败返还 | 是 | 诊断任务按成功、部分成功、失败计费 |
| 后台诊断统计 | 是 | 成本、失败率、质量、Prompt 版本效果 |

## 4.3 MVP1 明确不做

1. 不做平台自动爬虫。
2. 不做绕过平台规则的数据采集。
3. 不做自动发布。
4. 不做自动修改线上 Prompt。
5. 不做“AI 保证爆款”的确定承诺。
6. 不做账号级全自动归因。
7. 不做跨平台未授权数据聚合。
8. 不做评论区个人信息挖掘。
9. 不做黑灰产增长策略。
10. 不做以相似度提升为目标的 Prompt 进化。

## 4.4 长期演进方向

| 阶段 | 目标 | 能力 |
|---|---|---|
| MVP1 | 单作品轻量数据闭环 | 手动指标、单条诊断、候选进化 |
| MVP1.5 | 批量回填与诊断 | CSV 导入、批量诊断、用户账号基线 |
| MVP2 | 账号级表现诊断 | 内容栏目、选题、发布时间、受众画像、系列表现 |
| MVP3 | 生成结果与发布数据闭环 | 视频 Prompt、生成草稿、发布表现对齐 |
| MVP4 | Prompt 自进化系统 | 灰度实验、自动候选、人工审批、可回滚发布 |

---

# 5. 详细设计

## 5.1 总体业务流程

```text
用户选择已生成资产
→ 填写发布平台、发布时间、发布链接、使用的标题/封面/脚本版本
→ 录入或导入发布指标
→ 系统校验数据完整性和授权
→ 系统标准化指标并计算派生指标
→ 系统匹配账号历史基线或同类内容基线
→ 生成表现判断
→ 生成归因假设
→ 输出下一条优化建议
→ 生成 Prompt 进化候选
→ D13 风控检查
→ D12 Benchmark 回归
→ 人工评审
→ 灰度实验
→ 通过后进入 D10 Prompt Registry 版本发布流程
```

## 5.2 数据回填输入范围

### 5.2.1 P0 手动输入字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `asset_id` | string | 是 | 系统生成资产 ID，可以是脚本、标题、封面建议、视频 Prompt 或输出包 |
| `task_id` | string | 是 | 原生成任务 ID |
| `platform` | enum | 是 | douyin、kuaishou、xiaohongshu、bilibili、tiktok、youtube_shorts、other |
| `publish_url` | string | 否 | 用户发布链接；MVP1 不自动抓取 |
| `published_at` | datetime | 是 | 发布时间 |
| `metrics_captured_at` | datetime | 是 | 指标采集时间 |
| `view_count` | integer | 是 | 播放量或观看量 |
| `impression_count` | integer | 否 | 曝光量；部分平台没有 |
| `like_count` | integer | 否 | 点赞数 |
| `comment_count` | integer | 否 | 评论数 |
| `share_count` | integer | 否 | 分享数 |
| `save_count` | integer | 否 | 收藏数 |
| `follow_count` | integer | 否 | 转粉数 |
| `avg_watch_seconds` | number | 否 | 平均观看时长 |
| `completion_rate` | number | 否 | 完播率，0-1 |
| `click_count` | integer | 否 | 主页、商品、私信、链接点击 |
| `lead_count` | integer | 否 | 表单、私信、咨询等线索数 |
| `conversion_count` | integer | 否 | 成交或目标转化数 |
| `revenue_amount` | number | 否 | 直接归因收入；可选 |
| `ad_spend_amount` | number | 否 | 投放金额；自然流量可为 0 |
| `manual_notes` | string | 否 | 用户对表现的主观反馈 |
| `data_source_type` | enum | 是 | manual、csv、screenshot、api_future |
| `data_authorization_confirmed` | boolean | 是 | 用户确认有权上传和使用相关数据 |

### 5.2.2 P1/P2 扩展输入

1. 平台导出的 CSV。
2. 用户上传数据截图，OCR 解析后用户确认。
3. 评论情绪摘要，但必须脱敏和获得授权。
4. 流量来源占比。
5. 粉丝 / 非粉丝占比。
6. 留存曲线。
7. 标题封面 A/B 数据。
8. 投放计划和消耗数据。
9. 账号历史内容表现。
10. 团队成员标注的失败原因。

## 5.3 指标标准化规则

### 5.3.1 核心派生指标

| 指标 | 计算方式 | 说明 |
|---|---|---|
| `play_rate` | `view_count / impression_count` | 有曝光字段时可算，没有曝光时置为 null |
| `like_rate` | `like_count / view_count` | 点赞率 |
| `comment_rate` | `comment_count / view_count` | 评论率 |
| `share_rate` | `share_count / view_count` | 分享率 |
| `save_rate` | `save_count / view_count` | 收藏率 |
| `engagement_rate` | `(like + comment + share + save) / view_count` | 综合互动率 |
| `follow_rate` | `follow_count / view_count` | 转粉率 |
| `lead_rate` | `lead_count / view_count` | 线索率 |
| `conversion_rate` | `conversion_count / view_count` | 转化率 |
| `roi` | `revenue_amount / ad_spend_amount` | 投放或转化场景使用 |
| `relative_view_index` | `view_count / baseline_view_count` | 相对账号基线 |
| `relative_engagement_index` | `engagement_rate / baseline_engagement_rate` | 相对互动基线 |
| `relative_conversion_index` | `conversion_rate / baseline_conversion_rate` | 相对转化基线 |
| `data_maturity_hours` | `metrics_captured_at - published_at` | 数据成熟度 |

### 5.3.2 数据成熟度分层

| 等级 | 条件 | 系统行为 |
|---|---|---|
| `TOO_EARLY` | 发布不足 2 小时 | 只输出早期观察，不做归因 |
| `EARLY_SIGNAL` | 2-24 小时 | 输出弱归因，强调不确定性 |
| `STABLE_SIGNAL` | 24-72 小时 | 可进行常规诊断 |
| `MATURE_SIGNAL` | 超过 72 小时 | 可纳入 Prompt 表现聚合 |
| `OUTDATED` | 超过 30 天且平台环境变化明显 | 仅做历史参考，不直接指导 Prompt 进化 |

### 5.3.3 数据质量分层

| 等级 | 条件 | 系统行为 |
|---|---|---|
| `HIGH` | 核心指标完整、样本量足够、时间窗口合理、来源可信 | 可进入诊断与 Prompt 候选生成 |
| `MEDIUM` | 指标基本完整但缺少曝光/完播/转化之一 | 可诊断但降低置信度 |
| `LOW` | 只有播放量和少量互动 | 只输出基础建议，不生成 Prompt 进化候选 |
| `INVALID` | 指标异常、时间不合理、负数、明显伪造或缺授权 | 拒绝诊断，不扣费或返还 |

## 5.4 表现判断规则

D14 必须避免“播放量高就是成功”的粗暴判断。表现判断需要结合用户目标、内容类型和基线。

### 5.4.1 用户目标分类

| 目标 | 主要指标 | 辅助指标 |
|---|---|---|
| 曝光增长 | 播放量、曝光量、播放率 | 分享率、完播率 |
| 互动增长 | 互动率、评论率、收藏率 | 播放量、话题相关性 |
| 转粉增长 | 转粉率、粉丝增长 | 完播率、主页点击 |
| 私域线索 | 线索率、私信数、表单数 | 完播率、点击率 |
| 成交转化 | 转化率、GMV、ROI | 线索率、点击率 |
| 品牌信任 | 完播率、收藏率、正向反馈 | 评论情绪、分享率 |

### 5.4.2 表现等级

| 等级 | 判断口径 | 文案口径 |
|---|---|---|
| `OUTPERFORM` | 关键目标指标高于基线 150% 以上，且样本量足够 | 表现显著优于近期基线 |
| `ABOVE_BASELINE` | 关键目标指标高于基线 110%-150% | 表现优于近期平均 |
| `ON_BASELINE` | 关键目标指标在基线 ±10% | 表现接近近期平均 |
| `UNDERPERFORM` | 关键目标指标低于基线 70%-90% | 表现低于近期平均 |
| `SIGNIFICANT_DROP` | 关键目标指标低于基线 70% 以下 | 表现明显低于近期基线 |
| `INSUFFICIENT_DATA` | 样本量、时间窗口或字段不足 | 数据不足，暂不建议做强归因 |

## 5.5 归因原则

### 5.5.1 归因只能输出“假设”

系统输出必须使用以下口径：

1. “可能原因是……”
2. “从现有数据看，更像是……”
3. “证据支持度为……”
4. “还需要下一条内容验证……”
5. “不排除平台分发、发布时间、账号权重等外部因素影响……”

禁止使用：

1. “一定是因为……”
2. “绝对原因是……”
3. “只要这样改必爆……”
4. “平台算法判定为……”
5. “保证下一条提升……”

### 5.5.2 归因维度

| 维度 | 可诊断问题 | 输入证据 |
|---|---|---|
| 钩子 Hook | 开头吸引力不足、痛点不明确、利益点弱 | 前 3 秒结构、标题、完播率、播放率 |
| 标题封面 | 标题与内容不匹配、封面利益点不清晰 | 标题、封面建议、播放率、点击率 |
| 节奏结构 | 信息密度过高/过低、转折不足、铺垫过长 | ShotDNA、完播率、平均观看时长 |
| 人群匹配 | 内容对目标用户不够具体 | 用户画像、评论反馈、转粉率 |
| 价值表达 | 干货不足、痛点不强、卖点不清晰 | 脚本价值点、收藏率、评论关键词 |
| 信任建立 | 缺案例、缺证明、缺身份背书 | 脚本结构、转化率、评论质疑 |
| 产品植入 | 过早销售、转化路径不自然 | CTA、线索率、转化率、负面评论 |
| 情绪曲线 | 情绪平、冲突弱、缺反差 | Content DNA 情绪曲线、互动率 |
| 原创化执行 | 过度模板化、行业迁移不自然 | Creative IR、D13 风控、用户反馈 |
| 发布环境 | 发布时间、热点、平台波动 | 发布时间、平台、账号历史数据 |
| 执行质量 | 拍摄、剪辑、口播、素材影响 | 用户手动反馈、成片链接、截图 |
| 合规限制 | 风控导致表达保守或弱化 | RiskCheckReport、ComplianceCheckReport |

## 5.6 Prompt 进化规则

### 5.6.1 Prompt 进化不是自动改 Prompt

Prompt 进化必须分 7 步：

```text
诊断结论
→ 归因假设
→ 可验证优化动作
→ PromptEvolutionCandidate
→ D13 风控
→ D12 Benchmark
→ 人工评审 / 灰度实验
→ D10 PromptVersion 发布或回滚
```

### 5.6.2 进化候选类型

| 类型 | 说明 | 示例 |
|---|---|---|
| `VARIABLE_TUNING` | 调整 Prompt 变量权重或默认值 | 增强“目标用户痛点”变量权重 |
| `INSTRUCTION_PATCH` | 修改 Prompt 指令片段 | 在脚本生成 Prompt 中要求前 3 秒明确利益点 |
| `RUBRIC_UPDATE` | 修改评分或质检 Rubric | 增加“转化路径自然度”评分项 |
| `NEGATIVE_CONSTRAINT` | 增加禁止项 | 禁止使用过度夸张承诺标题 |
| `STRUCTURE_TEMPLATE` | 提炼表现好的结构模板 | “反常识开头 → 场景证明 → 行动建议”结构 |
| `INDUSTRY_ADAPTATION` | 为行业生成特定表达规则 | 本地生活行业增加“附近/到店/价格透明”表达 |
| `MODEL_PROMPT_ADAPTER` | 针对模型能力调整输出格式 | 对某视频模型增加镜头运动和主体约束字段 |
| `QUALITY_GATE_UPDATE` | 调整输出门槛 | 低于 75 分不得进入导出 |

### 5.6.3 进化候选禁止规则

以下候选必须直接拒绝或转人工复核：

1. 降低原创化强度。
2. 复制原视频台词、封面、角色、镜头或独特剧情。
3. 引导仿脸、仿声、盗用人物形象。
4. 引导规避平台审核。
5. 鼓励虚假宣传、夸大承诺、医疗金融等高风险承诺。
6. 为追求数据而删除 AIGC 标识或风险提示。
7. 使用未授权平台数据训练用户专属模板。
8. 将低置信度归因包装为确定规则。

---

# 6. 数据结构 / API / Prompt / 流程 / 页面 / 状态

## 6.1 核心枚举

```typescript
export type PublishPlatform =
  | 'douyin'
  | 'kuaishou'
  | 'xiaohongshu'
  | 'bilibili'
  | 'tiktok'
  | 'youtube_shorts'
  | 'wechat_channels'
  | 'other';

export type MetricsSourceType =
  | 'manual'
  | 'csv'
  | 'screenshot'
  | 'api_future'
  | 'admin_import';

export type DataQualityLevel =
  | 'HIGH'
  | 'MEDIUM'
  | 'LOW'
  | 'INVALID';

export type DataMaturityLevel =
  | 'TOO_EARLY'
  | 'EARLY_SIGNAL'
  | 'STABLE_SIGNAL'
  | 'MATURE_SIGNAL'
  | 'OUTDATED';

export type PerformanceLevel =
  | 'OUTPERFORM'
  | 'ABOVE_BASELINE'
  | 'ON_BASELINE'
  | 'UNDERPERFORM'
  | 'SIGNIFICANT_DROP'
  | 'INSUFFICIENT_DATA';

export type DiagnosisStatus =
  | 'draft'
  | 'validating_metrics'
  | 'metrics_invalid'
  | 'normalizing_metrics'
  | 'baseline_matching'
  | 'running_diagnosis'
  | 'risk_checking'
  | 'completed'
  | 'partial_completed'
  | 'blocked'
  | 'failed'
  | 'cancelled';

export type AttributionConfidence =
  | 'LOW'
  | 'MEDIUM'
  | 'HIGH';

export type PromptEvolutionCandidateStatus =
  | 'draft'
  | 'risk_checking'
  | 'benchmark_pending'
  | 'benchmark_failed'
  | 'manual_review_pending'
  | 'approved_for_experiment'
  | 'experiment_running'
  | 'experiment_won'
  | 'experiment_lost'
  | 'published'
  | 'rejected'
  | 'rolled_back';
```

## 6.2 JSON Schema：ContentPublishRecord

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ContentPublishRecord",
  "type": "object",
  "required": [
    "id",
    "user_id",
    "task_id",
    "asset_id",
    "platform",
    "published_at",
    "data_authorization_confirmed",
    "created_at"
  ],
  "properties": {
    "id": { "type": "string" },
    "user_id": { "type": "string" },
    "team_id": { "type": ["string", "null"] },
    "task_id": { "type": "string" },
    "asset_id": { "type": "string" },
    "content_dna_id": { "type": ["string", "null"] },
    "creative_ir_id": { "type": ["string", "null"] },
    "prompt_asset_ids": {
      "type": "array",
      "items": { "type": "string" }
    },
    "prompt_run_ids": {
      "type": "array",
      "items": { "type": "string" }
    },
    "risk_check_report_id": { "type": ["string", "null"] },
    "platform": { "type": "string" },
    "publish_url": { "type": ["string", "null"] },
    "published_at": { "type": "string", "format": "date-time" },
    "content_title_used": { "type": ["string", "null"] },
    "cover_variant_used": { "type": ["string", "null"] },
    "script_variant_used": { "type": ["string", "null"] },
    "video_prompt_variant_used": { "type": ["string", "null"] },
    "data_authorization_confirmed": { "type": "boolean" },
    "aigc_label_used": { "type": ["boolean", "null"] },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
```

## 6.3 JSON Schema：PublishMetricsRecord

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PublishMetricsRecord",
  "type": "object",
  "required": [
    "id",
    "publish_record_id",
    "user_id",
    "platform",
    "metrics_captured_at",
    "source_type",
    "raw_metrics",
    "data_quality_level"
  ],
  "properties": {
    "id": { "type": "string" },
    "publish_record_id": { "type": "string" },
    "user_id": { "type": "string" },
    "platform": { "type": "string" },
    "metrics_captured_at": { "type": "string", "format": "date-time" },
    "source_type": { "type": "string" },
    "source_file_asset_id": { "type": ["string", "null"] },
    "raw_metrics": {
      "type": "object",
      "properties": {
        "view_count": { "type": ["integer", "null"], "minimum": 0 },
        "impression_count": { "type": ["integer", "null"], "minimum": 0 },
        "like_count": { "type": ["integer", "null"], "minimum": 0 },
        "comment_count": { "type": ["integer", "null"], "minimum": 0 },
        "share_count": { "type": ["integer", "null"], "minimum": 0 },
        "save_count": { "type": ["integer", "null"], "minimum": 0 },
        "follow_count": { "type": ["integer", "null"], "minimum": 0 },
        "avg_watch_seconds": { "type": ["number", "null"], "minimum": 0 },
        "completion_rate": { "type": ["number", "null"], "minimum": 0, "maximum": 1 },
        "click_count": { "type": ["integer", "null"], "minimum": 0 },
        "lead_count": { "type": ["integer", "null"], "minimum": 0 },
        "conversion_count": { "type": ["integer", "null"], "minimum": 0 },
        "revenue_amount": { "type": ["number", "null"], "minimum": 0 },
        "ad_spend_amount": { "type": ["number", "null"], "minimum": 0 }
      }
    },
    "normalized_metrics": { "type": ["object", "null"] },
    "data_quality_level": { "type": "string" },
    "data_maturity_level": { "type": ["string", "null"] },
    "validation_errors": {
      "type": "array",
      "items": { "type": "string" }
    },
    "manual_notes": { "type": ["string", "null"] },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
```

## 6.4 JSON Schema：DiagnosisReport

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DiagnosisReport",
  "type": "object",
  "required": [
    "id",
    "publish_record_id",
    "metrics_record_id",
    "status",
    "performance_level",
    "summary",
    "evidence_items",
    "attribution_hypotheses",
    "recommendations",
    "confidence_score",
    "limitations"
  ],
  "properties": {
    "id": { "type": "string" },
    "publish_record_id": { "type": "string" },
    "metrics_record_id": { "type": "string" },
    "task_id": { "type": "string" },
    "asset_id": { "type": "string" },
    "status": { "type": "string" },
    "performance_level": { "type": "string" },
    "goal_type": { "type": "string" },
    "summary": { "type": "string", "minLength": 20 },
    "key_metric_findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["metric", "value", "baseline_value", "interpretation"],
        "properties": {
          "metric": { "type": "string" },
          "value": { "type": ["number", "integer", "null"] },
          "baseline_value": { "type": ["number", "integer", "null"] },
          "relative_index": { "type": ["number", "null"] },
          "interpretation": { "type": "string" }
        }
      }
    },
    "evidence_items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["evidence_type", "source_ref", "description"],
        "properties": {
          "evidence_type": { "type": "string" },
          "source_ref": { "type": "string" },
          "description": { "type": "string" },
          "strength": { "type": "string", "enum": ["weak", "medium", "strong"] }
        }
      }
    },
    "attribution_hypotheses": {
      "type": "array",
      "items": { "$ref": "#/definitions/AttributionHypothesis" }
    },
    "recommendations": {
      "type": "array",
      "items": { "$ref": "#/definitions/OptimizationRecommendation" }
    },
    "prompt_evolution_candidate_ids": {
      "type": "array",
      "items": { "type": "string" }
    },
    "confidence_score": { "type": "number", "minimum": 0, "maximum": 1 },
    "limitations": {
      "type": "array",
      "items": { "type": "string" }
    },
    "risk_check_report_id": { "type": ["string", "null"] },
    "model_call_log_ids": {
      "type": "array",
      "items": { "type": "string" }
    },
    "created_at": { "type": "string", "format": "date-time" }
  },
  "definitions": {
    "AttributionHypothesis": {
      "type": "object",
      "required": ["factor", "hypothesis", "confidence", "evidence_refs", "counter_evidence", "validation_action"],
      "properties": {
        "factor": { "type": "string" },
        "hypothesis": { "type": "string" },
        "confidence": { "type": "string", "enum": ["LOW", "MEDIUM", "HIGH"] },
        "evidence_refs": {
          "type": "array",
          "items": { "type": "string" }
        },
        "counter_evidence": {
          "type": "array",
          "items": { "type": "string" }
        },
        "validation_action": { "type": "string" }
      }
    },
    "OptimizationRecommendation": {
      "type": "object",
      "required": ["recommendation_type", "action", "expected_metric_impact", "risk_level", "next_test"],
      "properties": {
        "recommendation_type": { "type": "string" },
        "action": { "type": "string" },
        "expected_metric_impact": { "type": "string" },
        "risk_level": { "type": "string" },
        "next_test": { "type": "string" }
      }
    }
  }
}
```

## 6.5 JSON Schema：PromptEvolutionCandidate

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PromptEvolutionCandidate",
  "type": "object",
  "required": [
    "id",
    "source_diagnosis_report_id",
    "target_prompt_template_id",
    "target_prompt_version_id",
    "candidate_type",
    "proposed_change",
    "reasoning",
    "expected_improvement",
    "risk_assessment",
    "status"
  ],
  "properties": {
    "id": { "type": "string" },
    "source_diagnosis_report_id": { "type": "string" },
    "target_prompt_template_id": { "type": "string" },
    "target_prompt_version_id": { "type": "string" },
    "candidate_type": { "type": "string" },
    "proposed_change": {
      "type": "object",
      "required": ["change_summary", "patch_format", "patch_payload"],
      "properties": {
        "change_summary": { "type": "string" },
        "patch_format": { "type": "string", "enum": ["text_patch", "json_patch", "variable_default_patch", "rubric_patch"] },
        "patch_payload": { "type": "object" }
      }
    },
    "reasoning": { "type": "string" },
    "evidence_refs": {
      "type": "array",
      "items": { "type": "string" }
    },
    "expected_improvement": {
      "type": "object",
      "properties": {
        "primary_metric": { "type": "string" },
        "expected_direction": { "type": "string", "enum": ["increase", "decrease", "stabilize"] },
        "confidence": { "type": "string", "enum": ["LOW", "MEDIUM", "HIGH"] }
      }
    },
    "risk_assessment": {
      "type": "object",
      "properties": {
        "originality_risk": { "type": "string" },
        "compliance_risk": { "type": "string" },
        "quality_regression_risk": { "type": "string" },
        "cost_risk": { "type": "string" }
      }
    },
    "benchmark_result_id": { "type": ["string", "null"] },
    "risk_check_report_id": { "type": ["string", "null"] },
    "reviewer_id": { "type": ["string", "null"] },
    "status": { "type": "string" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
```

## 6.6 数据库表草案

### 6.6.1 `content_publish_records`

| 字段 | 类型 | 必填 | 索引 | 说明 |
|---|---|---:|---:|---|
| `id` | uuid | 是 | PK | 发布记录 ID |
| `user_id` | uuid | 是 | idx | 用户 ID |
| `team_id` | uuid | 否 | idx | 团队 ID |
| `task_id` | uuid | 是 | idx | 原生成任务 ID |
| `asset_id` | uuid | 是 | idx | 输出资产 ID |
| `content_dna_id` | uuid | 否 | idx | Content DNA ID |
| `creative_ir_id` | uuid | 否 | idx | Creative IR ID |
| `platform` | varchar | 是 | idx | 发布平台 |
| `publish_url` | text | 否 |  | 发布链接 |
| `published_at` | timestamptz | 是 | idx | 发布时间 |
| `data_authorization_confirmed` | boolean | 是 |  | 数据授权确认 |
| `aigc_label_used` | boolean | 否 |  | 用户是否标记 AIGC |
| `status` | varchar | 是 | idx | active、deleted、frozen |
| `created_at` | timestamptz | 是 |  | 创建时间 |
| `updated_at` | timestamptz | 是 |  | 更新时间 |

### 6.6.2 `publish_metrics_records`

| 字段 | 类型 | 必填 | 索引 | 说明 |
|---|---|---:|---:|---|
| `id` | uuid | 是 | PK | 指标记录 ID |
| `publish_record_id` | uuid | 是 | idx | 发布记录 |
| `user_id` | uuid | 是 | idx | 用户 ID |
| `platform` | varchar | 是 | idx | 平台 |
| `metrics_captured_at` | timestamptz | 是 | idx | 指标采集时间 |
| `source_type` | varchar | 是 | idx | manual/csv/screenshot/api_future |
| `source_file_asset_id` | uuid | 否 |  | CSV/截图文件 |
| `raw_metrics_json` | jsonb | 是 |  | 原始指标 |
| `normalized_metrics_json` | jsonb | 否 |  | 标准化指标 |
| `data_quality_level` | varchar | 是 | idx | HIGH/MEDIUM/LOW/INVALID |
| `data_maturity_level` | varchar | 否 | idx | 数据成熟度 |
| `validation_errors_json` | jsonb | 否 |  | 校验错误 |
| `is_mock` | boolean | 是 | idx | 是否 Mock 数据，生产默认 false |
| `created_at` | timestamptz | 是 |  | 创建时间 |

### 6.6.3 `performance_baselines`

| 字段 | 类型 | 必填 | 索引 | 说明 |
|---|---|---:|---:|---|
| `id` | uuid | 是 | PK | 基线 ID |
| `user_id` | uuid | 是 | idx | 用户 ID |
| `team_id` | uuid | 否 | idx | 团队 ID |
| `platform` | varchar | 是 | idx | 平台 |
| `baseline_type` | varchar | 是 | idx | account_recent、content_type、industry_future |
| `content_type` | varchar | 否 | idx | 口播/剧情/种草/知识等 |
| `window_start` | timestamptz | 是 |  | 窗口开始 |
| `window_end` | timestamptz | 是 |  | 窗口结束 |
| `sample_count` | integer | 是 |  | 样本数 |
| `metrics_json` | jsonb | 是 |  | 均值、中位数、分位数 |
| `confidence_level` | varchar | 是 |  | LOW/MEDIUM/HIGH |
| `created_at` | timestamptz | 是 |  | 创建时间 |

### 6.6.4 `diagnosis_reports`

| 字段 | 类型 | 必填 | 索引 | 说明 |
|---|---|---:|---:|---|
| `id` | uuid | 是 | PK | 诊断报告 ID |
| `user_id` | uuid | 是 | idx | 用户 ID |
| `publish_record_id` | uuid | 是 | idx | 发布记录 |
| `metrics_record_id` | uuid | 是 | idx | 指标记录 |
| `task_id` | uuid | 是 | idx | 原生成任务 |
| `asset_id` | uuid | 是 | idx | 输出资产 |
| `status` | varchar | 是 | idx | 诊断状态 |
| `performance_level` | varchar | 是 | idx | 表现等级 |
| `goal_type` | varchar | 是 | idx | 用户目标 |
| `summary` | text | 是 |  | 诊断摘要 |
| `report_json` | jsonb | 是 |  | 完整报告 |
| `confidence_score` | numeric | 是 |  | 0-1 |
| `risk_check_report_id` | uuid | 否 | idx | 风控报告 |
| `credit_transaction_id` | uuid | 否 | idx | 积分流水 |
| `created_at` | timestamptz | 是 |  | 创建时间 |

### 6.6.5 `attribution_hypotheses`

| 字段 | 类型 | 必填 | 索引 | 说明 |
|---|---|---:|---:|---|
| `id` | uuid | 是 | PK | 归因假设 ID |
| `diagnosis_report_id` | uuid | 是 | idx | 诊断报告 |
| `factor` | varchar | 是 | idx | 归因维度 |
| `hypothesis` | text | 是 |  | 假设描述 |
| `confidence` | varchar | 是 | idx | LOW/MEDIUM/HIGH |
| `evidence_refs_json` | jsonb | 是 |  | 证据引用 |
| `counter_evidence_json` | jsonb | 否 |  | 反证 |
| `validation_action` | text | 是 |  | 下一步验证动作 |
| `created_at` | timestamptz | 是 |  | 创建时间 |

### 6.6.6 `prompt_evolution_candidates`

| 字段 | 类型 | 必填 | 索引 | 说明 |
|---|---|---:|---:|---|
| `id` | uuid | 是 | PK | 候选 ID |
| `source_diagnosis_report_id` | uuid | 是 | idx | 来源诊断报告 |
| `target_prompt_template_id` | uuid | 是 | idx | 目标 Prompt 模板 |
| `target_prompt_version_id` | uuid | 是 | idx | 目标 Prompt 版本 |
| `candidate_type` | varchar | 是 | idx | 候选类型 |
| `proposed_change_json` | jsonb | 是 |  | 改动内容 |
| `reasoning` | text | 是 |  | 原因 |
| `expected_improvement_json` | jsonb | 是 |  | 预期提升 |
| `risk_assessment_json` | jsonb | 是 |  | 风险评估 |
| `risk_check_report_id` | uuid | 否 | idx | 风控报告 |
| `benchmark_result_id` | uuid | 否 | idx | Benchmark 结果 |
| `status` | varchar | 是 | idx | 状态 |
| `created_by` | uuid | 否 | idx | 创建人/系统 |
| `reviewer_id` | uuid | 否 | idx | 审核人 |
| `created_at` | timestamptz | 是 |  | 创建时间 |
| `updated_at` | timestamptz | 是 |  | 更新时间 |

### 6.6.7 其他表

| 表名 | 作用 |
|---|---|
| `prompt_evolution_reviews` | 记录人工评审、通过/拒绝原因、法务/风控意见 |
| `prompt_experiments` | 记录 Prompt A/B 或灰度实验配置 |
| `prompt_experiment_results` | 记录实验数据、样本量、效果、结论、回滚判断 |
| `prompt_performance_aggregates` | 按 PromptTemplate、PromptVersion、行业、平台、内容类型聚合表现 |
| `user_feedback_events` | 用户对诊断、脚本、标题、Prompt 的主观反馈 |
| `data_lineage_logs` | 记录从指标到报告到候选的血缘追踪 |
| `diagnosis_event_logs` | 记录诊断任务状态、失败原因、重试、人工处理 |

## 6.7 API 草案

### 6.7.1 创建发布记录

```http
POST /api/v1/publish-records
```

请求：

```json
{
  "task_id": "task_123",
  "asset_id": "asset_456",
  "platform": "douyin",
  "publish_url": "https://example.com/video/xxx",
  "published_at": "2026-05-25T10:00:00+08:00",
  "content_title_used": "为什么你的视频没人看？",
  "aigc_label_used": true,
  "data_authorization_confirmed": true
}
```

响应：

```json
{
  "publish_record_id": "pub_123",
  "status": "created",
  "next_step": "submit_metrics"
}
```

### 6.7.2 提交手动指标

```http
POST /api/v1/publish-records/{publish_record_id}/metrics/manual
```

请求：

```json
{
  "metrics_captured_at": "2026-05-26T10:00:00+08:00",
  "raw_metrics": {
    "view_count": 3200,
    "impression_count": 15000,
    "like_count": 180,
    "comment_count": 25,
    "share_count": 18,
    "save_count": 60,
    "follow_count": 32,
    "avg_watch_seconds": 8.5,
    "completion_rate": 0.31,
    "lead_count": 8
  },
  "manual_notes": "评论里有人问价格，但转化一般。"
}
```

响应：

```json
{
  "metrics_record_id": "metric_123",
  "data_quality_level": "HIGH",
  "data_maturity_level": "STABLE_SIGNAL",
  "normalized_metrics": {
    "engagement_rate": 0.0884,
    "follow_rate": 0.01,
    "lead_rate": 0.0025
  }
}
```

### 6.7.3 运行诊断

```http
POST /api/v1/diagnosis/run
```

请求：

```json
{
  "publish_record_id": "pub_123",
  "metrics_record_id": "metric_123",
  "goal_type": "lead_generation",
  "generate_prompt_evolution_candidates": true
}
```

响应：

```json
{
  "diagnosis_report_id": "diag_123",
  "task_id": "diagnosis_task_123",
  "status": "running_diagnosis",
  "credit_hold_id": "credit_hold_123"
}
```

### 6.7.4 获取诊断报告

```http
GET /api/v1/diagnosis-reports/{diagnosis_report_id}
```

响应：

```json
{
  "id": "diag_123",
  "status": "completed",
  "performance_level": "ABOVE_BASELINE",
  "confidence_score": 0.72,
  "summary": "该视频在线索目标上高于近期基线，但转化效率仍受 CTA 清晰度影响。",
  "attribution_hypotheses": [],
  "recommendations": [],
  "prompt_evolution_candidate_ids": ["pec_001"]
}
```

### 6.7.5 Prompt 进化候选评审

```http
POST /api/v1/admin/prompt-evolution-candidates/{candidate_id}/review
```

请求：

```json
{
  "decision": "approve_for_benchmark",
  "review_notes": "候选没有降低原创化强度，可进入 Benchmark。"
}
```

响应：

```json
{
  "candidate_id": "pec_001",
  "status": "benchmark_pending"
}
```

## 6.8 任务队列设计

| 队列 | 触发 | 输入 | 输出 | 重试 |
|---|---|---|---|---|
| `metrics.validate` | 提交指标 | PublishMetricsRecord | validation result | 1 次，不应重复收费 |
| `metrics.normalize` | 指标校验通过 | raw_metrics | normalized_metrics | 2 次 |
| `baseline.compute` | 新指标录入或定时 | user_id/platform/content_type | PerformanceBaseline | 2 次 |
| `diagnosis.run` | 用户发起诊断 | publish_record_id/metrics_record_id | DiagnosisReport | 2 次，失败返还诊断积分 |
| `attribution.generate` | 诊断指标准备完成 | metrics + assets + baseline | AttributionHypothesis[] | 2 次，失败降级为规则诊断 |
| `prompt_evolution.propose` | 诊断完成且质量足够 | DiagnosisReport + PromptVersion | PromptEvolutionCandidate[] | 1 次，失败不影响诊断报告展示 |
| `prompt_evolution.risk_check` | 候选生成 | candidate | RiskCheckReport | 2 次，失败转人工 |
| `prompt_evolution.benchmark` | 候选通过风控 | candidate | BenchmarkResult | 按 D12 规则 |
| `prompt_experiment.aggregate` | 实验数据回填 | experiment_id | ExperimentResult | 定时任务 |

## 6.9 日志与可观测性

每次 D14 任务必须写入：

1. `TaskStepLog`：任务步骤、状态、耗时、失败原因。
2. `ModelCallLog`：模型、Token、成本、延迟、失败原因。
3. `PromptRun`：诊断 Prompt、归因 Prompt、进化 Prompt 的版本、输入摘要、输出摘要。
4. `CreditTransaction`：冻结、扣费、返还、补偿。
5. `DataLineageLog`：指标记录 → 诊断报告 → 归因假设 → Prompt 候选 → Benchmark → PromptVersion。
6. `RiskEventLog`：敏感数据、违规建议、候选拦截、人工复核。
7. `AuditLog`：管理员查看、编辑、评审、导出、删除。

## 6.10 权限设计

| 角色 | 权限 |
|---|---|
| 普通用户 | 创建自己的发布记录、录入指标、查看自己的诊断报告、提交反馈、删除自己的数据 |
| 团队成员 | 查看被授权团队项目数据，不能审批 Prompt 候选 |
| 团队管理员 | 管理团队发布记录、诊断报告、数据删除请求 |
| 平台运营 | 查看脱敏诊断统计和失败原因，不可查看用户敏感商业数据明文 |
| Prompt 管理员 | 查看候选、提交 Benchmark、创建 Prompt 草稿 |
| 风控审核员 | 查看被风控命中的候选、诊断报告和投诉数据 |
| 超级管理员 | 配置指标口径、规则版本、Feature Flag；所有操作必须审计 |

## 6.11 页面设计

### 6.11.1 前台：数据回填页

页面目标：让用户将一次生成结果与真实发布数据绑定。

页面模块：

1. 选择生成资产。
2. 填写平台与发布时间。
3. 填写发布链接。
4. 选择是否使用系统生成的标题/脚本/Prompt。
5. 确认数据授权。
6. 录入指标。
7. 数据完整度提示。
8. 预计消耗积分。
9. 提交诊断按钮。

### 6.11.2 前台：诊断报告页

页面模块：

1. 表现总览。
2. 数据质量与成熟度。
3. 关键指标对比。
4. 归因假设列表。
5. 证据说明。
6. 不确定性提示。
7. 下一条优化建议。
8. Prompt 进化候选提示。
9. 风险提示。
10. 导出报告。
11. 用户反馈入口。

### 6.11.3 后台：Prompt 进化候选评审台

页面模块：

1. 候选列表。
2. 来源诊断报告。
3. 目标 Prompt 版本。
4. 改动 diff。
5. 预期影响指标。
6. D13 风控结果。
7. D12 Benchmark 状态。
8. 人工评审意见。
9. 批准实验 / 拒绝 / 退回修改。
10. 回滚记录。

### 6.11.4 后台：Prompt 表现看板

指标：

1. Prompt 版本使用次数。
2. 平均诊断得分。
3. 平均播放相对指数。
4. 平均互动相对指数。
5. 平均转化相对指数。
6. 失败率。
7. 风控命中率。
8. 单次成本。
9. 积分收入。
10. 毛利估算。
11. 用户复购/留存关联。

## 6.12 状态机

### 6.12.1 DiagnosisStatus

```text
draft
→ validating_metrics
→ metrics_invalid / normalizing_metrics
→ baseline_matching
→ running_diagnosis
→ risk_checking
→ completed / partial_completed / blocked / failed / cancelled
```

### 6.12.2 PromptEvolutionCandidateStatus

```text
draft
→ risk_checking
→ benchmark_pending
→ benchmark_failed / manual_review_pending
→ approved_for_experiment
→ experiment_running
→ experiment_won / experiment_lost
→ published / rejected / rolled_back
```

---

# 7. 异常与失败处理

## 7.1 指标异常

| 异常 | 处理 | 扣费 |
|---|---|---|
| 指标为空 | 阻止提交，提示必填字段 | 不扣费 |
| 负数指标 | 阻止提交 | 不扣费 |
| 完播率大于 1 | 阻止提交或自动识别百分比口径 | 不扣费 |
| 播放量小于互动数 | 提示校验异常，要求确认 | 未确认不扣费 |
| 发布时间晚于采集时间 | 阻止提交 | 不扣费 |
| 数据授权未确认 | 阻止提交 | 不扣费 |
| 数据过早 | 允许保存，提示稍后诊断 | 不扣费或只扣基础保存成本 |
| 数据不足 | 只输出轻量建议，不生成候选 | 按轻量诊断计费或返还差额 |

## 7.2 诊断异常

| 异常 | 处理 | 用户展示 |
|---|---|---|
| 无基线 | 使用规则基线或提示“暂无账号基线” | 降低置信度 |
| PromptRun 失败 | 自动重试，仍失败则降级规则诊断 | 展示部分报告 |
| AI 输出 JSON 解析失败 | JSON repair；失败则重试 | 不展示错误 JSON |
| 归因置信度过低 | 不输出强结论，只输出可测试问题 | 显示“证据不足” |
| 风控拦截 | 不展示违规建议，生成安全改写 | 显示拦截原因 |
| 任务超时 | 停止任务，记录成本，返还积分 | 提供客服入口 |

## 7.3 Prompt 进化异常

| 异常 | 处理 |
|---|---|
| 候选与 D13 风控冲突 | 拦截，状态 `rejected` 或转人工 |
| 候选 Benchmark 失败 | 不进入实验，记录失败原因 |
| 候选导致质量下降 | 自动回滚，不得发布 |
| 候选成本显著升高 | 转人工确认商业化影响 |
| 候选只基于单个低质量样本 | 禁止上线，只能作为观察项 |
| 实验样本不足 | 延长实验或判定无结论 |
| 人工审核超时 | 保持候选挂起，不影响现有 Prompt |

## 7.4 数据删除与投诉异常

1. 用户删除发布记录后，关联指标、诊断报告、候选必须进入软删除或冻结状态。
2. 权利投诉成立后，关联诊断报告不得继续用于 Prompt 进化。
3. 用户撤销数据授权后，停止使用其数据进行聚合分析。
4. 团队成员离职后，按团队权限决定是否保留业务数据；个人数据需脱敏。
5. 数据已进入聚合统计但可识别用户时，需重新聚合或脱敏。

---

# 8. 风控与合规

## 8.1 授权要求

用户提交发布数据前必须确认：

1. 其对发布视频、账号数据、截图、CSV、评论数据有合法使用权。
2. 不上传他人未授权后台数据。
3. 不上传包含无关个人隐私的数据。
4. 系统可在其授权范围内用于诊断、统计和改进其个人/团队 Prompt。
5. 平台不得未经同意将其原始商业数据公开展示给其他用户。

## 8.2 AIGC 标识要求

D14 的诊断报告需要记录用户是否在发布内容中使用 AIGC 标识。系统不得建议用户移除 AIGC 标识，不得将“未标识导致数据更好”作为优化建议。

## 8.3 风险分级

| 等级 | 场景 | 动作 |
|---|---|---|
| R0 | 正常指标和正常诊断 | 放行 |
| R1 | 数据字段缺失、低置信度 | 提示，不生成强归因 |
| R2 | 可能包含隐私截图、评论个人信息 | 脱敏或要求用户编辑 |
| R3 | 诊断建议涉及夸张营销、敏感行业承诺 | 自动重写或转人工 |
| R4 | Prompt 候选降低原创化、鼓励高相似 | 拦截并记录 |
| R5 | 引导仿脸仿声、搬运、规避审核、违法内容 | 拦截、冻结、人工复核 |

## 8.4 人工复核触发条件

1. Prompt 进化候选命中 R3 及以上。
2. 诊断报告涉及医疗、金融、法律、未成年人、政务等高风险领域。
3. 用户上传截图包含大量评论或个人信息。
4. 候选会影响生产 Prompt 的默认策略。
5. 实验结论与风控数据冲突。
6. 用户或权利人投诉。

## 8.5 投诉删除机制

D14 必须接入投诉删除链路：

```text
投诉提交
→ 定位 publish_record / metrics_record / diagnosis_report / prompt_candidate
→ 冻结相关资产
→ 人工复核
→ 删除或脱敏
→ 回滚 Prompt 候选使用
→ 记录审计日志
→ 通知用户
→ 如涉及扣费，按 D20/D23 处理补偿
```

## 8.6 隐私与数据安全

1. 原始截图默认私有存储。
2. 评论数据进入分析前必须脱敏。
3. 平台账号后台截图不得进入公开样本集。
4. 用户转化金额、线索数、投放成本属于敏感商业数据。
5. Prompt 表现聚合默认只使用脱敏统计。
6. 研发和运营后台只展示必要字段。
7. 所有导出、查看、删除必须进入 AuditLog。

---

# 9. 测试与验收标准

## 9.1 单元测试

| 模块 | 测试点 | 通过标准 |
|---|---|---|
| 指标校验 | 负数、百分比、时间、播放量与互动关系 | 异常必须被拦截或提示 |
| 指标标准化 | 派生指标公式 | 与人工计算一致 |
| 数据成熟度 | 发布时间和采集时间 | 分层准确 |
| 基线计算 | 均值、中位数、样本数 | 口径稳定，可复现 |
| 状态机 | 合法/非法状态流转 | 非法流转被拒绝 |
| 权限 | 用户/团队/管理员 | 越权访问被拒绝 |
| 计费幂等 | 重试、失败、取消 | 不重复扣费 |

## 9.2 Prompt 测试

| Prompt | 测试样本 | 通过标准 |
|---|---|---|
| 数据诊断 Prompt | 高表现、低表现、数据不足、无基线 | 不强行归因，输出证据与限制 |
| 归因假设 Prompt | 多因素冲突样本 | 必须给出置信度和反证 |
| 优化建议 Prompt | 不同行业与目标 | 建议具体、可执行、不过度承诺 |
| Prompt 进化 Prompt | 成功/失败诊断样本 | 候选必须指向可控改动 |
| 风控重写 Prompt | 高风险候选 | 必须降低风险且不损害核心价值 |

## 9.3 Benchmark 与回归

Prompt 进化候选进入实验前必须满足：

1. Schema 通过率 100%。
2. D13 风控通过或人工复核通过。
3. D12 相关 Benchmark 通过。
4. 不降低原创化评分。
5. 不降低合规评分。
6. 核心输出质量不得低于当前生产版本。
7. 单次成本提升超过阈值时必须人工审批。

## 9.4 E2E 测试

必须覆盖以下流程：

1. 用户选择生成资产 → 创建发布记录 → 手动录入指标 → 运行诊断 → 查看报告 → 生成候选 → 候选进入后台。
2. 数据不足 → 只输出轻量诊断 → 不生成候选 → 按轻量计费。
3. 指标非法 → 拦截 → 不扣费。
4. Prompt 候选命中风险 → 拦截 → 后台可见。
5. Benchmark 失败 → 候选不发布。
6. 人工审批通过 → 创建实验 → 聚合实验结果。
7. 用户删除数据 → 诊断报告冻结 → 候选停止使用。

## 9.5 验收门槛

D14 MVP1 验收必须满足：

1. 发布记录、指标、诊断报告、候选数据可完整落库。
2. 所有核心 API 有权限控制和错误码。
3. 指标标准化公式有单测。
4. 诊断 Prompt 输出符合 Schema。
5. 诊断报告包含证据、置信度和限制说明。
6. 数据不足时不得强行归因。
7. Prompt 进化候选不得自动上线。
8. 候选必须接入 D13 风控和 D12 Benchmark 状态。
9. 积分冻结、扣费、返还幂等。
10. 后台可查看成本、失败率、候选状态和审计日志。
11. 生产环境 Mock 默认关闭。
12. 删除与投诉链路可追踪。

---

# 10. AIcoding 研发任务拆分

## 10.1 任务总表

| 任务编号 | 任务名称 | 优先级 | 负责 Agent | 主要交付物 |
|---|---|---:|---|---|
| D14-T01 | 数据诊断领域模型与枚举 | P0 | 后端/架构 Agent | TS 类型、JSON Schema、枚举、状态机 |
| D14-T02 | 数据库迁移与索引 | P0 | 后端 Agent | publish/metrics/diagnosis/candidate 表 |
| D14-T03 | 发布记录与指标录入 API | P0 | 后端 Agent | REST API、权限、校验、错误码 |
| D14-T04 | 指标标准化与数据质量服务 | P0 | 数据/后端 Agent | MetricsNormalizer、DataQualityValidator |
| D14-T05 | 基线计算服务 | P0 | 数据 Agent | PerformanceBaselineService |
| D14-T06 | 诊断报告生成服务 | P0 | AI/后端 Agent | DiagnosisService、PromptRun 接入 |
| D14-T07 | 归因假设 Prompt 与 Schema | P0 | Prompt Agent | Prompt 模板、输出 Schema、测试样本 |
| D14-T08 | Prompt 进化候选服务 | P0 | AI/Prompt/后端 Agent | CandidateService、D10/D12/D13 联动 |
| D14-T09 | 风控与 Benchmark 联动 | P0 | 后端/风控 Agent | 风控状态、Benchmark 状态、阻断逻辑 |
| D14-T10 | 积分扣费与失败返还 | P0 | 商业化/后端 Agent | CreditTransaction 联动、幂等测试 |
| D14-T11 | 前台数据回填与报告页 | P0 | 前端 Agent | 数据回填页、诊断报告页、状态展示 |
| D14-T12 | 后台进化候选评审台 | P1 | 前端/后台 Agent | 候选列表、diff、审批、审计 |
| D14-T13 | Prompt 表现看板 | P1 | 数据/后台 Agent | 聚合指标、成本、质量趋势 |
| D14-T14 | 权限、脱敏与审计 | P0 | 安全/后端 Agent | RBAC、AuditLog、数据删除 |
| D14-T15 | 测试与验收 | P0 | 测试 Agent | 单测、API、Prompt、E2E、权限、计费 |
| D14-T16 | 交接与文档回写 | P0 | 交接 Agent | 更新 D16/D17/D20/D21 影响清单 |

## 10.2 D14-T01：领域模型与枚举

交付：

1. `PublishPlatform`、`MetricsSourceType`、`DataQualityLevel`、`DataMaturityLevel`、`PerformanceLevel`、`DiagnosisStatus`、`PromptEvolutionCandidateStatus`。
2. JSON Schema：`ContentPublishRecord`、`PublishMetricsRecord`、`DiagnosisReport`、`PromptEvolutionCandidate`。
3. 状态机校验函数。

验收：

1. 类型可被前后端共享。
2. 状态机非法流转被拒绝。
3. Schema 单测覆盖必填字段和异常字段。

## 10.3 D14-T02：数据库迁移与索引

交付：

1. 所有 D14 P0 表迁移脚本。
2. 外键和索引。
3. 软删除字段和审计字段。
4. 数据脱敏字段设计。

验收：

1. 迁移可重复执行。
2. 回滚脚本可用。
3. 查询性能满足单用户 1 万条发布记录的基本分页。
4. 禁止无索引大表扫描。

## 10.4 D14-T03：发布记录与指标 API

交付：

1. 创建/查询/更新/删除发布记录 API。
2. 手动指标录入 API。
3. 指标校验 API。
4. CSV 导入预留 API。
5. 错误码文档。

验收：

1. 用户只能访问自己的数据。
2. 团队权限正确。
3. 数据授权未确认不得提交。
4. 非法指标不得入库为可诊断状态。

## 10.5 D14-T04：指标标准化与质量服务

交付：

1. `DataQualityValidator`。
2. `MetricsNormalizer`。
3. 派生指标公式。
4. 数据成熟度判断。

验收：

1. 公式单测通过。
2. 各平台缺失字段不会导致任务崩溃。
3. 数据不足时输出低置信度状态。

## 10.6 D14-T06/T07：诊断与归因 AI 服务

交付：

1. 诊断 Prompt。
2. 归因 Prompt。
3. 输出 Schema。
4. PromptRun、ModelCallLog 接入。
5. JSON repair 与失败回退。

验收：

1. 输出必须包含证据、置信度、限制说明。
2. 不允许强因果语言。
3. 数据不足必须降低置信度。
4. D13 风控不过不得展示。

## 10.7 D14-T08：Prompt 进化候选服务

交付：

1. Candidate 生成服务。
2. 目标 PromptVersion 定位。
3. Patch 结构。
4. 风险评估。
5. D12/D13 状态字段。

验收：

1. 候选不会自动发布。
2. 候选必须绑定来源诊断报告。
3. 候选必须有证据引用。
4. 高风险候选自动进入拦截或人工复核。

---

# 11. AIcoding 执行提示词

## 11.1 总执行提示词

```text
你是本项目的工程级 AIcoding 执行 Agent，负责实现 D14「数据诊断、归因与提示词进化规则文档」。

你必须先读取并遵守：
1. D00：AIcoding 研发总控文档 / 项目上下文总索引
2. D04：总 PRD 文档
3. D05：功能模块拆分与独立研发边界文档
4. D07：Content DNA Schema 与拆解报告标准文档
5. D09：Creative IR Schema 与原创化重构规则文档
6. D10：Prompt 体系、变量、版本与测试文档
7. D12：AI 评测样本集、Benchmark 与回归测试文档
8. D13：质检评分、相似度风控与合规拦截文档
9. D16/D17：数据库/API/任务队列最终工程契约，如已有则以其为准

项目定位：
本项目是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具。D14 的目标是通过用户授权的发布数据回填，生成证据化诊断、归因假设和 Prompt 进化候选，不能承诺确定因果，不能自动抓取平台数据，不能自动替换生产 Prompt。

本次任务：
实现 D14 的 {填写任务编号和任务名称}。

硬性要求：
1. 不得自行扩大 MVP1 范围。
2. 不得做平台自动爬虫。
3. 不得自动发布或自动替换生产 Prompt。
4. 不得跳过授权、风控、日志、成本、测试。
5. 不得用 mock 冒充真实能力；如开发环境需要 mock，必须 is_mock=true 且生产关闭。
6. 所有输出必须有 Schema、状态、错误码、日志和测试。
7. Prompt 进化候选必须接入 D13 风控和 D12 Benchmark 状态。

交付格式：
1. 说明读取了哪些文档。
2. 说明实现了哪些文件和模块。
3. 说明数据库迁移、API、队列、页面、测试影响。
4. 说明如何运行测试。
5. 说明未完成项和风险。
```

## 11.2 后端 Agent 提示词

```text
请实现 D14 P0 后端能力：发布记录、指标录入、指标标准化、诊断任务、Prompt 进化候选的数据结构、数据库迁移、API、队列和日志。

必须交付：
1. D14 所有 P0 枚举与 TypeScript 类型。
2. content_publish_records、publish_metrics_records、performance_baselines、diagnosis_reports、attribution_hypotheses、prompt_evolution_candidates、data_lineage_logs 表迁移。
3. REST API：创建发布记录、提交手动指标、运行诊断、获取报告、候选查询。
4. 队列任务：metrics.validate、metrics.normalize、baseline.compute、diagnosis.run、attribution.generate、prompt_evolution.propose。
5. CreditTransaction、TaskStepLog、ModelCallLog、PromptRun、AuditLog 接入点。
6. 权限控制：用户只能访问自己或团队授权数据。
7. 单元测试、API 测试、计费幂等测试。

禁止：
1. 不得自动抓取平台数据。
2. 不得自动上线 Prompt。
3. 不得跳过 D13 风控和 D12 Benchmark 状态字段。
```

## 11.3 Prompt Agent 提示词

```text
请实现 D14 的诊断、归因和 Prompt 进化候选 Prompt 模板，并纳入 D10 Prompt Registry。

必须输出：
1. 数据诊断 Prompt：输入 PublishMetrics、Baseline、ContentDNA、CreativeIR、PromptAsset、RiskCheckReport，输出 DiagnosisReport。
2. 归因假设 Prompt：输出 AttributionHypothesis，必须包含 evidence_refs、counter_evidence、confidence、validation_action。
3. Prompt 进化候选 Prompt：输出 PromptEvolutionCandidate，只能生成候选，不得直接改生产 Prompt。
4. 每个 Prompt 必须有 input_schema、output_schema、failure_fallback、quality_rubric、red_team_cases。
5. 所有 Prompt 禁止强因果表达，必须保留不确定性说明。
6. 所有候选必须通过 D13 风控和 D12 Benchmark。

验收：
1. Schema 通过率 100%。
2. 数据不足样本不得强行归因。
3. 高风险候选必须被拒绝或转人工。
```

## 11.4 前端 Agent 提示词

```text
请实现 D14 前台页面：数据回填页、指标确认页、诊断报告页。

必须交付：
1. 用户选择生成资产并创建发布记录。
2. 手动录入发布指标。
3. 展示数据质量、数据成熟度和预计消耗积分。
4. 展示诊断任务进度和失败状态。
5. 展示诊断报告：表现总览、关键指标、归因假设、证据、不确定性、下一条建议、风险提示。
6. 用户反馈入口和删除数据入口。
7. 权限状态、空状态、异常状态。

禁止：
1. 不得在页面上展示“保证爆款”等承诺。
2. 不得把低置信度归因展示为确定结论。
3. 不得隐藏 AIGC 标识和风险提示。
```

## 11.5 测试 Agent 提示词

```text
请为 D14 编写完整测试：指标公式、Schema、API、队列、Prompt、风控、计费、权限、E2E。

必须覆盖：
1. 非法指标拦截。
2. 数据不足不强行归因。
3. 无基线时降级。
4. Prompt JSON repair。
5. D13 风控拦截候选。
6. D12 Benchmark 失败时阻止发布。
7. 任务失败返还积分。
8. 用户越权访问被拒绝。
9. 删除数据后相关诊断冻结。
10. Mock 数据生产环境不可用。

输出：
1. 测试文件列表。
2. 执行命令。
3. 覆盖率报告。
4. 未覆盖风险。
```

---

# 12. 不做事项

D14 明确不做以下事项：

1. 不做平台自动爬虫或绕过平台规则的数据采集。
2. 不做自动发布视频。
3. 不做自动替换生产 Prompt。
4. 不做“保证爆款”“确定因果”“算法内幕判断”。
5. 不做未经授权的数据聚合。
6. 不做评论区个人信息挖掘。
7. 不做黑灰产增长策略。
8. 不做移除 AIGC 标识的建议。
9. 不做为了提升数据而降低原创化强度的 Prompt 进化。
10. 不做仿脸、仿声、搬运、洗稿、克隆类优化建议。
11. 不做把单条低质量样本升级为全局 Prompt 规则。
12. 不做跨用户共享原始商业数据。
13. 不做未经 Benchmark 的 Prompt 发布。
14. 不做无审计的后台数据查看和导出。
15. 不做生产环境 Mock 数据。

---

# 13. 待确认问题

| 编号 | 问题 | 影响 | 建议决策 |
|---|---|---|---|
| Q1 | MVP1 是否开放 CSV 导入，还是只做手动录入 | 影响前端和解析成本 | 建议先手动录入，CSV 放 P1 |
| Q2 | 各平台指标口径是否需要单独配置 | 影响数据标准化准确性 | 建议 P0 做平台枚举，P1 做平台口径配置 |
| Q3 | 免费用户是否可运行完整诊断 | 影响商业化 | 建议免费只给摘要，完整归因消耗积分 |
| Q4 | Prompt 进化候选是否对用户可见 | 影响理解成本 | 建议前台只显示“系统已沉淀优化建议”，后台显示候选详情 |
| Q5 | 企业客户是否允许团队级 Prompt 自进化 | 影响权限与合规 | 建议 P1/P2 开启，需团队管理员审批 |
| Q6 | 用户删除数据后，聚合统计是否保留 | 影响隐私合规 | 建议只保留不可逆脱敏统计 |
| Q7 | 哪些行业必须禁用自动诊断建议 | 影响风控 | 需 D21 确认医疗、金融、法律等高风险行业口径 |
| Q8 | 实验成功的统计门槛如何定义 | 影响 Prompt 发布 | 建议 D12/D19 定义样本量和显著性门槛 |
| Q9 | 诊断积分定价如何与模型成本挂钩 | 影响毛利 | 需 D20 统一定价 |
| Q10 | 是否支持第三方平台 API 授权接入 | 影响合规和研发复杂度 | MVP1 不做，MVP2 后评估 |

---

# 14. 最终 Checklist

## 14.1 产品 Checklist

- [ ] D14 明确只做数据回填、诊断、归因假设、Prompt 进化候选。
- [ ] MVP1 不做自动抓取、自动发布、自动替换 Prompt。
- [ ] 诊断报告有证据、置信度、不确定性说明。
- [ ] 数据不足时不强行归因。
- [ ] Prompt 进化候选不对生产 Prompt 直接生效。
- [ ] 用户可删除或冻结自己的数据。

## 14.2 AI Checklist

- [ ] 诊断 Prompt 已纳入 Prompt Registry。
- [ ] 归因 Prompt 有输入输出 Schema。
- [ ] Prompt 进化候选 Prompt 有风控和 Benchmark 门禁。
- [ ] Prompt 输出不包含强因果和保证性承诺。
- [ ] JSON repair 和失败降级已实现。
- [ ] 红队样本覆盖伪因果、低数据、高风险行业、高相似候选。

## 14.3 工程 Checklist

- [ ] 数据库表已迁移。
- [ ] API 有鉴权、错误码和幂等。
- [ ] 队列状态机可追踪。
- [ ] TaskStepLog、PromptRun、ModelCallLog、CreditTransaction、AuditLog 已接入。
- [ ] 指标标准化公式有单测。
- [ ] D13 风控状态和 D12 Benchmark 状态已接入候选流程。
- [ ] 生产环境 Mock 默认关闭。

## 14.4 商业化 Checklist

- [ ] 数据诊断任务有积分预估。
- [ ] 成功、部分成功、失败、取消均有扣费/返还规则。
- [ ] 诊断模型成本可统计。
- [ ] Prompt 进化实验成本可统计。
- [ ] 后台可查看收入、成本、毛利、失败率。
- [ ] 企业版能力与免费版能力边界明确。

## 14.5 合规 Checklist

- [ ] 数据授权确认已前置。
- [ ] AIGC 标识不被弱化或移除。
- [ ] 截图和评论数据可脱敏。
- [ ] 高风险行业转人工复核。
- [ ] 投诉删除机制可定位到发布记录、指标、报告和候选。
- [ ] 数据删除、冻结、审计日志可用。
- [ ] 不把用户原始商业数据跨用户共享。

## 14.6 AIcoding 交付 Checklist

- [ ] Agent 已说明读取 D00 与相关专项文档。
- [ ] Agent 已说明修改文件列表。
- [ ] Agent 已说明数据库迁移。
- [ ] Agent 已说明 API 与队列。
- [ ] Agent 已说明测试命令和结果。
- [ ] Agent 已说明未完成项和风险。
- [ ] Agent 已回写 D16/D17/D20/D21 的影响清单。

---

# 附录 A：错误码建议

| 错误码 | 含义 | HTTP 状态 |
|---|---|---:|
| `D14_METRICS_AUTH_REQUIRED` | 未确认数据授权 | 400 |
| `D14_METRICS_INVALID_VALUE` | 指标值非法 | 400 |
| `D14_METRICS_TOO_EARLY` | 数据过早，不建议诊断 | 409 |
| `D14_METRICS_INSUFFICIENT` | 数据不足，只能轻量诊断 | 409 |
| `D14_PUBLISH_RECORD_NOT_FOUND` | 发布记录不存在 | 404 |
| `D14_ASSET_NOT_LINKED` | 发布记录未绑定系统资产 | 400 |
| `D14_DIAGNOSIS_FAILED` | 诊断失败 | 500 |
| `D14_RISK_BLOCKED` | 风控拦截 | 403 |
| `D14_BENCHMARK_FAILED` | Benchmark 未通过 | 409 |
| `D14_PERMISSION_DENIED` | 无权限访问 | 403 |
| `D14_DATA_DELETED` | 数据已删除或冻结 | 410 |

# 附录 B：D14 最小上线口径

D14 在 MVP1 上线时，不要求完成完整 Prompt 自进化系统，但必须完成以下最小能力：

1. 用户能把一次生成结果绑定到一条发布记录。
2. 用户能手动录入核心数据。
3. 系统能校验和标准化指标。
4. 系统能输出带证据和置信度的单作品诊断报告。
5. 系统能生成 Prompt 进化候选，但不自动上线。
6. 候选能进入风控、Benchmark 和人工评审状态流。
7. 所有任务有日志、成本、权限和测试。

# 附录 C：推荐 Feature Flag

| Feature Flag | 默认 | 说明 |
|---|---|---|
| `d14_manual_metrics_enabled` | true | 手动指标录入 |
| `d14_csv_import_enabled` | false | CSV 导入 |
| `d14_screenshot_ocr_enabled` | false | 截图 OCR |
| `d14_auto_platform_fetch_enabled` | false | 平台自动获取数据，MVP1 禁用 |
| `d14_diagnosis_enabled` | true | 单作品诊断 |
| `d14_prompt_evolution_candidate_enabled` | true | 生成进化候选 |
| `d14_auto_prompt_publish_enabled` | false | 自动发布 Prompt，必须禁用 |
| `d14_prompt_experiment_enabled` | false | Prompt 实验，P1/P2 开启 |
| `d14_enterprise_dashboard_enabled` | false | 企业看板 |

