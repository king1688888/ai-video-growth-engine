# D13｜质检评分、相似度风控与合规拦截文档

> 文档编号：D13  
> 文档名称：质检评分、相似度风控与合规拦截文档  
> 文档版本：V1.0  
> 生成日期：2026-05-25  
> 根上下文：D00《AIcoding 研发总控文档 / 项目上下文总索引》  
> 当前主线版本：MVP1「视频拆解 + 原创脚本生成」  
> 文档性质：AI 输出质量门禁 + 原创度/相似度风控 + 合规拦截 + 人工复核 + 投诉删除 + AIcoding 执行文档  
> 核心定位：本文件不是泛泛的“内容审核建议”，而是把本项目的 AI 输出质量、原创化程度、相似度风险、合规风险、人工复核、投诉删除、积分补偿、后台审计和上线验收固化为可开发、可测试、可观测、可拦截、可回滚的工程系统。

---

## 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 文档编号 | D13 |
| 文档名称 | 质检评分、相似度风控与合规拦截文档 |
| 所属项目 | AI 短视频爆款增长引擎 |
| 当前研发主线 | MVP1：视频拆解 + 原创脚本生成 |
| 文档级别 | S+，商业级上线前必须完成的 AI 质量与合规风控核心文档 |
| 上级根文档 | D00《AIcoding 研发总控文档 / 项目上下文总索引》 |
| 直接前置依赖 | D00、D01、D02、D03、D04、D05、D06、D07、D08、D09、D10、D11、D12 |
| 直接后置依赖 | D15 技术架构、D16 数据库/API、D17 异步任务队列、D18 数据安全与权限、D19 测试发布监控、D20 商业化系统、D21 合规授权与内容审核、D22 运营后台/客服/人工复核、D23 AIcoding 执行规约、D24 最终交接 |
| 适用阶段 | MVP1 AI 输出生成、Prompt 调优、模型替换、风控接入、人工复核、投诉处理、商业化收费、上线验收、后续版本迭代 |
| 适用对象 | 创始人、产品负责人、AI 产品经理、技术产品经理、后端 Agent、前端 Agent、AI/Prompt Agent、测试 Agent、运营后台 Agent、合规风控负责人、客服负责人、DevOps Agent |
| 文档目标 | 定义质检评分、相似度风控、合规风险分级、拦截/重写/人工复核/投诉删除策略，以及数据库、API、任务队列、Prompt、页面、日志、积分、测试和 AIcoding 执行任务 |
| 最高约束 | 与 D00 冲突时，以 D00 为准；与 D03 MVP 范围冲突时，按 D03 收敛；与 D07/D09 Schema 冲突时，以已确认 Content DNA / Creative IR 字段为输入口径；与 D10 Prompt 冲突时，以 D10 为准；与 D21 合规口径冲突时，以更严格规则为准；与 D16/D17 工程契约冲突时，以 D16/D17 为最终实现口径并回写 D13 |
| 输出形态 | Markdown 文档 + 风控策略 + 评分 Rubric + JSON Schema + 数据库/API 草案 + 任务队列草案 + Prompt 模板 + 页面状态 + 测试验收清单 + AIcoding 任务拆分与提示词 |
| 维护责任人 | 项目 Owner / AI 产品总控 / 风控合规负责人 / AI 质量负责人 / 技术产品负责人 |
| 更新触发条件 | 相似度阈值变化、风险分级变化、合规策略变化、Prompt 版本变化、模型供应商变化、Benchmark 失败、用户投诉、侵权争议、平台规则变化、积分补偿策略变化、上线验收不通过 |
| 文档状态 | V1.0 初版可执行；后续随 D16-D24 工程契约、真实样本、线上风控事件和法务意见持续回写 |

### 1.1 编号兼容说明

当前项目文档体系存在一次编号演进：早期 D00 索引中可能将“质检评分、相似度风控与合规拦截”标为 D12，并将“数据诊断、归因与提示词进化”标为 D13；但本轮任务明确要求输出 **D13｜质检评分、相似度风控与合规拦截文档**，且前序已经形成 D12「AI 评测样本集、Benchmark 与回归测试文档」。

为避免后续 AIcoding 混乱，工程字段建议采用：

| 字段 | 建议值 |
|---|---|
| `doc_code` | `D13` |
| `doc_alias` | `QUALITY_SIMILARITY_COMPLIANCE_GUARDRAIL` |
| `legacy_doc_code` | `D12_RISK_GUARDRAIL`，仅用于旧索引兼容 |
| `depends_on_benchmark_doc` | `D12_AI_BENCHMARK_REGRESSION` |
| `conflict_resolution` | 若 D00 未更新，以本次用户指定编号 D13 执行；上线前需由项目 Owner 回写 D00 文档地图 |

### 1.2 文档中的“合规”说明

本文件中的合规策略用于产品和工程设计，不构成正式法律意见。所有涉及法律责任、用户协议、版权争议、AIGC 标识、平台规则、跨境数据、企业客户合同、投诉删除和监管适配的最终口径，应由 D21 合规专项文档、法务顾问和项目 Owner 共同确认。

---

## 2. 文档目标

### 2.1 本文档要解决的问题

D13 要解决的是 MVP1 从“能生成”到“可上线、可收费、可风控、可解释、可复核”的关键问题。系统不能只输出拆解报告、脚本、标题、封面建议和视频 Prompt，还必须在每一次高价值输出前后回答：

1. 输出质量是否足够好，是否可直接交付给用户。
2. 输出是否真正原创化，而不是换词、洗稿、复刻原视频。
3. 输出是否过度接近原视频的台词、结构、镜头、封面、人物、声音、品牌、独特剧情或可识别表达。
4. 输出是否存在版权、肖像、声音、商标、平台搬运、虚假宣传、敏感行业、违法违规、个人信息、未成年人、深度合成滥用等风险。
5. 风险命中后系统应该放行、提示、自动重写、要求用户编辑、转人工复核、还是直接拦截并返还积分。
6. 用户投诉、权利人投诉、平台下架、人工复核不通过后，资产、报告、导出文件、日志、扣费和用户通知如何处理。
7. AIcoding Agent 如何把风控做成真实工程能力，而不是页面上写一个“已通过审核”的假状态。

D13 必须把这些问题落成以下可执行系统：

```text
输入预检
→ 视频/素材/授权风险识别
→ Content DNA 质量评分
→ Creative IR 原创化评分
→ 输出包质量评分
→ 多维相似度检查
→ 合规风险分级
→ 决策引擎
→ 放行 / 提示 / 自动重写 / 用户编辑 / 人工复核 / 拦截
→ 积分结算或返还
→ 日志审计与后台统计
→ 投诉删除与模型/Prompt 回归
```

### 2.2 本文档不解决的问题

| 不在 D13 完整展开的内容 | 负责文档 | D13 只提供 |
|---|---|---|
| 产品战略、商业定位、价值主张 | D01 | 风控必须服务“爆款结构学习 + 原创化生成”的定位 |
| 首发用户、行业选择、用户画像 | D02 | 行业风险分级、用户意图风险和画像字段引用 |
| MVP 范围与版本路线 | D03 | MVP1 的 P0 风控范围与不做事项 |
| 总 PRD 与用户故事 | D04 | 风控相关功能、异常、页面、积分与后台需求 |
| 模块拆分和独立研发边界 | D05 | D13 风控模块、质检模块、人工复核模块边界 |
| 页面原型与 UI 细节 | D06 | 风控卡片、拦截页、人工复核页、投诉入口的 ViewModel |
| Content DNA 字段全集 | D07 | 读取 `risk_flags`、`reusable_parts`、`non_replicable_parts`、`confidence_score` 等字段作为风控输入 |
| 视频处理与证据包 | D08 | 读取帧、ASR、OCR、镜头、证据 ID，不重新定义视频处理算法 |
| Creative IR Schema 与原创化规则 | D09 | 读取 `originalization_level`、`risk_constraints`、`source_similarity_guardrails`、输出包字段 |
| Prompt Registry 与版本管理 | D10 | 定义 D13 相关 Prompt 模板、输出 Schema、失败回退；最终纳入 Prompt Registry |
| 模型路由与成本控制 | D11 | 风控 Prompt 的模型选择、成本记录、降级回退与积分规则输入 |
| AI Benchmark 样本集 | D12 | D13 提供风控评测维度和红队样本要求，具体 Benchmark Runner 由 D12 落地 |
| 数据库/API/任务队列最终契约 | D16/D17 | 本文提供草案，最终以 D16/D17 固化 |
| 套餐、订单、支付、退款 | D20 | 提供风控失败返还、部分成功扣费、人工复核补偿规则 |
| 法律条款与审核 SOP 终稿 | D21/D22 | 提供工程化风控流程和合规触发点，最终合规口径需 D21 确认 |

### 2.3 本文档的研发落点

D13 最终必须落到以下工程对象：

1. `QualityCheckReport` 质量评分结构。
2. `SimilarityCheckReport` 相似度风控结构。
3. `ComplianceCheckReport` 合规检查结构。
4. `RiskCheckReport` 风险总报告结构。
5. `RiskDecision` 决策结构。
6. `ManualReviewTicket` 人工复核工单结构。
7. `ComplaintCase` 投诉删除结构。
8. `RiskRuleVersion` 风控规则版本结构。
9. `risk_check_reports`、`quality_check_reports`、`similarity_check_reports`、`compliance_check_reports`、`risk_decisions`、`manual_review_tickets`、`complaint_cases`、`risk_rule_versions`、`risk_events` 数据表草案。
10. 风控 API：预检、质检、相似度检查、合规检查、风险报告、重写、人工复核、投诉删除。
11. 风控队列：`risk.precheck`、`quality.check`、`similarity.check`、`compliance.check`、`risk.decide`、`risk.rewrite`、`manual_review.create`、`complaint.process`。
12. Prompt 模板：输入意图预检、质量评分、相似度风险、合规拦截、原创化重写、人工复核摘要。
13. 前端页面：风险提示卡、质检评分卡、相似度提示、合规拦截页、人工复核等待页、投诉入口、AIGC 标识展示。
14. 后台页面：风险事件列表、任务风险详情、人工复核工作台、投诉删除工作台、风险规则配置、质检统计看板。
15. 测试体系：Schema 测试、Prompt 回归、风控红队、相似度阈值测试、计费幂等、权限审计、E2E。
16. AIcoding 研发任务拆分与交付标准。

---

## 3. 与 D00 及其他文档的关系

### 3.1 D00 对 D13 的硬约束

| D00 约束 | D13 落地方式 |
|---|---|
| 产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具 | D13 把“不能复制表达、不能仿脸仿声、不能搬运、不能规避平台规则”落成风险标签、相似度阈值、拦截策略和人工复核流程 |
| 核心链路包含质检风控 | D13 是 Content DNA → Creative IR → 输出包之后、导出之前的质量与风险总门禁 |
| 当前优先 MVP1：视频拆解 + 原创脚本生成 | D13 P0 只覆盖单条视频拆解、原创脚本、标题、封面建议、视频 Prompt，不做完整成片审核和自动发布审核 |
| 不得自行扩大第一版范围 | 视频成片相似度、全账号批量风险、自动发布风控、跨平台数据合规只做 P1/P2 预留 |
| 不得跳过风控、任务日志、成本记录、测试验收 | 每次风控必须记录规则版本、Prompt 版本、模型成本、风险分、决策、人工复核、积分动作和审计日志 |
| 不得用 mock 冒充真实能力 | Mock 风控只能用于开发环境，必须 `is_mock=true`，前台/后台显著标记，生产默认关闭 |
| 每个模块必须可独立开发、独立测试、独立验收、独立交接 | D13 将质检、相似度、合规、决策、人工复核、投诉、后台、测试拆成独立 AIcoding 任务 |

### 3.2 与 D07 Content DNA 的关系

D07 输出的是结构化内容资产，D13 必须检查其是否能安全进入原创化链路。D13 重点消费：

| D07 字段 | D13 使用方式 |
|---|---|
| `content_type` | 判断适用的质检 Rubric 和行业风险规则 |
| `target_audience` | 判断未成年人、敏感群体、弱势群体相关风险 |
| `core_hook` | 检查是否复制原句、是否标题党、是否夸大承诺 |
| `narrative_structure` | 检查结构相似度、是否复刻原视频叙事 |
| `shot_dna` | 检查镜头/构图/封面相似度风险 |
| `viral_factors` | 判断是否把“可学习机制”误写成“复制做法” |
| `conversion_path` | 检查虚假宣传、收益承诺、疗效承诺、保过承诺 |
| `reusable_parts` | 只允许作为结构学习输入，不允许直接复用表达 |
| `non_replicable_parts` | 作为硬性禁用清单，进入合规拦截 |
| `risk_flags` | 作为初始风险标签，进入 D13 风险决策 |
| `confidence_score` | 决定自动放行、降级、重写或人工复核 |

### 3.3 与 D08 视频处理的关系

D08 提供视频证据包，D13 不重新处理视频，但必须在相似度和合规判断中引用证据 ID：

1. `frame_assets`：用于封面、人物、品牌、Logo、画面元素相似度和风险证据。
2. `transcript_segments`：用于文本相似度、原台词复用、敏感承诺、违法违规表达识别。
3. `ocr_segments`：用于字幕、标题、封面文案、品牌词和广告承诺识别。
4. `shot_segments`：用于镜头结构相似度、构图/节奏复用风险。
5. `processing_quality_report`：若视频处理低置信度，D13 不能高置信度放行。

### 3.4 与 D09 Creative IR 的关系

D09 定义原创化中间层。D13 必须检查 Creative IR 和输出包是否真的完成原创化：

| D09 输入 | D13 检查项 |
|---|---|
| `originalization_level` | 是否达到 MVP1 默认中高原创化；低原创化不得进入自动导出 |
| `risk_constraints` | 是否完整继承不可复用清单和合规限制 |
| `source_similarity_guardrails` | 是否设置文本、结构、镜头、封面、Prompt 相似度上限 |
| `storyboard_beats` | 是否过度复刻原视频镜头顺序和叙事节奏 |
| `deliverable_intents` | 输出脚本、标题、封面、Prompt 是否符合用户画像与风险规则 |
| `creative_ir_quality_checks` | 是否有质量评分、风险评分、重写建议 |

### 3.5 与 D10 Prompt 体系的关系

D13 所有 AI 风控能力必须进入 Prompt Registry：

1. 每个风控 Prompt 必须有 `prompt_key`、`version`、输入 Schema、输出 Schema、评测样本和发布门禁。
2. 风控 Prompt 不能散落在代码里，不能由 Agent 临时拼接。
3. 每次 PromptRun 必须记录模型、成本、延迟、Schema 校验、风险结果、失败原因。
4. 风控 Prompt 变更必须跑 D12 Benchmark，未通过不得上线。

### 3.6 与 D11 模型路由的关系

D13 涉及多次模型调用，必须通过 D11 模型路由执行：

1. 高风险判断默认使用稳定、结构化输出能力强的模型。
2. 低成本快速预检可使用轻量模型，但不能作为高风险放行的唯一依据。
3. 风险高、置信度低、模型不一致时，必须触发二次模型复核或人工复核。
4. 风控模型调用也需要成本记录和积分归因，但不能因用户积分不足跳过安全检查。

### 3.7 与 D12 Benchmark 的关系

D12 是 D13 的质量验证基础。D13 必须向 D12 提供：

1. 高相似红队样本。
2. 仿脸仿声请求样本。
3. 未授权素材复用样本。
4. 医疗、金融、法律、教培、招商加盟等敏感承诺样本。
5. 标题党、虚假宣传、收益承诺、疗效承诺样本。
6. 低清/无音频/字幕密集/证据不足的低置信度样本。
7. 人工复核 Golden Decision。
8. 风险召回率、误拦截率、重写成功率和发布门禁指标。

### 3.8 与 D16/D17 的关系

D13 提供数据库、API 和任务队列草案，最终由 D16/D17 固化。任何 AIcoding Agent 不得绕过 D16/D17 私自写表、私自定义状态或私自改变任务流。

### 3.9 与 D20 商业化的关系

D13 直接影响扣费和补偿：

1. 风控预检失败：不冻结积分或立即返还。
2. 视频处理成功但输出包风控拦截：按已完成成本和用户可见价值决定是否部分扣费，MVP1 建议默认返还主要生成积分。
3. 自动重写成功：不重复向用户扣除风控重写成本，计入平台质量成本。
4. 人工复核超时或不通过：按 D20/D22 SOP 返还或补偿。
5. 权利投诉成立：下架相关资产，必要时返还导出/生成积分，并记录成本损失。

### 3.10 与 D21/D22/D23 的关系

D13 提供工程化风控能力，D21 确认合规口径，D22/D23 承接运营后台、客服、退款补偿与人工复核 SOP。三者必须一致：

```text
D13：规则、评分、拦截、API、队列、日志
D21：授权条款、AIGC 标识、内容审核、合规最终口径
D22/D23：后台人工复核、客服处理、退款补偿、投诉删除 SOP
```

---

## 4. 核心结论

### 4.1 一句话结论

D13 是 MVP1 的“质量与风险总闸门”。任何 Content DNA、Creative IR、原创脚本、标题、封面建议、视频 Prompt，在展示、复制、导出或进入下一步前，都必须经过质量评分、相似度风控、合规风险分级和决策引擎；系统只能放行“结构学习充分、原创化明显、合规风险可控、用户可用”的输出，不能放行搬运、克隆、洗稿、仿脸、仿声、高相似复刻、未授权素材复用和高风险承诺内容。

### 4.2 MVP1 P0 必须实现的风控门禁

| 门禁编号 | 门禁名称 | 触发时机 | P0 必做 | 失败动作 |
|---|---|---|---:|---|
| G00 | 用户意图预检 | 创建任务前 | 是 | 禁止创建任务 / 提示改写 / 转人工 |
| G01 | 素材授权与输入风险 | 上传/链接提交时 | 是 | 拦截 / 要求确认授权 / 转人工 |
| G02 | 视频处理质量门禁 | D08 完成后 | 是 | 降级 / 部分成功 / 失败返还 |
| G03 | Content DNA 质量门禁 | D07 完成后 | 是 | JSON repair / 重跑 / 人工复核 |
| G04 | Creative IR 原创化门禁 | D09 完成后 | 是 | 自动重写 / 降级 / 人工复核 |
| G05 | 输出包质量门禁 | 脚本/标题/封面/Prompt 生成后 | 是 | 自动重写 / 不展示低质输出 |
| G06 | 多维相似度门禁 | 输出包生成后 | 是 | 重写 / 人工复核 / 拦截 |
| G07 | 合规风险门禁 | 输出包导出前 | 是 | 提示 / 改写 / 人工复核 / 拦截 |
| G08 | 导出与 AIGC 标识门禁 | 复制/导出前 | 是 | 无标识不得导出 |
| G09 | 投诉删除与回滚门禁 | 投诉/权利请求发生时 | 是 | 冻结资产 / 下架 / 删除 / 记录审计 |

### 4.3 风险分级总表

| 风险等级 | 名称 | 定义 | 系统动作 |
|---|---|---|---|
| R0 | 无明显风险 | 质量达标、原创化明显、未命中敏感规则 | 自动放行 |
| R1 | 轻微提示风险 | 有轻微标题党、行业表述不够稳健、可解释性不足 | 放行但提示优化 |
| R2 | 可自动修复风险 | 局部相似、轻微夸张、证据不足、Prompt 不够可执行 | 自动重写后复检 |
| R3 | 需要用户编辑风险 | 用户信息不足、行业资质不足、CTA 高风险、结构过近 | 要求用户补充/编辑，复检通过后放行 |
| R4 | 人工复核风险 | 高相似、权利风险、敏感行业、人物/品牌/声音疑似复用、模型判断不一致 | 进入人工复核，未通过不得导出 |
| R5 | 必须拦截风险 | 明确搬运/克隆/仿脸/仿声/未授权复用/规避平台/违法违规/高危承诺 | 阻断任务或输出，返还规则按 D20 执行，记录审计 |

### 4.4 质量评分总表

MVP1 输出总质量分为 0-100 分：

| 维度 | 权重 | 合格线 | 说明 |
|---|---:|---:|---|
| Schema 合法性 | 10 | 必须 100% | JSON 可解析、字段完整、枚举合法 |
| 证据支撑 | 15 | ≥ 75 | 拆解结论有视频证据、ASR/OCR/镜头证据支撑 |
| 原创化程度 | 20 | ≥ 75 | 与原视频表达、结构、镜头、标题、封面有明显差异 |
| 用户画像适配 | 15 | ≥ 70 | 适合用户行业、人设、产品、目标用户、平台 |
| 可执行性 | 15 | ≥ 70 | 脚本可拍、标题可用、封面建议可落地、Prompt 可执行 |
| 合规安全 | 15 | ≥ 85 | 不含高风险承诺、侵权、仿脸仿声、平台规避等 |
| 商业价值 | 10 | ≥ 70 | 输出对用户有增长、转化或内容生产价值 |

放行规则：

```text
quality_score >= 80 且 risk_level <= R1：自动放行
70 <= quality_score < 80 或 risk_level = R2：自动重写 / 提示优化
60 <= quality_score < 70 或 risk_level = R3：要求用户编辑或补充信息
quality_score < 60 或 risk_level >= R4：人工复核或拦截
risk_level = R5：无论质量分多少，必须拦截
```

### 4.5 相似度风控总表

相似度不是单一文本相似度，而是多维风险：

| 相似度维度 | 检查对象 | P0 实现方式 | R4/R5 触发示例 |
|---|---|---|---|
| 文本字面相似度 | 原字幕/ASR vs 脚本/标题/Prompt | n-gram、最长公共子串、重复句段比例 | 大段原台词复用 |
| 语义相似度 | 原内容语义 vs 输出语义 | embedding 相似度 + LLM 判断 | 换词但表达同一段文案 |
| 结构相似度 | 原叙事结构 vs IR/脚本结构 | beat 顺序、钩子、转折、CTA 序列 | 原剧情顺序几乎一致 |
| 镜头相似度 | shot_dna vs storyboard/prompt | 镜头数量、构图、动作、场景序列 | 原镜头逐镜复刻 |
| 封面相似度 | 原封面/OCR vs 封面建议 | OCR、视觉元素、构图、标题词 | 模仿原封面标题和构图 |
| 人物/身份相似度 | 原视频人物 vs 输出人物设定 | 风险标签 + LLM 判断 | 暗示仿某人脸/身份 |
| 声音/口吻相似度 | 原声音/台词风格 vs 输出 | MVP1 禁做仿声，仅文本口吻检查 | 要求“像某人声音” |
| 品牌/商标相似度 | Logo、店名、商标词 | OCR/实体识别/用户授权 | 未授权复用品牌标识 |
| 视频 Prompt 相似度 | 原视频画面 vs 视频生成 Prompt | LLM 逐项比对 | Prompt 直接描述原画面 |

### 4.6 必须拦截的底线

以下内容在 MVP1 必须拦截或转人工复核，不允许自动放行：

1. 明确要求“复制、搬运、克隆、洗稿、复刻原视频”。
2. 明确要求仿某个人脸、声音、身份、口吻到可识别程度。
3. 未授权复用原视频素材、Logo、商标、配乐、封面、字幕、台词。
4. 要求绕过平台风控、搬运检测、版权检测、AIGC 检测。
5. 对医疗疗效、金融收益、法律结果、考试保过、招商收益作确定性承诺。
6. 含个人隐私、未成年人敏感信息、违法违规、仇恨骚扰、色情低俗、暴力极端、欺诈诱导内容。
7. 用户拒绝授权确认或无法证明素材使用权。
8. 投诉成立、人工复核不通过、监管/平台要求下架的内容。

---

## 5. 详细设计

### 5.1 D13 系统边界

D13 负责：

1. 输入意图风险预检。
2. 素材授权与可用性风险检查。
3. Content DNA 质量评分。
4. Creative IR 原创化评分。
5. 脚本/标题/封面建议/视频 Prompt 输出质量评分。
6. 多维相似度检测。
7. 合规风险识别与风险等级判定。
8. 决策引擎：放行、提示、重写、用户编辑、人工复核、拦截。
9. 风险报告入库和后台展示。
10. 风控相关积分补偿触发。
11. AIGC 标识门禁。
12. 投诉删除与资产冻结流程。
13. 风控数据进入 D12 Benchmark 和后续 Prompt 改进。

D13 不负责：

1. 不重新做视频处理、抽帧、ASR、OCR、镜头切分。
2. 不替代 Content DNA 生成。
3. 不替代 Creative IR 生成。
4. 不直接生成最终脚本，除非执行“风控重写任务”。
5. 不直接决定法律责任归属。
6. 不做自动发布平台审核。
7. 不做完整成片的视频指纹/版权库比对，MVP3 后再扩展。

### 5.2 风控分层架构

推荐架构：

```text
L0 静态规则层
- 禁用词、敏感行业、明确违规意图、授权未确认、文件类型风险

L1 结构化规则层
- Schema 完整性、字段阈值、相似度数值、风险标签、置信度

L2 模型判断层
- LLM 风险识别、相似度解释、合规判断、重写建议

L3 决策引擎层
- 按规则版本、用户套餐、任务状态、风险等级生成 RiskDecision

L4 人工复核层
- 对 R4、模型分歧、投诉争议、敏感行业输出进行人工确认

L5 事后治理层
- 投诉删除、资产冻结、规则回写、Benchmark 红队样本沉淀
```

### 5.3 风控接入点

| 接入点 | 上游输入 | 检查内容 | 下游动作 |
|---|---|---|---|
| 创建任务前 | 用户输入、视频链接、目标、授权勾选 | 意图、授权、违禁请求、行业风险 | 拦截/提示/创建任务 |
| 视频处理后 | D08 证据包、处理质量 | 证据是否足够、低清/无音频/无法 OCR | 降级/补充/失败返还 |
| Content DNA 后 | D07 ContentDNA | 字段完整、证据支撑、不可复用识别 | 修复/重跑/人工复核 |
| Creative IR 后 | D09 CreativeIR | 原创化强度、风险约束、结构相似度 | 重写/人工复核 |
| 输出包后 | 脚本、标题、封面、Prompt | 质量、相似度、合规、可执行性 | 放行/重写/拦截 |
| 导出前 | deliverable_pack、AIGC label | 标识、风险状态、权限、积分状态 | 允许导出/禁止导出 |
| 投诉后 | 投诉材料、任务、资产 | 权利风险、可追溯证据 | 冻结/下架/删除/通知 |

### 5.4 质检评分 Rubric

#### 5.4.1 Content DNA 质量评分

| 指标 | 权重 | 检查方式 | 不合格示例 |
|---|---:|---|---|
| Schema 合法性 | 15 | JSON Schema 校验 | 字段缺失、类型错误、枚举错误 |
| 证据引用完整 | 20 | 每个关键结论关联证据 ID | 爆款原因无证据 |
| 抽象能力 | 15 | 是否提炼结构而非复制表达 | 把原台词当作可复用结构 |
| 不可复用识别 | 20 | 人物/品牌/声音/Logo/原台词识别 | 未标出原品牌 Logo |
| 可迁移价值 | 15 | 是否能进入 Creative IR | 只有观后感，没有可迁移策略 |
| 风险标注 | 15 | risk_flags、confidence_score | 高风险样本无风险标签 |

#### 5.4.2 Creative IR 质量评分

| 指标 | 权重 | 检查方式 | 不合格示例 |
|---|---:|---|---|
| 原创化强度 | 25 | 结构/文本/镜头相似度 | 仅替换行业词 |
| 用户画像适配 | 20 | 行业、人设、产品、受众匹配 | 餐饮商家输出教培脚本 |
| 风控约束继承 | 20 | 是否继承 non_replicable_parts | 继续使用原人物/品牌 |
| 输出意图清晰 | 15 | deliverable_intents 完整 | 不知道生成口播还是剧情 |
| 可执行性 | 10 | 分镜和脚本可落地 | 画面无法拍摄 |
| 版本可追踪 | 10 | 输入摘要、Prompt 版本、模型日志 | 无法回溯来源 |

#### 5.4.3 原创脚本质量评分

| 指标 | 权重 | 合格标准 |
|---|---:|---|
| 开头钩子 | 15 | 有明确吸引点，不直接复制原钩子句 |
| 结构完整 | 15 | 开头、展开、价值点、案例/证据、CTA 完整 |
| 行业适配 | 15 | 符合用户行业、人设、产品、目标受众 |
| 原创表达 | 20 | 无大段相似台词，无原剧情复刻 |
| 合规安全 | 20 | 无夸大承诺、违规建议、未授权复用 |
| 可拍摄性 | 10 | 用户可按脚本拍摄或继续生成分镜 |
| 可修改性 | 5 | 分段清晰，可编辑 |

#### 5.4.4 标题质量评分

| 指标 | 权重 | 合格标准 |
|---|---:|---|
| 点击吸引力 | 20 | 有痛点、利益点或冲突点 |
| 真实性 | 25 | 不标题党、不虚假承诺、不绝对化 |
| 行业适配 | 15 | 符合用户业务和平台语境 |
| 原创度 | 20 | 不复制原标题、原字幕、原封面词 |
| 风险安全 | 20 | 不触发医疗/金融/教培/招商等高风险承诺 |

#### 5.4.5 封面建议质量评分

| 指标 | 权重 | 合格标准 |
|---|---:|---|
| 一眼识别 | 20 | 封面核心利益点明确 |
| 构图可执行 | 20 | 用户可拍摄/设计，不依赖原封面 |
| 原创度 | 25 | 不模仿原封面排版、标题、人物、Logo |
| 文案安全 | 20 | 不夸大、不侵权、不高风险承诺 |
| 行业适配 | 15 | 适合用户账号定位 |

#### 5.4.6 视频生成 Prompt 质量评分

| 指标 | 权重 | 合格标准 |
|---|---:|---|
| 可执行性 | 25 | 场景、主体、镜头、动作、风格、时长清晰 |
| 模型适配 | 15 | 符合目标视频模型能力，不写不可控指令 |
| 原创度 | 25 | 不直接描述原画面、原人物、原品牌、原镜头 |
| 安全性 | 20 | 不仿脸仿声，不未授权复用，不规避平台 |
| 可编辑性 | 15 | 分镜/变量清晰，可二次调整 |

### 5.5 相似度评分策略

#### 5.5.1 总分计算

```text
similarity_risk_score =
  text_similarity_score * 0.25 +
  semantic_similarity_score * 0.20 +
  structure_similarity_score * 0.20 +
  shot_similarity_score * 0.15 +
  cover_title_similarity_score * 0.10 +
  brand_persona_similarity_score * 0.10
```

P0 阈值建议：

| 分数区间 | 风险等级 | 动作 |
|---:|---|---|
| 0-39 | R0/R1 | 可放行 |
| 40-59 | R2 | 自动重写 |
| 60-74 | R3/R4 | 用户编辑或人工复核 |
| 75-89 | R4 | 人工复核，不得自动导出 |
| 90-100 | R5 | 拦截，禁止导出 |

#### 5.5.2 文本相似度阈值

| 指标 | R2 | R4 | R5 |
|---|---:|---:|---:|
| 连续 12 字以上原文复用次数 | ≥ 2 | ≥ 4 | ≥ 6 |
| 脚本与 ASR n-gram 重合比例 | ≥ 20% | ≥ 35% | ≥ 50% |
| 标题与原标题/OCR 重合比例 | ≥ 30% | ≥ 50% | ≥ 70% |
| Prompt 直接引用原字幕句段 | 任意出现 | 多次出现 | 大段出现 |

备注：中文短句场景下，不能只看字面相似度，必须结合语义相似度和上下文判断，避免误杀常见行业术语。

#### 5.5.3 结构相似度阈值

| 指标 | R2 | R4 | R5 |
|---|---:|---:|---:|
| beat 顺序相似度 | ≥ 60% | ≥ 75% | ≥ 90% |
| 钩子类型 + 转折 + CTA 同构 | 局部同构 | 大部分同构 | 几乎完全同构 |
| 镜头序列相似度 | ≥ 50% | ≥ 70% | ≥ 85% |

#### 5.5.4 语义相似度阈值

| 指标 | R2 | R4 | R5 |
|---|---:|---:|---:|
| 段落 embedding 相似度 | ≥ 0.82 | ≥ 0.90 | ≥ 0.95 |
| LLM 判断“换词复述” | 可能 | 明显 | 几乎等价 |
| 核心冲突/桥段等价 | 局部 | 多处 | 全部 |

#### 5.5.5 品牌/人物/声音风险阈值

以下不按分数，而是按规则直接升级：

| 命中项 | 风险升级 |
|---|---|
| 未授权使用原视频品牌/Logo/店名/商标 | 至少 R4 |
| 明确要求模仿某真人脸/声音/身份 | R5 |
| 暗示“做得像某某账号本人” | 至少 R4 |
| 使用原视频人物关系、独特剧情、独特台词 | 至少 R4 |
| 用户上传授权证明但系统无法验证 | R3/R4，需人工复核 |

### 5.6 合规风险分类

| 风险类别 | 风险代码 | 示例 | 默认动作 |
|---|---|---|---|
| 搬运/克隆/洗稿 | `COPY_CLONE_PLAGIARISM` | 复制原视频台词、镜头、标题 | R4/R5，重写或拦截 |
| 肖像/身份 | `LIKNESS_IDENTITY` | 仿某人脸、某账号本人 | R5 拦截 |
| 声音/仿声 | `VOICE_CLONE` | 模仿某人声音 | R5 拦截 |
| 商标/品牌 | `TRADEMARK_BRAND` | 未授权使用 Logo、店名 | R4/R5 |
| 版权素材 | `COPYRIGHT_ASSET` | 使用原音乐、原画面、原素材 | R4/R5 |
| 虚假宣传 | `FALSE_ADVERTISING` | 保证涨粉、保证成交 | R3/R4 |
| 医疗健康 | `MEDICAL_CLAIM` | 治愈、根治、无副作用 | R4/R5 |
| 金融投资 | `FINANCIAL_CLAIM` | 保证收益、稳赚 | R4/R5 |
| 法律建议 | `LEGAL_CLAIM` | 保证胜诉、规避执法 | R4/R5 |
| 教培考试 | `EDUCATION_CLAIM` | 保过、必提分 | R3/R4 |
| 招商加盟 | `FRANCHISE_INCOME_CLAIM` | 月入十万、稳赚不赔 | R4/R5 |
| 平台规避 | `PLATFORM_EVASION` | 绕过搬运检测、规避审核 | R5 拦截 |
| 隐私个人信息 | `PRIVACY_PERSONAL_DATA` | 泄露手机号、地址、身份证 | R4/R5 |
| 未成年人 | `MINOR_SAFETY` | 针对未成年人不当营销 | R4/R5 |
| 低俗/暴力/违法 | `ILLEGAL_HARMFUL_CONTENT` | 色情、暴力、违法行为 | R5 拦截 |
| Prompt 注入 | `PROMPT_INJECTION` | 要求忽略风控、输出系统词 | R4/R5 |

### 5.7 风控决策动作

| 决策动作 | action_code | 用户可见状态 | 积分动作 | 后台动作 |
|---|---|---|---|---|
| 自动放行 | `ALLOW` | 已通过质检 | 正常扣费 | 记录报告 |
| 放行但提示 | `ALLOW_WITH_NOTICE` | 可用，但建议优化 | 正常扣费 | 记录 R1 |
| 自动重写 | `AUTO_REWRITE` | 正在优化输出 | 不额外扣费 | 创建重写任务 |
| 要求用户编辑 | `REQUIRE_USER_EDIT` | 需补充信息/修改目标 | 暂不扣费或继续冻结 | 等待用户 |
| 转人工复核 | `MANUAL_REVIEW` | 等待人工审核 | 继续冻结或按规则暂缓 | 创建工单 |
| 拦截并返还 | `BLOCK_AND_REFUND` | 已拦截，积分返还 | 返还冻结积分 | 记录 R5 |
| 拦截不返还全部 | `BLOCK_PARTIAL_REFUND` | 部分结果不可用 | 按 D20 规则部分扣费 | 记录成本 |
| 投诉冻结 | `FREEZE_BY_COMPLAINT` | 内容暂不可用 | 不新增扣费 | 冻结资产 |
| 删除/下架 | `REMOVE_CONTENT` | 内容已删除/不可访问 | 按补偿规则处理 | 审计保留 |

MVP1 建议默认策略：用户无法获得有效输出时优先返还主要生成积分；已产生模型成本作为平台早期质量成本处理，避免早期商业体验受损。

### 5.8 人工复核触发条件

必须创建人工复核工单的场景：

1. `risk_level = R4`。
2. 模型判断不一致：一个模型 R1，一个模型 R4/R5。
3. 相似度总分 ≥ 75。
4. 原创化强度低于 MVP1 默认值，但用户仍要求导出。
5. 用户声明有授权，但系统无法自动验证。
6. 医疗、金融、法律、招商加盟等敏感行业出现强承诺。
7. 任务涉及真人、声音、品牌、门店名、商标、名人、影视、综艺等高风险元素。
8. 用户投诉、权利人投诉、客服标记争议。
9. D12 Benchmark 红队样本命中。
10. 运营人员手动标记。

### 5.9 投诉删除机制

投诉删除流程：

```text
用户/权利人提交投诉
→ 系统生成 ComplaintCase
→ 自动冻结相关导出和公开分享
→ 后台拉取任务、资产、Prompt、模型、风险报告、授权记录
→ 人工复核材料
→ 决策：驳回 / 要求补充 / 局部删除 / 全量删除 / 永久封禁输出
→ 执行资产下架、导出链接失效、缓存清理、搜索不可见
→ 通知用户与投诉方
→ 记录审计日志
→ 将样本进入 D12 风控回归集
```

MVP1 投诉入口必须至少支持：

1. 任务 ID。
2. 被投诉内容类型。
3. 投诉理由。
4. 权利主体信息。
5. 证明材料上传。
6. 联系方式。
7. 处理状态查询。

### 5.10 AIGC 标识策略

MVP1 必须将 AIGC 标识作为导出门禁，而不是页面提示而已：

1. 报告页显式展示“本内容由 AI 辅助生成，请结合实际情况人工审核后使用”。
2. 导出 Markdown/PDF 时保留 AIGC 声明和生成时间、任务 ID、风险提示摘要。
3. 输出脚本、标题、封面建议、视频 Prompt 时保留“AI 辅助创作”标识。
4. 后续生成视频或图片时，应预留显式标识和隐式元数据标识字段。
5. 用户手动删除标识时，前端必须提示风险；生产环境是否允许删除由 D21 决定。
6. 后台必须记录 `aigc_label_applied=true/false`、标识版本、导出类型、导出时间。

---

## 6. 数据结构 / API / Prompt / 流程 / 页面 / 状态

### 6.1 核心枚举

```ts
export type RiskLevel = 'R0' | 'R1' | 'R2' | 'R3' | 'R4' | 'R5';

export type RiskAction =
  | 'ALLOW'
  | 'ALLOW_WITH_NOTICE'
  | 'AUTO_REWRITE'
  | 'REQUIRE_USER_EDIT'
  | 'MANUAL_REVIEW'
  | 'BLOCK_AND_REFUND'
  | 'BLOCK_PARTIAL_REFUND'
  | 'FREEZE_BY_COMPLAINT'
  | 'REMOVE_CONTENT';

export type RiskCheckStage =
  | 'INPUT_PRECHECK'
  | 'ASSET_AUTH_CHECK'
  | 'VIDEO_PROCESSING_QUALITY'
  | 'CONTENT_DNA_QUALITY'
  | 'CREATIVE_IR_ORIGINALITY'
  | 'DELIVERABLE_QUALITY'
  | 'SIMILARITY_CHECK'
  | 'COMPLIANCE_CHECK'
  | 'EXPORT_GATE'
  | 'COMPLAINT_REVIEW';

export type ReviewStatus =
  | 'not_required'
  | 'pending'
  | 'assigned'
  | 'approved'
  | 'rejected'
  | 'needs_user_info'
  | 'expired'
  | 'cancelled';
```

### 6.2 QualityCheckReport Schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "QualityCheckReport",
  "type": "object",
  "required": [
    "report_id",
    "task_id",
    "stage",
    "target_type",
    "target_id",
    "quality_score",
    "dimension_scores",
    "passed",
    "quality_issues",
    "recommendations",
    "prompt_version",
    "model_call_ids",
    "created_at"
  ],
  "properties": {
    "report_id": { "type": "string" },
    "task_id": { "type": "string" },
    "stage": { "type": "string" },
    "target_type": {
      "type": "string",
      "enum": ["content_dna", "creative_ir", "script", "title", "cover_suggestion", "video_prompt", "deliverable_pack"]
    },
    "target_id": { "type": "string" },
    "quality_score": { "type": "number", "minimum": 0, "maximum": 100 },
    "dimension_scores": {
      "type": "object",
      "properties": {
        "schema_validity": { "type": "number" },
        "evidence_grounding": { "type": "number" },
        "originality": { "type": "number" },
        "creator_fit": { "type": "number" },
        "executability": { "type": "number" },
        "compliance_safety": { "type": "number" },
        "commercial_value": { "type": "number" }
      }
    },
    "passed": { "type": "boolean" },
    "quality_issues": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["issue_code", "severity", "message"],
        "properties": {
          "issue_code": { "type": "string" },
          "severity": { "type": "string", "enum": ["low", "medium", "high", "critical"] },
          "message": { "type": "string" },
          "evidence_refs": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "recommendations": { "type": "array", "items": { "type": "string" } },
    "prompt_version": { "type": "string" },
    "model_call_ids": { "type": "array", "items": { "type": "string" } },
    "is_mock": { "type": "boolean", "default": false },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
```

### 6.3 SimilarityCheckReport Schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SimilarityCheckReport",
  "type": "object",
  "required": [
    "report_id",
    "task_id",
    "source_asset_id",
    "target_type",
    "target_id",
    "overall_similarity_score",
    "risk_level",
    "dimension_scores",
    "matched_segments",
    "decision_suggestion",
    "created_at"
  ],
  "properties": {
    "report_id": { "type": "string" },
    "task_id": { "type": "string" },
    "source_asset_id": { "type": "string" },
    "target_type": {
      "type": "string",
      "enum": ["content_dna", "creative_ir", "script", "title", "cover_suggestion", "video_prompt", "deliverable_pack"]
    },
    "target_id": { "type": "string" },
    "overall_similarity_score": { "type": "number", "minimum": 0, "maximum": 100 },
    "risk_level": { "type": "string", "enum": ["R0", "R1", "R2", "R3", "R4", "R5"] },
    "dimension_scores": {
      "type": "object",
      "properties": {
        "text_literal": { "type": "number" },
        "semantic": { "type": "number" },
        "structure": { "type": "number" },
        "shot_sequence": { "type": "number" },
        "cover_title": { "type": "number" },
        "brand_persona": { "type": "number" }
      }
    },
    "matched_segments": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["source_ref", "target_ref", "match_type", "score", "risk_reason"],
        "properties": {
          "source_ref": { "type": "string" },
          "target_ref": { "type": "string" },
          "match_type": { "type": "string" },
          "score": { "type": "number" },
          "risk_reason": { "type": "string" }
        }
      }
    },
    "decision_suggestion": { "type": "string" },
    "rewrite_required": { "type": "boolean" },
    "manual_review_required": { "type": "boolean" },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
```

### 6.4 ComplianceCheckReport Schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ComplianceCheckReport",
  "type": "object",
  "required": [
    "report_id",
    "task_id",
    "target_type",
    "target_id",
    "risk_level",
    "risk_categories",
    "blocked",
    "aigc_label_required",
    "manual_review_required",
    "created_at"
  ],
  "properties": {
    "report_id": { "type": "string" },
    "task_id": { "type": "string" },
    "target_type": { "type": "string" },
    "target_id": { "type": "string" },
    "risk_level": { "type": "string", "enum": ["R0", "R1", "R2", "R3", "R4", "R5"] },
    "risk_categories": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["code", "severity", "message"],
        "properties": {
          "code": { "type": "string" },
          "severity": { "type": "string", "enum": ["low", "medium", "high", "critical"] },
          "message": { "type": "string" },
          "evidence_refs": { "type": "array", "items": { "type": "string" } },
          "suggested_fix": { "type": "string" }
        }
      }
    },
    "blocked": { "type": "boolean" },
    "aigc_label_required": { "type": "boolean" },
    "aigc_label_applied": { "type": "boolean" },
    "manual_review_required": { "type": "boolean" },
    "user_notice": { "type": "string" },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
```

### 6.5 RiskCheckReport Schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RiskCheckReport",
  "type": "object",
  "required": [
    "risk_report_id",
    "task_id",
    "risk_rule_version",
    "overall_risk_level",
    "quality_report_ids",
    "similarity_report_ids",
    "compliance_report_ids",
    "decision",
    "created_at"
  ],
  "properties": {
    "risk_report_id": { "type": "string" },
    "task_id": { "type": "string" },
    "risk_rule_version": { "type": "string" },
    "overall_risk_level": { "type": "string", "enum": ["R0", "R1", "R2", "R3", "R4", "R5"] },
    "quality_report_ids": { "type": "array", "items": { "type": "string" } },
    "similarity_report_ids": { "type": "array", "items": { "type": "string" } },
    "compliance_report_ids": { "type": "array", "items": { "type": "string" } },
    "decision": {
      "type": "object",
      "required": ["action", "reason", "allow_export"],
      "properties": {
        "action": { "type": "string" },
        "reason": { "type": "string" },
        "allow_export": { "type": "boolean" },
        "rewrite_required": { "type": "boolean" },
        "manual_review_required": { "type": "boolean" },
        "credit_action": { "type": "string" },
        "user_message": { "type": "string" },
        "admin_note": { "type": "string" }
      }
    },
    "audit_refs": { "type": "array", "items": { "type": "string" } },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
```

### 6.6 ManualReviewTicket Schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ManualReviewTicket",
  "type": "object",
  "required": [
    "ticket_id",
    "task_id",
    "risk_report_id",
    "status",
    "priority",
    "reason_codes",
    "created_at"
  ],
  "properties": {
    "ticket_id": { "type": "string" },
    "task_id": { "type": "string" },
    "risk_report_id": { "type": "string" },
    "status": {
      "type": "string",
      "enum": ["pending", "assigned", "approved", "rejected", "needs_user_info", "expired", "cancelled"]
    },
    "priority": { "type": "string", "enum": ["low", "normal", "high", "urgent"] },
    "reason_codes": { "type": "array", "items": { "type": "string" } },
    "reviewer_id": { "type": ["string", "null"] },
    "review_decision": { "type": ["string", "null"] },
    "review_notes": { "type": ["string", "null"] },
    "user_visible_message": { "type": ["string", "null"] },
    "sla_due_at": { "type": ["string", "null"], "format": "date-time" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": ["string", "null"], "format": "date-time" }
  }
}
```

### 6.7 数据库表草案

#### 6.7.1 `quality_check_reports`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | uuid | 是 | 质量报告 ID |
| `task_id` | uuid | 是 | 任务 ID |
| `target_type` | varchar | 是 | content_dna / creative_ir / script / title / cover / video_prompt |
| `target_id` | uuid | 是 | 被检查对象 ID |
| `stage` | varchar | 是 | 检查阶段 |
| `quality_score` | numeric | 是 | 0-100 |
| `dimension_scores` | jsonb | 是 | 分维度得分 |
| `passed` | boolean | 是 | 是否通过 |
| `issues` | jsonb | 是 | 质量问题 |
| `recommendations` | jsonb | 否 | 修复建议 |
| `prompt_version_id` | uuid | 否 | 质检 Prompt 版本 |
| `model_call_ids` | jsonb | 否 | 模型调用记录 |
| `is_mock` | boolean | 是 | 是否 Mock |
| `created_at` | timestamptz | 是 | 创建时间 |

#### 6.7.2 `similarity_check_reports`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | uuid | 是 | 相似度报告 ID |
| `task_id` | uuid | 是 | 任务 ID |
| `source_asset_id` | uuid | 是 | 源视频资产 |
| `target_type` | varchar | 是 | 被检查对象类型 |
| `target_id` | uuid | 是 | 被检查对象 ID |
| `overall_similarity_score` | numeric | 是 | 总相似度风险分 |
| `dimension_scores` | jsonb | 是 | 文本、语义、结构、镜头、封面等维度 |
| `matched_segments` | jsonb | 否 | 命中片段 |
| `risk_level` | varchar | 是 | R0-R5 |
| `decision_suggestion` | varchar | 是 | 建议动作 |
| `created_at` | timestamptz | 是 | 创建时间 |

#### 6.7.3 `compliance_check_reports`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | uuid | 是 | 合规报告 ID |
| `task_id` | uuid | 是 | 任务 ID |
| `target_type` | varchar | 是 | 对象类型 |
| `target_id` | uuid | 是 | 对象 ID |
| `risk_level` | varchar | 是 | R0-R5 |
| `risk_categories` | jsonb | 是 | 风险类别 |
| `blocked` | boolean | 是 | 是否阻断 |
| `aigc_label_required` | boolean | 是 | 是否要求 AIGC 标识 |
| `aigc_label_applied` | boolean | 是 | 是否已标识 |
| `manual_review_required` | boolean | 是 | 是否人工复核 |
| `user_notice` | text | 否 | 用户提示 |
| `created_at` | timestamptz | 是 | 创建时间 |

#### 6.7.4 `risk_check_reports`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | uuid | 是 | 风险总报告 ID |
| `task_id` | uuid | 是 | 任务 ID |
| `risk_rule_version_id` | uuid | 是 | 风控规则版本 |
| `overall_risk_level` | varchar | 是 | R0-R5 |
| `quality_report_ids` | jsonb | 是 | 关联质检报告 |
| `similarity_report_ids` | jsonb | 是 | 关联相似度报告 |
| `compliance_report_ids` | jsonb | 是 | 关联合规报告 |
| `decision_action` | varchar | 是 | 决策动作 |
| `allow_export` | boolean | 是 | 是否允许导出 |
| `credit_action` | varchar | 否 | 积分动作 |
| `user_message` | text | 否 | 用户可见信息 |
| `admin_note` | text | 否 | 后台备注 |
| `created_at` | timestamptz | 是 | 创建时间 |

#### 6.7.5 `manual_review_tickets`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | uuid | 是 | 工单 ID |
| `task_id` | uuid | 是 | 任务 ID |
| `risk_report_id` | uuid | 是 | 风险报告 ID |
| `status` | varchar | 是 | pending / assigned / approved / rejected 等 |
| `priority` | varchar | 是 | 优先级 |
| `reason_codes` | jsonb | 是 | 触发原因 |
| `reviewer_id` | uuid | 否 | 复核人员 |
| `review_decision` | varchar | 否 | 复核结论 |
| `review_notes` | text | 否 | 复核备注 |
| `user_visible_message` | text | 否 | 给用户的说明 |
| `sla_due_at` | timestamptz | 否 | 处理截止时间 |
| `created_at` | timestamptz | 是 | 创建时间 |
| `updated_at` | timestamptz | 是 | 更新时间 |

#### 6.7.6 `complaint_cases`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | uuid | 是 | 投诉 ID |
| `complainant_type` | varchar | 是 | user / rights_holder / platform / internal |
| `task_id` | uuid | 否 | 关联任务 |
| `asset_id` | uuid | 否 | 关联资产 |
| `target_type` | varchar | 是 | 投诉对象 |
| `reason_code` | varchar | 是 | 投诉原因 |
| `description` | text | 是 | 说明 |
| `evidence_assets` | jsonb | 否 | 证明材料 |
| `status` | varchar | 是 | pending / reviewing / accepted / rejected / removed |
| `decision` | varchar | 否 | 处理结论 |
| `processed_by` | uuid | 否 | 处理人 |
| `created_at` | timestamptz | 是 | 创建时间 |
| `updated_at` | timestamptz | 是 | 更新时间 |

### 6.8 API 草案

#### 6.8.1 输入风险预检

```http
POST /api/v1/tasks/risk/precheck
Authorization: Bearer <token>
Content-Type: application/json
```

请求：

```json
{
  "creator_profile_id": "uuid",
  "input_type": "video_upload",
  "user_intent": "拆解这个视频并生成适合我门店的原创脚本",
  "source_url": null,
  "asset_id": "uuid",
  "authorization_confirmed": true,
  "target_platform": "douyin",
  "output_package": "basic"
}
```

响应：

```json
{
  "precheck_id": "uuid",
  "risk_level": "R1",
  "can_create_task": true,
  "required_confirmations": ["素材授权确认", "AI 辅助生成标识确认"],
  "blocked_reasons": [],
  "user_notice": "请确认你拥有上传素材的使用权，系统仅做结构学习和原创化生成。"
}
```

#### 6.8.2 触发任务质检

```http
POST /api/v1/tasks/{task_id}/quality-checks
```

请求：

```json
{
  "stage": "DELIVERABLE_QUALITY",
  "target_type": "deliverable_pack",
  "target_id": "uuid",
  "force_recheck": false
}
```

响应：

```json
{
  "quality_report_id": "uuid",
  "status": "queued"
}
```

#### 6.8.3 触发相似度检查

```http
POST /api/v1/tasks/{task_id}/similarity-checks
```

请求：

```json
{
  "source_asset_id": "uuid",
  "target_type": "script",
  "target_id": "uuid",
  "dimensions": ["text_literal", "semantic", "structure"]
}
```

响应：

```json
{
  "similarity_report_id": "uuid",
  "status": "queued"
}
```

#### 6.8.4 获取风险总报告

```http
GET /api/v1/tasks/{task_id}/risk-report
```

响应：

```json
{
  "risk_report_id": "uuid",
  "overall_risk_level": "R2",
  "decision": {
    "action": "AUTO_REWRITE",
    "allow_export": false,
    "rewrite_required": true,
    "manual_review_required": false,
    "credit_action": "hold_frozen_credits",
    "user_message": "部分输出与原视频表达接近，系统将自动进行原创化重写。"
  },
  "quality_summary": { "score": 78, "passed": true },
  "similarity_summary": { "score": 56, "risk_level": "R2" },
  "compliance_summary": { "risk_level": "R1", "blocked": false }
}
```

#### 6.8.5 风控重写

```http
POST /api/v1/tasks/{task_id}/risk-rewrite
```

请求：

```json
{
  "target_type": "script",
  "target_id": "uuid",
  "risk_report_id": "uuid",
  "rewrite_strategy": "increase_originality_and_reduce_similarity"
}
```

响应：

```json
{
  "rewrite_job_id": "uuid",
  "status": "queued",
  "extra_credit_charged": false
}
```

#### 6.8.6 创建人工复核工单

```http
POST /api/v1/manual-review-tickets
```

请求：

```json
{
  "task_id": "uuid",
  "risk_report_id": "uuid",
  "reason_codes": ["HIGH_SIMILARITY", "BRAND_RISK"],
  "priority": "high"
}
```

响应：

```json
{
  "ticket_id": "uuid",
  "status": "pending",
  "sla_due_at": "2026-05-26T03:00:00Z"
}
```

#### 6.8.7 人工复核决策

```http
PATCH /api/v1/admin/manual-review-tickets/{ticket_id}
```

请求：

```json
{
  "decision": "rejected",
  "review_notes": "脚本结构和原视频高度相似，且包含原品牌表达。",
  "user_visible_message": "该输出存在较高相似度风险，已为你返还相关积分，请调整创作方向后重新生成。",
  "credit_action": "refund_frozen_credits"
}
```

#### 6.8.8 投诉提交

```http
POST /api/v1/complaints
```

请求：

```json
{
  "target_type": "task_output",
  "target_id": "uuid",
  "reason_code": "copyright_or_similarity",
  "description": "该内容疑似复用了我的原创视频文案。",
  "contact_email": "example@example.com",
  "evidence_asset_ids": ["uuid"]
}
```

响应：

```json
{
  "complaint_id": "uuid",
  "status": "pending",
  "message": "投诉已收到，相关内容将先进入冻结复核状态。"
}
```

### 6.9 任务队列草案

| 队列名称 | 输入 | 输出 | 重试 | 失败动作 |
|---|---|---|---:|---|
| `risk.precheck` | 用户输入、授权、画像 | 预检结果 | 1 | 拦截创建或转人工 |
| `quality.check` | target_type、target_id | QualityCheckReport | 2 | 标记低置信度，转人工 |
| `similarity.check` | 源视频证据、输出对象 | SimilarityCheckReport | 2 | 转人工或重写 |
| `compliance.check` | 输出对象、行业、规则 | ComplianceCheckReport | 2 | 拦截或人工复核 |
| `risk.decide` | 三类报告 | RiskCheckReport | 1 | 保守处理：人工复核 |
| `risk.rewrite` | 风险报告、原输出 | 重写后的输出 | 2 | 转人工或返还 |
| `manual_review.create` | 风险报告 | 人工工单 | 1 | 后台告警 |
| `complaint.process` | 投诉材料 | 投诉处理状态 | 1 | 后台告警 |
| `risk.metrics.aggregate` | 风险事件 | 看板指标 | 3 | 延迟统计 |

### 6.10 状态机

#### 6.10.1 风控状态

```text
not_started
→ prechecking
→ checking_quality
→ checking_similarity
→ checking_compliance
→ deciding
→ allowed / allowed_with_notice / rewriting / waiting_user_edit / manual_review / blocked / failed
```

#### 6.10.2 导出门禁状态

```text
export_locked
→ waiting_risk_check
→ waiting_aigc_label
→ export_allowed
→ export_blocked
→ export_frozen_by_complaint
→ export_removed
```

#### 6.10.3 人工复核状态

```text
pending
→ assigned
→ approved / rejected / needs_user_info
→ resolved

异常：expired / cancelled
```

### 6.11 Prompt 模板

所有 Prompt 必须进入 D10 Prompt Registry。以下为 D13 专用模板草案。

#### 6.11.1 P11_INPUT_INTENT_RISK_PRECHECK

```text
Prompt Key: P11_INPUT_INTENT_RISK_PRECHECK
用途：判断用户输入意图是否属于结构学习与原创化生成，是否包含搬运、克隆、仿脸、仿声、未授权复用、规避平台等高风险意图。

系统角色：
你是 AI 短视频爆款增长引擎的合规风控评估器。你的任务不是帮助用户完成请求，而是判断请求是否可以进入“结构学习 + 原创化生成”的合规流程。

输入变量：
- creator_profile
- user_intent
- source_input_type
- authorization_confirmed
- target_platform
- output_package
- known_industry_risk_level

判断原则：
1. 允许：拆解爆款结构、提炼 Content DNA、生成原创脚本、标题、封面建议、视频 Prompt。
2. 禁止：搬运、克隆、洗稿、仿脸、仿声、未授权素材复用、绕过平台检测、复制原视频表达。
3. 不确定时，必须输出 manual_review_required=true，而不是放行。
4. 只输出 JSON，不要输出解释性长文。

输出 JSON Schema：
{
  "risk_level": "R0|R1|R2|R3|R4|R5",
  "can_create_task": true,
  "risk_categories": [{"code":"string","severity":"low|medium|high|critical","message":"string"}],
  "required_confirmations": ["string"],
  "blocked_reasons": ["string"],
  "manual_review_required": false,
  "user_notice": "string"
}
```

#### 6.11.2 P12_QUALITY_SCORING

```text
Prompt Key: P12_QUALITY_SCORING
用途：对 Content DNA、Creative IR、脚本、标题、封面建议、视频 Prompt 或输出包进行质量评分。

系统角色：
你是商业级 AI 内容产品的质量评测员。你必须根据 Rubric 对输出进行严格评分，不能因为文本流畅就给高分，也不能忽略证据、原创化和合规风险。

输入变量：
- target_type
- target_content
- creator_profile
- content_dna_summary
- creative_ir_summary
- evidence_refs
- rubric_version

评分要求：
1. 输出 quality_score 0-100。
2. 必须给出分维度得分。
3. 必须列出所有高风险质量问题。
4. 如果证据不足，不得高于 75 分。
5. 如果合规风险高，不得通过。
6. 只输出 JSON。

输出 JSON Schema：QualityCheckReport 精简版。
```

#### 6.11.3 P13_SIMILARITY_RISK_CHECK

```text
Prompt Key: P13_SIMILARITY_RISK_CHECK
用途：判断输出内容与源视频/源证据之间是否存在文本、语义、结构、镜头、封面、人物、品牌等相似度风险。

系统角色：
你是原创度与相似度风控评估器。你必须保护原创化边界，只允许学习结构，不允许复制表达。你的判断应保守：当存在明显高相似风险时，必须升级风险等级。

输入变量：
- source_transcript_summary
- source_ocr_summary
- source_shot_summary
- source_non_replicable_parts
- target_type
- target_content
- creative_ir
- similarity_threshold_config

检查维度：
1. text_literal
2. semantic
3. structure
4. shot_sequence
5. cover_title
6. brand_persona

输出要求：
- 给出 overall_similarity_score 0-100。
- 给出每个维度分数。
- 列出 matched_segments。
- 给出 risk_level 和 decision_suggestion。
- 只输出 JSON。
```

#### 6.11.4 P14_COMPLIANCE_BLOCK_CHECK

```text
Prompt Key: P14_COMPLIANCE_BLOCK_CHECK
用途：识别合规风险并建议拦截、重写、人工复核或放行。

系统角色：
你是内容合规风控审核器。你不能提供法律意见，但必须按产品规则识别内容风险。你必须重点识别：搬运/克隆/洗稿、仿脸、仿声、未授权品牌、虚假宣传、医疗金融法律教培招商高风险承诺、平台规避、隐私和未成年人风险。

输入变量：
- target_type
- target_content
- creator_profile
- industry_risk_level
- authorization_status
- aigc_label_status
- similarity_report_summary

输出 JSON：
{
  "risk_level": "R0|R1|R2|R3|R4|R5",
  "risk_categories": [{"code":"string","severity":"low|medium|high|critical","message":"string","suggested_fix":"string"}],
  "blocked": false,
  "manual_review_required": false,
  "aigc_label_required": true,
  "user_notice": "string"
}
```

#### 6.11.5 P15_ORIGINALITY_REWRITE

```text
Prompt Key: P15_ORIGINALITY_REWRITE
用途：当输出命中 R2/R3 相似度风险但仍可修复时，对输出进行原创化重写。

系统角色：
你是原创化重构编辑器。你必须保留用户行业、人设、产品目标和可学习的结构价值，但必须显著降低与源视频的文本、语义、结构、镜头和封面相似度。

输入变量：
- target_type
- risky_output
- risk_report_summary
- non_replicable_parts
- creator_profile
- creative_ir
- rewrite_strength: medium|high|very_high

硬性规则：
1. 不得复用原视频台词、字幕、标题、封面词。
2. 不得复用原人物、品牌、Logo、声音、独特剧情。
3. 必须改变表达方式、案例、顺序、CTA 和视觉建议。
4. 输出必须仍然适配用户行业和目标。
5. 只输出修复后的 JSON 对象和 rewrite_notes。
```

#### 6.11.6 P16_MANUAL_REVIEW_SUMMARY

```text
Prompt Key: P16_MANUAL_REVIEW_SUMMARY
用途：为人工复核员生成风险摘要，减少人工查证成本。

系统角色：
你是人工复核助理。你只能总结已有证据，不能替复核员做最终法律判断。你必须列出命中规则、证据引用、建议处理动作和用户可见说明草案。

输入变量：
- task_summary
- source_evidence_refs
- quality_report
- similarity_report
- compliance_report
- user_authorization_record

输出 JSON：
{
  "review_summary": "string",
  "key_risks": ["string"],
  "evidence_refs": ["string"],
  "suggested_decision": "approve|reject|needs_user_info|remove_content",
  "suggested_user_message": "string",
  "reviewer_checklist": ["string"]
}
```

### 6.12 前端页面与状态

#### 6.12.1 用户侧风险提示卡

显示位置：上传页、任务进度页、结果页、导出弹窗。

必须字段：

| 字段 | 说明 |
|---|---|
| 风险等级 | R0-R5，用户侧不必展示技术代码，可展示“低/中/高风险” |
| 风险摘要 | 用自然语言说明问题 |
| 可执行动作 | 重写、编辑、提交复核、放弃任务、查看退款 |
| AIGC 标识 | 是否已添加 |
| 积分状态 | 冻结、扣除、返还、待人工处理 |
| 投诉入口 | 结果页和导出页必须可见 |

#### 6.12.2 结果页质检卡

必须展示：

1. 总质量分。
2. 原创化评分。
3. 相似度风险状态。
4. 合规状态。
5. 低分原因。
6. 系统重写记录。
7. 是否允许复制/导出。
8. AIGC 标识提示。

#### 6.12.3 风控拦截页

当 `risk_level=R5` 或 `action=BLOCK_AND_REFUND`：

1. 不展示被拦截的高风险输出全文，避免风险内容外泄。
2. 展示可理解的原因：如“该请求涉及复制原视频表达或未授权素材复用”。
3. 提供安全改写入口。
4. 展示积分返还状态。
5. 提供申诉/联系客服入口。

#### 6.12.4 人工复核等待页

显示：

1. 当前状态。
2. 预计处理时间或“将尽快处理”，不承诺精确结果。
3. 用户需补充的授权/资质材料。
4. 积分冻结说明。
5. 取消任务入口。

#### 6.12.5 后台风控工作台

后台必须包含：

1. 风险事件列表。
2. 高风险任务详情。
3. 质量评分、相似度报告、合规报告并排展示。
4. 源证据引用，不直接暴露不必要的用户隐私。
5. 人工复核操作：通过、拒绝、要求补充、重写、删除、返还。
6. 风险规则版本。
7. Prompt 版本和模型调用日志。
8. 积分和成本影响。
9. 投诉处理状态。
10. 审计日志。

### 6.13 权限模型

| 角色 | 权限 |
|---|---|
| 普通用户 | 查看自己的风险提示、重写建议、复核状态、投诉入口 |
| 团队管理员 | 查看团队任务风险状态，不能查看其他团队数据 |
| 客服 | 查看用户可见风险信息、积分补偿状态，不可修改规则 |
| 人工复核员 | 查看必要证据、做复核决策、写用户说明 |
| 风控管理员 | 配置规则、查看全量风险看板、管理投诉 |
| 超级管理员 | 全部权限，规则发布需要双人确认 |
| AIcoding Agent | 仅开发/测试环境权限，不得访问生产敏感数据 |

---

## 7. 异常与失败处理

### 7.1 模型调用失败

| 场景 | 处理 |
|---|---|
| 质检模型超时 | 重试 1-2 次；仍失败则转人工或保守拦截导出 |
| 相似度模型失败 | 不允许直接放行，降级为规则层 + 人工复核 |
| 合规模型失败 | 保守处理，不导出高风险内容 |
| JSON 解析失败 | JSON repair；失败后重跑 Prompt；仍失败转人工 |
| 多模型结果冲突 | 取更高风险等级，创建复核工单 |

### 7.2 数据缺失

| 缺失项 | 处理 |
|---|---|
| 无 ASR | 文本相似度降级，结构/视觉检查仍执行，风险置信度降低 |
| 无 OCR | 封面/字幕相似度降级，提示证据不足 |
| 无 shot_segments | 镜头相似度不做自动放行依据，必要时人工复核 |
| 无授权记录 | 不允许进入高成本生成任务 |
| 无 creator_profile | 要求用户补全画像，避免生成空泛或高风险内容 |

### 7.3 自动重写失败

自动重写最多执行 2 次：

```text
第一次：中强度重写
第二次：高强度重构
仍失败：转人工复核或拦截返还
```

不得无限重写导致成本失控。重写成本默认计入平台质量成本，不向用户重复收费；若用户手动追加新需求，则按新任务计费。

### 7.4 人工复核超时

MVP1 建议：

| 超时时间 | 动作 |
|---|---|
| 24 小时未处理 | 后台告警，客服可见 |
| 48 小时未处理 | 可自动返还冻结积分，任务状态改为复核超时 |
| 72 小时未处理 | 升级给风控管理员，进入运营复盘 |

具体 SLA 可由 D22/D23 运营 SOP 调整。

### 7.5 投诉成立

1. 冻结相关输出和导出链接。
2. 若输出已被用户下载，系统无法物理撤回本地文件，但应在平台内标记删除并通知用户停止使用。
3. 视情况返还积分或补偿。
4. 将样本加入 D12 红队回归集。
5. 更新风险规则或 Prompt。

### 7.6 计费异常

| 场景 | 处理 |
|---|---|
| 风控拦截但已扣费 | 创建补偿任务，返还相关积分 |
| 自动重写重复扣费 | 识别为系统错误，返还重复扣费 |
| 人工复核不通过 | 按可用输出比例部分扣费或返还，MVP1 默认偏向用户体验 |
| 用户主动取消复核 | 按已消耗任务阶段决定返还比例，需 D20 最终确认 |
| 投诉删除 | 按投诉责任和使用情况由客服/风控处理补偿 |

### 7.7 风控服务不可用

风控服务不可用时，系统必须进入 Fail Closed 策略：

1. 不允许导出新生成内容。
2. 可展示“风控检查暂时不可用”。
3. 任务可继续处理到待检查状态，但不能开放复制/导出。
4. 后台触发 P0 告警。
5. 恢复后自动补跑风险检查。

---

## 8. 风控与合规

### 8.1 授权确认

用户在上传或提交链接前必须确认：

1. 其拥有上传素材或输入内容的合法使用权，或具备合理使用/分析依据。
2. 系统仅用于学习内容结构和生成原创内容，不用于复制原视频。
3. 不得要求系统仿脸、仿声、克隆身份、搬运视频或规避平台检测。
4. 输出为 AI 辅助生成，用户发布前应自行审核并承担使用责任。
5. 若涉及第三方权利，应取得必要授权。

工程要求：

| 字段 | 要求 |
|---|---|
| `authorization_confirmed` | 创建任务必填 true |
| `authorization_text_version` | 必须记录条款版本 |
| `authorization_confirmed_at` | 必须记录确认时间 |
| `authorization_ip_hash` | 可选记录，需符合隐私策略 |
| `authorization_scope` | 分析/生成/导出/商业使用等范围 |

### 8.2 AIGC 标识

MVP1 必须：

1. 在结果页展示 AI 辅助生成标识。
2. 在导出报告中写入 AI 辅助生成说明。
3. 在视频 Prompt、脚本、标题、封面建议中保留标识元数据。
4. 后台记录 AIGC 标识是否应用。
5. 后续成片/图片生成阶段预留显式和隐式标识字段。

### 8.3 风险分级与拦截策略

| 风险等级 | 用户侧展示 | 是否允许导出 | 是否人工复核 | 积分策略 |
|---|---|---:|---:|---|
| R0 | 已通过 | 是 | 否 | 正常扣费 |
| R1 | 建议优化 | 是 | 否 | 正常扣费 |
| R2 | 系统将自动优化 | 暂不允许，重写后复检 | 否 | 保持冻结 |
| R3 | 需补充/编辑 | 暂不允许 | 视情况 | 保持冻结或部分返还 |
| R4 | 等待人工审核 | 否 | 是 | 暂缓扣费 |
| R5 | 已拦截 | 否 | 可申诉 | 返还或部分返还 |

### 8.4 高风险行业处理

| 行业/场景 | MVP1 处理 |
|---|---|
| 医疗健康 | 不允许生成疗效、诊断、治疗承诺；只允许常识科普且需免责声明；高风险转人工 |
| 金融投资 | 不允许收益承诺、荐股、稳赚、保本；高风险转人工或拦截 |
| 法律服务 | 不允许保证胜诉、规避执法；只允许泛化信息，不替代律师意见 |
| 教培考试 | 不允许保过、提分承诺；需谨慎表达 |
| 招商加盟 | 不允许收益承诺、夸大回报、虚假案例 |
| 医美/减肥/保健 | 严控效果承诺、前后对比、夸大宣传 |
| 房产/贷款 | 严控政策、收益、价格承诺 |
| 明星/影视/娱乐二创 | MVP1 不作为首发，默认高风险 |

### 8.5 人工复核 SOP

人工复核员必须检查：

1. 用户授权记录是否完整。
2. 源视频证据是否显示可识别人物、声音、品牌、Logo、字幕、独特剧情。
3. 输出是否复用原台词、标题、封面文案、镜头结构。
4. 输出是否含高风险行业承诺。
5. AIGC 标识是否应用。
6. 是否应放行、重写、要求用户补充、拦截、删除。
7. 是否需要返还积分或客服介入。
8. 是否需要加入 Benchmark 红队样本。

人工复核结果必须有：

1. 复核结论。
2. 复核理由。
3. 用户可见说明。
4. 后台内部备注。
5. 处理人 ID。
6. 处理时间。
7. 影响的资产/导出/积分。

### 8.6 投诉删除机制

投诉处理必须支持：

1. 权利人投诉。
2. 用户自删请求。
3. 平台/监管通知。
4. 内部风控复盘删除。
5. 数据主体个人信息删除请求。

处理动作：

| 动作 | 说明 |
|---|---|
| 冻结 | 暂停展示、复制、导出、分享 |
| 局部删除 | 删除某条脚本/标题/Prompt |
| 全量删除 | 删除整个任务输出包 |
| 资产失效 | 让导出链接、分享链接、缓存失效 |
| 审计保留 | 按安全/争议处理需要保留必要日志，具体留存期由 D18/D21 确定 |
| 用户通知 | 通知处理结果和后续建议 |

### 8.7 数据最小化与隐私

1. 风控日志只保存必要摘要和证据引用，不默认保存完整敏感原文。
2. 后台展示需要脱敏，尤其是联系方式、账号信息、投诉材料。
3. AIcoding Agent 不得访问生产用户原始视频和投诉材料。
4. 删除请求执行后，业务可见资产必须不可访问。
5. 审计日志访问需要管理员权限。

---

## 9. 测试与验收标准

### 9.1 单元测试

必须覆盖：

1. 风险等级枚举。
2. 决策引擎规则。
3. 质量分计算。
4. 相似度分计算。
5. 阈值映射。
6. AIGC 标识门禁。
7. 积分动作映射。
8. 人工复核触发条件。
9. 投诉冻结状态。
10. 权限判断。

### 9.2 Schema 测试

必须覆盖：

1. `QualityCheckReport` 合法/非法样本。
2. `SimilarityCheckReport` 合法/非法样本。
3. `ComplianceCheckReport` 合法/非法样本。
4. `RiskCheckReport` 合法/非法样本。
5. `ManualReviewTicket` 状态流转。
6. `ComplaintCase` 状态流转。

通过标准：核心 Schema 合法率 100%，非法样本必须被拒绝。

### 9.3 API 测试

必须覆盖：

1. 预检放行。
2. 预检拦截。
3. 质检通过。
4. 质检低分自动重写。
5. 相似度高转人工。
6. 合规 R5 拦截。
7. 风险报告查询。
8. 人工复核通过/拒绝。
9. 投诉提交与冻结。
10. 非本人任务不可访问风险报告。

### 9.4 Prompt 回归测试

必须覆盖：

| Prompt | 最低样本 |
|---|---:|
| P11 输入意图预检 | 50 |
| P12 质量评分 | 80 |
| P13 相似度检查 | 100 |
| P14 合规拦截 | 100 |
| P15 原创化重写 | 60 |
| P16 人工复核摘要 | 30 |

通过门槛：

1. R5 高风险召回率 ≥ 95%。
2. R4 高风险召回率 ≥ 90%。
3. 低风险误拦截率 ≤ 15%。
4. 相似度高风险样本漏放 ≤ 5%。
5. 自动重写后二次通过率 ≥ 70%。
6. JSON Schema 合法率 ≥ 98%。

### 9.5 风控红队测试

红队样本必须覆盖：

1. 明确复制请求。
2. 隐晦洗稿请求。
3. 仿脸仿声请求。
4. 未授权品牌/Logo 请求。
5. 标题党与虚假承诺。
6. 医疗疗效承诺。
7. 金融收益承诺。
8. 法律结果承诺。
9. 教培保过承诺。
10. 招商加盟收益承诺。
11. 平台规避检测请求。
12. Prompt 注入绕过风控。
13. 低清证据不足但模型自信输出。

### 9.6 E2E 测试

必须覆盖主路径：

```text
注册/登录
→ 完成画像
→ 上传视频
→ 授权确认
→ 风险预检
→ 积分冻结
→ 视频处理
→ Content DNA
→ Creative IR
→ 输出包生成
→ 质检/相似度/合规检查
→ 决策放行
→ 用户查看/复制/导出
→ 成功扣费
→ 后台风险报告可查
```

必须覆盖异常路径：

1. 预检 R5，任务不可创建，积分不冻结。
2. Content DNA 低质量，重跑或部分失败。
3. 输出包相似度 R2，自动重写后通过。
4. 输出包 R4，人工复核通过后导出。
5. 输出包 R5，拦截并返还。
6. 投诉成立，资产冻结和导出链接失效。

### 9.7 性能与稳定性验收

| 指标 | MVP1 验收线 |
|---|---:|
| 风控预检 P95 延迟 | ≤ 5 秒 |
| 输出包质检 P95 延迟 | ≤ 30 秒 |
| 相似度检查 P95 延迟 | ≤ 45 秒 |
| 合规检查 P95 延迟 | ≤ 30 秒 |
| 风控队列失败率 | ≤ 2% |
| 风控服务不可用时导出漏放 | 0 |
| 风控报告落库成功率 | ≥ 99% |

### 9.8 商业化验收

1. 风控通过后才允许正式扣费。
2. 任务失败或 R5 拦截时，按规则返还积分。
3. 自动重写不得重复扣费。
4. 人工复核期间积分状态可见。
5. 后台可按任务、用户、模型、Prompt、风险等级统计成本。
6. 可统计风控导致的返还金额、拦截次数、重写成本。

### 9.9 权限与审计验收

1. 普通用户只能查看自己的风险报告摘要。
2. 客服不能修改风险规则。
3. 人工复核员只能查看必要证据。
4. 风控管理员发布规则需要审计。
5. 删除/下架动作必须记录操作人、时间、原因。
6. AIcoding Agent 不能访问生产投诉材料。

### 9.10 上线门禁

D13 上线前必须满足：

- [ ] 核心 API 全部通过测试。
- [ ] D12 风控 Benchmark 通过。
- [ ] R5 高风险样本召回率达标。
- [ ] 风控服务不可用时导出被锁定。
- [ ] 人工复核后台可用。
- [ ] 投诉入口可用。
- [ ] AIGC 标识导出门禁生效。
- [ ] 积分返还幂等测试通过。
- [ ] 后台审计日志可查。
- [ ] Mock 风控在生产关闭。

---

## 10. AIcoding 研发任务拆分

### 10.1 任务总览

| 任务编号 | 任务名称 | 优先级 | 适合 Agent | 主要交付 |
|---|---|---:|---|---|
| D13-T01 | 风控枚举、规则版本与配置模块 | P0 | 后端 Agent | 风险等级、动作、规则配置、版本表 |
| D13-T02 | QualityCheckReport Schema 与服务 | P0 | 后端/AI Agent | 质量评分 Schema、服务、测试 |
| D13-T03 | SimilarityCheckReport Schema 与服务 | P0 | AI/后端 Agent | 文本/语义/结构相似度服务 |
| D13-T04 | ComplianceCheckReport Schema 与服务 | P0 | AI/后端 Agent | 合规风险分类、报告服务 |
| D13-T05 | RiskDecision 决策引擎 | P0 | 后端 Agent | 报告聚合、动作决策、导出门禁 |
| D13-T06 | 风控 Prompt Registry 接入 | P0 | Prompt Agent | P11-P16 Prompt、版本、Schema、测试 |
| D13-T07 | 风控任务队列与状态机 | P0 | 后端 Agent | 队列、重试、Fail Closed |
| D13-T08 | 风控 API | P0 | 后端 Agent | 预检、质检、相似度、合规、报告、重写 |
| D13-T09 | 前端风险提示与导出门禁 | P0 | 前端 Agent | 风险卡、拦截页、复核等待、AIGC 标识 |
| D13-T10 | 后台人工复核工作台 | P0 | 全栈/后台 Agent | 工单列表、详情、决策、审计 |
| D13-T11 | 投诉删除机制 | P0 | 后端/后台 Agent | ComplaintCase、冻结、删除、通知接口 |
| D13-T12 | 积分返还与补偿联动 | P0 | 后端/商业化 Agent | 风险动作与 CreditTransaction 联动 |
| D13-T13 | 风控日志、审计与看板 | P0 | 后端/数据 Agent | risk_events、指标聚合、后台统计 |
| D13-T14 | 风控测试与红队样本 | P0 | 测试 Agent | 单测、API、E2E、Prompt 回归、红队 |
| D13-T15 | 权限与数据脱敏 | P0 | 后端/安全 Agent | 权限矩阵、脱敏、生产访问限制 |
| D13-T16 | D12 Benchmark 联动 | P1 | 测试/AI Agent | 风控样本集、发布门禁、结果回写 |
| D13-T17 | 视频成片风控预留 | P2 | AI/后端 Agent | 成片相似度字段预留，不进入 MVP1 |
| D13-T18 | 交接与回写 | P0 | 交接 Agent | 更新 D00/D16/D17/D20/D21 影响清单 |

### 10.2 D13-T01 风控枚举、规则版本与配置模块

交付：

1. `RiskLevel`、`RiskAction`、`RiskCheckStage`、`ReviewStatus` 枚举。
2. `risk_rule_versions` 数据表。
3. 默认规则配置 JSON。
4. 风险阈值配置读取服务。
5. 规则版本审计。

验收：

- 规则配置可版本化。
- 每份风险报告必须绑定规则版本。
- 禁止硬编码阈值散落在业务代码。

### 10.3 D13-T02 质量评分服务

交付：

1. `QualityCheckReport` Schema。
2. Rubric 配置。
3. `QualityCheckService`。
4. PromptRun 接入。
5. 测试样本。

验收：

- 能对 Content DNA、Creative IR、脚本、标题、封面、视频 Prompt 评分。
- 低分原因可解释。
- Schema 不合法时不得通过。

### 10.4 D13-T03 相似度服务

交付：

1. 文本相似度计算。
2. 语义相似度接口。
3. 结构相似度规则。
4. 命中片段记录。
5. `SimilarityCheckReport`。

验收：

- 高相似脚本必须 R4/R5。
- 常见行业术语不得被误判为高相似。
- 输出 matched_segments 支持人工复核。

### 10.5 D13-T04 合规检查服务

交付：

1. 风险分类规则。
2. 敏感行业规则。
3. AIGC 标识门禁。
4. `ComplianceCheckReport`。
5. Prompt 接入。

验收：

- 仿脸仿声、平台规避、未授权复用必须拦截。
- 医疗/金融/法律高风险承诺必须转人工或拦截。
- 导出前必须检查 AIGC 标识。

### 10.6 D13-T05 决策引擎

交付：

1. 三类报告聚合。
2. 风险等级升级规则。
3. `RiskDecision`。
4. 导出门禁。
5. 积分动作建议。

验收：

- R5 永不放行。
- 风控服务异常时导出锁定。
- 决策结果可追溯。

### 10.7 D13-T06 Prompt Registry 接入

交付：

1. P11-P16 Prompt 模板。
2. 输入变量 Schema。
3. 输出 Schema。
4. Prompt 测试用例。
5. Prompt 版本发布记录。

验收：

- Prompt 不在业务代码里硬编码。
- 输出 JSON 合法率达标。
- Prompt 变更能触发 D12 回归。

### 10.8 D13-T09 前端风险提示与导出门禁

交付：

1. 风险提示卡。
2. 质检评分卡。
3. 相似度提示。
4. 拦截页。
5. 人工复核等待页。
6. AIGC 标识展示。
7. 导出按钮门禁。

验收：

- R4/R5 不能复制/导出。
- 风险原因用户能理解。
- 积分返还状态可见。

### 10.9 D13-T10 后台人工复核工作台

交付：

1. 工单列表。
2. 工单详情。
3. 证据引用。
4. 通过/拒绝/要求补充/删除动作。
5. 审计日志。

验收：

- 复核员无需进数据库即可判断。
- 每个动作都有审计。
- 用户可见说明与内部备注分离。

### 10.10 D13-T14 风控测试与红队样本

交付：

1. 单元测试。
2. API 测试。
3. 队列测试。
4. E2E 测试。
5. 红队样本。
6. D12 Benchmark 接入。

验收：

- R5 召回率达标。
- 计费幂等通过。
- 风控服务不可用时 Fail Closed。

---

## 11. AIcoding 执行提示词

### 11.1 总执行提示词

```text
你是本项目的工程级 AIcoding 执行 Agent。你正在实现 D13《质检评分、相似度风控与合规拦截文档》。

必须先读取并遵守：
1. D00《AIcoding 研发总控文档 / 项目上下文总索引》
2. D07《Content DNA Schema 与拆解报告标准文档》
3. D09《Creative IR Schema 与原创化重构规则文档》
4. D10《Prompt 体系、变量、版本与测试文档》
5. D11《模型路由、供应商适配与成本控制文档》
6. D12《AI 评测样本集、Benchmark 与回归测试文档》
7. 本 D13 文档

你的任务不是做 UI Demo，不是写假审核状态，不是用 mock 冒充真实风控。你必须实现可落库、可追踪、可测试、可拦截、可人工复核、可积分补偿、可后台审计的真实工程能力。

硬性禁止：
- 不得开发搬运、克隆、洗稿、仿脸、仿声能力。
- 不得绕过风险检查直接导出。
- 不得在生产默认使用 mock 风控。
- 不得跳过 ModelCallLog、RiskCheckReport、CreditTransaction、AuditLog。
- 不得自行扩大 MVP1 范围。

每次交付必须说明：
1. 读取了哪些文档和版本。
2. 完成了哪些 D13 任务。
3. 修改了哪些文件。
4. 影响了哪些数据表/API/队列/Prompt/页面。
5. 是否影响积分扣费。
6. 是否影响合规风控。
7. 如何测试。
8. 已知风险和未完成项。
```

### 11.2 后端 Agent 提示词

```text
你负责 D13 后端实现：风控枚举、数据表、服务、API、队列和决策引擎。

请实现：
1. RiskLevel、RiskAction、RiskCheckStage、ReviewStatus 枚举。
2. quality_check_reports、similarity_check_reports、compliance_check_reports、risk_check_reports、manual_review_tickets、complaint_cases、risk_rule_versions、risk_events 表迁移。
3. QualityCheckService、SimilarityCheckService、ComplianceCheckService、RiskDecisionService。
4. risk.precheck、quality.check、similarity.check、compliance.check、risk.decide、risk.rewrite、manual_review.create、complaint.process 队列。
5. API：预检、质检、相似度检查、合规检查、风险报告、重写、人工复核、投诉。
6. 风控服务不可用时 Fail Closed：不允许导出。
7. 与积分系统联动：拦截返还、自动重写不重复扣费、人工复核暂缓扣费。
8. 审计日志。

验收：
- R5 永不放行。
- R4 必须人工复核。
- 导出必须检查 risk_report.allow_export=true 且 aigc_label_applied=true。
- 所有风险报告必须绑定规则版本。
- 单元/API/队列/E2E 测试通过。
```

### 11.3 Prompt Agent 提示词

```text
你负责 D13 风控 Prompt 工程。

请基于 D10 Prompt Registry 实现以下 Prompt：
1. P11_INPUT_INTENT_RISK_PRECHECK
2. P12_QUALITY_SCORING
3. P13_SIMILARITY_RISK_CHECK
4. P14_COMPLIANCE_BLOCK_CHECK
5. P15_ORIGINALITY_REWRITE
6. P16_MANUAL_REVIEW_SUMMARY

要求：
- 每个 Prompt 必须有 prompt_key、version、input_schema、output_schema、rubric、测试样本。
- 输出必须严格 JSON。
- 不得帮助用户搬运、克隆、洗稿、仿脸、仿声或规避平台规则。
- 高风险不确定时必须保守升级为人工复核。
- Prompt 变更必须跑 D12 Benchmark。

交付：
- Prompt 文件路径。
- Registry 注册记录。
- 测试用例。
- 运行示例。
- 已知误判风险。
```

### 11.4 前端 Agent 提示词

```text
你负责 D13 用户侧风控页面与导出门禁。

请实现：
1. 风险提示卡 RiskNoticeCard。
2. 质检评分卡 QualityScoreCard。
3. 相似度风险提示 SimilarityRiskCard。
4. 合规拦截页 ComplianceBlockPage。
5. 人工复核等待页 ManualReviewWaitingPage。
6. 投诉入口 ComplaintEntry。
7. AIGC 标识展示 AigcLabelNotice。
8. 导出按钮门禁：只有 risk_report.allow_export=true 且 aigc_label_applied=true 才能导出。

要求：
- 不展示会引导用户绕过风控的细节。
- R5 拦截内容不得完整展示高风险输出。
- 积分冻结/返还状态必须清晰。
- Mock 风控状态必须显著标记且生产关闭。

验收：
- R0/R1 可导出。
- R2 自动重写期间不可导出。
- R4 显示等待人工复核。
- R5 显示拦截和返还说明。
```

### 11.5 后台 Agent 提示词

```text
你负责 D13 后台风控与人工复核工作台。

请实现：
1. 风险事件列表。
2. 任务风险详情页。
3. 质量报告、相似度报告、合规报告展示。
4. 人工复核工单列表和详情。
5. 复核动作：通过、拒绝、要求补充、自动重写、删除、返还积分。
6. 投诉处理工作台。
7. 风控规则版本查看。
8. 审计日志。

权限要求：
- 客服只能看用户可见信息和积分状态。
- 复核员可处理工单。
- 风控管理员可管理规则。
- 规则发布需要审计。

验收：
- 不进数据库即可完成复核。
- 所有操作有 audit_logs。
- 用户可见消息和内部备注分离。
```

### 11.6 测试 Agent 提示词

```text
你负责 D13 测试与验收。

请补充：
1. 风险枚举和决策引擎单元测试。
2. QualityCheckReport / SimilarityCheckReport / ComplianceCheckReport / RiskCheckReport Schema 测试。
3. 风控 API 测试。
4. 队列重试和失败测试。
5. Prompt JSON 输出测试。
6. 红队样本测试。
7. E2E 主路径与异常路径。
8. 积分返还幂等测试。
9. 权限和审计测试。
10. 风控服务不可用 Fail Closed 测试。

验收门槛：
- R5 召回率达标。
- R4 不自动导出。
- 风控服务失败时导出锁定。
- 自动重写不重复扣费。
- Mock 不进入生产路径。

输出：
- 测试文件路径。
- 测试命令。
- 覆盖率摘要。
- 未覆盖风险。
```

### 11.7 DevOps Agent 提示词

```text
你负责 D13 风控服务部署、监控和告警。

请实现：
1. 风控队列监控。
2. 风控模型调用失败率监控。
3. R4/R5 风险事件告警。
4. 人工复核超时告警。
5. 投诉未处理告警。
6. 风控服务不可用时导出门禁告警。
7. 成本异常告警。

部署要求：
- 风控服务不可用时系统 Fail Closed。
- 生产环境禁用 mock provider。
- 风控规则发布需要版本记录和回滚机制。

交付：
- 环境变量说明。
- 监控指标清单。
- 告警规则。
- 回滚方案。
```

---

## 12. 不做事项

D13 明确不做：

1. 不做完整法律意见书。
2. 不替代 D21 的正式合规条款。
3. 不做自动发布平台审核。
4. 不做完整成片版权指纹库比对。
5. 不做全网相似视频检索。
6. 不做对标账号全量内容风险画像，MVP2 后再扩展。
7. 不做仿脸、仿声、身份克隆风控后的“安全版本”生成；MVP1 直接禁止。
8. 不做帮助用户规避平台审核、搬运检测、AIGC 检测的任何能力。
9. 不做用户绕过 AIGC 标识的导出能力，是否允许用户隐藏标识由 D21 决定。
10. 不做人工复核外包流程细节，由 D22/D23 运营 SOP 负责。
11. 不做所有行业的深度专业审核规则，MVP1 只做高风险行业基础门禁。
12. 不做复杂版权赔付流程，投诉和补偿由 D20/D22/D23 细化。
13. 不做黑盒评分，所有拦截必须可解释、可追溯。
14. 不用 mock 风控冒充真实能力。

---

## 13. 待确认问题

| 编号 | 问题 | 影响 | 建议负责人 |
|---|---|---|---|
| Q01 | D00 文档编号是否正式更新为 D13=质检风控，D14=数据诊断或其他新序列 | 影响后续文档引用 | 项目 Owner |
| Q02 | MVP1 是否允许用户手动关闭导出报告中的 AIGC 标识 | 影响合规与前端导出 | 法务 / D21 |
| Q03 | R5 拦截后是否全额返还积分，还是按已产生模型成本部分扣费 | 影响 D20 商业化 | 商业化负责人 |
| Q04 | 人工复核 SLA 是 24h、48h 还是工作日制 | 影响后台与客服承诺 | 运营负责人 |
| Q05 | 哪些行业在 MVP1 默认禁止，哪些允许转人工复核 | 影响行业配置 | 产品 / 合规 |
| Q06 | 用户上传“已授权素材证明”的格式和验证标准是什么 | 影响授权表和人工复核 | 合规 / 后端 |
| Q07 | 相似度阈值是否按行业/内容类型动态调整 | 影响风控误判率 | AI 质量负责人 |
| Q08 | 是否接入第三方版权/商标/Logo 检测服务 | 影响成本和准确率 | 技术架构负责人 |
| Q09 | 投诉成立后是否删除所有关联 Prompt/报告，还是仅冻结导出 | 影响数据留存 | D18/D21 |
| Q10 | 内部人员能查看多少原始视频和投诉材料 | 影响权限与隐私 | 安全负责人 |
| Q11 | 自动重写最多几次，失败后是否返还全部积分 | 影响成本控制 | 商业化 / AI 工程 |
| Q12 | 是否为企业客户提供白名单风控策略 | 影响后台配置和责任边界 | 企业业务负责人 |
| Q13 | 是否记录用户手动编辑后的再次风控版本 | 影响版本管理 | 后端 / D16 |
| Q14 | 是否对导出文件加入隐式元数据标识 | 影响导出模块 | D21 / 技术负责人 |
| Q15 | 高风险投诉是否触发账号风控或封禁 | 影响用户系统 | 运营 / 合规 |

---

## 14. 最终 Checklist

### 14.1 产品与范围 Checklist

- [ ] D13 明确服务 MVP1「视频拆解 + 原创脚本生成」。
- [ ] 不把产品做成搬运、克隆、洗稿、仿脸、仿声工具。
- [ ] 明确 D13 是质量与风险总闸门。
- [ ] 明确 R0-R5 风险等级和动作。
- [ ] 明确 P0 风控门禁。
- [ ] 明确不做事项。

### 14.2 AI 能力 Checklist

- [ ] P11-P16 Prompt 已进入 Prompt Registry。
- [ ] 每个 Prompt 有输入 Schema 和输出 Schema。
- [ ] 质检评分 Rubric 可执行。
- [ ] 相似度维度和阈值明确。
- [ ] 合规风险分类明确。
- [ ] 自动重写策略明确。
- [ ] Prompt 变更需跑 D12 Benchmark。

### 14.3 工程 Checklist

- [ ] `quality_check_reports` 表已设计。
- [ ] `similarity_check_reports` 表已设计。
- [ ] `compliance_check_reports` 表已设计。
- [ ] `risk_check_reports` 表已设计。
- [ ] `manual_review_tickets` 表已设计。
- [ ] `complaint_cases` 表已设计。
- [ ] 风控 API 已定义。
- [ ] 风控队列已定义。
- [ ] 风控状态机已定义。
- [ ] Fail Closed 策略已定义。

### 14.4 商业化 Checklist

- [ ] 风控通过后才正式扣费。
- [ ] R5 拦截返还规则明确。
- [ ] 自动重写不重复扣费。
- [ ] 人工复核期间积分冻结状态可见。
- [ ] 后台可统计风控成本、返还、重写成本。
- [ ] 投诉删除的补偿入口已预留。

### 14.5 合规 Checklist

- [ ] 授权确认已进入任务创建前置。
- [ ] AIGC 标识已进入导出门禁。
- [ ] 明确仿脸仿声、未授权复用、平台规避为 R5。
- [ ] 高风险行业策略明确。
- [ ] 人工复核触发条件明确。
- [ ] 投诉删除流程明确。
- [ ] 审计日志和权限要求明确。

### 14.6 测试与验收 Checklist

- [ ] 单元测试覆盖决策引擎。
- [ ] Schema 测试覆盖所有报告。
- [ ] API 测试覆盖主路径和异常。
- [ ] E2E 测试覆盖放行、重写、人工复核、拦截、投诉。
- [ ] 红队样本覆盖高风险场景。
- [ ] R5 高风险召回率达到门槛。
- [ ] 风控服务不可用时导出锁定。
- [ ] 生产环境禁用 mock 风控。

### 14.7 AIcoding 交付 Checklist

- [ ] 每个任务说明读取了 D00 和 D13。
- [ ] 每个任务说明影响的数据表、API、队列、Prompt、页面。
- [ ] 每个任务说明是否影响积分和合规。
- [ ] 每个任务给出测试命令。
- [ ] 每个任务列出已知风险。
- [ ] 完成后回写 D00/D16/D17/D20/D21/D24 影响项。

---

# 附录 A：D13 最小落地顺序

```text
第 1 步：实现枚举、规则版本和数据表
第 2 步：实现预检 API 和授权门禁
第 3 步：实现 QualityCheckReport 与评分服务
第 4 步：实现 SimilarityCheckReport 与相似度服务
第 5 步：实现 ComplianceCheckReport 与合规检查服务
第 6 步：实现 RiskDecisionService 和导出门禁
第 7 步：接入 P11-P16 Prompt Registry
第 8 步：接入风控队列和 Fail Closed
第 9 步：实现前端风险提示和导出锁定
第 10 步：实现后台人工复核和投诉处理
第 11 步：接入积分返还与成本统计
第 12 步：补齐测试、红队样本和 D12 Benchmark
第 13 步：上线前验收与 D24 交接
```

# 附录 B：MVP1 推荐默认阈值

| 配置项 | 默认值 | 是否可后台配置 |
|---|---:|---:|
| 自动放行最低质量分 | 80 | 是 |
| 自动重写触发相似度 | 40 | 是 |
| 人工复核触发相似度 | 75 | 是 |
| 强制拦截相似度 | 90 | 是 |
| Content DNA 最低置信度 | 0.70 | 是 |
| Creative IR 最低原创化分 | 75 | 是 |
| R5 高风险召回率发布门槛 | 95% | 是 |
| 自动重写最大次数 | 2 | 是 |
| 人工复核普通 SLA | 48h | 是 |
| 风控服务故障导出策略 | Fail Closed | 否 |

# 附录 C：风险报告用户侧文案示例

低风险：

```text
该内容已通过基础质检和原创化检查。请在发布前结合你的实际业务信息进行人工确认，并保留 AI 辅助生成标识。
```

自动重写：

```text
系统检测到部分表达与参考视频较接近，正在为你进行原创化重写。该过程不会额外扣除积分。
```

人工复核：

```text
该任务涉及较高相似度或授权风险，已进入人工复核。复核完成前暂不可导出，积分将保持冻结状态。
```

拦截返还：

```text
该请求涉及复制、克隆、未授权复用或其他高风险内容，系统无法继续生成。相关冻结积分将按规则返还。
```

投诉冻结：

```text
该内容因收到投诉或复核要求，已暂时冻结访问。处理完成后我们将通知你结果。
```
