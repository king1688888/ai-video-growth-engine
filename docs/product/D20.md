# D20｜测试、验收、发布、监控与告警文档

> 文档编号：D20  
> 文档名称：测试、验收、发布、监控与告警文档  
> 文档版本：V1.0  
> 生成日期：2026-05-25  
> 根上下文：D00《AIcoding 研发总控文档 / 项目上下文总索引》  
> 当前主线版本：MVP1「视频拆解 + 原创脚本生成」  
> 文档性质：测试体系 + 验收门禁 + 发布流程 + 监控观测 + 告警响应 + 事故复盘 + AIcoding 执行文档  
> 核心定位：D20 不是泛泛的 QA 建议，而是把 MVP1 从“模块可运行”推进到“可测试、可验收、可发布、可监控、可告警、可回滚、可持续运维”的商业级上线门禁系统。

---

## 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 文档编号 | D20 |
| 文档名称 | 测试、验收、发布、监控与告警文档 |
| 所属项目 | AI 短视频爆款增长引擎 |
| 当前版本 | MVP1：视频拆解 + 原创脚本生成 |
| 文档级别 | S+，商业级上线前必须完成的测试发布与运维门禁文档 |
| 上级根文档 | D00《AIcoding 研发总控文档 / 项目上下文总索引》 |
| 直接前置依赖 | D00、D03、D04、D05、D06、D07、D08、D09、D10、D11、D12、D13、D15、D16、D17、D18、D19 |
| 直接后置依赖 | D21 合规、授权、AIGC 标识与内容审核文档；D22 运营后台、客服、退款补偿与人工复核 SOP 文档；D23 AIcoding 执行规约；D24 最终交付、交接、迭代维护与版本治理文档 |
| 适用阶段 | MVP1 自动化研发、模块联调、集成测试、内测发布、商业化上线、生产监控、事故响应、版本回滚、交接维护 |
| 适用对象 | 创始人、产品负责人、AI 产品经理、技术产品经理、测试 Agent、后端 Agent、前端 Agent、AI/Prompt Agent、DevOps Agent、运营后台 Agent、风控合规负责人、客服负责人、项目 Owner |
| 文档目标 | 固化测试分层、验收标准、发布门禁、CI/CD、灰度发布、监控指标、告警规则、事故处理、回滚策略、上线 Checklist 与 AIcoding 执行任务 |
| 最高约束 | 与 D00 冲突时，以 D00 为准；与 D16 数据库/API 冲突时，以 D16 为工程契约；与 D17 任务状态冲突时，以 D17 为状态机口径；与 D19 计费口径冲突时，以 D19 为商业化最终口径；与 D21/D22 合规安全冲突时，以更严格规则为准 |
| 输出形态 | Markdown 文档 + 测试矩阵 + 发布门禁 + 数据结构草案 + API 草案 + 队列草案 + Prompt 模板 + 监控指标 + 告警规则 + AIcoding 任务拆分 |
| 维护责任人 | 项目 Owner / AI 产品总控 / 测试负责人 / DevOps 负责人 / 技术架构负责人 |
| 更新触发条件 | MVP 范围变化、核心 API 变化、任务状态变化、Prompt/模型变化、风控规则变化、积分规则变化、发布失败、线上事故、成本异常、投诉事件、监控盲区暴露 |
| 文档状态 | V1.0 初版可执行；后续需随 D21-D24、真实环境、真实监控数据和线上事故持续回写 |

### 1.1 编号兼容说明

当前项目历史文档存在编号演进：D00 V1.0 的文档地图中曾将「测试、验收、上线发布与监控告警文档」标为 D19，将「商业化系统、套餐积分与成本结算文档」标为 D20；但当前项目已形成 D19《商业化系统、套餐积分、订单与成本结算文档》，本轮任务明确要求生成 **D20｜测试、验收、发布、监控与告警文档**。

为避免 AIcoding 多 Agent 混乱，工程实现必须使用稳定别名，不得将文档编号硬编码为业务判断逻辑：

| 字段 | 建议值 |
|---|---|
| `doc_code` | `D20` |
| `doc_alias` | `TEST_ACCEPTANCE_RELEASE_MONITORING_ALERTING` |
| `legacy_or_alt_doc_code` | `D19_TEST_RELEASE_MONITORING` |
| `commercial_doc_alias` | `COMMERCIAL_BILLING_CREDIT_ORDER_COST_SETTLEMENT` |
| `conflict_resolution` | 文件命名和本轮交付按 D20 执行；若后续 D00 回写调整编号，代码、数据库、API 仍以 `doc_alias` 作为稳定引用 |

### 1.2 本文档的边界声明

D20 负责定义 MVP1 的测试、验收、发布、监控、告警、回滚与事故处理体系。D20 不替代 D04 总 PRD、D16 数据库/API、D17 队列状态、D19 商业化计费、D21 合规法务、D22 运营客服 SOP，但 D20 是所有这些能力能否进入内测、预发、生产和商业化上线的统一门禁。

D20 的判断原则是：

```text
没有测试证据，不算完成。
没有验收门禁，不准上线。
没有监控告警，不准商业化放量。
没有回滚预案，不准生产发布。
没有事故复盘，不准继续迭代同类风险。
```

---

## 2. 文档目标

### 2.1 本文档要解决的问题

D20 要解决的是 MVP1 从“功能能跑”到“商业级可上线”的最后一公里问题。本项目是 AI 视频理解 + Prompt 生成 + 风控质检 + 异步任务 + 积分扣费 + 文件资产 + 后台运营的复合型系统，不能只依赖人工点几遍页面判断是否可上线。

D20 必须解决以下 12 个问题：

1. **测试如何分层**：单元测试、API 契约测试、集成测试、队列测试、AI Benchmark、Prompt 回归、风控红队、计费幂等、权限安全、E2E、性能压测、生产 Smoke 如何组合。
2. **验收如何门禁化**：模块验收、集成验收、上线验收、商业化验收、合规验收、运营验收分别由谁签署、用什么证据通过。
3. **AI 输出如何可验证**：Content DNA、Creative IR、脚本、标题、封面建议、视频 Prompt、质检风控必须有 Schema、Prompt 版本、样本集、评分、失败回退和上线门槛。
4. **发布如何可控**：分支策略、CI/CD、数据库迁移、环境变量、Feature Flag、预发验证、灰度发布、生产发布、回滚策略必须明确。
5. **长任务如何可观测**：视频处理、ASR/OCR、多模态理解、Content DNA、Creative IR、输出包、风控、导出、积分扣费的每一步都要可追踪。
6. **商业化如何防错**：报价、冻结、扣费、返还、成本、毛利、退款、人工补偿必须被测试和监控，不能出现重复扣费、失败不返还、成本不可见。
7. **合规如何上线前拦截**：授权确认、AIGC 标识、风险分级、拦截策略、人工复核、投诉删除、日志审计必须进入发布门禁。
8. **监控指标如何定义**：业务指标、AI 质量指标、任务稳定性、模型成本、队列积压、错误率、延迟、风控命中、投诉删除、积分异常都要有指标。
9. **告警如何分级响应**：P0/P1/P2/P3 告警的触发条件、通知对象、响应动作、升级机制和恢复标准必须明确。
10. **事故如何处理**：线上故障、模型供应商异常、成本暴涨、风控漏拦、支付回调异常、数据泄露疑似事件必须有 Runbook。
11. **AIcoding 如何执行**：测试、发布、监控能力必须拆成可由 Manus / Codex / Claude Code / Hermes Agent 执行的任务，而不是让 Agent 自己猜。
12. **什么情况下不准上线**：任何跳过风控、无测试证据、无成本日志、无回滚、无人工复核入口、无投诉删除机制的版本都不得发布。

### 2.2 本文档不解决的问题

| 不在 D20 完整展开的内容 | 负责文档 | D20 只提供 |
|---|---|---|
| 产品战略、商业定位、用户价值 | D01 | 发布验收必须遵守的定位与不做边界 |
| MVP 范围和版本路线 | D03 | MVP1 P0/P1/P2 的验收门禁和不准扩展规则 |
| 总 PRD 与用户故事 | D04 | 将需求转换为测试用例与上线验收证据 |
| 模块拆分和研发边界 | D05 | 模块级验收、交接标准和测试覆盖要求 |
| 页面原型与交互细节 | D06 | 页面测试、E2E 路径、状态验收 |
| Content DNA / Creative IR 字段全集 | D07/D09 | AI Schema 测试、输出质量门槛和回归规则 |
| 视频处理和多模态理解 | D08 | 视频处理链路测试、性能与失败降级验收 |
| Prompt 体系和模型路由 | D10/D11 | Prompt 回归、模型调用观测、成本异常告警 |
| AI Benchmark 样本集 | D12 | 将 Benchmark 纳入 CI/CD 和发布门禁 |
| 质检相似度风控 | D13 | 风控红队测试、拦截验收、线上风险监控 |
| 技术架构 | D15 | 测试、部署、监控架构落地方式 |
| 数据库/API | D16 | 测试数据、API 契约、质量门禁数据表草案 |
| 队列/文件生命周期 | D17 | 队列状态、资产状态、清理、重试、补偿的测试和监控 |
| 安全隐私与权限 | D18 | 安全测试、权限测试、数据删除与审计监控 |
| 商业化系统 | D19 | 计费测试、成本监控、毛利告警、失败返还验收 |
| 合规与运营 SOP | D21/D22 | 合规验收与运营响应入口，不替代法务和客服 SOP |

### 2.3 本文档的研发落点

D20 最终必须落到以下工程对象：

1. 测试目录与测试命令：`tests/unit`、`tests/contract`、`tests/integration`、`tests/e2e`、`tests/ai-regression`、`tests/security`、`tests/billing`、`tests/load`。
2. CI/CD Pipeline：lint、typecheck、unit、contract、integration、AI Benchmark、E2E、security scan、migration dry-run、build、deploy staging、smoke、manual approval、production deploy、post-deploy check。
3. 发布门禁配置：`release_gates`、`quality_gate_reports`、`benchmark_runs`、`risk_redteam_runs`、`billing_reconciliation_runs`。
4. 监控指标：API、队列、任务、AI 调用、模型成本、积分交易、风控、导出、对象存储、数据库、前端错误、后台操作。
5. 告警规则：P0/P1/P2/P3 分级、阈值、通知对象、升级路径和自动动作。
6. 后台页面：测试运行记录、发布版本、质量门禁、监控看板、告警中心、事故记录、回滚记录。
7. Runbook：模型供应商故障、队列积压、支付回调异常、重复扣费、成本暴涨、风险漏拦、投诉删除失败、数据库迁移失败、生产回滚。
8. AIcoding 任务拆分与提示词：测试 Agent、DevOps Agent、后端 Agent、前端 Agent、AI/Prompt Agent、风控 Agent、商业化 Agent 可直接执行。

---

## 3. 与 D00 及其他文档的关系

### 3.1 D00 对 D20 的硬约束

| D00 约束 | D20 落地方式 |
|---|---|
| 产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具 | 发布门禁必须包含合规风控测试，任何出现“复制、仿脸、仿声、洗稿、高相似复用”的输出都阻断发布 |
| 当前优先 MVP1：视频拆解 + 原创脚本生成 | D20 的 P0 测试、验收、监控只围绕单条视频上传、处理、拆解、原创脚本/标题/封面/视频 Prompt、风控、导出、积分、后台展开 |
| 不得自行扩大第一版范围 | 发布前检查 Feature Flag，确保账号级抓取、自动发布、完整视频生成、批量搬运等后置能力不进入 MVP1 生产路径 |
| 不得跳过风控、任务日志、成本记录、测试验收 | D20 将风控报告、任务日志、模型成本、积分流水、自动化测试作为上线硬门禁 |
| 不得用 mock 冒充真实能力 | 测试环境可用 mock，但预发/生产必须明确 mock 标识，生产默认关闭；任何 mock 输出不得作为商业化真实能力上线 |
| 每个模块必须可独立开发、独立测试、独立验收、独立交接 | D20 定义模块级 DoD、集成级 DoD、发布级 DoD、交接证据包 |
| 每次交付必须说明读取文档、完成内容、修改文件、测试结果、风险 | D20 的 AIcoding 交付模板强制要求 Agent 输出测试命令、覆盖范围、失败项、风险和回滚建议 |

### 3.2 D20 在文档体系中的位置

```text
D00 项目总控
  → D04 总 PRD / D05 模块边界 / D15 技术架构
    → D16 数据库/API / D17 队列文件 / D18 安全 / D19 商业化
      → D20 测试验收发布监控告警
        → D21 合规终审
        → D22 运营后台与人工复核 SOP
        → D24 最终交付与交接
```

D20 是上线门禁文档。它不重新定义业务，但决定每个业务模块是否具备上线资格。

### 3.3 D20 对其他文档的输出

| 输出对象 | D20 输出内容 |
|---|---|
| D16 数据库/API | 质量门禁、发布版本、测试运行、告警事件、事故记录、健康检查 API 草案 |
| D17 队列文件 | 队列监控、任务超时、死信队列、资产清理、补偿任务的测试与告警 |
| D18 安全权限 | 权限测试、审计日志、敏感日志脱敏、数据删除监控、密钥泄露告警 |
| D19 商业化 | 计费幂等测试、成本告警、毛利异常、失败返还验收、支付回调监控 |
| D21 合规 | 合规测试、授权确认验收、AIGC 标识检测、投诉删除流程验证 |
| D22 运营后台 | 告警中心、事故工单、人工复核、客服补偿、投诉处理的测试证据 |
| D24 最终交接 | 发布记录、测试报告、已知问题、监控入口、告警规则、Runbook、回滚方案 |

---

## 4. 核心结论

### 4.1 MVP1 上线必须通过九类门禁

MVP1 进入内测、商业化灰度或正式生产前，必须通过以下 9 类门禁：

| 门禁 | 是否 P0 必须 | 通过标准 |
|---|---:|---|
| 范围门禁 | 是 | 所有 P0 已完成，P1/P2 默认关闭或 Feature Flag 隔离，不出现 D00 禁止能力 |
| 数据/API 门禁 | 是 | D16 核心 API 契约测试通过，数据库迁移可回滚，错误码一致 |
| 异步任务门禁 | 是 | D17 主任务 DAG 可完成、可重试、可补偿、可追踪、不会重复扣费 |
| AI 质量门禁 | 是 | D12 Benchmark 通过，核心 Prompt 输出 Schema 合法率、字段完整率、质量评分达标 |
| 风控合规门禁 | 是 | D13/D21 风险用例通过，高风险样本拦截或转人工，投诉删除链路可执行 |
| 商业化门禁 | 是 | D19 报价、冻结、扣费、返还、成本、毛利、补偿测试通过 |
| 安全权限门禁 | 是 | D18 权限、审计、脱敏、密钥、数据删除、后台访问测试通过 |
| E2E 与体验门禁 | 是 | 用户主流程、后台主流程、失败流程、积分不足、风控拦截、导出流程全部通过 |
| 监控告警门禁 | 是 | P0/P1 指标可观测，核心告警可触发，生产 Smoke 和回滚流程可执行 |

### 4.2 测试不是单次动作，而是持续门禁

D20 要求测试贯穿以下阶段：

```text
需求冻结
→ 模块开发
→ PR 合并
→ 集成联调
→ 预发环境
→ 内测验收
→ 商业化灰度
→ 生产发布
→ 发布后监控
→ 事故复盘
→ 回归测试补充
```

任何线上事故、投诉、成本异常、风控漏拦、积分争议，都必须反向补充测试用例和监控规则。

### 4.3 AI 能力必须纳入发布门禁

AI 输出不能只靠人工主观验收。MVP1 的 AI 能力必须至少满足：

| 能力 | 最低上线要求 |
|---|---|
| 视频处理证据包 | 样本视频处理成功率达标；ASR/OCR/抽帧/镜头切分有可追溯证据 |
| Content DNA | JSON Schema 合法；字段完整；事实依据可追溯；不可复用内容有标记 |
| Creative IR | 原创化强度达标；不直接复用原台词、镜头、人物、商标、素材 |
| 脚本/标题/封面/视频 Prompt | 适配用户画像和行业；输出可读、可用、合规；不诱导搬运或仿冒 |
| 质检风控 | 高风险样本必须拦截或转人工；中风险样本必须提示或重构 |
| 失败回退 | Prompt 解析失败、模型超时、低质量输出必须可重试、降级、返还或人工复核 |

### 4.4 发布后必须监控四条生命线

MVP1 生产监控至少围绕四条生命线：

1. **任务生命线**：任务创建率、完成率、失败率、超时率、平均耗时、队列积压、死信数量。
2. **AI 质量生命线**：Schema 合法率、Benchmark 趋势、JSON repair 比例、风控命中、低质量输出、用户差评。
3. **商业化生命线**：报价成功率、冻结成功率、扣费成功率、返还成功率、重复扣费、模型成本、任务毛利、成本异常。
4. **合规安全生命线**：授权缺失、AIGC 标识缺失、高风险放行、人工复核积压、投诉删除失败、越权访问、敏感日志。

---

## 5. 详细设计

### 5.1 测试体系总览

| 测试层 | 测试对象 | 触发时机 | 是否阻断发布 | 核心产物 |
|---|---|---|---:|---|
| L1 单元测试 | 纯函数、工具类、服务方法、Schema 校验 | 每次 PR | 是 | 单元测试报告 |
| L2 类型与静态检查 | TypeScript/Python 类型、lint、格式 | 每次 PR | 是 | 静态检查报告 |
| L3 API 契约测试 | OpenAPI、请求响应、错误码、权限、幂等 | 每次 PR / 预发 | 是 | API 合同测试报告 |
| L4 数据库迁移测试 | migration、索引、约束、回滚、种子数据 | 每次 DB 变更 | 是 | Migration dry-run 报告 |
| L5 集成测试 | 后端、Worker、对象存储、Redis/队列、模型路由 | 每次合并主分支 | 是 | 集成测试报告 |
| L6 队列与长任务测试 | D17 主 DAG、重试、超时、死信、补偿 | 每次任务链路变更 | 是 | 队列测试报告 |
| L7 AI Benchmark / Prompt 回归 | D07-D13 AI 输出、Schema、质量、风控 | Prompt/模型/Schema 变化 | 是 | BenchmarkRun 报告 |
| L8 计费幂等测试 | 报价、冻结、扣费、返还、订单、Webhook | 商业化相关变更 | 是 | 账务一致性报告 |
| L9 安全权限测试 | RBAC/ABAC、文件签名、后台权限、脱敏 | 每次权限/后台变更 | 是 | 安全测试报告 |
| L10 E2E 测试 | 用户主流程、后台流程、失败流程 | 预发 / 发布前 | 是 | E2E 报告 |
| L11 性能与容量测试 | API、上传、队列、AI 调用并发、导出 | 内测前 / 放量前 | 条件阻断 | 压测报告 |
| L12 生产 Smoke 测试 | 健康检查、关键 API、样例任务、监控告警 | 每次生产发布后 | 是 | Smoke 结果 |

### 5.2 MVP1 必测主流程

MVP1 必测主流程如下：

```text
注册/登录
→ 用户画像 Onboarding
→ 上传视频或输入合规链接
→ 授权确认
→ 任务报价
→ 积分冻结
→ 视频处理
→ 多模态理解
→ Content DNA
→ Creative IR
→ 原创脚本 / 标题 / 封面建议 / 视频 Prompt
→ 质检风控
→ 结果页展示
→ 导出报告
→ 成功扣费
→ 历史记录
→ 后台查看任务、成本、风控、积分流水
```

每个主流程必须覆盖以下状态：

1. 正常成功。
2. 积分不足。
3. 视频格式不支持。
4. 用户未确认授权。
5. 视频处理失败。
6. 模型调用超时。
7. Prompt 输出 JSON 解析失败。
8. 风控中风险提示。
9. 风控高风险强制重构。
10. 极高风险拦截并进入人工复核。
11. 任务取消。
12. 失败返还积分。
13. 导出失败重试。
14. 投诉删除。
15. 后台人工补偿。

### 5.3 环境设计

| 环境 | 用途 | 数据策略 | 发布权限 | 备注 |
|---|---|---|---|---|
| local | 本地开发和单元测试 | 假数据、脱敏样本、mock 模型可用 | 开发 Agent | 不可连接生产密钥 |
| dev | 多 Agent 联调 | 测试数据、mock/真实模型可切换 | 研发负责人 | 必须标记 mock 状态 |
| staging | 预发验收 | 脱敏样本、真实队列、真实对象存储、可控模型调用 | 测试/DevOps/Owner | 与生产配置尽量一致 |
| production | 生产服务 | 真实用户数据，严格权限、审计和备份 | 项目 Owner / DevOps | 禁止测试脏数据和 mock 默认路径 |
| benchmark | AI 回归测试环境 | 固定样本集、固定 Golden Output、可回放 | AI 质量负责人 | 不得污染生产数据 |

### 5.4 测试数据与样本管理

测试数据必须按用途分级：

| 数据类型 | 来源 | 用途 | 规则 |
|---|---|---|---|
| Synthetic Fixtures | 人工构造 | 单元/API/边界测试 | 可提交仓库，不包含真实隐私 |
| Public Safe Samples | 合规公开样本或自制样本 | 视频处理和 AI Benchmark | 必须记录授权和来源 |
| Red Team Samples | 风控红队样本 | 相似度、仿脸仿声、侵权风险测试 | 只能在受控环境，不能用于展示 |
| Billing Fixtures | 构造订单和积分流水 | 计费幂等与退款测试 | 不连接真实支付扣款 |
| Production Shadow Data | 生产匿名聚合数据 | 监控趋势和容量估算 | 只允许脱敏聚合，不允许导出原始素材 |

禁止将真实用户上传视频、原始 Prompt、支付信息、手机号、邮箱、第三方密钥写入测试仓库或日志。

### 5.5 发布流程

MVP1 发布流程必须按以下顺序执行：

```text
1. Scope Freeze：确认本次版本范围，不新增 P1/P2 能力
2. Code Freeze：冻结代码，除修复 Blocker 外不再合并功能 PR
3. CI Full Run：完整执行静态检查、单测、契约、集成、Benchmark、E2E
4. Migration Dry-run：预发数据库迁移和回滚演练
5. Staging Deploy：部署预发环境
6. Staging Smoke：执行预发健康检查和样例任务
7. UAT Acceptance：产品、测试、风控、商业化、运营验收
8. Release Review：检查发布门禁和风险清单
9. Production Deploy：按灰度策略发布生产
10. Production Smoke：生产健康检查和最小样例任务
11. Monitoring Hold：发布后观察 30-120 分钟
12. Release Complete：确认无 P0/P1 异常后标记完成
13. Post-release Report：输出发布报告、遗留问题和回归补充项
```

### 5.6 发布策略

| 发布类型 | 适用场景 | 策略 | 回滚要求 |
|---|---|---|---|
| Patch | 小修复、文案、非核心逻辑 | 快速预发 + Smoke + 生产发布 | 必须能回滚到上一构建 |
| Minor | 新增 P0 子能力或重要优化 | 全量回归 + 灰度 | 必须有 Feature Flag 和 DB 回滚方案 |
| Major | 核心链路或模型策略变化 | 全量门禁 + 人工审批 + 小流量灰度 | 必须提供回滚、降级、暂停任务策略 |
| Hotfix | 线上 P0/P1 故障修复 | 只修故障点 + 必要测试 + Owner 审批 | 必须记录例外和事后补测试 |

### 5.7 监控架构

监控架构分为 6 层：

```text
前端埋点与错误上报
→ API 日志与指标
→ Worker / 队列指标
→ AI Prompt / ModelCall 指标
→ 商业化与风控业务指标
→ 告警、事故、复盘与回归补充
```

所有关键链路必须带统一追踪字段：

```text
trace_id
request_id
user_id_hash
org_id
task_id
job_id
asset_id
prompt_run_id
model_call_id
credit_hold_id
release_id
```

严禁在日志中记录原始视频内容、完整 ASR 文本、用户隐私、支付密钥、模型 API Key、Webhook Secret、未脱敏的个人信息。

---

## 6. 数据结构 / API / Prompt / 流程 / 页面 / 状态

### 6.1 核心数据结构草案

> 最终数据库字段以 D16 为准，D20 仅定义测试发布监控域的工程需求草案。

#### 6.1.1 `test_cases`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | string | 是 | 测试用例 ID，例如 `TC-E2E-001` |
| `suite_id` | string | 是 | 所属测试套件 |
| `module_code` | string | 是 | 关联模块，例如 `VIDEO_PROCESSING`、`BILLING` |
| `doc_alias` | string | 否 | 关联文档稳定别名 |
| `test_type` | enum | 是 | `unit` / `contract` / `integration` / `ai_regression` / `e2e` / `security` / `billing` / `load` |
| `priority` | enum | 是 | `P0` / `P1` / `P2` |
| `title` | string | 是 | 用例标题 |
| `preconditions` | jsonb | 否 | 前置条件 |
| `input_fixture_ref` | string | 否 | 测试数据引用 |
| `expected_result` | jsonb | 是 | 期望结果 |
| `automation_level` | enum | 是 | `automated` / `semi_automated` / `manual` |
| `owner_role` | string | 是 | 负责人角色 |
| `status` | enum | 是 | `active` / `deprecated` / `blocked` |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

#### 6.1.2 `test_runs`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | string | 是 | 测试运行 ID |
| `suite_id` | string | 是 | 测试套件 ID |
| `environment` | enum | 是 | `local` / `dev` / `staging` / `production_smoke` |
| `git_sha` | string | 是 | 代码版本 |
| `branch` | string | 是 | 分支 |
| `trigger_type` | enum | 是 | `pr` / `merge` / `scheduled` / `manual` / `release_gate` |
| `status` | enum | 是 | `running` / `passed` / `failed` / `blocked` / `cancelled` |
| `total_count` | int | 是 | 总用例数 |
| `passed_count` | int | 是 | 通过数 |
| `failed_count` | int | 是 | 失败数 |
| `blocked_count` | int | 是 | 阻塞数 |
| `duration_ms` | int | 是 | 耗时 |
| `report_url` | string | 否 | 测试报告链接 |
| `failure_summary` | jsonb | 否 | 失败摘要 |
| `started_at` | datetime | 是 | 开始时间 |
| `finished_at` | datetime | 否 | 结束时间 |

#### 6.1.3 `release_versions`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | string | 是 | 发布版本 ID |
| `version` | string | 是 | 版本号，例如 `v0.1.0-mvp1` |
| `release_type` | enum | 是 | `patch` / `minor` / `major` / `hotfix` |
| `environment` | enum | 是 | 目标环境 |
| `git_sha` | string | 是 | 发布代码版本 |
| `status` | enum | 是 | 发布状态 |
| `scope_summary` | text | 是 | 本次范围摘要 |
| `risk_summary` | jsonb | 否 | 风险摘要 |
| `gate_summary` | jsonb | 是 | 门禁结果摘要 |
| `rollback_version_id` | string | 否 | 可回滚版本 |
| `approved_by` | string[] | 否 | 审批人角色或用户 ID |
| `deployed_at` | datetime | 否 | 发布时间 |
| `completed_at` | datetime | 否 | 完成时间 |

#### 6.1.4 `release_gates`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | string | 是 | 门禁 ID |
| `release_id` | string | 是 | 关联发布版本 |
| `gate_type` | enum | 是 | `scope` / `api_contract` / `db_migration` / `queue` / `ai_benchmark` / `risk` / `billing` / `security` / `e2e` / `monitoring` |
| `required` | boolean | 是 | 是否必须通过 |
| `status` | enum | 是 | `pending` / `passed` / `failed` / `waived` |
| `evidence_ref` | string | 否 | 报告、日志或截图引用 |
| `blocker_reason` | text | 否 | 阻断原因 |
| `waiver_reason` | text | 否 | 豁免原因，P0 默认不允许豁免 |
| `checked_by` | string | 否 | 检查人或系统 |
| `checked_at` | datetime | 否 | 检查时间 |

#### 6.1.5 `alert_rules`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | string | 是 | 告警规则 ID |
| `name` | string | 是 | 告警名称 |
| `metric_name` | string | 是 | 指标名 |
| `severity` | enum | 是 | `P0` / `P1` / `P2` / `P3` |
| `condition` | jsonb | 是 | 阈值条件 |
| `window_minutes` | int | 是 | 检测窗口 |
| `cooldown_minutes` | int | 是 | 冷却时间 |
| `notify_targets` | string[] | 是 | 通知对象 |
| `auto_actions` | jsonb | 否 | 自动动作，例如暂停任务、关闭高成本模型 |
| `enabled` | boolean | 是 | 是否启用 |

#### 6.1.6 `alert_events`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | string | 是 | 告警事件 ID |
| `rule_id` | string | 是 | 告警规则 ID |
| `severity` | enum | 是 | P0/P1/P2/P3 |
| `status` | enum | 是 | `open` / `acknowledged` / `resolved` / `suppressed` |
| `metric_snapshot` | jsonb | 是 | 触发时指标快照 |
| `related_task_id` | string | 否 | 关联任务 |
| `related_release_id` | string | 否 | 关联发布 |
| `incident_id` | string | 否 | 关联事故 |
| `triggered_at` | datetime | 是 | 触发时间 |
| `acknowledged_at` | datetime | 否 | 确认时间 |
| `resolved_at` | datetime | 否 | 恢复时间 |

#### 6.1.7 `incident_records`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| `id` | string | 是 | 事故 ID |
| `severity` | enum | 是 | P0/P1/P2/P3 |
| `category` | enum | 是 | `availability` / `ai_quality` / `billing` / `compliance` / `security` / `data` / `cost` / `release` |
| `title` | string | 是 | 标题 |
| `status` | enum | 是 | `open` / `acknowledged` / `mitigating` / `resolved` / `postmortem_done` |
| `impact_summary` | text | 是 | 影响范围 |
| `root_cause` | text | 否 | 根因 |
| `mitigation_actions` | jsonb | 否 | 处置动作 |
| `follow_up_tasks` | jsonb | 否 | 后续任务 |
| `created_by` | string | 是 | 创建人/系统 |
| `created_at` | datetime | 是 | 创建时间 |
| `resolved_at` | datetime | 否 | 恢复时间 |

### 6.2 枚举定义

#### 6.2.1 `ReleaseStatus`

```text
draft
ci_running
ci_failed
qa_ready
staging_deployed
regression_running
pending_approval
approved
canary_deploying
production_deploying
monitoring_hold
completed
blocked
rolled_back
cancelled
```

#### 6.2.2 `HealthStatus`

```text
healthy
degraded
unhealthy
unknown
maintenance
```

#### 6.2.3 `IncidentSeverity`

| 等级 | 含义 | 示例 |
|---|---|---|
| P0 | 生产核心链路不可用、严重合规/安全/计费事故 | 大量任务无法完成；重复扣费；高风险内容放行；疑似数据泄露 |
| P1 | 重要链路受损或成本快速异常 | 队列严重积压；模型失败率飙升；风控人工复核积压 |
| P2 | 局部功能异常，有替代路径 | 导出失败率升高；部分模型降级；非核心后台页面错误 |
| P3 | 低影响问题或优化项 | 文案错误、低频埋点缺失、非关键图表延迟 |

### 6.3 API 契约草案

#### 6.3.1 健康检查 API

```http
GET /api/v1/health/live
GET /api/v1/health/ready
GET /api/v1/health/dependencies
GET /api/v1/version
```

`/health/live` 只判断服务进程是否存活；`/health/ready` 判断是否可接流量；`/health/dependencies` 检查数据库、Redis/队列、对象存储、模型路由、支付回调配置、监控上报状态。

响应示例：

```json
{
  "status": "degraded",
  "environment": "staging",
  "version": "v0.1.0-mvp1",
  "git_sha": "abc123",
  "release_id": "rel_20260525_001",
  "checks": [
    {"name": "database", "status": "healthy", "latency_ms": 12},
    {"name": "queue", "status": "healthy", "latency_ms": 8},
    {"name": "object_storage", "status": "healthy", "latency_ms": 30},
    {"name": "model_router", "status": "degraded", "message": "primary provider timeout rate elevated"}
  ],
  "timestamp": "2026-05-25T00:00:00Z"
}
```

#### 6.3.2 发布管理 API

```http
POST /api/v1/admin/releases
GET /api/v1/admin/releases
GET /api/v1/admin/releases/{release_id}
POST /api/v1/admin/releases/{release_id}/run-gates
POST /api/v1/admin/releases/{release_id}/approve
POST /api/v1/admin/releases/{release_id}/deploy-staging
POST /api/v1/admin/releases/{release_id}/deploy-production
POST /api/v1/admin/releases/{release_id}/rollback
```

权限要求：

| API | 权限 |
|---|---|
| 创建发布 | `release:write` |
| 查看发布 | `release:read` |
| 执行门禁 | `release:gate:run` |
| 审批发布 | `release:approve` |
| 生产部署 | `release:deploy:production` |
| 回滚 | `release:rollback` |

#### 6.3.3 测试与门禁 API

```http
GET /api/v1/admin/test-runs
GET /api/v1/admin/test-runs/{run_id}
GET /api/v1/admin/release-gates/{release_id}
POST /api/v1/admin/release-gates/{gate_id}/waive
POST /api/v1/admin/release-gates/{gate_id}/attach-evidence
```

P0 门禁默认不允许豁免。如需例外，必须由项目 Owner、技术负责人、测试负责人共同确认，并在 D24 交接中记录风险。

#### 6.3.4 监控告警 API

```http
GET /api/v1/admin/monitoring/overview
GET /api/v1/admin/monitoring/task-metrics
GET /api/v1/admin/monitoring/ai-quality
GET /api/v1/admin/monitoring/billing-cost
GET /api/v1/admin/alerts
POST /api/v1/admin/alerts/{alert_event_id}/ack
POST /api/v1/admin/alerts/{alert_event_id}/resolve
GET /api/v1/admin/incidents
POST /api/v1/admin/incidents
POST /api/v1/admin/incidents/{incident_id}/resolve
POST /api/v1/admin/incidents/{incident_id}/postmortem
```

### 6.4 任务队列设计

D20 不重写 D17 的主业务队列，但需要增加测试、发布、监控相关队列：

| 队列 | 用途 | 输入 | 输出 | 失败处理 |
|---|---|---|---|---|
| `q_test_run` | 执行测试套件 | `suite_id`、`git_sha`、`env` | `test_run` | 失败写入门禁，不重试无限次 |
| `q_ai_benchmark_run` | 执行 AI 回归测试 | `benchmark_suite_id`、`prompt_version`、`model_policy` | `benchmark_run` | 失败阻断发布 |
| `q_release_gate_check` | 汇总发布门禁 | `release_id` | `release_gate_report` | 失败进入 blocked |
| `q_smoke_check` | 预发/生产 Smoke | `environment`、`release_id` | `smoke_check_result` | P0 失败自动标记发布异常 |
| `q_monitor_snapshot` | 周期聚合监控指标 | 指标窗口 | `monitoring_snapshot` | 失败告警 P2 |
| `q_alert_evaluate` | 执行告警规则 | 指标快照 | `alert_event` | 失败告警 P1 |
| `q_billing_reconciliation` | 账务一致性检查 | 时间窗口 | `billing_reconciliation_report` | 发现异常告警 P0/P1 |
| `q_post_release_report` | 发布后报告 | `release_id` | `post_release_report` | 失败告警 P2 |

### 6.5 日志规范

所有服务日志必须采用结构化 JSON，不允许只输出自然语言字符串。

最小字段：

```json
{
  "timestamp": "2026-05-25T00:00:00Z",
  "level": "info",
  "service": "api",
  "environment": "production",
  "trace_id": "tr_xxx",
  "request_id": "req_xxx",
  "release_id": "rel_xxx",
  "user_id_hash": "u_hash_xxx",
  "org_id": "org_xxx",
  "task_id": "task_xxx",
  "job_id": "job_xxx",
  "prompt_run_id": "pr_xxx",
  "model_call_id": "mc_xxx",
  "credit_hold_id": "hold_xxx",
  "risk_level": "medium",
  "event_name": "task.step.completed",
  "error_code": null,
  "duration_ms": 1234,
  "metadata": {}
}
```

敏感字段处理规则：

| 字段类别 | 日志规则 |
|---|---|
| 用户邮箱、手机号 | 只允许 hash 或后四位脱敏 |
| 原视频、帧图、音频 | 只记录 asset_id，不记录公开 URL |
| ASR/OCR 原文 | 默认不记录；必要时只记录短摘要和证据 ID |
| Prompt 输入输出 | 默认不进普通日志，进入受控 PromptRun 表，权限受限 |
| API Key、Webhook Secret | 绝对禁止记录 |
| 支付信息 | 只记录订单 ID、支付状态、金额，不记录敏感支付凭据 |

### 6.6 Prompt 设计

D20 涉及的 AI Prompt 仅用于测试辅助、发布风险摘要、事故归因辅助和监控异常解释，不得自动替代人工发布审批。

#### 6.6.1 `P20_TEST_CASE_GENERATOR`

用途：根据 PRD、模块文档、API 契约生成测试用例草案。

输入 Schema：

```json
{
  "doc_alias": "string",
  "module_code": "string",
  "requirements": ["string"],
  "api_contract_refs": ["string"],
  "risk_rules": ["string"],
  "billing_rules": ["string"],
  "priority_scope": "P0|P1|P2"
}
```

输出 Schema：

```json
{
  "test_cases": [
    {
      "id": "string",
      "title": "string",
      "test_type": "unit|contract|integration|ai_regression|e2e|security|billing|load",
      "priority": "P0|P1|P2",
      "preconditions": ["string"],
      "steps": ["string"],
      "expected_result": ["string"],
      "automation_suggestion": "automated|semi_automated|manual",
      "blocker_if_failed": true
    }
  ],
  "coverage_notes": ["string"],
  "missing_information": ["string"]
}
```

质量标准：P0 需求必须至少生成正常路径、异常路径、权限路径、风控路径、计费路径测试用例；输出不得生成与 D00 禁止能力相关的测试目标。

失败回退：如果输出 JSON 不合法，执行 JSON repair；如果仍失败，标记为 `manual_test_design_required`。

#### 6.6.2 `P20_RELEASE_RISK_SUMMARY`

用途：汇总本次发布范围、测试结果、失败项、风险、回滚建议。

输出 Schema：

```json
{
  "release_risk_level": "low|medium|high|blocker",
  "summary": "string",
  "passed_gates": ["string"],
  "failed_gates": ["string"],
  "open_risks": [
    {
      "risk": "string",
      "severity": "P0|P1|P2|P3",
      "recommended_action": "string",
      "release_blocking": true
    }
  ],
  "rollback_notes": ["string"],
  "approval_recommendation": "approve|approve_with_risk|block"
}
```

约束：该 Prompt 只能辅助总结，不能自动审批发布。`approval_recommendation=approve` 仍必须经过人工审批。

#### 6.6.3 `P20_INCIDENT_TRIAGE`

用途：根据告警、日志摘要、指标快照辅助判断事故级别和处理路径。

输出 Schema：

```json
{
  "incident_category": "availability|ai_quality|billing|compliance|security|data|cost|release",
  "severity": "P0|P1|P2|P3",
  "likely_causes": ["string"],
  "immediate_actions": ["string"],
  "data_needed": ["string"],
  "customer_impact": "string",
  "requires_rollback": true,
  "requires_manual_review": true,
  "requires_customer_notice": false
}
```

约束：不得读取原始用户隐私内容；不得自行决定是否通知用户，用户通知由项目 Owner、法务/合规和客服 SOP 决定。

#### 6.6.4 `P20_MONITORING_ANOMALY_EXPLAINER`

用途：解释监控异常，例如成本上升、失败率升高、队列积压。

输出 Schema：

```json
{
  "anomaly_type": "cost_spike|failure_rate|latency|queue_backlog|quality_drop|risk_spike|billing_mismatch",
  "confidence": 0.0,
  "possible_causes": ["string"],
  "suggested_checks": ["string"],
  "safe_mitigations": ["string"],
  "unsafe_actions_to_avoid": ["string"]
}
```

### 6.7 后台页面设计

#### 6.7.1 质量门禁页 `/admin/quality-gates`

展示：

1. 当前发布版本。
2. 各门禁状态：范围、API、DB、队列、AI、风控、商业化、安全、E2E、监控。
3. 每个门禁的报告链接、失败项、阻断原因。
4. 人工审批记录。
5. 豁免记录，P0 不允许普通豁免。

#### 6.7.2 发布中心 `/admin/releases`

展示：

1. 发布版本列表。
2. 当前状态。
3. 关联 git sha。
4. 发布范围。
5. 灰度比例。
6. 回滚版本。
7. 发布后观察窗口。
8. 生产 Smoke 结果。

#### 6.7.3 监控总览 `/admin/monitoring`

核心卡片：

1. 任务成功率。
2. 任务平均耗时。
3. 队列积压。
4. 模型调用失败率。
5. Prompt JSON 合法率。
6. 风控命中率。
7. 积分扣费/返还异常。
8. 单任务平均成本。
9. 投诉删除待处理。
10. P0/P1 告警数量。

#### 6.7.4 告警中心 `/admin/alerts`

展示：

1. 当前打开告警。
2. 告警级别。
3. 触发规则。
4. 影响范围。
5. 关联任务/用户/发布。
6. 确认、静默、解决操作。
7. 自动生成事故记录入口。

#### 6.7.5 事故详情 `/admin/incidents/{id}`

展示：

1. 事故级别与类型。
2. 时间线。
3. 影响用户/任务/金额/风险内容。
4. 已执行缓解动作。
5. 是否回滚。
6. 是否返还积分或人工补偿。
7. 是否需要投诉删除或用户通知。
8. 复盘结论和后续测试/监控补充项。

### 6.8 状态机

#### 6.8.1 发布状态机

```text
draft
→ ci_running
→ qa_ready
→ staging_deployed
→ regression_running
→ pending_approval
→ approved
→ canary_deploying
→ production_deploying
→ monitoring_hold
→ completed
```

异常状态：

```text
ci_failed
blocked
rolled_back
cancelled
```

规则：

1. `ci_failed` 不得进入 `qa_ready`。
2. `blocked` 不得进入 `approved`，除非阻断门禁被修复并重新通过。
3. `monitoring_hold` 期间出现 P0/P1 发布相关告警，必须进入 `blocked` 或 `rolled_back`。
4. `rolled_back` 后必须创建事故记录或发布异常记录。

#### 6.8.2 告警状态机

```text
open
→ acknowledged
→ resolved
```

辅助状态：

```text
suppressed
escalated
linked_to_incident
```

规则：

1. P0 告警必须被确认，不允许自动静默。
2. P1 告警超过响应窗口未确认，自动升级。
3. 已解决告警必须有恢复指标证据。

#### 6.8.3 事故状态机

```text
open
→ acknowledged
→ mitigating
→ resolved
→ postmortem_done
```

规则：

1. P0/P1 事故必须填写影响范围、根因、处理动作、补偿动作、后续测试补充。
2. 未完成复盘的 P0/P1 事故不得关闭后续发布门禁。

---

## 7. 异常与失败处理

### 7.1 CI / 自动化测试失败

| 场景 | 系统动作 | 人工动作 | 是否阻断发布 |
|---|---|---|---:|
| 单元测试失败 | PR 标红，不允许合并 | 修复后重新跑 | 是 |
| API 契约测试失败 | 阻断合并或发布 | 对齐 D16 契约或回写文档 | 是 |
| E2E 主流程失败 | 版本进入 blocked | 排查页面、API、队列、数据 | 是 |
| 非 P0 用例失败 | 标记风险 | 由测试负责人判断是否延期 | 视情况 |
| 测试环境故障 | 标记 `blocked_by_infra` | DevOps 修复环境 | 是，直到恢复 |

### 7.2 AI Benchmark 失败

| 失败类型 | 判定 | 处理 |
|---|---|---|
| Schema 合法率不足 | JSON 输出不合法或字段缺失 | 回滚 Prompt/模型版本，修复后重跑 |
| 内容事实幻觉 | 输出无证据支撑 | 降低模型温度、加强证据引用、进入人工复核 |
| 原创化不足 | 与原视频表达/结构过近 | 强制 Creative IR 高强度重构 |
| 风控漏拦 | 高风险样本被放行 | 阻断发布，更新 D13 规则和红队样本 |
| 质量评分下降 | 与上版本相比显著下降 | 阻断模型/Prompt 替换，需人工审批 |

### 7.3 发布失败

| 场景 | 处理策略 |
|---|---|
| 构建失败 | 停止发布，回到 `blocked` |
| 数据库迁移失败 | 立即执行回滚脚本；若无法回滚，进入事故处理 |
| 预发 Smoke 失败 | 不允许生产发布 |
| 生产部署失败 | 自动回滚到上一稳定版本，创建发布异常记录 |
| 生产 Smoke 失败 | 暂停流量或回滚；P0 进入事故流程 |
| 监控观察期异常 | 暂停放量，必要时关闭 Feature Flag 或回滚 |

### 7.4 监控与告警失败

| 场景 | 风险 | 处理 |
|---|---|---|
| 指标不上报 | 监控盲区 | P1 告警，发布不允许完成 |
| 告警规则误报 | 噪音过大 | 调整阈值，但不得关闭 P0 核心告警 |
| 告警未通知到人 | 响应失效 | 检查通知渠道，补充升级机制 |
| 日志缺少 trace_id | 无法排查 | 阻断发布，修复日志标准 |
| 成本指标缺失 | 毛利不可控 | 阻断商业化放量 |

### 7.5 商业化异常

| 场景 | 系统动作 | 积分/成本处理 |
|---|---|---|
| 报价失败 | 不创建任务 | 不冻结积分 |
| 积分冻结失败 | 不入队 | 不调用模型 |
| 任务成功但扣费失败 | 标记账务异常，进入 reconciliation | 不重复扣费，人工复核 |
| 模型失败但扣费已发生 | 自动返还或人工补偿 | 写入 `CreditTransaction` 和 `AuditLog` |
| Webhook 重复回调 | 幂等处理 | 不重复入账 |
| 成本超过阈值 | 告警并暂停高成本模型或降级 | 保留任务成本日志 |

### 7.6 合规与风控异常

| 场景 | 系统动作 | 发布影响 |
|---|---|---|
| 授权确认缺失 | 阻断任务创建 | 阻断发布 |
| AIGC 标识缺失 | 阻断导出或发布结果展示 | 阻断发布 |
| 高风险内容被放行 | 创建 P0/P1 事故，回滚相关 Prompt/规则 | 阻断发布 |
| 人工复核队列积压 | 告警并限制高风险任务 | 视级别阻断放量 |
| 投诉删除失败 | P1/P0 告警，进入运营/合规流程 | 阻断商业化放量 |

---

## 8. 风控与合规

### 8.1 发布前合规门禁

以下任一项不通过，不允许进入商业化上线：

1. 上传页必须有素材授权确认。
2. 对标内容使用边界必须在用户流程中可见。
3. 输出报告和导出文件必须预留 AIGC 标识。
4. 高风险行业或高风险内容必须触发拦截或人工复核。
5. 高相似内容必须强制重构，不允许直接输出。
6. 风控日志、审核日志、模型调用日志必须可查询。
7. 投诉删除入口必须可用。
8. 人工复核后台必须可用或至少有可执行的轻量流程。
9. 生成内容不得包含“1:1 复刻”“仿脸”“仿声”“搬运”“洗稿”等产品能力暗示。
10. 任何生产发布不得绕过风控服务。

### 8.2 风险分级与发布动作

| 风险等级 | 发布动作 |
|---|---|
| 低风险 | 可进入正常发布，保留日志 |
| 中风险 | 允许发布，但必须有用户提示、日志、后续监控 |
| 高风险 | 不允许默认放行，必须强制重构或人工复核 |
| 极高风险 | 禁止发布相关能力，必须拦截、返还积分或转人工处理 |

### 8.3 人工复核门禁

以下能力如果未实现，至少必须有可执行的临时人工流程，否则不允许商业化放量：

1. 高风险任务查看。
2. 风险证据查看。
3. 放行 / 驳回 / 要求重构。
4. 复核日志。
5. 积分返还或补偿联动。
6. 投诉删除联动。

### 8.4 投诉删除验收

投诉删除必须至少测试：

1. 用户提交投诉。
2. 权利人投诉记录创建。
3. 关联任务、资产、报告、导出文件。
4. 删除或下架用户可见资产。
5. 保留必要审计日志。
6. 触发客服/人工复核。
7. 如涉及扣费或补偿，联动积分流水。
8. 删除失败触发 P1 告警。

### 8.5 数据安全发布门禁

1. 生产环境密钥不得出现在前端、日志、错误堆栈、测试报告中。
2. 后台必须按角色控制访问。
3. 用户资产下载必须使用签名 URL 或受控代理。
4. 日志中不得出现原始视频公开 URL、完整 ASR/OCR、支付敏感信息。
5. 数据删除必须覆盖派生资产、导出文件、PromptRun 受控内容和对象存储引用。
6. 生产数据库迁移必须有备份和回滚策略。

---

## 9. 测试与验收标准

### 9.1 模块级 DoD

每个模块完成必须满足：

1. 读取并遵守 D00 和对应专项文档。
2. 实现 P0 功能，不自行扩展 P1/P2。
3. 有单元测试或合理解释无法单测的部分。
4. 有 API 契约测试或组件测试。
5. 有错误码和异常处理。
6. 有结构化日志。
7. 有权限校验。
8. 有测试数据和验收样例。
9. 与积分、风控、任务状态、模型成本相关的模块必须有幂等测试。
10. 交付说明包含修改文件、测试命令、风险和未完成项。

### 9.2 集成级 DoD

MVP1 集成验收必须满足：

1. 用户主流程可端到端完成。
2. 失败路径可端到端处理。
3. 风控拦截可端到端触发。
4. 积分冻结、扣费、返还与任务状态一致。
5. 后台能查看任务、模型调用、成本、风控、积分流水。
6. 任务重试不会重复扣费或重复创建报告。
7. 取消任务会按规则解冻或结算。
8. 导出文件可生成、可下载、可删除。
9. 日志可通过 `trace_id` 串联前端、API、Worker、模型调用和计费。
10. 监控看板能看到核心指标。

### 9.3 AI 质量验收标准

| 指标 | MVP1 最低要求 | 失败动作 |
|---|---:|---|
| 核心 JSON Schema 合法率 | ≥ 98% | 阻断发布 |
| 必填字段完整率 | ≥ 95% | 阻断发布或降级 |
| JSON repair 触发率 | ≤ 5% | 超过则需修 Prompt/模型 |
| Content DNA 证据可追溯率 | ≥ 90% | 阻断 AI 能力放量 |
| Creative IR 原创化合格率 | ≥ 90% | 强制重构规则调整 |
| 高风险红队召回率 | ≥ 95% | 阻断发布 |
| 极高风险放行率 | 0 | 阻断发布并创建事故 |
| 输出可用性人工抽检 | ≥ 80% | 不允许商业化放量 |
| 标题/封面违规诱导率 | 0 P0 样本 | 阻断发布 |

说明：以上为 MVP1 最低上线门槛，后续应根据真实用户反馈和 D12 Benchmark 持续提高。

### 9.4 商业化验收标准

| 项目 | 验收标准 |
|---|---|
| 报价 | 不同视频长度、模式、用户权益能生成合理报价 |
| 冻结 | 任务入队前必须冻结积分，冻结失败不入队 |
| 扣费 | 任务成功后扣费，且同一任务不重复扣费 |
| 返还 | 系统失败、模型失败、风控拦截、取消任务按规则返还 |
| 成本 | 每次模型调用写入成本日志，可按任务聚合 |
| 毛利 | 后台能查看收入、成本、毛利、异常任务 |
| Webhook | 支付重复回调幂等，不重复入账 |
| 补偿 | 客服补偿必须有权限控制和审计日志 |

### 9.5 性能和稳定性验收标准

| 指标 | MVP1 建议门槛 |
|---|---:|
| 普通 API p95 延迟 | ≤ 800ms |
| 任务状态查询 p95 | ≤ 500ms |
| 文件上传成功率 | ≥ 98% |
| 视频处理任务成功率 | ≥ 90%，失败必须可解释和补偿 |
| 总任务超时 | 遵守 D00/D17 的 15-30 分钟建议窗口，超时进入补偿 |
| 队列积压告警 | P1 队列超过阈值必须告警 |
| 生产健康检查可用性 | ≥ 99%，内测阶段可按实际资源调整 |
| 重试后重复扣费 | 0 |
| 死信任务无记录 | 0，所有死信必须可查 |

### 9.6 生产发布验收标准

生产发布完成必须满足：

1. 当前版本状态为 `monitoring_hold` 或 `completed`。
2. 生产 Smoke 全部通过。
3. 无 P0/P1 发布相关告警。
4. 核心任务样例可完成。
5. 积分扣费/返还样例可完成。
6. 后台看板数据正常刷新。
7. 告警通知渠道可用。
8. 回滚版本可用。
9. 发布报告已生成。
10. 已知问题进入 D24 或 issue backlog。

---

## 10. AIcoding 研发任务拆分

### 10.1 任务总览

| 任务编号 | 任务名称 | 负责人 Agent | 优先级 | 依赖 | 验收产物 |
|---|---|---|---:|---|---|
| D20-T01 | 测试目录与测试命令初始化 | 测试 Agent / 后端 Agent | P0 | D15/D16 | 测试目录、脚本、CI 命令 |
| D20-T02 | API 契约测试与错误码测试 | 后端 Agent / 测试 Agent | P0 | D16 | API contract tests |
| D20-T03 | 数据库迁移 dry-run 与回滚测试 | 后端 Agent / DevOps Agent | P0 | D16 | migration test report |
| D20-T04 | 异步任务 DAG 集成测试 | 后端 Agent / Worker Agent | P0 | D17 | queue integration tests |
| D20-T05 | AI Benchmark 接入 CI | AI/Prompt Agent / 测试 Agent | P0 | D12/D10/D11 | benchmark CI gate |
| D20-T06 | 风控红队与合规门禁测试 | 风控 Agent / 测试 Agent | P0 | D13/D21 | redteam suite |
| D20-T07 | 商业化计费幂等与返还测试 | 商业化 Agent / 后端 Agent | P0 | D19 | billing test suite |
| D20-T08 | E2E 主流程测试 | 前端 Agent / 测试 Agent | P0 | D06/D04 | E2E tests |
| D20-T09 | 安全权限与日志脱敏测试 | 安全 Agent / 测试 Agent | P0 | D18 | security tests |
| D20-T10 | 发布门禁与发布后台 | DevOps Agent / 后端 Agent / 前端 Agent | P0 | D15-D19 | release gates page/API |
| D20-T11 | 健康检查与版本 API | 后端 Agent / DevOps Agent | P0 | D15 | health APIs |
| D20-T12 | 监控指标埋点 | 后端 Agent / Worker Agent / 前端 Agent | P0 | D15-D19 | metrics events |
| D20-T13 | 告警规则与告警中心 | DevOps Agent / 后端 Agent | P0 | D20-T12 | alert rules & page |
| D20-T14 | 事故记录与 Runbook | DevOps Agent / 运营后台 Agent | P1 | D20-T13 | incident module |
| D20-T15 | 生产 Smoke 与回滚脚本 | DevOps Agent | P0 | D20-T10/D20-T11 | smoke & rollback scripts |
| D20-T16 | 发布报告与交接清单 | 测试 Agent / 交接 Agent | P1 | 全部 | release report template |

### 10.2 任务详细说明

#### D20-T01：测试目录与测试命令初始化

输入：D15 技术架构、D16 API 契约、现有代码仓库。  
输出：统一测试目录、测试脚本、CI 基础命令。  
验收：`npm test`、`npm run test:contract`、`npm run test:e2e`、`npm run test:ai` 或对应技术栈命令可执行；失败能返回非零退出码。

#### D20-T05：AI Benchmark 接入 CI

输入：D12 Benchmark 样本、D10 Prompt Registry、D11 Model Router。  
输出：`ai-regression` 测试任务、BenchmarkRun 记录、发布门禁。  
验收：Prompt 或模型版本变化时自动触发；核心样本失败会阻断发布；报告能展示 Schema 合法率、质量评分、风控召回。

#### D20-T07：商业化计费幂等与返还测试

输入：D19 商业化规则、D16 数据表、D17 任务状态。  
输出：计费测试套件。  
必须覆盖：报价、冻结、成功扣费、模型失败返还、风控拦截返还、取消任务返还、重复 Webhook、重复任务回调、后台人工补偿。  
验收：同一 `task_id`、`credit_hold_id`、`payment_event_id` 重复调用不会重复扣费或入账。

#### D20-T10：发布门禁与发布后台

输入：所有测试结果、Benchmark、风控报告、商业化测试、安全测试、监控状态。  
输出：发布门禁数据表、API、后台页。  
验收：任何 P0 门禁失败，发布版本进入 `blocked`，生产部署按钮不可用。

#### D20-T12：监控指标埋点

输入：任务、模型、积分、风控、队列、API 事件。  
输出：结构化指标事件和监控聚合。  
验收：可按 `release_id`、`task_id`、`model`、`prompt_id`、`risk_level`、`credit_transaction_type` 聚合查询核心指标。

---

## 11. AIcoding 执行提示词

### 11.1 通用执行提示词

```text
你是本项目的工程级 AIcoding 执行 Agent，负责实现 D20《测试、验收、发布、监控与告警文档》中的指定任务。

必须先读取并遵守：
1. D00《AIcoding 研发总控文档 / 项目上下文总索引》
2. D20《测试、验收、发布、监控与告警文档》
3. 与本任务相关的专项文档：D04、D05、D10、D12、D13、D15、D16、D17、D18、D19、D21/D22（如已存在）

项目定位：
本项目是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具。任何测试、发布、监控、告警实现都不得绕过风控、日志、成本、测试和验收。

你的任务：
{粘贴 D20-Txx 任务内容}

必须交付：
1. 代码实现或配置文件。
2. 自动化测试。
3. 必要的数据库迁移或 API 契约更新。
4. 日志、指标、错误码和权限控制。
5. 本地执行命令和预期结果。
6. 变更文件清单。
7. 风险、未完成项和回滚建议。

硬性禁止：
1. 不允许只做 UI Demo。
2. 不允许用 mock 冒充真实能力；如使用 mock，必须明确标记并默认不进入生产。
3. 不允许跳过积分、成本、风控、日志、审计、测试。
4. 不允许自行扩大 MVP1 范围。
5. 不允许把用户隐私、密钥、原始视频内容写入日志。

完成后按以下格式汇报：
- 已读取文档：
- 完成内容：
- 修改文件：
- 新增/更新 API：
- 新增/更新数据表：
- 测试命令：
- 测试结果：
- 监控/日志：
- 风险与未完成项：
- 回滚建议：
```

### 11.2 测试 Agent 提示词

```text
你是本项目的测试 Agent。请基于 D20 和相关专项文档，为 MVP1 生成并实现自动化测试。

测试范围必须覆盖：
1. 用户主流程 E2E。
2. 视频处理、Content DNA、Creative IR、输出包、风控、导出。
3. API 契约、错误码、权限。
4. 积分报价、冻结、扣费、返还、重复回调幂等。
5. Prompt 输出 Schema 和 Benchmark。
6. 高风险内容拦截、人工复核、投诉删除。
7. 生产 Smoke 测试。

输出：
1. 测试用例清单。
2. 自动化测试代码。
3. 测试数据 fixtures。
4. CI 命令。
5. 测试报告示例。
6. 未覆盖风险。

禁止：
不得将真实用户数据、支付密钥、模型密钥、原始视频素材提交到测试仓库。
```

### 11.3 DevOps Agent 提示词

```text
你是本项目的 DevOps Agent。请基于 D20 实现发布、健康检查、监控、告警、回滚能力。

必须实现：
1. CI/CD pipeline：lint、typecheck、unit、contract、integration、AI Benchmark、E2E、security、build、staging deploy、smoke、manual approval、production deploy、post-deploy check。
2. 健康检查 API 接入部署平台。
3. 发布版本记录与 release_id 注入。
4. 预发和生产环境变量校验。
5. 数据库迁移 dry-run 和回滚脚本。
6. 核心监控指标上报。
7. P0/P1/P2 告警规则。
8. 回滚流程和 Runbook。

验收：
生产发布前任一 P0 门禁失败必须阻断发布；发布后 Smoke 失败必须触发回滚或阻断完成。
```

### 11.4 AI/Prompt Agent 提示词

```text
你是本项目的 AI/Prompt 测试与回归 Agent。请将 D12 Benchmark、D10 Prompt Registry、D11 Model Router、D13 风控规则接入 D20 发布门禁。

必须实现：
1. Prompt 输出 JSON Schema 校验。
2. Content DNA / Creative IR / 输出包的 BenchmarkRun。
3. 风控红队样本回归。
4. Prompt 版本变更自动触发回归。
5. 模型替换自动触发回归。
6. 失败报告进入 release_gates。
7. 失败回退建议：回滚 Prompt、回滚模型、降级模型、转人工复核。

禁止：
不得为了通过测试降低风控强度；不得将高风险样本作为产品演示素材。
```

### 11.5 商业化测试 Agent 提示词

```text
你是本项目的商业化测试 Agent。请基于 D19 与 D20 实现计费、积分、成本、毛利、退款补偿的测试与监控。

必须覆盖：
1. 任务报价。
2. 积分冻结。
3. 成功扣费。
4. 系统失败返还。
5. 模型失败返还。
6. 风控拦截返还或部分返还。
7. 用户取消结算。
8. 支付成功/失败/取消/重复 Webhook。
9. 后台人工补偿。
10. 成本账本和毛利统计。
11. 成本超过阈值告警。

验收：
同一任务、同一支付事件、同一模型回调重复执行，不得重复扣费、重复入账或重复返还。
```

---

## 12. 不做事项

MVP1 的 D20 明确不做以下事项：

1. 不做完整企业级 SRE 平台，只做支撑 MVP1 商业化上线的测试发布监控告警体系。
2. 不做自动公开发布到短视频平台的监控和告警。
3. 不做账号级全量抓取、批量搬运、自动复刻、自动发布的测试链路。
4. 不做仿脸、仿声、明星影视复刻、未授权素材复用相关能力的正向测试；只做风险拦截测试。
5. 不把 AI 生成的发布建议作为自动审批依据。
6. 不允许用 mock 监控数据冒充真实生产监控。
7. 不在生产环境跑破坏性压测。
8. 不将真实用户原始视频、完整 ASR/OCR、支付隐私写入测试仓库或日志。
9. 不允许 P0 门禁长期豁免。
10. 不为了上线速度跳过风控、计费、成本、日志、权限、投诉删除测试。
11. 不承诺 AI 归因或监控解释一定正确；AI 只做辅助总结，最终决策由负责人确认。
12. 不把 D20 编号写死为业务逻辑，使用稳定 `doc_alias`。

---

## 13. 待确认问题

| 编号 | 问题 | 影响 | 建议确认人 | 优先级 |
|---|---|---|---|---:|
| Q20-01 | 最终部署平台选择是什么 | 决定 CI/CD、健康检查、回滚实现 | 技术负责人 / DevOps | P0 |
| Q20-02 | 监控与告警工具栈使用哪套 | 决定指标上报 SDK、Dashboard、通知渠道 | DevOps | P0 |
| Q20-03 | 告警通知渠道使用什么 | 决定 P0/P1 响应效率 | 项目 Owner / 运营负责人 | P0 |
| Q20-04 | 生产发布是否需要人工双人审批 | 决定 release approval 实现 | 项目 Owner | P0 |
| Q20-05 | MVP1 内测用户量和并发目标 | 决定压测指标和队列阈值 | 产品负责人 / 技术负责人 | P0 |
| Q20-06 | AI Benchmark 样本集规模和行业覆盖 | 决定 AI 发布门禁强度 | AI 产品负责人 | P0 |
| Q20-07 | 高风险行业最终清单 | 决定风控红队测试和拦截规则 | 合规负责人 | P0 |
| Q20-08 | 成本告警阈值和单用户成本上限 | 决定商业化监控 | 商业化负责人 | P0 |
| Q20-09 | 失败返还和部分扣费最终规则 | 决定测试断言 | 商业化负责人 / 客服负责人 | P0 |
| Q20-10 | 投诉删除 SLA | 决定告警阈值和运营流程 | 合规 / 运营 | P1 |
| Q20-11 | 是否需要状态页或用户侧故障提示 | 影响事故沟通 | 项目 Owner / 运营 | P1 |
| Q20-12 | 是否允许生产灰度小流量真实用户 | 决定 canary 策略 | 项目 Owner | P1 |
| Q20-13 | 日志留存周期 | 影响成本、安全、合规 | 技术负责人 / 合规 | P1 |
| Q20-14 | 数据备份和恢复目标 RPO/RTO | 影响数据库和对象存储设计 | 技术负责人 | P1 |
| Q20-15 | 是否需要企业客户专属监控报表 | 影响后台与套餐 | 商业化负责人 | P2 |

---

## 14. 最终 Checklist

### 14.1 文档与范围 Checklist

- [ ] 已读取 D00 并确认产品定位不是搬运、克隆、洗稿、仿脸、仿声工具。
- [ ] 已确认本次只服务 MVP1「视频拆解 + 原创脚本生成」。
- [ ] 已确认 P1/P2 功能默认关闭或 Feature Flag 隔离。
- [ ] 已处理 D20 与历史 D19/D20 编号兼容问题。
- [ ] 已将 D20 输出回写到 D24 交接清单。

### 14.2 测试 Checklist

- [ ] 单元测试可执行。
- [ ] 类型检查和 lint 可执行。
- [ ] API 契约测试可执行。
- [ ] 数据库迁移 dry-run 和回滚测试可执行。
- [ ] 队列主 DAG 集成测试可执行。
- [ ] AI Benchmark 可执行并能阻断发布。
- [ ] 风控红队测试可执行。
- [ ] 商业化计费幂等测试可执行。
- [ ] 权限与安全测试可执行。
- [ ] E2E 主流程测试可执行。
- [ ] 生产 Smoke 测试可执行。

### 14.3 验收 Checklist

- [ ] 用户主流程通过。
- [ ] 失败路径通过。
- [ ] 风控拦截路径通过。
- [ ] 人工复核路径通过或有临时可执行 SOP。
- [ ] 投诉删除路径通过。
- [ ] 积分冻结、扣费、返还通过。
- [ ] 模型成本记录通过。
- [ ] 后台任务、成本、风控、积分流水可查。
- [ ] 导出报告可生成、下载、删除。
- [ ] 任务日志、模型日志、审核日志、审计日志可查。

### 14.4 发布 Checklist

- [ ] 发布版本已创建。
- [ ] release_id 已注入服务。
- [ ] CI 全量通过。
- [ ] 预发部署完成。
- [ ] 预发 Smoke 通过。
- [ ] UAT 验收通过。
- [ ] 所有 P0 门禁通过。
- [ ] 生产发布审批完成。
- [ ] 回滚版本可用。
- [ ] 数据库备份和迁移回滚方案确认。
- [ ] 生产 Smoke 通过。
- [ ] 发布后观察窗口完成。

### 14.5 监控告警 Checklist

- [ ] API 健康检查可用。
- [ ] 依赖健康检查可用。
- [ ] 任务成功率、失败率、超时率可监控。
- [ ] 队列积压和死信可监控。
- [ ] 模型调用失败率、延迟、成本可监控。
- [ ] Prompt JSON 合法率、Benchmark 趋势可监控。
- [ ] 风控命中、拦截、人工复核积压可监控。
- [ ] 积分扣费、返还、重复扣费、成本异常可监控。
- [ ] 投诉删除失败可告警。
- [ ] P0/P1/P2 告警规则已配置。
- [ ] 告警通知渠道已验证。

### 14.6 合规安全 Checklist

- [ ] 上传授权确认可验证。
- [ ] AIGC 标识预留可验证。
- [ ] 高风险内容拦截可验证。
- [ ] 高相似输出强制重构可验证。
- [ ] 后台权限控制可验证。
- [ ] 敏感日志脱敏可验证。
- [ ] 数据删除覆盖派生资产可验证。
- [ ] 生产密钥未暴露。
- [ ] 审计日志可查询。

### 14.7 AIcoding 交付 Checklist

- [ ] 每个 Agent 输出已读取文档。
- [ ] 每个 Agent 输出完成内容。
- [ ] 每个 Agent 输出修改文件。
- [ ] 每个 Agent 输出测试命令。
- [ ] 每个 Agent 输出测试结果。
- [ ] 每个 Agent 输出风险与未完成项。
- [ ] 每个 Agent 输出回滚建议。
- [ ] 所有交付证据进入发布门禁或 D24 交接。

---

# 附录 A：MVP1 最小发布门禁样例

```yaml
release_gates:
  - gate_type: scope
    required: true
    pass_condition: "P0 complete; P1/P2 disabled by feature flag"
  - gate_type: api_contract
    required: true
    pass_condition: "all P0 API contract tests passed"
  - gate_type: db_migration
    required: true
    pass_condition: "migration dry-run passed and rollback plan exists"
  - gate_type: queue
    required: true
    pass_condition: "main task DAG success/failure/retry/compensation tests passed"
  - gate_type: ai_benchmark
    required: true
    pass_condition: "schema_valid_rate >= 0.98 and high_risk_release_rate == 0"
  - gate_type: risk
    required: true
    pass_condition: "redteam P0 cases blocked or sent to manual review"
  - gate_type: billing
    required: true
    pass_condition: "quote/hold/charge/refund/idempotency tests passed"
  - gate_type: security
    required: true
    pass_condition: "RBAC, signed URL, log redaction, secret checks passed"
  - gate_type: e2e
    required: true
    pass_condition: "MVP1 primary happy path and failure paths passed"
  - gate_type: monitoring
    required: true
    pass_condition: "health checks, P0/P1 alerts, production smoke configured"
```

# 附录 B：P0 告警规则样例

```yaml
alert_rules:
  - name: "生产核心任务成功率过低"
    severity: P0
    metric_name: "video_analysis_task_success_rate"
    condition: "value < 0.70 for 15 minutes and task_count >= 10"
    auto_actions:
      - "pause_new_high_cost_tasks"
      - "notify_owner_and_devops"
  - name: "疑似重复扣费"
    severity: P0
    metric_name: "duplicate_credit_charge_detected"
    condition: "count > 0"
    auto_actions:
      - "pause_billing_settlement_for_affected_tasks"
      - "create_incident"
  - name: "极高风险内容放行"
    severity: P0
    metric_name: "critical_risk_content_released"
    condition: "count > 0"
    auto_actions:
      - "disable_related_prompt_version"
      - "create_manual_review_ticket"
      - "create_incident"
  - name: "生产密钥疑似泄露到日志"
    severity: P0
    metric_name: "secret_pattern_detected_in_logs"
    condition: "count > 0"
    auto_actions:
      - "rotate_secret_required"
      - "create_security_incident"
```

# 附录 C：发布报告模板

```markdown
# 发布报告

## 1. 基础信息

- release_id：
- version：
- git_sha：
- release_type：
- environment：
- deployed_at：
- release_owner：

## 2. 本次范围

## 3. 发布门禁结果

| 门禁 | 状态 | 证据 | 备注 |
|---|---|---|---|

## 4. 测试结果

## 5. AI Benchmark 结果

## 6. 风控红队结果

## 7. 商业化计费测试结果

## 8. 安全权限测试结果

## 9. 生产 Smoke 结果

## 10. 监控观察

## 11. 已知问题

## 12. 回滚方案

## 13. 是否允许完成发布

- [ ] 允许
- [ ] 不允许

审批人：
日期：
```

# 附录 D：事故复盘模板

```markdown
# 事故复盘

## 1. 事故基础信息

- incident_id：
- severity：
- category：
- started_at：
- detected_at：
- resolved_at：
- affected_users：
- affected_tasks：
- affected_amount_or_credits：

## 2. 影响范围

## 3. 时间线

## 4. 根因分析

## 5. 临时缓解动作

## 6. 最终修复动作

## 7. 用户/客服/合规处理

## 8. 是否需要积分返还或人工补偿

## 9. 缺失的测试用例

## 10. 缺失的监控告警

## 11. 后续行动

| 行动 | 负责人 | 截止时间 | 状态 |
|---|---|---|---|
```

