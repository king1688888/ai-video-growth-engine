# D22｜运营后台、客服、退款补偿与人工复核 SOP 文档

> 文档编号：D22  
> 文档名称：运营后台、客服、退款补偿与人工复核 SOP 文档  
> 文档版本：V1.0  
> 生成日期：2026-05-25  
> 根上下文：D00《AIcoding 研发总控文档 / 项目上下文总索引》  
> 当前主线版本：MVP1「视频拆解 + 原创脚本生成」  
> 文档性质：运营后台 + 客服工单 + 退款补偿 + 人工复核 + 投诉删除 + 审计闭环 + AIcoding 执行文档  
> 核心定位：D22 不是普通“后台管理页”或“客服话术文档”，而是把 AI 短视频爆款增长引擎在商业化上线后必然发生的任务失败、积分争议、内容风险、用户投诉、权利人投诉、人工复核、退款补偿、后台审计和运营质检，固化为可研发、可测试、可审计、可运营、可交接的工程级 SOP。

---

## 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 文档编号 | D22 |
| 文档名称 | 运营后台、客服、退款补偿与人工复核 SOP 文档 |
| 所属项目 | AI 短视频爆款增长引擎 |
| 当前版本 | MVP1：视频拆解 + 原创脚本生成 |
| 文档级别 | A+，商业化上线与持续运营关键支撑文档 |
| 上级根文档 | D00《AIcoding 研发总控文档 / 项目上下文总索引》 |
| 直接前置依赖 | D00、D04、D05、D06、D07、D08、D09、D10、D11、D13、D16、D17、D18、D19、D20、D21 |
| 直接后置依赖 | D24 最终交付、交接、迭代维护与版本治理文档 |
| 适用阶段 | MVP1 内测、商业化上线、客服运营、内容审核、退款补偿、人工复核、投诉删除、事故响应、交接维护 |
| 适用对象 | 创始人、产品负责人、AI 产品经理、技术产品经理、后端 Agent、前端 Agent、AI/Prompt Agent、测试 Agent、运营后台 Agent、客服 Agent、风控复核员、合规负责人、财务协作人、DevOps Agent |
| 文档目标 | 定义运营后台、客服工单、任务诊断、退款补偿、人工复核、投诉删除、后台权限、审计日志、AI 辅助客服、测试验收与 AIcoding 执行任务 |
| 最高约束 | 与 D00 冲突时，以 D00 为准；与 D16 数据库/API 冲突时，以 D16 为工程实现口径并回写 D22；与 D17 任务状态冲突时，以 D17 为状态机口径；与 D18 权限和隐私冲突时，以更严格数据保护规则为准；与 D19 计费退款规则冲突时，以 D19 为商业化结算口径；与 D21 合规复核和投诉删除冲突时，以 D21 的更严格合规口径为准；与 D20 发布门禁冲突时，以 D20 阻断上线标准为准 |
| 工程别名 | `OPS_SUPPORT_REFUND_MANUAL_REVIEW` |
| 稳定模块名 | `ops_admin`、`support_ticketing`、`refund_compensation`、`manual_review`、`complaint_takedown` |
| 更新触发条件 | 工单类型变化、退款策略变化、补偿阈值变化、人工复核策略变化、投诉删除规则变化、合规事件、支付异常、积分异常、后台权限变化、上线事故、用户投诉激增 |
| 文档状态 | V1.0 初版可执行；上线前必须由项目 Owner、运营负责人、合规负责人、技术负责人、商业化负责人共同复核 |

### 1.1 编号兼容说明

当前项目存在一次文档编号演进：D00 V1.0 的文档地图中曾将「运营后台、客服、退款补偿与人工复核 SOP 文档」列为 D23，同时 D21 的后置依赖已经将该文档称为 D22。本轮任务由用户明确指定生成 **D22｜运营后台、客服、退款补偿与人工复核 SOP 文档**，因此本文件按 D22 执行。

为避免 AIcoding 多 Agent 混乱，工程实现必须使用稳定别名，不得把文档编号写死进业务逻辑：

| 字段 | 建议值 |
|---|---|
| `doc_code` | `D22` |
| `doc_alias` | `OPS_SUPPORT_REFUND_MANUAL_REVIEW` |
| `legacy_or_alt_doc_code` | `D23_OPS_SUPPORT_MANUAL_REVIEW` |
| `security_doc_alias` | `DATA_SECURITY_PRIVACY_GOVERNANCE_RBAC` |
| `compliance_doc_alias` | `COMPLIANCE_AUTHORIZATION_AIGC_LABEL_CONTENT_REVIEW` |
| `commercial_doc_alias` | `COMMERCIAL_BILLING_CREDIT_ORDER_COST_SETTLEMENT` |
| `conflict_resolution` | 本轮文件命名和交付按 D22 执行；代码、数据库、API、日志使用 `doc_alias` 作为稳定引用 |

### 1.2 本文档边界声明

D22 负责把运营后台和客服 SOP 落成工程能力，但不替代正式法律意见、财税规则、支付渠道退款规则、企业合同 SLA、平台申诉规则和人力组织制度。涉及现金退款、权利人投诉、隐私泄露、重大合规风险和企业合同违约的最终处理口径，需要由项目 Owner、合规负责人、财务/法务顾问共同确认。

D22 的核心原则是：

```text
用户遇到失败时能被解释；
系统发生扣费争议时能被追溯；
高风险内容需要复核时能被处理；
投诉删除发生时能被执行；
客服补偿发生时能被审计；
运营后台不是万能入口，而是受权限、流程、日志和合规约束的受控工作台。
```

---

## 2. 文档目标

### 2.1 本文档要解决的问题

D22 要解决 MVP1 商业化上线后「真实用户运营与异常闭环」的问题。AI 短视频爆款增长引擎不是一次性 Prompt Demo，而是包含视频上传、异步处理、模型调用、结构化拆解、原创脚本生成、风控审核、积分扣费、报告导出、投诉删除的复合系统。上线后必然出现以下情况：

1. 用户上传视频失败、任务卡住、模型超时、导出失败。
2. 用户认为结果质量不达标，要求重试、补偿或退款。
3. 用户积分被重复冻结、重复扣除、未及时返还。
4. 风控拦截后用户不理解，需要人工解释或复核。
5. 高风险内容需要人工复核后决定放行、重写、补充授权、拒绝或删除。
6. 权利人投诉疑似侵权、肖像、声音、商标、隐私、平台搬运风险。
7. 后台人员需要查看任务证据、模型成本、风险报告和积分流水，但不能越权查看用户隐私。
8. 客服需要标准话术和处理 SOP，但不能承诺违规能力、不能诱导绕过风控。
9. 运营需要统计失败率、工单原因、退款成本、补偿成本、风控命中率、人工复核效率。
10. AIcoding Agent 需要把这些能力做成真实工程系统，而不是后台页面上几个假按钮。

D22 必须把以下运营链路固化为可执行工程系统：

```text
用户问题 / 系统异常 / 风控事件 / 投诉案件
→ 自动创建或人工创建工单
→ AI 辅助分类与摘要
→ 权限校验与证据包聚合
→ 客服处理 / 技术排查 / 合规复核 / 财务退款
→ 用户通知
→ 积分释放 / 返还 / 补偿 / 现金退款 / 拒绝
→ 人工复核决策 / 投诉删除 / 资产清理
→ 审计日志
→ 后台指标统计
→ 测试验收与持续优化
```

### 2.2 本文档不解决的问题

| 不在 D22 完整展开的内容 | 负责文档 | D22 只提供 |
|---|---|---|
| 产品战略、商业闭环、长期壁垒 | D01 | 后台和客服必须服务“结构学习 + 原创化生成”的定位 |
| MVP 范围和版本路线 | D03 | MVP1 后台与客服的 P0/P1/P2 边界 |
| 总 PRD、用户故事、前台流程 | D04/D06 | 工单入口、投诉入口、失败页、复核等待页、积分返还页的后台承接 |
| Content DNA / Creative IR / Prompt 细节 | D07/D09/D10 | 后台只展示必要摘要、版本、风险和质量结论，不重写 AI Schema |
| 视频处理与任务状态机 | D08/D17 | 后台消费任务状态、步骤日志、失败原因和资产生命周期 |
| 模型路由与成本控制 | D11 | 后台消费模型调用日志、成本异常和供应商失败信息 |
| 质检评分、相似度风控 | D13 | 后台消费风险报告，并提供人工复核入口和 SOP |
| 数据库/API 最终契约 | D16 | D22 给出运营领域草案，最终字段以 D16 回写为准 |
| 安全隐私与权限治理 | D18 | D22 引用 RBAC/ABAC、脱敏、审计、最小必要原则 |
| 商业化套餐、积分、订单、成本结算 | D19 | D22 定义客服退款补偿 SOP 与后台操作，不重新定义价格体系 |
| 测试发布监控 | D20 | D22 提供运营验收项、SLA 指标、后台告警输入 |
| 合规、授权、AIGC 标识、投诉删除 | D21 | D22 将 D21 复核/投诉/删除规则落成运营 SOP |
| 最终交接与版本治理 | D24 | D22 输出后台操作手册和交接清单输入 |

### 2.3 本文档的研发落点

D22 最终必须落到以下工程对象：

1. 运营后台路由与页面：`/admin/overview`、`/admin/tasks`、`/admin/users`、`/admin/support-tickets`、`/admin/manual-reviews`、`/admin/refunds`、`/admin/complaints`、`/admin/audit-logs`、`/admin/config`。
2. 工单系统：用户工单、系统自动工单、客服回复、内部备注、状态流转、SLA、升级、关闭、满意度。
3. 任务诊断后台：任务状态、步骤日志、模型调用、成本、风控、积分流水、失败原因、重试入口、导出状态。
4. 人工复核队列：复核触发、证据包、复核决策、用户通知、积分处理、审计日志。
5. 退款补偿系统：释放冻结、返还积分、人工补偿积分、现金退款申请、审批、执行、幂等、审计。
6. 投诉删除系统：投诉接收、初筛、暂停展示、转人工、删除任务、资产清理、用户/投诉人通知、审计。
7. 后台权限模型：客服、客服组长、风控复核员、计费管理员、合规管理员、超级管理员、只读审计员等角色。
8. 审计日志：所有后台查看、编辑、补偿、退款、复核、删除、导出操作必须记录。
9. AI 辅助运营能力：工单分类、工单摘要、补偿建议、复核摘要、投诉初筛、客服回复草稿、成本异常归因摘要。
10. 运营统计：工单量、关闭率、超时率、失败原因 TopN、补偿成本、退款成本、人工复核通过率、投诉成立率、客服处理时长。
11. 测试体系：RBAC 测试、工单 API 测试、退款幂等测试、复核决策测试、投诉删除 E2E、审计日志测试、AI 辅助输出回归测试。

---

## 3. 与 D00 及其他文档的关系

### 3.1 D00 对 D22 的硬约束

| D00 约束 | D22 落地方式 |
|---|---|
| 产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具 | 客服、人工复核和补偿 SOP 不得帮助用户绕过原创化和风控；被拦截的禁止能力不得通过人工处理放行 |
| 当前 MVP1 是视频拆解 + 原创脚本生成 | 后台 P0 只覆盖单条视频拆解任务、报告、原创脚本/标题/封面/Prompt、导出、积分、复核、投诉删除 |
| 不得自行扩大第一版范围 | 后台不做完整 CRM、复杂呼叫中心、企业工单流转、全量自动发布审核、视频生成成片审核 |
| 不得跳过风控、任务日志、成本记录、测试验收 | 工单处理必须展示并记录任务日志、模型成本、风控结果、积分流水、客服操作、测试证据 |
| 不得用 mock 冒充真实能力 | 后台操作按钮必须接真实 API、真实权限、真实审计；测试环境 mock 必须显式标记，生产默认关闭 |
| 每个模块必须可独立开发、独立测试、独立验收、独立交接 | D22 将工单、人工复核、退款补偿、投诉删除、运营看板、审计日志拆成独立 AIcoding 任务 |
| 商业级上线必须具备失败补偿、后台人工补偿、权限分级 | D22 将这些能力设为 P0 发布门禁 |

### 3.2 D22 在文档体系中的位置

```text
D00 项目总控
  → D04 总 PRD / D05 模块边界 / D06 页面流程
  → D13 质检评分与相似度风控
  → D16 数据库/API / D17 队列文件生命周期 / D18 权限隐私
  → D19 商业化积分退款 / D20 测试发布监控 / D21 合规审核
      → D22 运营后台、客服、退款补偿与人工复核 SOP
          → D24 最终交付、交接、迭代维护与版本治理
```

D22 不重新定义核心产品能力，而是把已定义的任务、风控、计费、合规、安全能力，组织成运营人员可以安全使用的后台与 SOP。

### 3.3 与关键文档的接口关系

| 文档 | D22 依赖内容 | D22 输出给该文档或模块的内容 |
|---|---|---|
| D04 总 PRD | 用户失败、投诉、反馈、历史、积分、后台需求 | 后台 SOP、工单入口、异常闭环 |
| D05 模块边界 | 模块职责、独立验收原则 | 运营后台模块边界和交付任务 |
| D06 页面原型 | 用户端投诉、反馈、失败页、复核等待页 | 对应后台页面状态和客服处理动作 |
| D13 风控 | 风险等级、人工复核触发、相似度报告 | 复核队列、复核 SOP、复核统计 |
| D16 数据库/API | 表字段、权限、统一错误码 | support/refund/review/complaint/audit API 草案 |
| D17 队列与资产 | 任务步骤、失败、重试、补偿、删除队列 | 运营触发重试、补偿、删除、通知任务 |
| D18 安全隐私 | 数据分级、RBAC/ABAC、审计、脱敏 | 后台权限矩阵、最小必要证据包、敏感字段展示规则 |
| D19 商业化 | 积分冻结、扣费、返还、退款、人工补偿 | 客服补偿 SOP、审批流、后台补偿统计 |
| D20 测试发布 | 上线门禁、监控告警、事故响应 | 运营验收用例、SLA 指标、告警处理 SOP |
| D21 合规 | 授权、AIGC 标识、内容审核、投诉删除 | 人工复核 SOP、投诉处理 SOP、删除执行与通知流程 |

---

## 4. 核心结论

### 4.1 一句话结论

> D22 必须把「后台可查、客服可答、复核可判、积分可退、投诉可删、操作可审计、风险不可绕过」做成 MVP1 商业化上线的运营闭环。

### 4.2 MVP1 P0 必须实现的运营后台能力

| P0 能力 | 是否上线硬门禁 | 说明 |
|---|---:|---|
| 后台登录与 RBAC | 是 | 不同角色只能看和操作必要数据 |
| 任务诊断页 | 是 | 查看任务状态、步骤日志、失败原因、模型成本、积分流水、风控报告 |
| 用户与积分查询 | 是 | 查看用户基础信息、积分余额、交易流水、订单摘要，敏感信息脱敏 |
| 客服工单系统 | 是 | 用户提交、客服创建、AI 分类、状态流转、回复、关闭、SLA |
| 人工复核队列 | 是 | R3/R4 风险、投诉、用户申诉、低置信度进入复核 |
| 退款补偿后台 | 是 | 释放冻结、返还积分、人工补偿、现金退款申请、审批和审计 |
| 投诉删除处理 | 是 | 接收投诉、暂停展示、复核、删除派生资产、通知、审计 |
| 后台审计日志 | 是 | 查看、修改、补偿、删除、复核、导出全部记录 |
| 运营指标看板 | 是 | 失败率、工单量、退款补偿、复核量、投诉量、成本异常 |
| AI 辅助客服 | P1 可上线，但 P0 可预留 | 只做建议和草稿，不自动做最终决策 |

### 4.3 后台不是万能权限入口

后台必须遵循以下限制：

1. 后台人员不能直接修改 AI 输出内容，除非创建新版本并留下审计记录。
2. 客服不能手动放行 D21 明确禁止的高风险请求。
3. 客服不能删除审计日志。
4. 客服不能绕过积分系统直接给用户交付高成本输出。
5. 客服不能查看用户原始视频、抽帧、ASR/OCR 全文，除非该工单确有处理必要且权限满足。
6. 任何补偿、退款、删除、复核决策必须有原因码、操作者、时间、关联工单和审计日志。
7. AI 辅助客服只能给出建议，不能自动批准退款、自动放行高风险内容或自动删除资产。

### 4.4 MVP1 默认角色模型

| 角色 | 核心权限 | 禁止权限 |
|---|---|---|
| `support_agent` 客服 | 查看自己的工单、任务摘要、积分摘要、回复用户、创建补偿申请 | 现金退款审批、删除资产、放行高风险、查看完整隐私数据 |
| `support_lead` 客服组长 | 分派工单、审批小额积分补偿、升级合规/技术 | 现金退款最终执行、越权查看用户素材 |
| `manual_reviewer` 人工复核员 | 查看复核证据包、作出复核建议或决策 | 调整积分、修改订单、删除审计日志 |
| `compliance_admin` 合规管理员 | 投诉处理、合规复核、用户限制、删除触发审批 | 修改支付订单、直接改余额 |
| `billing_admin` 计费管理员 | 查看订单、执行已审批退款、积分补偿、账务核对 | 放行内容风险、删除投诉证据 |
| `ops_admin` 运营管理员 | 看板、配置、工单分派、基础运营动作 | 超额补偿、清除审计、绕过合规 |
| `super_admin` 超级管理员 | 紧急处置、权限管理、重大事故处理 | 不允许无审计操作；不允许删除不可变审计日志 |
| `read_only_auditor` 只读审计员 | 只读查看审计、工单、复核、账务摘要 | 所有写操作 |
| `system_worker` 系统任务 | 执行补偿、删除、通知、异步队列 | 不能模拟人工审批 |

### 4.5 工单优先级和 SLA 原则

| 优先级 | 场景 | 首次响应目标 | 解决目标 | 升级规则 |
|---|---|---:|---:|---|
| P0 紧急 | 隐私泄露、重大误扣费、大面积系统失败、权利人正式投诉、支付拒付风险 | 30 分钟内 | 4 小时内给处理结论 | 立即升级合规/技术/Owner |
| P1 高 | 任务失败未返还、重复扣费、R4 人工复核、企业客户阻塞 | 2 小时内 | 1 个工作日内 | 超时升级 support_lead |
| P2 中 | 输出质量投诉、导出失败、积分到账延迟、普通复核 | 1 个工作日内 | 2 个工作日内 | 超时升级 |
| P3 低 | 使用咨询、功能建议、普通反馈 | 2 个工作日内 | 5 个工作日内 | 可合并处理 |

### 4.6 退款补偿总原则

1. **未扣费优先释放冻结**：任务未完成且未正式扣费，应释放冻结额度。
2. **系统失败优先自动返积分**：系统、队列、模型供应商导致无可用交付，应自动返还已扣积分。
3. **质量争议不自动现金退款**：质量主观争议先走人工确认，优先补偿积分或重跑任务。
4. **重复扣费必须自动纠正**：重复冻结、重复扣费、Webhook 重复回调必须幂等修复。
5. **合规拦截不等于全额赔付**：若用户请求本身违反使用边界，可释放未消耗部分，但不补偿违规意图导致的成本。
6. **现金退款只在必要场景执行**：支付错误、重复收款、法律或协议要求、订阅退款、企业合同约定。
7. **补偿不能绕过风控**：不能用补偿积分帮助用户继续生成被拦截的禁止内容。
8. **所有补偿必须可审计**：原因码、金额、审批人、执行人、关联工单、关联任务、前后余额必须完整。

---

## 5. 详细设计

### 5.1 运营后台总体架构

```text
前台用户入口
  ├─ 任务失败页：申请帮助 / 重试 / 退款申请
  ├─ 结果页：质量反馈 / 申诉复核 / 投诉删除
  ├─ 积分中心：账单争议 / 充值问题
  └─ 合规入口：权利人投诉 / 删除请求

后台运营系统
  ├─ Overview 看板
  ├─ 用户与组织查询
  ├─ 任务诊断中心
  ├─ 客服工单中心
  ├─ 人工复核队列
  ├─ 退款补偿中心
  ├─ 投诉删除中心
  ├─ 成本与积分异常看板
  ├─ 规则与话术配置
  └─ 审计日志中心

后端服务
  ├─ Support Ticket Service
  ├─ Manual Review Service
  ├─ Refund Compensation Service
  ├─ Complaint Takedown Service
  ├─ Admin Audit Service
  ├─ Notification Service
  └─ Ops AI Assistant Service

依赖系统
  ├─ D16 数据库/API
  ├─ D17 队列与资产生命周期
  ├─ D18 权限与数据治理
  ├─ D19 积分订单成本结算
  ├─ D20 监控告警
  └─ D21 合规审核与投诉删除
```

### 5.2 后台模块总览

| 模块 | 目标 | P0/P1 | 主要用户 |
|---|---|---:|---|
| Overview 看板 | 运营总览、异常总览、SLA 总览 | P0 | ops_admin、support_lead |
| 用户与组织查询 | 查用户状态、套餐、积分、历史任务 | P0 | support_agent、billing_admin |
| 任务诊断中心 | 排查任务卡住、失败、扣费、风控原因 | P0 | support_agent、tech_support |
| 客服工单中心 | 接收、分类、处理、回复、升级、关闭 | P0 | support_agent、support_lead |
| 人工复核队列 | 风险内容、用户申诉、低置信度、人审决策 | P0 | manual_reviewer、compliance_admin |
| 退款补偿中心 | 释放冻结、返积分、补偿、现金退款申请 | P0 | billing_admin、support_lead |
| 投诉删除中心 | 用户/权利人投诉、暂停展示、删除派生资产 | P0 | compliance_admin、support_lead |
| 审计日志中心 | 后台操作留痕、导出审计、事故追踪 | P0 | read_only_auditor、super_admin |
| 配置中心 | SLA、原因码、话术模板、补偿阈值 | P1 | ops_admin |
| AI 辅助运营 | 分类、摘要、建议、草稿、归因 | P1 | support_agent、reviewer |

### 5.3 客服工单 SOP

#### 5.3.1 工单来源

| 来源 | 创建方式 | 默认分类 |
|---|---|---|
| 任务失败页 | 用户点击“联系客服/申请帮助” | `TASK_FAILURE` |
| 任务进度页 | 系统检测任务超时自动创建 | `TASK_STUCK` |
| 结果页反馈 | 用户点击“结果不满意/质量问题” | `QUALITY_COMPLAINT` |
| 积分中心 | 用户点击“积分/扣费有问题” | `BILLING_DISPUTE` |
| 风控拦截页 | 用户点击“申请人工复核” | `MANUAL_REVIEW_APPEAL` |
| 合规投诉页 | 用户/权利人提交投诉 | `COMPLIANCE_COMPLAINT` |
| 后台创建 | 客服或运营主动创建 | 按实际情况 |
| 系统告警 | 任务失败率/支付异常/模型异常告警 | `SYSTEM_INCIDENT` |

#### 5.3.2 工单标准流程

```text
工单创建
→ 自动绑定 user_id / org_id / task_id / order_id / risk_report_id
→ AI 辅助分类与摘要
→ 系统计算优先级和 SLA
→ 客服领取或系统分派
→ 权限校验并加载最小必要证据包
→ 客服判断处理路径
   ├─ 使用咨询：直接回复
   ├─ 任务失败：查看任务诊断，触发重试 / 返还 / 升级技术
   ├─ 质量投诉：查看输出与评分，触发重写 / 补偿申请 / 拒绝
   ├─ 计费争议：核对订单、积分、成本，触发返还 / 退款申请
   ├─ 风控复核：转人工复核队列
   ├─ 投诉删除：转投诉删除流程
   └─ 安全/隐私：升级合规与安全负责人
→ 用户通知
→ 工单关闭或等待用户确认
→ 满意度收集
→ 数据进入运营统计
```

#### 5.3.3 工单关闭条件

工单只能在满足以下条件之一时关闭：

1. 用户问题已回复并 72 小时内无进一步响应。
2. 任务已重试成功、返还积分、补偿完成或给出明确拒绝原因。
3. 人工复核已给出决策并通知用户。
4. 投诉删除已处理或正式驳回，并保留审计记录。
5. 现金退款已提交支付渠道或明确不符合退款条件。
6. 工单被合并到主工单，子工单标记为 duplicate。

### 5.4 任务诊断 SOP

任务诊断后台必须按以下顺序排查：

1. **基础信息**：task_id、user_id、org_id、创建时间、当前状态、任务类型、视频长度、文件大小、是否测试任务。
2. **授权与输入**：授权确认记录、输入来源声明、文件安全校验结果、风险预检结果。
3. **队列状态**：是否入队、当前步骤、重试次数、死信队列、Worker 节点、超时时间。
4. **处理结果**：抽帧、ASR、OCR、镜头切分、多模态证据包是否完整。
5. **AI 调用**：PromptRun、模型供应商、模型名称、耗时、失败原因、JSON Schema 校验结果。
6. **风控质检**：QualityCheckReport、SimilarityCheckReport、ComplianceCheckReport、RiskCheckReport。
7. **计费状态**：报价、冻结、扣费、返还、成本记录、毛利估算。
8. **导出状态**：导出文件是否生成、签名 URL 是否有效、AIGC 标识是否写入。
9. **可执行动作**：重试、局部重试、释放冻结、返还积分、转人工复核、创建技术工单、创建补偿申请。

任务诊断页不得默认展示完整用户原始视频、完整 ASR/OCR 文本或用户商业敏感字段。默认展示摘要；需要展开时必须记录 `sensitive_view` 审计日志。

### 5.5 人工复核 SOP

#### 5.5.1 复核触发条件

| 触发条件 | 来源 | 默认优先级 |
|---|---|---:|
| R3/R4 风险命中 | D13/D21 风控结果 | P1 |
| 相似度高但可重写 | 相似度风控 | P1 |
| 模型审核置信度低 | 合规/质检 Prompt | P2 |
| 用户申诉风控误拦截 | 风控拦截页 | P2 |
| 权利人投诉 | 投诉入口 | P0/P1 |
| 疑似肖像/声音/商标/隐私问题 | 合规审核 | P0/P1 |
| 多次命中高风险策略 | 用户风险历史 | P1 |
| 企业客户重要任务 | 客户标签 + 风险命中 | P1 |

#### 5.5.2 复核证据包

人工复核证据包必须遵循「最小必要」原则，包含：

1. 任务摘要：task_id、任务类型、用户行业、目标平台、创建时间。
2. 输入摘要：视频来源声明、授权记录、视频元数据摘要、输入意图摘要。
3. 输出摘要：拆解报告摘要、脚本摘要、标题摘要、封面建议摘要、视频 Prompt 摘要。
4. 风控摘要：风险等级、命中规则、相似度分数、不可复用项、合规风险标签。
5. 商业化摘要：冻结积分、已消耗成本、扣费状态、是否可返还。
6. 历史摘要：用户历史风险、历史投诉、历史补偿，默认只显示统计，不显示隐私详情。
7. 审计摘要：相关客服处理记录、系统事件、前置复核结论。

#### 5.5.3 复核决策

| 决策 | 动作 | 用户可见说明 | 积分处理 |
|---|---|---|---|
| `APPROVE` | 放行输出 | 已通过人工复核 | 正常结算 |
| `APPROVE_WITH_NOTICE` | 放行 + 增强提示 | 请发布前再次确认授权、事实和平台规则 | 正常结算 |
| `REWRITE_REQUIRED` | 要求自动重写或用户编辑 | 当前内容相似度或风险偏高，需要重写 | 成功后结算；失败返还未消耗部分 |
| `REQUEST_MORE_EVIDENCE` | 要求补充授权或说明 | 需要补充素材来源或授权证明 | 保持冻结或暂停结算 |
| `REJECT_AND_REFUND` | 拒绝并返还 | 当前请求不符合使用边界，已返还可返还积分 | 返还未消耗部分 |
| `BLOCK_AND_SANCTION` | 拦截并限制账号 | 请求涉及禁止能力或滥用，已限制相关操作 | 按条款处理，通常不补偿违规成本 |
| `TAKEDOWN` | 删除或暂停展示 | 投诉成立或风险过高，已处理相关资产 | 按 D19/D21 规则处理 |

#### 5.5.4 复核时限

| 类型 | 目标处理时间 | 超时动作 |
|---|---:|---|
| P0 隐私/权利人/重大合规 | 4 小时内给初步结论 | 自动升级 compliance_admin + Owner |
| P1 高风险任务/企业客户 | 1 个工作日 | 升级 support_lead + manual_review_lead |
| P2 普通申诉 | 2 个工作日 | 进入超时队列 |
| P3 低风险质量复核 | 5 个工作日 | 可延迟或合并处理 |

### 5.6 退款补偿 SOP

#### 5.6.1 退款补偿类型

| 类型 | 适用场景 | 默认处理 |
|---|---|---|
| `release_frozen_credits` | 任务未正式扣费、取消、前置风控拦截 | 释放冻结 |
| `system_failure_refund` | 系统故障、队列失败、任务卡死无可用输出 | 自动或人工返还积分 |
| `model_failure_refund` | 模型失败且无可用降级输出 | 返还已扣积分或未消耗部分 |
| `duplicate_charge_refund` | 重复冻结、重复扣费、Webhook 重复处理 | 自动返还重复部分并告警 |
| `partial_success_adjustment` | 部分输出成功，部分失败 | 按实际成功模块结算，返还失败模块 |
| `quality_complaint_compensation` | 输出质量经人工确认不达标 | 补偿积分或提供免费重跑 |
| `compliance_block_release` | 风控/合规拦截 | 释放未消耗冻结；违规请求不额外补偿 |
| `legal_complaint_refund` | 权利人投诉或平台通知成立 | 按合规处理和合同条款执行 |
| `cash_refund_request` | 支付错误、重复收款、订阅退款、法定退款 | 进入财务/支付渠道退款审批 |
| `enterprise_sla_credit` | 企业合同 SLA 违约 | 按合同补偿额度或账期处理 |

#### 5.6.2 审批阈值建议

| 补偿/退款动作 | 操作人 | 审批要求 |
|---|---|---|
| 释放冻结 | 系统自动 / support_agent | 无需审批，但必须有日志 |
| 自动返还系统失败积分 | 系统自动 / support_agent | 单任务限额内无需审批 |
| 小额积分补偿 | support_lead | 需原因码和关联工单 |
| 中额积分补偿 | billing_admin + support_lead | 双人审批 |
| 大额积分补偿 | billing_admin + super_admin | 双人审批 + 审计备注 |
| 现金退款 | billing_admin | 需支付渠道记录和财务/法务规则 |
| 企业合同补偿 | ops_admin + billing_admin + Owner | 按合同和账期处理 |
| 投诉成立后的退款 | compliance_admin + billing_admin | 与 takedown 处理关联 |

> 具体金额阈值属于待确认项，MVP1 可先用配置项 `compensation_thresholds` 管理，不写死到代码中。

#### 5.6.3 退款补偿执行流程

```text
创建退款/补偿申请
→ 校验权限
→ 校验关联任务 / 订单 / 积分流水 / 工单
→ 计算可退金额或可补偿积分
→ 校验是否重复退款 / 重复补偿
→ 判断是否需要审批
→ 审批通过
→ 执行释放冻结 / 返还积分 / 调整积分 / 发起现金退款
→ 写入 CreditTransaction / RefundRequest / ManualCreditAdjustment / AuditLog
→ 通知用户
→ 更新工单状态
→ 进入后台统计
```

### 5.7 投诉删除 SOP

#### 5.7.1 投诉入口

MVP1 必须提供以下入口：

1. 用户端：我的任务 / 结果页 / 设置页中的删除与投诉入口。
2. 权利人入口：公开投诉表单或客服邮箱入口，可提交权利类型、证据、联系方式、涉及任务或导出链接。
3. 后台入口：客服代用户创建投诉案件。
4. 系统入口：风控规则命中严重风险自动创建投诉/删除候选。

#### 5.7.2 投诉类型

| 类型 | 示例 | 初始动作 |
|---|---|---|
| `COPYRIGHT` | 原视频、文案、图片、音乐疑似复用 | 暂停相关导出展示，转人工 |
| `TRADEMARK` | Logo、商标、品牌元素争议 | 暂停相关输出，转人工 |
| `LIKENESS` | 肖像、形象争议 | 限制访问，转人工 |
| `VOICE` | 声音、音色争议 | 限制访问，转人工 |
| `PRIVACY` | 个人信息泄露 | 立即限制访问，升级安全负责人 |
| `AIGC_LABEL` | 缺少或错误 AIGC 标识 | 补标识或暂停导出 |
| `FALSE_ADVERTISING` | 虚假宣传、夸大承诺 | 暂停输出，转人工 |
| `PLATFORM_RULE` | 平台规则、搬运争议 | 暂停输出，转人工 |
| `OTHER` | 其他争议 | 初筛后分类 |

#### 5.7.3 投诉处理流程

```text
投诉提交
→ 系统生成 complaint_case
→ 自动确认是否有公开链接/导出文件/任务资产
→ P0/P1 风险先暂停展示或限制访问
→ AI 辅助初筛：分类、摘要、证据完整度
→ 合规人员人工复核
→ 决策：驳回 / 请求补充材料 / 部分删除 / 全量删除 / 用户限制 / 退款补偿
→ 若删除：创建 takedown_job
→ 执行派生资产清理
→ 审计日志记录必要哈希、任务 ID、风险类型、处理结论
→ 通知用户和投诉人
→ 工单关闭或进入申诉期
```

#### 5.7.4 删除范围

投诉成立或用户删除任务时，必须按 D17/D18/D21 的规则清理派生资产，包括：

1. 原始上传视频。
2. 代理视频。
3. 抽帧图片。
4. 音频文件。
5. ASR/OCR 文本。
6. 多模态证据包。
7. Content DNA。
8. Creative IR。
9. 原创脚本、标题、封面建议、视频 Prompt。
10. 导出文件。
11. 公开分享链接。
12. CDN 缓存与搜索索引。
13. 后台预览缓存。

审计日志不得保留完整侵权内容，只保留必要元数据、哈希、任务 ID、风险类型、处理结论、时间和操作者。

### 5.8 AI 辅助客服与运营设计

AI 辅助能力必须遵守：

1. AI 只提供分类、摘要、建议、草稿，不做最终退款、复核、删除、封禁决策。
2. AI 输出必须显示置信度和依据字段。
3. AI 不能向客服建议绕过风控、降低原创化、复制原素材、仿脸仿声。
4. AI 不能暴露用户隐私字段；输入给 AI 前必须脱敏。
5. AI 建议被采纳、修改、拒绝都要记录，用于后续评测。
6. 低置信度、模型失败、Schema 不合法时回退到人工模板。

---

## 6. 数据结构 / API / Prompt / 流程 / 页面 / 状态

### 6.1 核心枚举

#### 6.1.1 工单枚举

```ts
export enum SupportTicketCategory {
  TASK_FAILURE = 'TASK_FAILURE',
  TASK_STUCK = 'TASK_STUCK',
  QUALITY_COMPLAINT = 'QUALITY_COMPLAINT',
  BILLING_DISPUTE = 'BILLING_DISPUTE',
  REFUND_REQUEST = 'REFUND_REQUEST',
  MANUAL_REVIEW_APPEAL = 'MANUAL_REVIEW_APPEAL',
  COMPLIANCE_COMPLAINT = 'COMPLIANCE_COMPLAINT',
  DATA_DELETE_REQUEST = 'DATA_DELETE_REQUEST',
  ACCOUNT_ACCESS = 'ACCOUNT_ACCESS',
  HOW_TO_USE = 'HOW_TO_USE',
  FEATURE_REQUEST = 'FEATURE_REQUEST',
  SYSTEM_INCIDENT = 'SYSTEM_INCIDENT',
  OTHER = 'OTHER'
}

export enum SupportTicketStatus {
  OPEN = 'OPEN',
  TRIAGED = 'TRIAGED',
  ASSIGNED = 'ASSIGNED',
  WAITING_USER = 'WAITING_USER',
  WAITING_INTERNAL = 'WAITING_INTERNAL',
  WAITING_MANUAL_REVIEW = 'WAITING_MANUAL_REVIEW',
  WAITING_REFUND_APPROVAL = 'WAITING_REFUND_APPROVAL',
  WAITING_TAKEDOWN = 'WAITING_TAKEDOWN',
  RESOLVED = 'RESOLVED',
  CLOSED = 'CLOSED',
  DUPLICATE = 'DUPLICATE',
  ESCALATED = 'ESCALATED'
}

export enum SupportPriority {
  P0 = 'P0',
  P1 = 'P1',
  P2 = 'P2',
  P3 = 'P3'
}
```

#### 6.1.2 退款补偿枚举

```ts
export enum RefundCompensationType {
  RELEASE_FROZEN_CREDITS = 'RELEASE_FROZEN_CREDITS',
  SYSTEM_FAILURE_REFUND = 'SYSTEM_FAILURE_REFUND',
  MODEL_FAILURE_REFUND = 'MODEL_FAILURE_REFUND',
  DUPLICATE_CHARGE_REFUND = 'DUPLICATE_CHARGE_REFUND',
  PARTIAL_SUCCESS_ADJUSTMENT = 'PARTIAL_SUCCESS_ADJUSTMENT',
  QUALITY_COMPLAINT_COMPENSATION = 'QUALITY_COMPLAINT_COMPENSATION',
  COMPLIANCE_BLOCK_RELEASE = 'COMPLIANCE_BLOCK_RELEASE',
  LEGAL_COMPLAINT_REFUND = 'LEGAL_COMPLAINT_REFUND',
  CASH_REFUND_REQUEST = 'CASH_REFUND_REQUEST',
  ENTERPRISE_SLA_CREDIT = 'ENTERPRISE_SLA_CREDIT'
}

export enum RefundCompensationStatus {
  DRAFT = 'DRAFT',
  PENDING_APPROVAL = 'PENDING_APPROVAL',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
  EXECUTING = 'EXECUTING',
  EXECUTED = 'EXECUTED',
  FAILED = 'FAILED',
  CANCELLED = 'CANCELLED'
}
```

#### 6.1.3 人工复核枚举

```ts
export enum ManualReviewStatus {
  PENDING = 'PENDING',
  CLAIMED = 'CLAIMED',
  IN_REVIEW = 'IN_REVIEW',
  REQUESTING_MORE_EVIDENCE = 'REQUESTING_MORE_EVIDENCE',
  DECIDED = 'DECIDED',
  EXECUTING_ACTION = 'EXECUTING_ACTION',
  COMPLETED = 'COMPLETED',
  EXPIRED = 'EXPIRED',
  CANCELLED = 'CANCELLED'
}

export enum ManualReviewDecision {
  APPROVE = 'APPROVE',
  APPROVE_WITH_NOTICE = 'APPROVE_WITH_NOTICE',
  REWRITE_REQUIRED = 'REWRITE_REQUIRED',
  REQUEST_MORE_EVIDENCE = 'REQUEST_MORE_EVIDENCE',
  REJECT_AND_REFUND = 'REJECT_AND_REFUND',
  BLOCK_AND_SANCTION = 'BLOCK_AND_SANCTION',
  TAKEDOWN = 'TAKEDOWN'
}
```

#### 6.1.4 投诉删除枚举

```ts
export enum ComplaintType {
  COPYRIGHT = 'COPYRIGHT',
  TRADEMARK = 'TRADEMARK',
  LIKENESS = 'LIKENESS',
  VOICE = 'VOICE',
  PRIVACY = 'PRIVACY',
  AIGC_LABEL = 'AIGC_LABEL',
  FALSE_ADVERTISING = 'FALSE_ADVERTISING',
  PLATFORM_RULE = 'PLATFORM_RULE',
  OTHER = 'OTHER'
}

export enum ComplaintCaseStatus {
  SUBMITTED = 'SUBMITTED',
  TRIAGED = 'TRIAGED',
  NEED_MORE_EVIDENCE = 'NEED_MORE_EVIDENCE',
  ACCESS_RESTRICTED = 'ACCESS_RESTRICTED',
  IN_MANUAL_REVIEW = 'IN_MANUAL_REVIEW',
  REJECTED = 'REJECTED',
  TAKEDOWN_PENDING = 'TAKEDOWN_PENDING',
  TAKEDOWN_EXECUTED = 'TAKEDOWN_EXECUTED',
  RESOLVED = 'RESOLVED',
  APPEALED = 'APPEALED'
}
```

### 6.2 核心 JSON Schema

#### 6.2.1 `SupportTicket` Schema

```json
{
  "type": "object",
  "required": ["ticket_id", "user_id", "category", "status", "priority", "created_at"],
  "properties": {
    "ticket_id": { "type": "string" },
    "org_id": { "type": ["string", "null"] },
    "user_id": { "type": "string" },
    "created_by": { "type": "string", "enum": ["USER", "ADMIN", "SYSTEM"] },
    "category": { "type": "string" },
    "status": { "type": "string" },
    "priority": { "type": "string" },
    "subject": { "type": "string", "maxLength": 160 },
    "description": { "type": "string", "maxLength": 8000 },
    "related_task_id": { "type": ["string", "null"] },
    "related_order_id": { "type": ["string", "null"] },
    "related_risk_report_id": { "type": ["string", "null"] },
    "related_complaint_id": { "type": ["string", "null"] },
    "assigned_to": { "type": ["string", "null"] },
    "sla_due_at": { "type": ["string", "null"], "format": "date-time" },
    "ai_triage": { "$ref": "#/definitions/AITriageResult" },
    "resolution_summary": { "type": ["string", "null"] },
    "closed_at": { "type": ["string", "null"], "format": "date-time" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  },
  "definitions": {
    "AITriageResult": {
      "type": "object",
      "properties": {
        "suggested_category": { "type": "string" },
        "suggested_priority": { "type": "string" },
        "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
        "summary": { "type": "string" },
        "required_actions": { "type": "array", "items": { "type": "string" } },
        "risk_flags": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
```

#### 6.2.2 `AdminCaseEvidencePack` Schema

```json
{
  "type": "object",
  "required": ["evidence_pack_id", "case_type", "case_id", "data_minimization_level", "created_at"],
  "properties": {
    "evidence_pack_id": { "type": "string" },
    "case_type": { "type": "string", "enum": ["SUPPORT", "MANUAL_REVIEW", "REFUND", "COMPLAINT"] },
    "case_id": { "type": "string" },
    "data_minimization_level": { "type": "string", "enum": ["SUMMARY_ONLY", "MASKED_DETAIL", "FULL_DETAIL_APPROVED"] },
    "task_summary": { "type": "object" },
    "authorization_summary": { "type": "object" },
    "processing_summary": { "type": "object" },
    "ai_output_summary": { "type": "object" },
    "risk_summary": { "type": "object" },
    "billing_summary": { "type": "object" },
    "audit_summary": { "type": "object" },
    "sensitive_fields_included": { "type": "array", "items": { "type": "string" } },
    "access_reason": { "type": "string" },
    "created_by": { "type": "string" },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
```

#### 6.2.3 `RefundCompensationRequest` Schema

```json
{
  "type": "object",
  "required": ["request_id", "user_id", "type", "status", "amount_credits", "reason_code"],
  "properties": {
    "request_id": { "type": "string" },
    "user_id": { "type": "string" },
    "org_id": { "type": ["string", "null"] },
    "type": { "type": "string" },
    "status": { "type": "string" },
    "related_task_id": { "type": ["string", "null"] },
    "related_order_id": { "type": ["string", "null"] },
    "related_ticket_id": { "type": ["string", "null"] },
    "amount_credits": { "type": "integer", "minimum": 0 },
    "amount_cash_minor": { "type": "integer", "minimum": 0 },
    "currency": { "type": "string", "default": "USD" },
    "reason_code": { "type": "string" },
    "reason_detail": { "type": "string" },
    "calculation_snapshot": { "type": "object" },
    "requires_approval": { "type": "boolean" },
    "approved_by": { "type": ["string", "null"] },
    "executed_by": { "type": ["string", "null"] },
    "idempotency_key": { "type": "string" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
```

#### 6.2.4 `ManualReviewTicket` Schema

```json
{
  "type": "object",
  "required": ["review_id", "source", "status", "priority", "risk_level", "created_at"],
  "properties": {
    "review_id": { "type": "string" },
    "source": { "type": "string", "enum": ["RISK_ENGINE", "USER_APPEAL", "SUPPORT_ESCALATION", "COMPLAINT", "SYSTEM_LOW_CONFIDENCE"] },
    "status": { "type": "string" },
    "priority": { "type": "string" },
    "risk_level": { "type": "string" },
    "related_task_id": { "type": ["string", "null"] },
    "related_ticket_id": { "type": ["string", "null"] },
    "related_complaint_id": { "type": ["string", "null"] },
    "evidence_pack_id": { "type": "string" },
    "assigned_to": { "type": ["string", "null"] },
    "decision": { "type": ["string", "null"] },
    "decision_reason_code": { "type": ["string", "null"] },
    "decision_note": { "type": ["string", "null"] },
    "user_visible_message": { "type": ["string", "null"] },
    "billing_action": { "type": ["string", "null"] },
    "decided_by": { "type": ["string", "null"] },
    "decided_at": { "type": ["string", "null"], "format": "date-time" },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
```

#### 6.2.5 `ComplaintCase` Schema

```json
{
  "type": "object",
  "required": ["complaint_id", "complaint_type", "status", "submitter_type", "created_at"],
  "properties": {
    "complaint_id": { "type": "string" },
    "complaint_type": { "type": "string" },
    "status": { "type": "string" },
    "submitter_type": { "type": "string", "enum": ["USER", "RIGHTSHOLDER", "PLATFORM", "INTERNAL", "OTHER"] },
    "submitter_contact_hash": { "type": ["string", "null"] },
    "related_task_id": { "type": ["string", "null"] },
    "related_asset_id": { "type": ["string", "null"] },
    "related_export_id": { "type": ["string", "null"] },
    "public_url": { "type": ["string", "null"] },
    "claim_summary": { "type": "string" },
    "evidence_files": { "type": "array", "items": { "type": "string" } },
    "initial_action": { "type": ["string", "null"] },
    "manual_review_id": { "type": ["string", "null"] },
    "takedown_job_id": { "type": ["string", "null"] },
    "resolution": { "type": ["string", "null"] },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
```

### 6.3 数据库表草案

> 最终字段、索引、外键、约束由 D16 回写固化。本节是 D22 运营领域的最小工程草案。

#### 6.3.1 `support_tickets`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 工单 ID |
| `org_id` | uuid nullable | 组织 ID |
| `user_id` | uuid | 用户 ID |
| `created_by_type` | enum | USER / ADMIN / SYSTEM |
| `created_by_id` | uuid nullable | 创建人 |
| `category` | enum | 工单分类 |
| `status` | enum | 工单状态 |
| `priority` | enum | P0-P3 |
| `subject` | varchar | 标题 |
| `description` | text | 描述，需脱敏存储 |
| `related_task_id` | uuid nullable | 关联任务 |
| `related_order_id` | uuid nullable | 关联订单 |
| `related_risk_report_id` | uuid nullable | 关联风险报告 |
| `assigned_to` | uuid nullable | 当前处理人 |
| `sla_due_at` | timestamptz nullable | SLA 截止 |
| `ai_triage_json` | jsonb | AI 分类结果 |
| `resolution_summary` | text nullable | 解决摘要 |
| `closed_at` | timestamptz nullable | 关闭时间 |
| `created_at / updated_at / deleted_at` | timestamptz | 通用字段 |

索引建议：`user_id`、`status`、`priority`、`category`、`assigned_to`、`related_task_id`、`sla_due_at`。

#### 6.3.2 `support_ticket_messages`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 消息 ID |
| `ticket_id` | uuid | 工单 ID |
| `sender_type` | enum | USER / ADMIN / SYSTEM / AI_ASSISTANT |
| `sender_id` | uuid nullable | 发送者 |
| `message_type` | enum | PUBLIC_REPLY / INTERNAL_NOTE / STATUS_EVENT / AI_DRAFT |
| `body` | text | 内容，敏感字段需脱敏 |
| `attachments_json` | jsonb | 附件引用 |
| `is_visible_to_user` | boolean | 用户是否可见 |
| `created_at` | timestamptz | 创建时间 |

#### 6.3.3 `admin_case_evidence_packs`

用于保存工单、复核、投诉、退款处理时的证据快照。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 证据包 ID |
| `case_type` | enum | SUPPORT / MANUAL_REVIEW / REFUND / COMPLAINT |
| `case_id` | uuid | 案件 ID |
| `data_minimization_level` | enum | SUMMARY_ONLY / MASKED_DETAIL / FULL_DETAIL_APPROVED |
| `snapshot_json` | jsonb | 摘要快照 |
| `sensitive_fields_included` | jsonb | 敏感字段列表 |
| `access_reason` | text | 创建原因 |
| `created_by` | uuid | 创建者 |
| `created_at` | timestamptz | 创建时间 |

#### 6.3.4 `manual_review_tickets`

如果 D13/D21/D16 已定义同名表，D22 不重复建表，只补充运营字段。

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 复核 ID |
| `source` | enum | 来源 |
| `status` | enum | 状态 |
| `priority` | enum | 优先级 |
| `risk_level` | enum | 风险等级 |
| `related_task_id` | uuid nullable | 关联任务 |
| `related_ticket_id` | uuid nullable | 关联工单 |
| `related_complaint_id` | uuid nullable | 关联投诉 |
| `evidence_pack_id` | uuid | 证据包 |
| `assigned_to` | uuid nullable | 复核员 |
| `decision` | enum nullable | 决策 |
| `decision_reason_code` | varchar nullable | 原因码 |
| `decision_note` | text nullable | 内部说明 |
| `user_visible_message` | text nullable | 用户可见说明 |
| `billing_action` | varchar nullable | 计费动作 |
| `decided_by` | uuid nullable | 决策人 |
| `decided_at` | timestamptz nullable | 决策时间 |
| `created_at / updated_at` | timestamptz | 通用字段 |

#### 6.3.5 `refund_compensation_requests`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 申请 ID |
| `user_id` | uuid | 用户 ID |
| `org_id` | uuid nullable | 组织 ID |
| `type` | enum | 补偿/退款类型 |
| `status` | enum | 状态 |
| `related_task_id` | uuid nullable | 关联任务 |
| `related_order_id` | uuid nullable | 关联订单 |
| `related_ticket_id` | uuid nullable | 关联工单 |
| `amount_credits` | integer | 积分数量 |
| `amount_cash_minor` | integer | 现金退款最小货币单位 |
| `currency` | varchar | 币种 |
| `reason_code` | varchar | 原因码 |
| `reason_detail` | text | 原因说明 |
| `calculation_snapshot` | jsonb | 计算快照 |
| `requires_approval` | boolean | 是否需审批 |
| `approved_by` | uuid nullable | 审批人 |
| `executed_by` | uuid nullable | 执行人 |
| `idempotency_key` | varchar unique | 幂等键 |
| `created_at / updated_at` | timestamptz | 通用字段 |

#### 6.3.6 `manual_credit_adjustments`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 调整 ID |
| `user_id` | uuid | 用户 ID |
| `credit_account_id` | uuid | 积分账户 |
| `adjustment_type` | enum | CREDIT_GRANT / CREDIT_DEDUCT / CREDIT_RELEASE |
| `amount` | integer | 积分数 |
| `reason_code` | varchar | 原因码 |
| `related_request_id` | uuid | 退款补偿申请 |
| `related_ticket_id` | uuid nullable | 工单 |
| `before_balance` | integer | 调整前余额 |
| `after_balance` | integer | 调整后余额 |
| `approved_by` | uuid nullable | 审批人 |
| `executed_by` | uuid | 执行人 |
| `created_at` | timestamptz | 创建时间 |

#### 6.3.7 `complaint_cases`

字段参考 6.2.5；需要额外索引：`status`、`complaint_type`、`related_task_id`、`created_at`、`public_url_hash`。

#### 6.3.8 `ops_audit_logs`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | uuid | 日志 ID |
| `actor_id` | uuid | 操作人 |
| `actor_role` | varchar | 操作人角色 |
| `action` | varchar | 操作动作 |
| `resource_type` | varchar | 资源类型 |
| `resource_id` | uuid | 资源 ID |
| `before_snapshot` | jsonb nullable | 操作前摘要 |
| `after_snapshot` | jsonb nullable | 操作后摘要 |
| `reason_code` | varchar nullable | 原因码 |
| `ip_hash` | varchar nullable | IP 哈希 |
| `user_agent_hash` | varchar nullable | UA 哈希 |
| `sensitive_view_level` | varchar nullable | 敏感访问等级 |
| `created_at` | timestamptz | 创建时间 |

审计日志应使用追加写，不允许普通后台用户修改或删除。

### 6.4 API 契约草案

#### 6.4.1 用户端 API

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| POST | `/api/v1/support/tickets` | 创建工单 | 登录用户 |
| GET | `/api/v1/support/tickets` | 查看我的工单列表 | 登录用户 |
| GET | `/api/v1/support/tickets/{ticket_id}` | 查看我的工单详情 | 工单所有者 |
| POST | `/api/v1/support/tickets/{ticket_id}/messages` | 回复工单 | 工单所有者 |
| POST | `/api/v1/tasks/{task_id}/refund-request` | 针对任务申请退款/补偿 | 任务所有者 |
| POST | `/api/v1/tasks/{task_id}/manual-review-appeal` | 风控结果申诉 | 任务所有者 |
| POST | `/api/v1/complaints` | 提交投诉/权利人投诉 | 公开/登录均可，需风控 |
| POST | `/api/v1/data-delete-requests` | 用户删除请求 | 登录用户 |

#### 6.4.2 后台 API

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| GET | `/api/admin/overview` | 运营总览 | `ops_admin` |
| GET | `/api/admin/users` | 用户搜索 | `support_agent+` |
| GET | `/api/admin/users/{user_id}` | 用户摘要 | `support_agent+`，敏感脱敏 |
| GET | `/api/admin/tasks/{task_id}/diagnostics` | 任务诊断详情 | `support_agent+` |
| POST | `/api/admin/tasks/{task_id}/retry` | 触发重试 | `support_lead+`，需原因 |
| GET | `/api/admin/support-tickets` | 工单列表 | `support_agent+` |
| POST | `/api/admin/support-tickets` | 后台创建工单 | `support_agent+` |
| PATCH | `/api/admin/support-tickets/{ticket_id}` | 更新状态/分派/优先级 | `support_agent+` |
| POST | `/api/admin/support-tickets/{ticket_id}/messages` | 回复或内部备注 | `support_agent+` |
| POST | `/api/admin/support-tickets/{ticket_id}/escalate` | 升级工单 | `support_agent+` |
| GET | `/api/admin/manual-reviews` | 复核队列 | `manual_reviewer+` |
| POST | `/api/admin/manual-reviews/{review_id}/claim` | 领取复核 | `manual_reviewer+` |
| POST | `/api/admin/manual-reviews/{review_id}/decision` | 提交复核决策 | `manual_reviewer+` 或 `compliance_admin` |
| GET | `/api/admin/refund-compensations` | 补偿/退款列表 | `billing_admin`、`support_lead` |
| POST | `/api/admin/refund-compensations` | 创建补偿申请 | `support_agent+` |
| POST | `/api/admin/refund-compensations/{id}/approve` | 审批 | `billing_admin+` |
| POST | `/api/admin/refund-compensations/{id}/execute` | 执行 | `billing_admin+` 或系统 Worker |
| GET | `/api/admin/complaints` | 投诉列表 | `compliance_admin+` |
| POST | `/api/admin/complaints/{id}/decision` | 投诉决策 | `compliance_admin+` |
| POST | `/api/admin/complaints/{id}/takedown` | 触发删除 | `compliance_admin+` |
| GET | `/api/admin/audit-logs` | 审计日志 | `read_only_auditor+` |
| GET | `/api/admin/config/support` | SLA/原因码/话术配置 | `ops_admin+` |
| PATCH | `/api/admin/config/support` | 更新配置 | `ops_admin+`，需审计 |

#### 6.4.3 Internal Worker API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/internal/ops/events` | 任务/风控/支付系统创建运营事件 |
| POST | `/api/internal/refund-compensations/{id}/execute` | 执行积分返还或释放冻结 |
| POST | `/api/internal/complaints/{id}/restrict-access` | 限制相关资产访问 |
| POST | `/api/internal/takedown-jobs/{job_id}/execute` | 执行资产删除 |
| POST | `/api/internal/notifications/send` | 发送用户通知 |
| POST | `/api/internal/support-ai/run` | 执行 AI 分类/摘要/草稿 |

#### 6.4.4 标准错误码

| 错误码 | 含义 | 用户/后台动作 |
|---|---|---|
| `OPS_PERMISSION_DENIED` | 无后台权限 | 提示联系管理员 |
| `OPS_SENSITIVE_ACCESS_DENIED` | 无权查看敏感证据 | 申请升级权限 |
| `TICKET_NOT_FOUND` | 工单不存在 | 检查 ID |
| `TICKET_ALREADY_CLOSED` | 工单已关闭 | 需要重新打开 |
| `REVIEW_ALREADY_DECIDED` | 复核已决策 | 不允许重复决策 |
| `REFUND_DUPLICATE_REQUEST` | 重复退款/补偿申请 | 返回已有申请 |
| `REFUND_EXCEEDS_ALLOWED_AMOUNT` | 超过可退额度 | 需审批或拒绝 |
| `CREDIT_ADJUSTMENT_FAILED` | 积分调整失败 | 进入补偿失败队列 |
| `COMPLAINT_EVIDENCE_REQUIRED` | 投诉证据不足 | 请求补充材料 |
| `TAKEDOWN_ALREADY_EXECUTED` | 已执行删除 | 返回历史记录 |
| `AUDIT_LOG_WRITE_FAILED` | 审计日志写入失败 | 阻断高风险操作 |
| `AI_TRIAGE_FAILED` | AI 分类失败 | 回退人工分类 |

### 6.5 任务队列设计

| 队列 | 作用 | 生产者 | 消费者 |
|---|---|---|---|
| `support_ai_triage_queue` | 工单 AI 分类、摘要、建议 | 工单服务 | Ops AI Worker |
| `manual_review_queue` | 人工复核创建和 SLA 监控 | 风控/客服/投诉 | 复核服务 |
| `refund_compensation_queue` | 退款补偿异步执行 | 后台/系统 | Billing Worker |
| `complaint_takedown_queue` | 投诉删除和资产清理 | 投诉服务 | Takedown Worker |
| `ops_notification_queue` | 用户通知、邮件、站内信 | 工单/复核/退款 | Notification Worker |
| `ops_audit_export_queue` | 审计日志导出 | 审计后台 | Export Worker |
| `ops_sla_monitor_queue` | SLA 超时扫描 | 定时任务 | Ops Scheduler |
| `ops_metrics_rollup_queue` | 运营指标聚合 | 定时任务 | Metrics Worker |

### 6.6 Prompt 设计

#### 6.6.1 Prompt 总原则

D22 涉及的 AI Prompt 只用于运营辅助，不得自动执行最终业务动作。所有 Prompt 必须：

1. 有 Prompt ID、版本、输入 Schema、输出 Schema。
2. 记录 PromptRun、ModelCallLog、成本、延迟、失败原因。
3. 输出 JSON，失败时允许 JSON repair，但不得伪造字段。
4. 对用户隐私、支付信息、原始视频内容进行脱敏后再输入模型。
5. 禁止输出绕过风控、规避平台、复制素材、仿脸仿声等建议。
6. 低置信度输出必须交由人工处理。

#### 6.6.2 `P22_SUPPORT_TRIAGE`：工单分类与摘要 Prompt

**用途**：根据用户工单、关联任务摘要、积分摘要、风险摘要，输出工单分类、优先级、摘要和建议处理路径。

**输入 Schema**：

```json
{
  "ticket_text": "用户描述，已脱敏",
  "user_plan_summary": "用户套餐摘要",
  "task_summary": "任务状态摘要，可为空",
  "billing_summary": "积分/订单摘要，可为空",
  "risk_summary": "风险摘要，可为空",
  "history_summary": "用户历史工单摘要，可为空"
}
```

**输出 Schema**：

```json
{
  "suggested_category": "TASK_FAILURE | QUALITY_COMPLAINT | BILLING_DISPUTE | MANUAL_REVIEW_APPEAL | COMPLIANCE_COMPLAINT | HOW_TO_USE | OTHER",
  "suggested_priority": "P0 | P1 | P2 | P3",
  "confidence": 0.0,
  "summary": "不超过 120 字的工单摘要",
  "required_actions": ["CHECK_TASK_DIAGNOSTICS", "CHECK_BILLING_LEDGER", "ESCALATE_MANUAL_REVIEW"],
  "risk_flags": ["POSSIBLE_DUPLICATE_CHARGE"],
  "user_visible_reply_draft": "客服可编辑的回复草稿",
  "internal_notes": "内部处理建议"
}
```

**Prompt 模板**：

```text
你是 AI 短视频爆款增长引擎的运营客服辅助系统。请根据输入信息对工单进行分类、摘要和处理建议。

产品边界：本产品只做爆款结构学习与原创化生成，不支持搬运、克隆、洗稿、仿脸、仿声、绕过平台规则或未授权素材复用。若用户诉求涉及这些能力，必须标记风险并建议人工复核或拒绝。

要求：
1. 只输出合法 JSON。
2. 不要承诺退款、放行、删除或法律结论；只能给客服建议。
3. 对支付、隐私、权利人投诉、重大系统故障标记高优先级。
4. 如果信息不足，required_actions 中加入 REQUEST_MORE_INFO。
5. 用户可见回复必须礼貌、具体、避免法律绝对化承诺。

输入：
{{input_json}}

输出 JSON Schema：
{{output_schema}}
```

**质量标准**：分类准确率 ≥ 85%；P0/P1 风险召回率 ≥ 95%；不得输出违规承诺；JSON 合法率 ≥ 99%。

**失败回退**：AI 调用失败、置信度 < 0.65、JSON 不合法时，工单进入人工待分类状态，使用静态分类模板。

#### 6.6.3 `P22_REFUND_SUGGESTION`：补偿建议 Prompt

**用途**：根据任务状态、成本、积分、失败原因、用户历史和规则配置，给客服提供补偿建议。

**输出 Schema**：

```json
{
  "suggested_action": "NO_REFUND | RELEASE_FROZEN | REFUND_CREDITS | COMPENSATE_CREDITS | FREE_RERUN | CASH_REFUND_REVIEW | ESCALATE",
  "confidence": 0.0,
  "reason_code": "SYSTEM_FAILURE | MODEL_FAILURE | USER_CANCELLED | QUALITY_CONFIRMED | POLICY_BLOCK | DUPLICATE_CHARGE | UNKNOWN",
  "calculation": {
    "frozen_credits": 0,
    "charged_credits": 0,
    "model_cost_estimate": 0,
    "refundable_credits": 0,
    "suggested_compensation_credits": 0
  },
  "requires_approval": true,
  "internal_note": "建议说明",
  "user_visible_message_draft": "给用户的解释草稿"
}
```

**特别限制**：该 Prompt 不能直接执行补偿，不能覆盖 D19 计费规则，不能建议对违规请求额外补偿。

#### 6.6.4 `P22_MANUAL_REVIEW_SUMMARY`：人工复核摘要 Prompt

**用途**：将风险报告、任务摘要、输出摘要整理为人工复核员可读证据摘要。

**输出 Schema**：

```json
{
  "case_summary": "复核案件摘要",
  "risk_reasons": ["相似度偏高", "疑似未授权素材"],
  "safe_to_show_fields": ["script_summary", "risk_score"],
  "needs_sensitive_access": false,
  "recommended_decision": "APPROVE_WITH_NOTICE | REWRITE_REQUIRED | REQUEST_MORE_EVIDENCE | REJECT_AND_REFUND | ESCALATE",
  "confidence": 0.0,
  "reviewer_checklist": ["确认授权记录", "查看相似度报告"]
}
```

#### 6.6.5 `P22_COMPLAINT_TRIAGE`：投诉初筛 Prompt

**用途**：对投诉内容进行分类、证据完整度检查、初始动作建议。

**输出 Schema**：

```json
{
  "complaint_type": "COPYRIGHT | TRADEMARK | LIKENESS | VOICE | PRIVACY | AIGC_LABEL | FALSE_ADVERTISING | PLATFORM_RULE | OTHER",
  "priority": "P0 | P1 | P2 | P3",
  "evidence_completeness": "SUFFICIENT | PARTIAL | INSUFFICIENT",
  "initial_action": "RESTRICT_ACCESS | REQUEST_MORE_EVIDENCE | MANUAL_REVIEW | REJECT_AS_SPAM",
  "confidence": 0.0,
  "summary": "投诉摘要",
  "missing_evidence": ["权利证明", "涉及链接"],
  "internal_note": "内部建议"
}
```

#### 6.6.6 `P22_ADMIN_REPLY_DRAFT`：客服回复草稿 Prompt

**用途**：根据处理结果生成客服可编辑回复草稿。

**限制**：必须避免承诺法律结论、保证流量结果、保证平台通过、承诺绕过风控、承诺现金退款，必须使用“我们将/已经根据规则处理”的中性表述。

### 6.7 页面设计

#### 6.7.1 `/admin/overview` 运营总览

必须展示：

1. 今日新增工单、待处理工单、超时工单。
2. 任务失败率、任务卡住数、死信队列数。
3. 今日返还积分、人工补偿积分、现金退款申请数。
4. 人工复核待处理、通过率、重写率、拒绝率。
5. 投诉案件数、已限制访问数、删除任务数。
6. 模型成本异常、单任务亏损 TopN。
7. P0/P1 告警列表。

#### 6.7.2 `/admin/tasks/{task_id}` 任务诊断页

页面区块：

1. 任务基础信息。
2. 用户和套餐摘要。
3. 输入与授权摘要。
4. 任务状态机和步骤日志。
5. 视频处理产物摘要。
6. PromptRun 和 ModelCallLog。
7. 成本与积分流水。
8. 风控/质检/合规报告。
9. 导出文件状态。
10. 可执行操作：创建工单、局部重试、释放冻结、创建补偿申请、转人工复核、限制访问。

#### 6.7.3 `/admin/support-tickets` 工单中心

列表字段：

1. ticket_id。
2. 用户摘要。
3. 分类。
4. 优先级。
5. 状态。
6. SLA。
7. 关联任务/订单/投诉。
8. 处理人。
9. 最近更新时间。

详情页必须包含：消息线程、内部备注、证据包、AI 摘要、操作历史、关联退款/复核/投诉。

#### 6.7.4 `/admin/manual-reviews` 人工复核页

必须展示：

1. 待复核队列。
2. 风险等级和触发原因。
3. 最小必要证据包。
4. 复核决策按钮。
5. 用户可见说明编辑框。
6. 计费动作选择。
7. 决策确认弹窗。
8. 审计日志。

#### 6.7.5 `/admin/refunds` 退款补偿页

必须展示：

1. 退款补偿申请列表。
2. 关联任务、订单、工单。
3. 可退积分/现金计算结果。
4. 是否需审批。
5. 审批流。
6. 执行状态。
7. 前后积分余额。
8. 审计日志。

#### 6.7.6 `/admin/complaints` 投诉删除页

必须展示：

1. 投诉类型、状态、优先级。
2. 投诉人类型和联系方式哈希。
3. 涉及任务、资产、导出链接。
4. 初筛结果和证据完整度。
5. 是否已限制访问。
6. 人工复核记录。
7. 删除任务状态。
8. 用户/投诉人通知记录。
9. 审计日志。

### 6.8 状态机

#### 6.8.1 工单状态机

```text
OPEN
→ TRIAGED
→ ASSIGNED
→ WAITING_USER / WAITING_INTERNAL / WAITING_MANUAL_REVIEW / WAITING_REFUND_APPROVAL / WAITING_TAKEDOWN
→ RESOLVED
→ CLOSED

任意非 CLOSED 状态 → ESCALATED
任意状态 → DUPLICATE（需关联主工单）
CLOSED → REOPENED（可映射为 OPEN，需记录 reopen_reason）
```

#### 6.8.2 退款补偿状态机

```text
DRAFT
→ PENDING_APPROVAL
→ APPROVED / REJECTED
→ EXECUTING
→ EXECUTED / FAILED

FAILED → PENDING_APPROVAL（人工修复后重试）
APPROVED → CANCELLED（未执行前可取消，需审批）
```

#### 6.8.3 人工复核状态机

```text
PENDING
→ CLAIMED
→ IN_REVIEW
→ REQUESTING_MORE_EVIDENCE / DECIDED
→ EXECUTING_ACTION
→ COMPLETED

PENDING/CLAIMED/IN_REVIEW 超时 → EXPIRED + ESCALATED
```

#### 6.8.4 投诉案件状态机

```text
SUBMITTED
→ TRIAGED
→ NEED_MORE_EVIDENCE / ACCESS_RESTRICTED / IN_MANUAL_REVIEW
→ REJECTED / TAKEDOWN_PENDING
→ TAKEDOWN_EXECUTED
→ RESOLVED

RESOLVED → APPEALED（有申诉时）
```

---

## 7. 异常与失败处理

### 7.1 工单自动分类失败

| 场景 | 处理 |
|---|---|
| AI 分类调用失败 | 工单进入 `OPEN`，标记 `ai_triage_failed=true`，由客服手动分类 |
| JSON Schema 不合法 | 尝试一次 JSON repair，仍失败则人工分类 |
| 置信度低于 0.65 | 不自动设置分类，只展示建议 |
| AI 建议包含违规承诺 | 丢弃 AI 输出，记录 Prompt 违规事件，进入人工处理 |

### 7.2 工单重复提交

处理规则：

1. 同一用户、同一任务、同一类别 24 小时内多次提交，提示合并。
2. 后台可将工单标记为 `DUPLICATE` 并关联主工单。
3. 合并不得丢失用户原始消息和附件。
4. 合并动作必须写审计日志。

### 7.3 任务诊断证据缺失

| 缺失项 | 处理 |
|---|---|
| StepLog 缺失 | 标记为工程异常，创建内部技术工单 |
| ModelCallLog 缺失 | 阻断自动退款计算，转人工核对 |
| CreditTransaction 缺失 | 阻断补偿执行，升级 billing_admin |
| RiskReport 缺失 | 不允许放行高风险任务，转人工复核 |
| 授权记录缺失 | 不允许导出或继续处理，提示用户补充授权 |

### 7.4 退款补偿执行失败

1. 执行失败时不得重复创建新申请，应更新原申请状态为 `FAILED`。
2. 使用 `idempotency_key` 防止重复返还或重复现金退款。
3. 如果积分账户锁冲突，使用事务重试，最多 3 次。
4. 如果支付渠道退款失败，记录渠道错误码，通知 billing_admin。
5. 如果审计日志写入失败，高风险补偿操作必须回滚或进入冻结状态。
6. 所有失败必须进入 `refund_compensation_queue` 死信监控。

### 7.5 人工复核超时

处理规则：

1. P0 超时立即升级 compliance_admin + Owner。
2. P1 超时升级 support_lead + manual_review_lead。
3. 超时期间相关输出保持限制访问或复核等待，不得自动放行。
4. 超时可触发用户通知：“正在人工复核，已升级处理”。
5. 超时次数进入运营指标，作为人力和规则优化依据。

### 7.6 投诉删除执行失败

| 失败点 | 处理 |
|---|---|
| 对象存储删除失败 | 重试 3 次，仍失败进入安全工单 |
| CDN 缓存清理失败 | 标记部分完成，继续重试并限制访问 |
| 数据库软删除失败 | 阻断关闭投诉，升级技术 |
| 审计日志写入失败 | 不允许标记删除完成 |
| 用户通知失败 | 不影响删除，但进入通知重试队列 |

### 7.7 后台权限误配置

1. 发现客服可查看超权限敏感数据，立即撤销相关权限并记录安全事件。
2. 所有后台权限变更必须二次确认，并写入审计日志。
3. P0 数据权限事故必须触发 D20 事故响应流程。
4. 权限矩阵变更必须补充测试用例。

### 7.8 AI 辅助运营输出异常

| 异常 | 回退 |
|---|---|
| 分类错误 | 客服手动修正，记录纠正样本 |
| 建议过度补偿 | 不执行 AI 建议，走审批 |
| 回复草稿语气不当 | 客服编辑或使用静态话术 |
| 提示用户绕过规则 | 丢弃输出，标记 Prompt 风险 |
| 暴露敏感信息 | 阻断展示，升级安全排查 |

### 7.9 用户恶意滥用客服或补偿

触发条件：

1. 短时间内大量提交相同工单。
2. 多次质量投诉均无事实依据。
3. 多次请求违规内容后要求补偿。
4. 多账号刷免费额度或补偿积分。
5. 伪造权利人投诉。

处理动作：

1. 降低优先级或合并工单。
2. 限制补偿资格。
3. 标记用户风险。
4. 要求补充证据。
5. 严重时限制账号并转合规。

---

## 8. 风控与合规

### 8.1 授权与合规边界

客服和运营后台必须遵守：

1. 不得向用户承诺可以复制对标视频、仿脸、仿声、洗稿、搬运或规避平台检测。
2. 对用户上传素材和输入链接的授权争议，客服只能要求补充授权或转人工复核，不能替用户确认其拥有权利。
3. AIGC 标识相关争议必须按 D21 处理，不能建议用户移除必要标识。
4. 对医疗、金融、法律、未成年人等高风险行业问题，必须使用更严格策略。
5. 权利人投诉成立后，优先限制访问和清理派生资产，再处理补偿争议。

### 8.2 后台权限与数据最小化

| 数据类型 | 默认展示 | 查看完整内容条件 |
|---|---|---|
| 用户手机号/邮箱 | 脱敏 | support_lead+ 且有工单处理需要 |
| 原始视频 | 不展示，仅摘要 | compliance_admin 或 manual_reviewer，且记录原因 |
| 抽帧图片 | 缩略摘要或风险帧 | 复核场景下可查看必要帧 |
| ASR/OCR 全文 | 默认摘要 | 需要判断内容风险时可查看片段 |
| Prompt / AI 输出 | 展示摘要和版本 | 任务诊断和复核可查看必要内容 |
| 积分/订单 | 展示摘要 | billing_admin 可查看完整流水 |
| 投诉证据 | 最小展示 | compliance_admin 可查看，需审计 |

### 8.3 审计要求

以下操作必须写入不可变审计日志：

1. 后台登录和敏感数据查看。
2. 查看完整视频、抽帧、ASR/OCR、投诉证据。
3. 工单分派、升级、关闭、重新打开。
4. 人工复核领取、决策、修改用户可见说明。
5. 释放冻结、返还积分、人工补偿、现金退款申请。
6. 投诉案件状态修改、限制访问、删除资产。
7. 用户限制、账号标记、风险标签调整。
8. 配置变更：SLA、补偿阈值、原因码、话术模板、权限角色。

审计日志应至少包含：`actor_id`、`role`、`action`、`resource_type`、`resource_id`、`reason_code`、`before_snapshot`、`after_snapshot`、`ip_hash`、`created_at`。

### 8.4 风险分级与后台动作限制

| 风险等级 | 后台动作 |
|---|---|
| R0/R1 | 客服可直接处理普通问题，必要时补偿小额积分 |
| R2 | 客服可处理，但涉及输出放行需查看风险报告 |
| R3 | 必须人工复核，客服不能直接放行 |
| R4 | compliance_admin 或 manual_reviewer 处理，相关输出默认限制访问 |
| R5 | 直接拦截，客服不能放行；可处理退款/投诉/限制账号 |

### 8.5 退款补偿与风控联动

1. 系统错误导致用户损失：按规则补偿。
2. 用户违规请求被拦截：释放未消耗冻结，但不补偿违规意图成本。
3. 高风险内容人工复核不通过：按实际已消耗成本和条款决定返还。
4. 投诉成立：优先执行删除和限制访问，再计算补偿。
5. 恶意滥用用户：可限制补偿资格。
6. 企业客户：按合同 SLA，但不得突破合规底线。

### 8.6 投诉删除与数据保留

投诉删除后：

1. 用户可访问资产必须删除或限制访问。
2. 派生资产必须进入 takedown 清理流程。
3. 审计日志保留必要哈希和元数据，不保留完整争议内容。
4. 财务流水、积分流水、合规处理记录按必要期限保留。
5. 若用户要求删除个人数据，需与 D18 的数据删除流程联动。

### 8.7 客服话术合规要求

客服回复必须遵守：

1. 使用“AI 辅助生成”“结构学习”“原创化改写”“合规审核”等表述。
2. 不使用“复刻”“克隆”“洗稿”“搬运”“仿脸”“仿声”等承诺性表达。
3. 对结果质量不承诺流量、涨粉、转化效果。
4. 对投诉不做法律定性，只说明平台已按规则处理。
5. 对退款不承诺超出协议和支付渠道规则的现金退款。
6. 对风控拦截说明“出于授权、相似度、合规或平台风险控制”，给出可执行下一步。

---

## 9. 测试与验收标准

### 9.1 单元测试

| 测试对象 | 必测项 |
|---|---|
| 工单分类枚举 | 分类、状态、优先级、SLA 计算正确 |
| 权限判断 | 不同角色访问能力符合矩阵 |
| 补偿计算 | 冻结释放、积分返还、部分成功结算、重复扣费修正 |
| 幂等键 | 同一退款/补偿请求不会重复执行 |
| 审计写入 | 高风险操作必须生成审计日志 |
| 状态机 | 工单、复核、退款、投诉状态合法流转 |
| 敏感字段脱敏 | 邮箱、手机号、原文、链接等脱敏正确 |

### 9.2 API 测试

必须覆盖：

1. 用户创建工单。
2. 后台工单列表查询、分派、回复、关闭。
3. 任务诊断查询。
4. 人工复核领取、决策、执行后续动作。
5. 退款补偿申请、审批、执行。
6. 投诉提交、初筛、限制访问、删除执行。
7. 审计日志查询。
8. 权限不足返回正确错误码。
9. 幂等请求返回同一结果。
10. 分页、筛选、排序、时间范围查询。

### 9.3 E2E 主流程测试

#### 9.3.1 任务失败自动返还流程

```text
创建任务并冻结积分
→ 模拟模型失败
→ 任务状态 failed
→ 系统创建工单或失败事件
→ 自动创建 refund_compensation_request
→ 执行积分返还
→ 用户收到通知
→ 后台可查看任务、成本、返还、审计
```

通过标准：不重复扣费，积分余额正确，工单/补偿/审计完整。

#### 9.3.2 质量投诉人工补偿流程

```text
用户提交质量投诉
→ AI 分类为 QUALITY_COMPLAINT
→ 客服查看任务和质量评分
→ 转人工判断输出不达标
→ 创建小额积分补偿
→ support_lead 审批
→ Billing Worker 执行补偿
→ 工单关闭
```

通过标准：补偿有原因码、审批、积分流水和审计日志。

#### 9.3.3 风控拦截人工复核流程

```text
任务输出命中 R3 风险
→ 创建 manual_review_ticket
→ 用户看到复核等待页
→ 复核员查看证据包
→ 决策 REWRITE_REQUIRED
→ 系统触发重写或要求用户编辑
→ 成功后重新风控
→ 结算或返还
```

通过标准：客服不能直接放行，复核决策可追溯，用户可见说明正确。

#### 9.3.4 权利人投诉删除流程

```text
权利人提交投诉
→ complaint_case 创建
→ AI 初筛为 COPYRIGHT/P1
→ 系统限制相关导出访问
→ 合规人员复核成立
→ 创建 takedown_job
→ 删除派生资产
→ 通知用户和投诉人
→ 审计日志保留必要元数据
```

通过标准：资产访问被限制，删除任务可追踪，审计不保留完整侵权内容。

### 9.4 RBAC / ABAC 测试

必须验证：

1. `support_agent` 无法执行现金退款。
2. `support_agent` 无法查看完整原始视频，除非有特殊授权。
3. `manual_reviewer` 无法调整积分。
4. `billing_admin` 无法放行高风险内容。
5. `read_only_auditor` 不能执行任何写操作。
6. `super_admin` 的所有高风险操作也必须写审计日志。
7. 组织隔离和用户数据隔离有效。

### 9.5 AI Prompt 回归测试

| Prompt | 指标 | 门槛 |
|---|---|---:|
| P22_SUPPORT_TRIAGE | 分类准确率 | ≥ 85% |
| P22_SUPPORT_TRIAGE | P0/P1 风险召回 | ≥ 95% |
| P22_REFUND_SUGGESTION | 不建议越权退款 | 100% |
| P22_MANUAL_REVIEW_SUMMARY | 风险摘要完整率 | ≥ 90% |
| P22_COMPLAINT_TRIAGE | 投诉类型识别准确率 | ≥ 85% |
| P22_ADMIN_REPLY_DRAFT | 合规话术通过率 | ≥ 95% |
| 全部 Prompt | JSON 合法率 | ≥ 99% |

红队样本必须包含：要求复刻、仿脸、仿声、绕过风控、未授权素材、虚假投诉、恶意退款、隐私泄露、重复扣费、投诉删除。

### 9.6 性能与稳定性验收

| 指标 | MVP1 门槛 |
|---|---:|
| 后台列表页 P95 响应 | < 1.5 秒 |
| 工单详情页 P95 响应 | < 2 秒 |
| 任务诊断页 P95 响应 | < 3 秒 |
| 退款补偿执行成功率 | ≥ 99% |
| 审计日志写入成功率 | ≥ 99.9% |
| 工单 AI 分类失败回退 | 100% 可人工处理 |
| 删除任务最终一致性 | 可重试、可观测、可审计 |

### 9.7 上线阻断项

以下任一项不通过，不允许商业化上线：

- [ ] 后台无 RBAC 或角色越权。
- [ ] 退款补偿没有幂等保护。
- [ ] 人工复核决策没有审计日志。
- [ ] 投诉删除不能限制访问或清理派生资产。
- [ ] 客服可绕过风控直接放行 R4/R5 内容。
- [ ] 后台可查看完整敏感数据但不写审计日志。
- [ ] 任务失败无法查到原因、成本和积分状态。
- [ ] AI 辅助客服可以自动执行退款、删除或放行。
- [ ] 生产环境存在未标记 mock 数据或假按钮。
- [ ] D20 发布门禁未覆盖 D22 关键流程。

---

## 10. AIcoding 研发任务拆分

### 10.1 任务总览

| 任务编号 | 任务名称 | 优先级 | 主要交付物 | 依赖 |
|---|---|---:|---|---|
| D22-T01 | 运营后台枚举、数据模型与迁移草案 | P0 | enums、schema、migration、seed | D16/D18/D19/D21 |
| D22-T02 | 后台 RBAC/ABAC 权限与审计中间件 | P0 | admin auth、permission guard、audit writer | D18 |
| D22-T03 | 任务诊断中心后端 API 与页面 | P0 | diagnostics API、admin task page | D16/D17/D19/D13 |
| D22-T04 | 客服工单系统后端 | P0 | ticket CRUD、message、SLA、assignment | D16/D18 |
| D22-T05 | 客服工单前端页面 | P0 | ticket list/detail/reply/internal note | D06/D22-T04 |
| D22-T06 | 人工复核队列与决策流 | P0 | review queue、evidence pack、decision API/page | D13/D21 |
| D22-T07 | 退款补偿申请、审批与执行 | P0 | refund service、credit adjustment、idempotency | D19 |
| D22-T08 | 投诉删除处理与 takedown 联动 | P0 | complaint case、restrict access、takedown trigger | D17/D18/D21 |
| D22-T09 | 运营 Overview 与指标聚合 | P0 | dashboard API/page、metrics rollup | D20 |
| D22-T10 | 后台审计日志中心 | P0 | audit list/search/export | D18 |
| D22-T11 | AI 辅助客服 Prompt 与 PromptRun 接入 | P1 | triage、summary、refund suggestion、reply draft | D10/D11 |
| D22-T12 | 通知模板与用户通知 | P1 | notification templates、send hooks | D06/D20 |
| D22-T13 | 自动化测试与 E2E 验收 | P0 | tests、fixtures、CI gate | D20 |
| D22-T14 | 运营配置中心 | P1 | SLA、原因码、话术、补偿阈值配置 | D22 |
| D22-T15 | D24 交接包输入 | P0 | SOP、权限矩阵、Runbook、已知风险 | D24 |

### 10.2 D22-T01：运营后台枚举、数据模型与迁移草案

**目标**：建立工单、复核、退款补偿、投诉、审计日志的基础数据模型。

**交付物**：

1. TypeScript/Python 枚举定义。
2. ORM model 或 SQL migration。
3. 数据库索引和唯一约束。
4. seed 数据：原因码、SLA 配置、默认角色。
5. Schema 校验测试。

**验收标准**：迁移可执行，可回滚；表字段支持 D22 P0 流程；不与 D16 既有表冲突；审计字段完整。

### 10.3 D22-T02：后台 RBAC/ABAC 权限与审计中间件

**目标**：所有后台 API 统一经过权限检查和审计日志记录。

**交付物**：

1. `requireAdminRole()` 中间件。
2. `checkAdminPermission(action, resource, context)`。
3. `writeOpsAuditLog()`。
4. 敏感数据查看审计。
5. 权限测试用例。

**验收标准**：角色矩阵全部通过测试；越权返回 `OPS_PERMISSION_DENIED`；敏感访问写审计。

### 10.4 D22-T03：任务诊断中心

**目标**：让客服能快速判断任务失败、卡住、扣费、风控原因。

**交付物**：

1. `GET /api/admin/tasks/{task_id}/diagnostics`。
2. 后台任务诊断页面。
3. 任务步骤时间线组件。
4. ModelCallLog、CostLedger、CreditTransaction、RiskReport 摘要组件。
5. 可执行动作按钮：创建工单、申请退款、转复核、限制访问。

**验收标准**：任一任务可查状态、步骤、成本、积分、风险；敏感信息默认脱敏；所有操作写审计。

### 10.5 D22-T04/T05：客服工单系统

**目标**：建立可用工单中心。

**交付物**：

1. 用户创建工单 API。
2. 后台工单列表/详情 API。
3. 消息线程、内部备注、附件引用。
4. 工单分派、升级、关闭、重新打开。
5. SLA 计算和超时标记。
6. 工单页面。

**验收标准**：用户可提交，客服可回复，内部备注不对用户可见，SLA 可查询，工单状态合法流转。

### 10.6 D22-T06：人工复核队列与决策流

**目标**：承接 D13/D21 的人工复核要求。

**交付物**：

1. 复核队列 API。
2. 证据包生成器。
3. 复核详情页。
4. 决策 API。
5. 决策后动作：放行、提示、重写、补证、拒绝返还、限制、删除。
6. 用户通知事件。

**验收标准**：R3/R4 风险不能被客服直接放行；复核决策可追踪；决策后任务、计费、通知状态一致。

### 10.7 D22-T07：退款补偿申请、审批与执行

**目标**：实现受控退款补偿闭环。

**交付物**：

1. 退款补偿申请 API。
2. 可退金额/积分计算服务。
3. 审批流。
4. 积分释放、返还、补偿执行。
5. 现金退款申请预留。
6. 幂等保护和审计日志。

**验收标准**：重复请求不重复返还；所有补偿有原因码和审计；超阈值需要审批；补偿不能绕过风控。

### 10.8 D22-T08：投诉删除处理与 takedown 联动

**目标**：处理用户/权利人投诉，并联动 D17/D18/D21 删除流程。

**交付物**：

1. 投诉提交 API。
2. 投诉后台列表和详情。
3. 证据附件管理。
4. 限制访问动作。
5. Takedown job 创建和状态展示。
6. 用户/投诉人通知。

**验收标准**：投诉成立后可限制访问并清理派生资产；审计日志不保留完整侵权内容；删除状态可追踪。

### 10.9 D22-T09：运营 Overview 与指标聚合

**目标**：提供运营健康视图。

**交付物**：

1. 工单量、超时率、处理时长。
2. 任务失败原因 TopN。
3. 退款补偿金额和次数。
4. 人工复核队列与决策统计。
5. 投诉删除统计。
6. 成本异常和单任务亏损统计。

**验收标准**：指标口径清晰，支持时间筛选，统计值与底层数据一致。

### 10.10 D22-T11：AI 辅助客服 Prompt 与 PromptRun 接入

**目标**：实现可控的 AI 辅助运营能力。

**交付物**：

1. Prompt 模板注册。
2. 输入脱敏工具。
3. PromptRun 调用与日志。
4. JSON Schema 校验。
5. AI 结果展示和人工采纳/修改/拒绝记录。
6. Prompt 回归测试。

**验收标准**：AI 不自动执行动作；低置信度回退人工；违规建议被拦截；输出有 Prompt 版本和成本日志。

---

## 11. AIcoding 执行提示词

### 11.1 D22 总执行提示词

```text
你是本项目的工程级 AIcoding 执行 Agent，负责实现 D22《运营后台、客服、退款补偿与人工复核 SOP 文档》相关模块。

必须先读取并遵守：
1. D00《AIcoding 研发总控文档 / 项目上下文总索引》
2. D04《总 PRD 文档》
3. D05《功能模块拆分与独立研发边界文档》
4. D13《质检评分、相似度风控与合规拦截文档》
5. D16《数据库 Schema、数据字典与 API 契约文档》
6. D17《异步任务队列、文件存储与生成资产生命周期文档》
7. D18《安全、隐私、数据治理与权限设计文档》
8. D19《商业化系统、套餐积分、订单与成本结算文档》
9. D20《测试、验收、发布、监控与告警文档》
10. D21《合规、授权、AIGC 标识与内容审核文档》
11. D22 本文档

核心约束：
- 本产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具。
- 后台和客服不能绕过风控、合规、授权、AIGC 标识、积分计费和审计日志。
- 不允许只做 UI Demo，不允许用 mock 冒充真实能力，除非明确标记并生产关闭。
- 所有后台敏感操作必须有权限校验、原因码、审计日志和测试。

本次任务：{填写 D22-Txx 任务名称}

你必须输出：
1. 读取了哪些文档。
2. 本次任务边界。
3. 新增/修改的文件。
4. 数据库/API/队列/权限/日志影响。
5. 风控/合规/商业化影响。
6. 测试命令与测试结果。
7. Mock 或未完成项说明。
8. 风险和后续回写建议。
```

### 11.2 后端 Agent 提示词

```text
请实现 D22 后端模块：{support_ticketing / manual_review / refund_compensation / complaint_takedown / ops_audit}。

实现要求：
1. 使用现有项目技术栈和 D16 数据库/API 规范。
2. 所有 API 必须有鉴权、RBAC/ABAC、统一错误码、幂等设计。
3. 涉及积分、退款、补偿必须通过 D19 的积分账户和交易流水，不允许直接改余额。
4. 涉及投诉删除必须通过 D17 的 takedown job 和 D18 的数据生命周期，不允许直接硬删无审计。
5. 涉及人工复核必须关联 RiskReport / ComplianceReviewReport / EvidencePack。
6. 所有后台写操作必须调用 audit log。
7. 添加单元测试、API 测试和权限测试。

交付后说明：
- API 列表
- 数据表变化
- 权限矩阵
- 审计动作列表
- 测试结果
```

### 11.3 前端 Agent 提示词

```text
请实现 D22 运营后台前端页面：{页面名称}。

页面要求：
1. 遵守 D06 信息架构和 D22 页面状态。
2. 不展示未脱敏敏感信息；敏感详情必须二次确认并写审计。
3. 所有操作按钮必须接真实 API；不可做无后端假按钮。
4. 风控、复核、退款、删除等高风险动作必须有确认弹窗、原因码输入和权限不足状态。
5. 显示任务状态、工单状态、复核状态、退款补偿状态和审计记录。
6. 支持空状态、加载状态、失败状态、权限不足状态、SLA 超时状态。
7. 添加组件测试或 E2E 测试。

交付后说明：
- 页面路由
- 组件列表
- API 依赖
- 状态覆盖
- 测试结果
```

### 11.4 AI/Prompt Agent 提示词

```text
请实现 D22 AI 辅助客服 Prompt：{P22_SUPPORT_TRIAGE / P22_REFUND_SUGGESTION / P22_MANUAL_REVIEW_SUMMARY / P22_COMPLAINT_TRIAGE / P22_ADMIN_REPLY_DRAFT}。

要求：
1. Prompt 必须注册到 Prompt Registry，包含 prompt_id、version、owner、input_schema、output_schema。
2. 输入必须先脱敏，不能包含完整原始视频、完整隐私字段、完整支付信息。
3. 输出必须是 JSON，并通过 Schema 校验。
4. Prompt 只能提供建议、摘要、草稿，不允许自动执行退款、放行、删除、封禁。
5. 必须明确产品边界：不支持搬运、克隆、洗稿、仿脸、仿声、绕过平台规则。
6. 设计至少 20 条回归测试样本，其中包含违规请求、重复扣费、系统失败、质量投诉、投诉删除、隐私风险。
7. 输出失败回退策略和质量门槛。

交付后说明：
- Prompt 文本
- 输入/输出 Schema
- 测试样本
- 评测结果
- 失败回退
```

### 11.5 测试 Agent 提示词

```text
请为 D22 模块设计并实现测试。

必须覆盖：
1. 工单创建、分派、回复、关闭。
2. 工单 AI 分类失败回退。
3. 任务诊断读取权限和脱敏。
4. 人工复核领取、决策、状态流转。
5. 退款补偿申请、审批、执行、幂等。
6. 投诉提交、限制访问、删除任务、审计日志。
7. RBAC/ABAC 越权测试。
8. 审计日志写入失败时的阻断策略。
9. E2E：任务失败自动返还、质量投诉补偿、风控人工复核、权利人投诉删除。
10. 生产禁用 mock 检查。

输出测试报告：
- 测试命令
- 覆盖模块
- 通过/失败结果
- 未覆盖风险
- 上线阻断项
```

### 11.6 DevOps / 监控 Agent 提示词

```text
请为 D22 运营后台模块配置监控、告警和 Runbook。

必须配置指标：
1. 工单创建量、待处理量、超时量。
2. P0/P1 工单响应时间。
3. 人工复核队列堆积和超时。
4. 退款补偿执行失败率。
5. 投诉删除执行失败率。
6. 审计日志写入失败率。
7. AI 分类失败率和低置信度率。
8. 后台 4xx/5xx 错误率。
9. 权限拒绝次数异常。
10. 高风险操作数量。

必须输出：
- Dashboard 配置
- 告警阈值
- 事故处理 Runbook
- 回滚策略
- 值班处理流程
```

---

## 12. 不做事项

MVP1 的 D22 明确不做：

1. 不做完整 CRM、呼叫中心、工单机器人全自动闭环。
2. 不做 AI 自动批准退款、自动现金退款、自动删除资产、自动封禁用户。
3. 不做后台绕过风控的一键放行。
4. 不做客服手动修改用户 AI 结果并覆盖原版本。
5. 不做无审计的数据库后台改数。
6. 不做无审批的大额补偿。
7. 不做跨平台自动申诉、自动发布平台工单。
8. 不做完整财税、发票、企业合同账期系统。
9. 不做复杂多级企业客服 SLA 和专属客户成功系统。
10. 不做舆情监控、外部社媒投诉自动抓取。
11. 不做绕过平台审核或指导规避平台规则的话术库。
12. 不做对用户原始素材的后台无限制浏览。
13. 不做永久保留完整投诉争议内容。
14. 不做批量删除审计日志或篡改历史记录。
15. 不做针对高风险灰产用户的特殊补偿通道。

---

## 13. 待确认问题

上线前需要项目 Owner 确认以下问题：

1. D22 最终编号是否回写 D00 为 D22，还是 D00 保持 D23 旧口径并使用 doc_alias 兼容。
2. MVP1 是否启用现金退款，还是只支持积分返还和人工补偿。
3. 小额/中额/大额积分补偿阈值分别是多少。
4. 哪些角色可以审批补偿，是否需要双人审批。
5. 权利人投诉公开入口采用邮箱、表单还是后台录入。
6. 投诉处理 SLA 是否需要按地区和法律要求区分。
7. 用户删除任务后的数据保留周期和审计保留字段。
8. 企业客户是否需要独立 SLA、专属工单入口和账单补偿规则。
9. 是否需要接入第三方客服系统，还是先自研轻量工单。
10. 支付渠道退款 API 选择和退款失败处理规则。
11. 客服通知渠道：站内信、邮件、短信、微信/企业微信是否进入 MVP1。
12. AI 辅助客服是否进入 P0，还是只保留接口和 Prompt 草案。
13. 后台敏感视频/抽帧查看是否需要二次审批。
14. 审计日志保存周期和导出权限。
15. 是否需要客服绩效指标：响应时长、关闭时长、满意度、补偿金额。
16. 是否需要设置恶意用户补偿黑名单或风控策略。
17. 质量投诉的“可补偿”标准由谁最终判定。
18. 人工复核员是否允许修改用户可见说明模板。
19. 投诉成立后的现金退款是否必须人工逐单确认。
20. 是否建立重大运营事故复盘模板并纳入 D24。

---

## 14. 最终 Checklist

### 14.1 文档与范围 Checklist

- [ ] 已明确 D22 编号兼容和 `doc_alias`。
- [ ] 已遵守 D00 产品定位和不做事项。
- [ ] 已明确 MVP1 只覆盖视频拆解 + 原创脚本生成相关后台运营。
- [ ] 已明确后台不是绕过风控的万能入口。
- [ ] 已明确 D22 与 D16/D17/D18/D19/D20/D21 的接口关系。

### 14.2 运营后台 Checklist

- [ ] `/admin/overview` 可查看核心运营指标。
- [ ] `/admin/tasks/{task_id}` 可诊断任务状态、成本、积分、风控。
- [ ] `/admin/support-tickets` 可处理工单。
- [ ] `/admin/manual-reviews` 可执行人工复核。
- [ ] `/admin/refunds` 可申请、审批、执行补偿和退款。
- [ ] `/admin/complaints` 可处理投诉和删除。
- [ ] `/admin/audit-logs` 可查询审计记录。

### 14.3 客服工单 Checklist

- [ ] 用户可创建工单。
- [ ] 系统可自动创建异常工单。
- [ ] 工单可分类、分派、升级、关闭。
- [ ] 工单有 SLA。
- [ ] 客服可回复用户并添加内部备注。
- [ ] AI 分类失败可回退人工。
- [ ] 工单消息和附件权限可控。

### 14.4 人工复核 Checklist

- [ ] R3/R4 风险可进入复核队列。
- [ ] 用户申诉可进入复核队列。
- [ ] 复核证据包遵守最小必要原则。
- [ ] 复核决策枚举完整。
- [ ] 决策后动作与任务、计费、通知联动。
- [ ] 客服不能直接放行高风险。
- [ ] 复核全过程有审计。

### 14.5 退款补偿 Checklist

- [ ] 支持释放冻结。
- [ ] 支持系统失败返积分。
- [ ] 支持重复扣费纠正。
- [ ] 支持质量投诉补偿申请。
- [ ] 支持现金退款申请预留。
- [ ] 支持审批阈值配置。
- [ ] 支持幂等执行。
- [ ] 所有补偿写入 CreditTransaction 和 AuditLog。

### 14.6 投诉删除 Checklist

- [ ] 用户和权利人均有投诉入口。
- [ ] 投诉类型完整。
- [ ] 高风险投诉可先限制访问。
- [ ] 投诉可转人工复核。
- [ ] 删除任务可清理派生资产。
- [ ] 删除结果可通知用户和投诉人。
- [ ] 审计日志不保留完整争议内容。

### 14.7 权限与审计 Checklist

- [ ] 后台角色矩阵已实现。
- [ ] 敏感数据默认脱敏。
- [ ] 敏感查看需要原因码和审计。
- [ ] 高风险操作必须写审计。
- [ ] 审计日志不可被普通后台修改或删除。
- [ ] 权限测试覆盖所有角色。

### 14.8 AI 辅助客服 Checklist

- [ ] Prompt 已注册版本。
- [ ] 输入已脱敏。
- [ ] 输出 JSON Schema 合法。
- [ ] AI 只做建议，不执行最终动作。
- [ ] 低置信度回退人工。
- [ ] 红队样本通过。
- [ ] AI 建议采纳/修改/拒绝可记录。

### 14.9 测试与上线 Checklist

- [ ] 单元测试通过。
- [ ] API 测试通过。
- [ ] RBAC/ABAC 测试通过。
- [ ] 退款补偿幂等测试通过。
- [ ] 人工复核 E2E 通过。
- [ ] 投诉删除 E2E 通过。
- [ ] 审计日志测试通过。
- [ ] 生产环境 mock 默认关闭。
- [ ] D20 发布门禁包含 D22 流程。
- [ ] D24 交接包包含后台 SOP、权限矩阵、Runbook、已知风险。

---

## 结语

D22 的价值不在于“有一个后台”，而在于让 AI 短视频爆款增长引擎真正具备商业化运营能力：当任务失败、用户投诉、积分争议、内容风险、权利人投诉、人工复核、退款补偿发生时，系统能够给出可解释、可处理、可追溯、可审计的闭环。只有 D22 与 D16/D17/D18/D19/D20/D21 联动完成，MVP1 才能从“可生成”升级为“可上线、可收费、可运营、可持续迭代”。
