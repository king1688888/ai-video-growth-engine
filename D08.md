# D08｜视频处理与多模态理解工作流文档

> 文档编号：D08  
> 文档名称：视频处理与多模态理解工作流文档  
> 文档版本：V1.0  
> 生成日期：2026-05-22  
> 根上下文：D00《AIcoding 研发总控文档 / 项目上下文总索引》  
> 前置文档：D00、D01、D02、D03、D04、D05、D06、D07  
> 后置文档：D09 Creative IR Schema 与原创化重构规则、D10 Prompt 体系、D11 模型路由、D12 AI 评测样本集、D13 质检评分与相似度风控、D15-D17 工程架构/数据库/API/任务队列、D18 数据安全、D19 商业化、D20 测试发布监控、D21 合规审核、D22 运营后台、D23 AIcoding 执行规约、D24 最终交接  
> 当前主线版本：MVP1「视频拆解 + 原创脚本生成」  
> 文档性质：视频处理管线 + 多模态理解输入包 + 工程状态机 + AI Prompt/Schema 契约 + AIcoding 执行任务文档  
> 核心定位：本文件不是“视频算法科普”，而是把用户上传的视频转化为可校验、可存储、可追踪、可计费、可风控、可供 D07 Content DNA 稳定消费的多模态证据资产。

---

## 1. 文档元信息

| 项目 | 内容 |
|---|---|
| 文档编号 | D08 |
| 文档名称 | 视频处理与多模态理解工作流文档 |
| 所属项目 | AI 短视频爆款增长引擎 |
| 当前研发主线 | MVP1：视频拆解 + 原创脚本生成 |
| 文档级别 | S，视频处理与多模态理解核心工程文档 |
| 上级根文档 | D00《AIcoding 研发总控文档 / 项目上下文总索引》 |
| 直接前置依赖 | D00、D01、D02、D03、D04、D05、D06、D07 |
| 直接后置依赖 | D09、D10、D11、D12、D13、D15、D16、D17、D18、D19、D20、D21、D22、D23、D24 |
| 适用阶段 | MVP1 视频拆解研发、AI 证据包构建、异步任务编排、Prompt 工程、接口联调、商业化扣费、内测上线验收 |
| 适用对象 | 创始人、产品负责人、AI 产品经理、技术产品经理、后端 Agent、AI 工程 Agent、Prompt Agent、前端 Agent、测试 Agent、DevOps Agent、风控合规负责人、运营后台负责人 |
| 文档目标 | 定义视频上传后从资产校验、元数据解析、抽帧、音频提取、ASR、OCR、镜头切分、多模态证据包、视频理解结果到 Content DNA 输入的完整工作流、Schema、API、任务状态、异常降级、风控合规、商业化计费与 AIcoding 任务拆分 |
| 最高约束 | 与 D00 冲突时，以 D00 为准；与 D03 MVP 范围冲突时，先按 D03 收敛；与 D04/D05 模块边界冲突时，以已确认 PRD 与模块边界为准；与 D07 Content DNA 输入要求冲突时，应回写 D07；与 D16/D17 最终工程契约冲突时，以 D16/D17 为实现口径并回写 D08 |
| 输出形态 | Markdown 文档 + 数据结构草案 + API 草案 + 任务状态机 + Prompt 模板 + 失败回退矩阵 + 测试验收清单 + AIcoding 执行提示词 |
| 维护责任人 | 项目 Owner / AI 产品总控 / AI 工程负责人 / 技术产品负责人 |
| 更新触发条件 | 视频上传限制变化、ASR/OCR/多模态模型供应商变化、抽帧策略变化、镜头切分策略变化、D07 Content DNA Schema 变化、D11 成本模型变化、D13/D21 风控策略变化、D16/D17 工程契约变化、Benchmark 失败、上线验收不通过 |
| 文档状态 | V1.0 初版可执行；后续随 D09-D24 深化后回写修订 |

---

## 2. 文档目标

### 2.1 本文档要解决的问题

D08 解决的是 MVP1 中“用户上传的一条视频如何被稳定转化为 AI 可理解、工程可追踪、成本可核算、风控可审计的多模态证据包”的问题。它必须避免两类失败：第一，视频处理链路不稳定，导致 Content DNA 输入缺失、报告幻觉、任务卡死、积分无法补偿；第二，AI 直接看视频后自由发挥，缺少可追溯证据、字段 Schema、Prompt 版本、失败回退和质量评测。

D08 必须解决以下 12 个问题：

1. **视频输入边界**：MVP1 支持哪些视频格式、大小、时长、比例、编码、上传方式；哪些输入必须拒绝或转人工。
2. **资产安全与授权前置**：上传前如何确认素材授权、禁止搬运/仿脸/仿声/未授权复用、AIGC 标识和投诉删除入口。
3. **视频处理流水线**：如何解析元数据、生成代理文件、抽帧、提取音频、执行 ASR、执行 OCR、镜头切分、帧描述、证据包构建。
4. **多模态理解边界**：哪些内容由 D08 生成，哪些必须交给 D07 Content DNA、D09 Creative IR、D13 风控处理，避免模块越界。
5. **结构化输出 Schema**：定义 `VideoProcessingResult`、`FrameAsset`、`TranscriptSegment`、`OCRSegment`、`ShotSegment`、`MultimodalEvidencePack`、`VideoUnderstandingResult`、`ProcessingQualityReport`。
6. **Prompt 与模型调用**：定义帧描述、ASR 清洗、OCR 聚合、视频时间线理解、多模态证据归纳的 Prompt、输入变量、输出 Schema、失败回退和质量标准。
7. **任务状态与队列**：定义异步任务步骤、状态机、重试、幂等、取消、部分成功、降级、失败补偿和后台可观测性。
8. **工程落地契约**：明确数据库草案、API 草案、对象存储、日志、权限、测试和部署影响。
9. **商业化闭环**：明确处理成本、积分冻结、扣费节点、失败返还、部分成功扣费、成本统计和后台看板。
10. **风控合规嵌入**：明确授权、AIGC 标识、风险分级、可疑内容标记、人工复核、投诉删除和数据留存策略。
11. **测试验收标准**：定义单元测试、集成测试、E2E、Prompt 回归、视频样本 Benchmark、性能压测、计费幂等和上线门槛。
12. **AIcoding 可执行任务**：输出可直接交给 Manus / Codex / Claude Code / Hermes Agent 的任务拆分和执行提示词。

### 2.2 本文档不解决的问题

| 不在 D08 完整展开的内容 | 负责文档 | D08 只提供 |
|---|---|---|
| 产品战略、商业定位、价值主张 | D01 | 视频处理能力对商业闭环的支撑方式 |
| 首发用户画像与行业选择 | D02 | 读取 CreatorProfile 快照作为处理上下文 |
| MVP0-MVP4 完整路线图 | D03 | 继承 MVP1 P0 范围，不扩展到账号级/成片生成 |
| 总 PRD 与完整用户故事 | D04 | 承接上传、任务、报告、计费、后台的需求 |
| 模块拆分与独立研发边界 | D05 | 细化 M07 视频处理管线与 M08 多模态理解前置部分 |
| 页面原型、按钮、视觉稿 | D06 | 提供上传、进度、失败、后台诊断需要的 ViewModel 与状态 |
| Content DNA 完整字段与报告标准 | D07 | 提供 D07 可消费的 `MultimodalEvidencePack` 与 `VideoUnderstandingResult` |
| Creative IR 与原创化重构 | D09 | 只提供视频证据，不生成原创脚本和重构方案 |
| Prompt 全体系、版本管理平台 | D10 | 定义 D08 相关 Prompt 草案与版本要求 |
| 模型路由与成本控制细则 | D11 | 提供模型调用类型、成本计量字段和降级要求 |
| AI Benchmark 样本集全集 | D12 | 提供视频处理与多模态理解评测维度 |
| 相似度风控与合规拦截最终规则 | D13/D21 | 提供风险候选信号、不可复用素材证据和人工复核触发条件 |
| 完整技术架构、数据库、API、任务队列 | D15-D17 | 提供草案，最终以 D15-D17 为准 |
| 商业化套餐、订单、支付、退款 | D19 | 提供处理成本、积分冻结/返还和后台指标输入 |
| 数据安全、权限治理与隐私保护 | D18 | 提供视频资产、帧、音频、文本证据的数据分级和访问约束 |
| 运营后台、客服、人工复核 SOP | D22 | 提供后台排查字段、复核触发和补偿输入 |

### 2.3 本文档的研发落点

D08 最终必须落到以下工程对象：

1. `video_processing_jobs` / `video_processing_results` 数据对象。
2. `frame_assets`、`audio_assets`、`transcript_segments`、`ocr_segments`、`shot_segments`、`video_understanding_results` 数据对象。
3. `MultimodalEvidencePack` JSON Schema，作为 D07 Content DNA 的强输入。
4. `ProcessingQualityReport`，作为降级、置信度和部分成功的判断依据。
5. 视频处理队列：metadata、normalize、audio、asr、frames、ocr、shot_detection、frame_description、evidence_pack、video_understanding。
6. 内部 API：运行、查询、重试、取消、回放处理结果。
7. 前端 ViewModel：上传校验结果、任务进度、步骤状态、失败原因、降级提示、积分状态。
8. 后台 ViewModel：处理步骤日志、模型成本、帧/ASR/OCR/镜头诊断、低置信度原因、复核入口。
9. Prompt 模板：`P01_FRAME_DESCRIPTION`、`P01_ASR_CLEANUP`、`P01_OCR_AGGREGATION`、`P01_VIDEO_UNDERSTANDING`、`P01_EVIDENCE_PACK_REPAIR`。
10. 测试样本：常见视频、无音频、低清、强字幕、屏幕录制、口播、探店、横竖屏、超短、接近上限、异常文件、高风险素材。
11. AIcoding 研发任务：可分模块实现、测试、验收和交接。

---

## 3. 与 D00 及其他文档的关系

### 3.1 D00 对 D08 的硬约束

| D00 约束 | D08 落地方式 |
|---|---|
| 产品是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具 | D08 只做视频证据提取与结构理解，不提供下载搬运、仿脸仿声、素材复用、规避平台规则能力 |
| 核心链路为对标内容输入 → 多模态拆解 → Content DNA → Creative IR → 原创输出 → 风控 → 数据回填 | D08 负责“对标内容输入 → 多模态拆解 → D07 Content DNA 输入包”这一段 |
| 当前优先 MVP1：视频拆解 + 原创脚本生成 | D08 只支持单条视频处理与拆解，不做账号全量抓取、完整视频生成、自动发布 |
| 不得自行扩大第一版范围 | 视频链接下载、账号抓取、长视频批量处理、成片生成均 P1/P2 预留，默认关闭 |
| 不得跳过风控、任务日志、成本记录、测试验收 | 每个处理步骤必须有 TaskStepLog / ProcessingLog；ASR/OCR/多模态调用必须有 ModelCallLog 或成本日志；输出必须有质量报告和风险候选标记 |
| 不得用 mock 冒充真实能力 | Mock ASR/OCR/帧描述只能用于开发环境，必须有 `is_mock=true`、页面和后台标识，生产默认关闭 |
| 每个模块必须可独立开发、独立测试、独立验收、独立交接 | D08 拆分为元数据、抽帧、ASR、OCR、镜头切分、证据包、视频理解、质检、计费、后台诊断等任务 |

### 3.2 D01 对 D08 的输入

D01 已明确 MVP1 首发商业闭环：上传对标视频 → 授权确认 → 积分预估与冻结 → 视频处理与多模态拆解 → Content DNA / 报告 / 原创脚本 / 标题 / 封面 / Prompt → 风控 → 成功扣费 / 失败返还 → 后台记录任务、成本、质量、风险和转化。

D08 承接其中的“视频处理与多模态拆解”部分，并必须输出可被计费、可被追踪、可被 D07 消费的数据资产。D08 的处理结果质量直接决定用户第一 Aha Moment：如果 ASR/OCR/关键帧/镜头切分不足，后续 Content DNA 会幻觉、泛化或输出不可用脚本。

### 3.3 D02 对 D08 的输入

D02 已确定首发用户优先服务企业 IP / 老板 IP、本地生活商家，知识 IP / 教培和 MCN 为 P1 试点。D08 的处理策略应优先覆盖这些视频类型：

| 用户类型 | 典型视频形态 | D08 处理重点 |
|---|---|---|
| 企业 IP / 老板 IP | 口播、观点、案例、避坑、清单 | ASR 准确、前 3 秒钩子、字幕识别、镜头节奏、CTA 位置 |
| 本地生活商家 | 探店、门店展示、服务流程、活动优惠、案例展示 | 门店场景、产品/服务画面、字幕/价格/地址 OCR、镜头切分、品牌/Logo 风险候选 |
| 知识 IP / 教培 | 干货、教程、课程引导、知识点拆解 | ASR/OCR 聚合、知识点时间线、夸大承诺风险候选 |
| MCN / 内容团队 | 多形态视频、批量样本 | 稳定的处理结果、批量诊断字段、可回放证据 |

### 3.4 D03 对 D08 的输入

D03 将当前研发主线收敛为 MVP1：注册登录 → 用户画像 → 上传对标视频 → 授权确认 → 风险预检 → 积分预估/冻结 → 异步任务 → 视频处理 → 多模态理解 → Content DNA → 拆解报告 → 原创输出 → 风控质检 → 扣费/返还 → 查看/导出/历史/后台。

D08 在该链路中承担：

```text
asset_id + authorization_ack + risk_precheck + creator_profile_snapshot
→ video_processing_job
→ metadata / audio / frames / ASR / OCR / shot_segments
→ multimodal_evidence_pack
→ video_understanding_result
→ D07 Content DNA generation input
```

### 3.5 D04 对 D08 的输入

D04 已将视频上传、格式/大小/时长、安全扫描、对象存储、异步任务、任务状态、模型调用日志、积分冻结/扣费/返还和后台任务详情列为 MVP1 P0。D08 必须把这些需求落为工程契约：

1. 文件不合规时不得创建高成本处理任务。
2. 未完成授权确认不得读取视频内容。
3. 任务执行前必须完成积分预估和冻结。
4. 每个可重试步骤必须幂等。
5. ASR/OCR/镜头切分可降级，但必须标记 `partial_completed`、降低置信度并影响扣费/返还。
6. 多模态模型调用必须记录供应商、模型、输入摘要、输出摘要、成本、延迟、错误和 Prompt 版本。

### 3.6 D05 对 D08 的输入

D05 将 D08 主要对应到两个模块：

| 模块 | D08 负责程度 | 边界 |
|---|---:|---|
| M07 视频处理管线模块 | 主要负责 | 元数据、抽帧、音频、ASR、OCR、镜头切分、ProcessingQualityReport |
| M08 多模态理解与 Content DNA 模块 | 负责前置输入与视频理解层 | `VideoUnderstandingResult` 与 `MultimodalEvidencePack`；最终 Content DNA 字段由 D07 管理 |

D08 必须严格遵守：M07 不生成脚本、不生成报告、不做最终风控、不扣费；M08 不生成最终原创脚本、不做最终拦截、不做账号级 DNA。

### 3.7 D06 对 D08 的输入

D06 已定义上传页、任务进度页、结果页、历史页、积分中心、后台任务详情页和人工复核页。D08 必须提供这些页面可用的状态与字段：

1. 上传校验：格式、大小、时长、比例、安全扫描、授权状态。
2. 处理进度：当前步骤、步骤进度、是否可取消、是否可重试、是否已冻结积分。
3. 失败提示：失败步骤、用户可读原因、下一步动作、返还规则。
4. 降级提示：ASR/OCR/镜头切分缺失、低置信度原因、是否仍可生成报告。
5. 后台诊断：每个步骤的输入输出摘要、耗时、成本、错误码、重试次数、模型调用 ID。

### 3.8 D07 对 D08 的输入与约束

D07 要求 Content DNA 必须基于视频元数据、关键帧、ASR、OCR、镜头切分、用户画像、目标平台、内容目标和风控预检生成。D08 必须提供这些字段，并遵守以下要求：

| D07 需要 | D08 必须输出 |
|---|---|
| 视频元数据 | `metadata`：时长、比例、分辨率、fps、音轨、编码、文件大小 |
| 关键帧 | `frames` / `frame_descriptions`：时间戳、图片引用、抽取原因、视觉摘要、置信度 |
| ASR | `transcript_segments`：时间戳、文本、置信度、语言、说话人可选 |
| OCR | `ocr_segments`：时间戳、文本、坐标、置信度、来源帧 |
| 镜头切分 | `shot_segments`：起止时间、代表帧、镜头类型、切分置信度、fallback 标识 |
| 输入完整性 | `input_completeness`：必需输入是否齐备、缺失项和降级原因 |
| 处理置信度 | `processing_quality_report`：各子步骤得分、整体置信度、可否进入 D07 |
| 风险候选 | `risk_candidates`：Logo/水印/人脸/疑似名人/未成年人/医疗金融承诺等候选信号 |

### 3.9 D08 对后续文档的输出

| 后续文档 | D08 输出 |
|---|---|
| D09 | 可信视频证据、镜头段落、文本摘要、可迁移结构证据，供 Creative IR 原创化重构使用 |
| D10 | D08 Prompt 模板、变量、Schema、版本、回归测试要求 |
| D11 | ASR/OCR/多模态模型调用类型、成本字段、降级和模型路由要求 |
| D12 | 视频处理和视频理解 Benchmark 样本类型、评测指标、回归测试入口 |
| D13 | 风险候选信号、不可复用素材证据、相似度风险输入 |
| D15 | 视频处理服务、Worker、对象存储、多模态模型网关的架构要求 |
| D16 | 数据库表、字段、API、错误码、枚举、权限 |
| D17 | 队列名称、状态机、重试、资产生命周期、清理策略 |
| D18 | 原视频、抽帧、音频、ASR/OCR 文本的数据分级、脱敏、访问和删除 |
| D19 | 处理成本、积分预估、失败返还、部分成功计费、后台毛利统计 |
| D20 | 测试、监控、告警、性能和上线验收 |
| D21/D22 | 授权、AIGC 标识、人工复核、投诉删除、客服补偿 |
| D23/D24 | AIcoding 任务执行、交付说明、最终验收和交接清单 |

---

## 4. 核心结论

### 4.1 一句话结论

D08 的核心结论是：MVP1 的视频处理与多模态理解不能做成“把视频丢给大模型直接写报告”，而必须先把视频转化为可追踪证据链：元数据、关键帧、ASR、OCR、镜头段落、帧描述、风险候选和处理质量报告，再由结构化 `MultimodalEvidencePack` 驱动 D07 Content DNA，降低幻觉、控制成本、支持失败降级、积分补偿和合规审计。

### 4.2 D08 的 P0/P1/P2 范围

| 范围 | 内容 | 是否进入 MVP1 |
|---|---|---:|
| P0 | 本地视频上传后的处理管线 | 是 |
| P0 | 文件格式、大小、时长、安全扫描结果校验 | 是 |
| P0 | FFmpeg/FFprobe 元数据解析 | 是 |
| P0 | 音频提取与 ASR | 是，可降级 |
| P0 | 关键帧抽取 | 是 |
| P0 | OCR 识别与文本聚合 | 是，可降级 |
| P0 | 镜头切分 / 固定时间片 fallback | 是 |
| P0 | 帧描述 / 多模态证据包 | 是 |
| P0 | 视频理解结果 `VideoUnderstandingResult` | 是 |
| P0 | 处理质量报告和低置信度原因 | 是 |
| P0 | 任务状态、日志、成本、失败返还 | 是 |
| P1 | 视频链接输入后的合规下载/解析 | 预留，默认关闭 |
| P1 | 长视频裁剪和片段选择 | 预留 |
| P1 | 批量视频处理 | 预留 |
| P1 | 更精细的人声/多说话人分离 | 预留 |
| P2 | 对标账号自动抓取和全量作品下载 | 不做 |
| P2 | 直接生成成片、拼接、字幕和 MP4 导出 | 不做，MVP3 后置 |
| P2 | 人脸识别、声纹识别、仿脸仿声 | 禁止 |
| P2 | 平台反爬、规避审核、下载搬运能力 | 禁止 |

### 4.3 MVP1 推荐视频输入边界

| 项目 | MVP1 默认值 | 处理策略 | 备注 |
|---|---:|---|---|
| 支持格式 | `mp4`、`mov`、`webm`、`m4v` | 非支持格式拒绝 | 前端和后端都校验 |
| 推荐编码 | H.264/H.265/VP9/AAC | 不支持编码尝试转码，失败拒绝 | 转码成本需记录 |
| 时长范围 | 5 秒 - 180 秒 | 小于 5 秒拒绝；超过 180 秒拒绝或提示后续版本 | 15-90 秒为首批 Benchmark 主覆盖 |
| 推荐时长 | 15 秒 - 90 秒 | 质量与成本最优 | 90-180 秒可处理但积分更高 |
| 文件大小 | 首发默认 200MB，可配置到 500MB | 超限拒绝 | 最终以 D16/D17 配置为准 |
| 分辨率 | 最低 360p，推荐 720p+ | 低于 360p 可拒绝或低置信度 | 低清会影响 OCR/视觉理解 |
| 宽高比 | 竖屏 9:16、横屏 16:9、方形 1:1 | 均支持 | 记录到 metadata |
| 音轨 | 有无均可 | 无音频则 ASR 跳过并降级 | 不应整体失败 |
| 字幕/画面文字 | 有无均可 | 有文字时 OCR 应识别 | OCR 失败可降级 |
| 水印/Logo | 可出现但标记风险候选 | 不做去水印、不复用素材 | 风险交给 D13/D21 |
| 明星/影视/综艺/搬运素材 | 高风险 | 默认提示/拦截/转人工 | 不进入高相似生成链路 |

### 4.4 D08 的关键交付物

MVP1 中，每个进入视频处理的任务至少应产出以下数据：

1. `video_processing_job_id`。
2. `video_processing_result_id`。
3. `metadata`。
4. `audio_asset`，如有音频。
5. `transcript_segments`，如 ASR 成功。
6. `frame_assets`。
7. `frame_descriptions`。
8. `ocr_segments`，如 OCR 成功。
9. `shot_segments`。
10. `multimodal_evidence_pack`。
11. `video_understanding_result`。
12. `processing_quality_report`。
13. `risk_candidates`。
14. `model_call_log_ids` / `processing_log_ids`。
15. `task_step_log_ids`。
16. `cost_usage_summary`。
17. `degradation_flags`。
18. `ready_for_content_dna` 布尔值。

### 4.5 D08 上线门槛

D08 模块只有达到以下条件，才允许进入 MVP1 内测：

1. P0 支持格式视频上传后处理成功率 ≥ 80%，目标 ≥ 90%。
2. `VideoProcessingResult` Schema 合法率 ≥ 98%。
3. 标准 15-90 秒视频处理链路 P95 不超过 6 分钟；90-180 秒视频允许更长但必须有进度提示。
4. 元数据解析成功率 ≥ 95%。
5. 抽帧成功率 ≥ 95%。
6. 有音频视频 ASR 成功率目标 ≥ 85%；失败可降级且必须有提示。
7. 有清晰字幕/画面文字视频 OCR 有效识别率目标 ≥ 80%；失败可降级且必须有提示。
8. 镜头切分失败时固定时间片 fallback 成功率 ≥ 99%。
9. `MultimodalEvidencePack` 可被 D07 P02 Content DNA Prompt 消费。
10. 每个处理步骤均有 TaskStepLog / ProcessingLog，后台可定位失败。
11. 涉及模型调用的 ASR/OCR/帧描述/视频理解均记录 ModelCallLog、Prompt 版本、成本、延迟和错误。
12. 任务失败不会造成积分丢失；重复回调、重试和取消不会重复扣费。
13. 高风险素材候选能进入 `risk_candidates`，不得直接输出规避或复用建议。
14. 开发环境 Mock 数据必须显著标识，生产默认关闭。

---

## 5. 详细设计

## 5.1 D08 总体工作流

### 5.1.1 标准流程

```text
用户完成画像与授权确认
→ 上传视频并生成 asset_id
→ 文件安全扫描与基础校验
→ 积分预估与冻结
→ 创建 VideoAnalysisTask
→ 创建 VideoProcessingJob
→ 解析元数据 metadata
→ 生成可处理代理文件 proxy_video（必要时）
→ 提取音频 audio_asset
→ 执行 ASR transcript_segments
→ 抽取关键帧 frame_assets
→ 执行 OCR ocr_segments
→ 镜头切分 shot_segments
→ 帧描述 frame_descriptions
→ 构建 MultimodalEvidencePack
→ 执行 P01_VIDEO_UNDERSTANDING
→ 生成 VideoUnderstandingResult
→ 生成 ProcessingQualityReport
→ 判断 ready_for_content_dna
→ 触发 D07 Content DNA 生成
```

### 5.1.2 降级流程

```text
视频处理子步骤失败
→ 判断是否关键失败
→ 关键失败：任务 failed，触发积分返还和后台告警
→ 可降级失败：记录 degradation_flags
→ 进入 partial_completed
→ 降低 processing_confidence
→ 前端/后台展示低置信度说明
→ D07 以低置信度生成 Content DNA 或转人工复核
```

关键失败包括：原文件无法读取、元数据完全缺失、视频无可用画面、对象存储不可访问、授权确认缺失、安全扫描不通过。

可降级失败包括：ASR 失败、OCR 失败、镜头切分失败、个别帧描述失败、部分模型超时、低清导致置信度不足。

### 5.1.3 处理层级

| 层级 | 职责 | 产物 | 是否可降级 |
|---|---|---|---:|
| 文件层 | 文件读取、格式、大小、时长、编码、安全扫描 | `AssetValidationResult` | 否，失败则拒绝 |
| 元数据层 | FFprobe/媒体信息解析 | `VideoMetadata` | 否，缺核心字段则失败 |
| 媒体预处理层 | 代理文件、音频提取、帧抽取 | `AudioAsset`、`FrameAsset` | 部分可降级 |
| 文本识别层 | ASR、OCR、文本清洗 | `TranscriptSegment`、`OCRSegment` | 是 |
| 时间结构层 | 镜头切分、固定时间片 fallback | `ShotSegment` | 是 |
| 视觉理解层 | 帧描述、场景摘要、主体识别 | `FrameDescription` | 是，但影响置信度 |
| 证据包层 | 聚合 metadata、frames、ASR、OCR、shots | `MultimodalEvidencePack` | 是，带缺失说明 |
| 视频理解层 | 时间线、情绪/节奏/风险候选初步归纳 | `VideoUnderstandingResult` | 是，失败可由 D07 降级使用证据包 |
| 质量评估层 | 完整性、置信度、是否可进入 D07 | `ProcessingQualityReport` | 否，必须产出 |

## 5.2 视频上传与资产校验

### 5.2.1 输入前置条件

视频处理任务必须满足以下前置条件，否则不得进入高成本队列：

| 条件 | 是否必需 | 失败处理 |
|---|---:|---|
| 用户已登录 | 是 | 阻断，跳转登录 |
| CreatorProfile 可用 | 是 | 阻断，引导补充画像 |
| 授权确认完成 | 是 | 阻断，展示授权说明 |
| 风险预检已完成 | 是 | 未完成则先执行；高风险按策略拦截/复核 |
| asset_id 已生成 | 是 | 阻断，重新上传 |
| 安全扫描通过 | 是 | 拦截，生成后台安全事件 |
| 积分预估和冻结完成 | 是 | 余额不足则阻断；冻结失败不得执行 |
| 文件格式/时长/大小合规 | 是 | 阻断，提示可接受范围 |

### 5.2.2 AssetValidationResult

```json
{
  "asset_id": "ast_123",
  "user_id": "usr_123",
  "file_name": "demo.mp4",
  "mime_type": "video/mp4",
  "file_size_bytes": 104857600,
  "duration_sec": 72.4,
  "container_format": "mp4",
  "video_codec": "h264",
  "audio_codec": "aac",
  "width": 1080,
  "height": 1920,
  "fps": 30,
  "aspect_ratio": "9:16",
  "has_audio": true,
  "security_scan_status": "passed",
  "validation_status": "accepted",
  "validation_errors": [],
  "validation_warnings": ["contains_watermark_candidate"],
  "created_at": "2026-05-22T10:00:00Z"
}
```

### 5.2.3 文件校验规则

| 校验项 | P0 规则 | 错误码 | 用户提示 |
|---|---|---|---|
| 文件为空 | 拒绝 | `ASSET_EMPTY_FILE` | 文件为空，请重新上传 |
| MIME 不支持 | 拒绝 | `ASSET_UNSUPPORTED_FORMAT` | 当前仅支持 mp4/mov/webm/m4v |
| 超过大小 | 拒绝 | `ASSET_SIZE_EXCEEDED` | 文件超过当前版本上限，请压缩后上传 |
| 时长过短 | 拒绝 | `ASSET_DURATION_TOO_SHORT` | 视频过短，无法形成有效拆解 |
| 时长过长 | 拒绝或 P1 裁剪 | `ASSET_DURATION_TOO_LONG` | MVP1 建议上传 3 分钟以内视频 |
| 无视频流 | 拒绝 | `ASSET_NO_VIDEO_STREAM` | 未检测到有效视频画面 |
| 安全扫描失败 | 拦截 | `ASSET_SECURITY_BLOCKED` | 文件安全校验未通过 |
| 无音频 | 接受但降级 | `ASSET_NO_AUDIO_WARNING` | 未检测到音频，ASR 将跳过 |
| 低分辨率 | 接受但低置信度 | `ASSET_LOW_RESOLUTION_WARNING` | 画面清晰度较低，OCR/视觉识别可能不完整 |

## 5.3 元数据解析

### 5.3.1 元数据解析目标

元数据解析是 D08 的第一关键步骤。它决定是否能进入后续处理、如何估算成本、如何设置抽帧策略、如何给前端展示进度和如何让 D07 判断视频证据完整性。

### 5.3.2 VideoMetadata Schema

```json
{
  "duration_ms": 72400,
  "width": 1080,
  "height": 1920,
  "fps": 30,
  "frame_count_estimated": 2172,
  "aspect_ratio": "9:16",
  "orientation": "vertical",
  "container_format": "mp4",
  "video_codec": "h264",
  "audio_codec": "aac",
  "audio_channels": 2,
  "audio_sample_rate": 44100,
  "bitrate": 5200000,
  "has_audio": true,
  "has_subtitle_stream": false,
  "color_space": "yuv420p",
  "rotation": 0,
  "source_file_size_bytes": 104857600,
  "metadata_parser": "ffprobe",
  "metadata_confidence": 0.98
}
```

### 5.3.3 元数据失败处理

| 场景 | 处理 |
|---|---|
| FFprobe 无法读取 | 重试一次；仍失败则任务失败，全额返还冻结积分 |
| 缺少时长 | 尝试备用解析；仍失败则任务失败 |
| 缺少分辨率 | 尝试读取首帧；仍失败则任务失败 |
| 编码不支持 | 尝试转码生成 proxy；转码失败则任务失败 |
| 文件可读但内容异常 | 标记 `metadata_confidence < 0.6`，转人工或失败 |

## 5.4 视频代理文件与音频提取

### 5.4.1 代理文件策略

MVP1 不强制转码所有视频，但建议在以下场景生成 proxy：

1. 原视频编码不稳定或无法直接抽帧。
2. 原视频码率过高，处理成本明显增加。
3. 原视频存在旋转/方向信息，需要标准化。
4. 原视频帧率异常，需要稳定抽帧。

Proxy 文件要求：

| 项目 | 建议 |
|---|---|
| 格式 | mp4 |
| 视频编码 | H.264 |
| 音频编码 | AAC 或 WAV 单独音频 |
| 分辨率 | 保持原比例，最长边可降到 1280 用于处理 |
| 用途 | 仅内部处理，不对用户暴露 |
| 留存 | 按资产生命周期清理 |

### 5.4.2 AudioAsset Schema

```json
{
  "audio_asset_id": "aud_123",
  "task_id": "task_123",
  "asset_id": "ast_123",
  "storage_key": "internal/audio/task_123.wav",
  "duration_ms": 72400,
  "sample_rate": 16000,
  "channels": 1,
  "format": "wav",
  "extraction_status": "completed",
  "extraction_error": null,
  "created_at": "2026-05-22T10:01:00Z"
}
```

### 5.4.3 音频处理规则

| 场景 | 处理 |
|---|---|
| 有音频 | 提取 16kHz 单声道音频用于 ASR |
| 无音频 | 写入 `has_audio=false`，跳过 ASR，不失败 |
| 音频过短 | ASR 可跳过，低置信度提示 |
| 音频质量差 | ASR 输出低置信度，D07 报告提示 |
| 音频提取失败 | 重试，失败后可跳过 ASR 并降级 |

## 5.5 抽帧策略

### 5.5.1 抽帧目标

抽帧不是为了复用视频画面，而是为了给多模态理解和 D07 Content DNA 提供视觉证据。抽帧图像不得作为用户可复用素材输出，不得用于生成高相似画面，不得暴露永久公开链接。

### 5.5.2 P0 抽帧策略

MVP1 采用混合抽帧策略：

1. **固定时间抽帧**：每 2-3 秒抽 1 帧，确保全局覆盖。
2. **首段密集抽帧**：前 3 秒至少抽 2-3 帧，服务钩子识别。
3. **镜头边界抽帧**：若镜头切分可用，每个镜头取起始帧、中间帧或代表帧。
4. **字幕/文字高密度帧**：若 OCR 初筛发现文字密集，可补抽局部帧。
5. **最大帧数限制**：15-90 秒视频建议 12-32 帧；90-180 秒视频建议 24-48 帧，超出需要成本控制。

### 5.5.3 FrameAsset Schema

```json
{
  "frame_id": "frm_001",
  "task_id": "task_123",
  "asset_id": "ast_123",
  "timestamp_ms": 3000,
  "storage_key": "internal/frames/task_123/00003000.jpg",
  "width": 720,
  "height": 1280,
  "extraction_reason": "hook_window|fixed_interval|shot_boundary|text_dense|representative",
  "shot_id": "shot_001",
  "perceptual_hash": "phash_xxx",
  "is_blurry": false,
  "brightness_score": 0.72,
  "text_density_estimate": 0.36,
  "face_candidate_count": 1,
  "logo_or_watermark_candidate": true,
  "created_at": "2026-05-22T10:02:00Z"
}
```

### 5.5.4 抽帧失败处理

| 场景 | 处理 |
|---|---|
| 全部抽帧失败 | 关键失败，任务失败，全额返还或转人工 |
| 个别时间点抽帧失败 | 跳过并记录，不影响整体 |
| 抽帧数量不足 3 帧 | 标记低置信度；无法补抽则转人工/失败 |
| 帧模糊或黑屏过多 | 增加补抽；仍不足则低置信度 |
| 对象存储写入失败 | 重试；失败则该步骤失败 |

## 5.6 ASR 语音识别

### 5.6.1 ASR 目标

ASR 负责提取语音文本证据，帮助识别钩子、内容结构、观点、案例、CTA 和转化路径。ASR 输出不得用于洗稿，不得将原台词作为可复用脚本输出。

### 5.6.2 TranscriptSegment Schema

```json
{
  "segment_id": "asr_001",
  "task_id": "task_123",
  "start_ms": 0,
  "end_ms": 3200,
  "text": "原始识别文本片段",
  "text_summary": "片段摘要，不作为可复用文案",
  "language": "zh-CN",
  "speaker_label": "speaker_1",
  "confidence": 0.91,
  "words": [
    {"word": "示例", "start_ms": 200, "end_ms": 600, "confidence": 0.93}
  ],
  "provider": "asr_provider",
  "model": "asr_model",
  "is_low_confidence": false,
  "risk_terms": [],
  "created_at": "2026-05-22T10:03:00Z"
}
```

### 5.6.3 ASR 清洗规则

1. 去除明显语气词、重复词，但保留原始文本字段。
2. 保留时间戳，不合并过长段落。
3. 对低置信度词标记，不强行修正为确定内容。
4. 高风险词、夸大承诺词、医疗/金融/法律结论词写入 `risk_terms`。
5. 输出给 D07 时优先提供片段摘要和时间戳，避免大段原文被下游复用。

### 5.6.4 ASR 失败降级

| 场景 | 处理 |
|---|---|
| 视频无音频 | `asr_available=false`，继续处理 |
| 音频质量差 | 输出低置信度 ASR，继续处理 |
| ASR 服务超时 | 重试，必要时切换备用供应商 |
| ASR 完全失败 | 继续 OCR/视觉处理，标记 `degradation_flags=[asr_failed]` |
| ASR 与 OCR 冲突 | 保留两者，交给视频理解阶段做证据冲突说明 |

## 5.7 OCR 画面文字识别

### 5.7.1 OCR 目标

OCR 负责识别字幕、标题卡、价格、门店信息、屏幕录制文本、产品卖点、CTA 等画面文字。OCR 不负责复制原字幕，也不允许把原字幕转成用户可用脚本。

### 5.7.2 OCRSegment Schema

```json
{
  "ocr_id": "ocr_001",
  "task_id": "task_123",
  "frame_id": "frm_001",
  "timestamp_ms": 3000,
  "text": "识别到的画面文字",
  "text_summary": "文字摘要",
  "bbox": {"x": 120, "y": 900, "w": 840, "h": 160},
  "confidence": 0.87,
  "text_role": "subtitle|title_card|cta|price|address|logo|watermark|screen_text|unknown",
  "is_repeated_subtitle": false,
  "risk_terms": ["限时", "保证"],
  "created_at": "2026-05-22T10:04:00Z"
}
```

### 5.7.3 OCR 聚合规则

1. 相邻帧重复字幕应合并成一个 `ocr_text_cluster`。
2. 字幕、标题卡、CTA、价格/地址、Logo/水印要区分角色。
3. 低置信度 OCR 不得作为确定事实，只能作为候选证据。
4. 价格、疗效、收益、保过、权威资质等敏感表达写入风险候选。
5. OCR 原文只保留在受限证据层，下游报告优先展示摘要。

### 5.7.4 OCR 失败降级

| 场景 | 处理 |
|---|---|
| 视频无字幕/无文字 | `ocr_available=false`，不影响整体 |
| OCR 服务失败 | 重试或换模型；仍失败则降级 |
| 低清导致 OCR 质量差 | 标记低置信度，报告提示 |
| OCR 输出乱码 | 清洗后仍无效则丢弃该段并记录 |
| OCR 识别到大量 Logo/水印 | 标记 `logo_or_watermark_candidate=true`，交给风险模块 |

## 5.8 镜头切分与时间结构

### 5.8.1 镜头切分目标

镜头切分用于形成视频时间结构，帮助 D07 生成 Shot DNA、节奏分析和情绪曲线。镜头切分不是为了指导用户照拍原镜头，而是为了理解结构用途。

### 5.8.2 ShotSegment Schema

```json
{
  "shot_id": "shot_001",
  "task_id": "task_123",
  "start_ms": 0,
  "end_ms": 3200,
  "duration_ms": 3200,
  "representative_frame_id": "frm_001",
  "boundary_confidence": 0.83,
  "segmentation_mode": "scene_detection|fixed_interval_fallback|manual|model_based",
  "visual_change_score": 0.76,
  "text_change_score": 0.62,
  "audio_change_score": 0.41,
  "shot_type_candidate": "talking_head|product_closeup|store_scene|screen_recording|text_card|mixed|unknown",
  "notes": []
}
```

### 5.8.3 切分策略

P0 推荐组合：

1. 基于画面变化的场景检测。
2. 基于字幕/文字变化的辅助切分。
3. 基于音频停顿/语义段落的辅助切分。
4. 失败时按固定时间片 fallback：短视频每 3-5 秒一段，长一些的视频每 5-8 秒一段。
5. 首 3 秒单独成段或密集标记，支持钩子识别。

### 5.8.4 镜头切分失败降级

| 场景 | 处理 |
|---|---|
| 场景检测失败 | 使用固定时间片 fallback |
| 镜头过碎 | 合并小于 800ms 的短段 |
| 镜头过长 | 按语义/固定时间再次切分 |
| 无法判断镜头类型 | 标记 `unknown`，不影响下游 |
| 切分置信度低 | 标记 `segmentation_confidence < 0.6`，D07 低置信度提示 |

## 5.9 帧描述与视觉候选识别

### 5.9.1 帧描述目标

帧描述负责将关键帧转成文本证据，便于后续模型在不反复读取大量图片的情况下理解画面。但帧描述必须客观、证据化、低幻觉，不做创意生成。

### 5.9.2 FrameDescription Schema

```json
{
  "frame_id": "frm_001",
  "timestamp_ms": 3000,
  "visual_summary": "一名人物在办公室对镜头口播，画面下方有大字字幕。",
  "scene_type": "office|store|street|home|studio|screen|mixed|unknown",
  "subjects": ["person", "subtitle", "product"],
  "actions": ["talking_to_camera"],
  "on_screen_text_summary": "字幕大意：提醒用户避免某类错误",
  "brand_logo_candidates": ["疑似品牌标识"],
  "face_identity_candidate": true,
  "minor_candidate": false,
  "unsafe_visual_candidates": [],
  "confidence": 0.84,
  "provider": "vision_model_provider",
  "model": "vision_model_name"
}
```

### 5.9.3 视觉候选识别边界

允许识别：

1. 场景类型。
2. 人物是否出现。
3. 商品/门店/屏幕/字幕/Logo/水印是否出现。
4. 是否可能存在未成年人、高风险医疗金融画面、血腥暴力、擦边内容等候选信号。
5. 镜头用途和画面信息密度。

禁止识别：

1. 具体自然人身份。
2. 人脸比对、明星识别、身份克隆。
3. 声纹识别。
4. 去水印或规避平台审核。
5. 生成复刻原画面的指令。

## 5.10 多模态证据包构建

### 5.10.1 MultimodalEvidencePack 作用

`MultimodalEvidencePack` 是 D08 的核心输出，也是 D07 Content DNA 的主要输入。它必须把视频处理结果组织为“证据包”，而不是散落的模型输出。D07 后续的每个关键判断都应尽量能追溯到证据包里的时间戳、ASR、OCR、帧或镜头。

### 5.10.2 MultimodalEvidencePack Schema

```json
{
  "evidence_pack_id": "mep_123",
  "task_id": "task_123",
  "asset_id": "ast_123",
  "schema_version": "multimodal_evidence_pack.v1.0",
  "metadata": {},
  "input_completeness": {
    "metadata_available": true,
    "frames_available": true,
    "asr_available": true,
    "ocr_available": true,
    "shot_segments_available": true,
    "frame_descriptions_available": true,
    "missing_items": [],
    "degradation_flags": []
  },
  "frames": [],
  "frame_descriptions": [],
  "transcript_segments": [],
  "ocr_segments": [],
  "shot_segments": [],
  "timeline_evidence": [
    {
      "time_range": "00:00-00:03",
      "frame_refs": ["frm_001", "frm_002"],
      "asr_refs": ["asr_001"],
      "ocr_refs": ["ocr_001"],
      "shot_refs": ["shot_001"],
      "evidence_summary": "开头出现口播和大字字幕，表达一个痛点提醒。"
    }
  ],
  "risk_candidates": [],
  "processing_quality_report": {},
  "created_at": "2026-05-22T10:06:00Z"
}
```

### 5.10.3 证据包质量要求

1. 必须包含 `metadata`。
2. 必须至少包含 3 个可用 frame 或明确失败。
3. 必须包含 `input_completeness`。
4. 必须包含 `timeline_evidence`，即按时间段聚合视觉、语音、文字和镜头证据。
5. 不得包含永久公开文件链接，只能包含内部 `storage_key` 或短期签名 URL 引用。
6. ASR/OCR 原文仅作为受限证据，不应被下游直接复制为用户脚本。
7. 风险候选不得被删除，即使模型不确定也应保留为 candidate。

## 5.11 视频理解结果

### 5.11.1 VideoUnderstandingResult 定义

`VideoUnderstandingResult` 是 D08 在进入 D07 前的“证据摘要层”。它不是 Content DNA，也不是拆解报告。它只负责对多模态证据进行客观归纳，帮助 D07 更稳定地识别内容结构。

### 5.11.2 VideoUnderstandingResult Schema

```json
{
  "video_understanding_id": "vur_123",
  "task_id": "task_123",
  "schema_version": "video_understanding.v1.0",
  "video_brief": "视频客观摘要，不做爆款归因，不输出可复用脚本。",
  "timeline_summary": [
    {
      "time_range": "00:00-00:03",
      "what_happens": "人物口播提出一个问题，字幕强调痛点。",
      "visual_evidence_refs": ["frm_001"],
      "asr_refs": ["asr_001"],
      "ocr_refs": ["ocr_001"],
      "shot_refs": ["shot_001"],
      "confidence": 0.86
    }
  ],
  "visible_elements_summary": {
    "people_present": true,
    "product_present": false,
    "store_or_location_present": false,
    "screen_recording_present": false,
    "text_overlay_present": true,
    "logo_or_watermark_present": true
  },
  "speech_text_summary": "口播主要围绕一个痛点展开。",
  "ocr_text_summary": "字幕强调风险和解决方向。",
  "rhythm_observations": ["前3秒信息密度较高", "中段以解释为主"],
  "risk_candidates": [
    {
      "risk_type": "logo_or_watermark|face_identity|minor|copyright|medical_claim|financial_claim|legal_claim|misleading_promise|adult_sensitive|other",
      "risk_level_candidate": "low|medium|high",
      "time_range": "00:00-00:03",
      "evidence_refs": ["frm_001"],
      "reason": "画面疑似包含第三方水印"
    }
  ],
  "uncertainty_notes": [],
  "ready_for_content_dna": true
}
```

### 5.11.3 VideoUnderstandingResult 边界

允许：

1. 客观描述视频中发生了什么。
2. 汇总时间线。
3. 标记视觉、文本、语音和镜头证据。
4. 标记风险候选和不确定性。
5. 为 D07 提供证据引用。

禁止：

1. 生成原创脚本。
2. 输出爆款原因最终结论。
3. 提供复刻提示词。
4. 指导用户复制原台词、镜头、人物、封面或声音。
5. 对自然人身份做识别或判断。

## 5.12 处理质量报告

### 5.12.1 ProcessingQualityReport Schema

```json
{
  "processing_quality_report_id": "pqr_123",
  "task_id": "task_123",
  "overall_processing_score": 0.82,
  "ready_for_content_dna": true,
  "dimensions": {
    "metadata_quality": 0.98,
    "frame_quality": 0.86,
    "asr_quality": 0.84,
    "ocr_quality": 0.76,
    "shot_segmentation_quality": 0.72,
    "frame_description_quality": 0.80,
    "timeline_alignment_quality": 0.78
  },
  "degradation_flags": ["ocr_low_confidence"],
  "blocking_errors": [],
  "low_confidence_reasons": ["部分字幕较模糊，OCR 置信度偏低"],
  "recommended_downstream_action": "continue|continue_with_low_confidence|manual_review|fail_and_refund",
  "cost_usage_summary": {
    "video_seconds_processed": 72.4,
    "frames_extracted": 24,
    "asr_audio_seconds": 72.4,
    "ocr_frames_processed": 24,
    "vision_model_frames_processed": 12,
    "estimated_cost_usd": 0.35
  }
}
```

### 5.12.2 进入 D07 的判定

| 条件 | 动作 |
|---|---|
| `overall_processing_score >= 0.70` 且无阻断错误 | 正常进入 D07 |
| `0.50 <= score < 0.70` | 低置信度进入 D07，前端展示提示 |
| `score < 0.50` | 转人工或失败返还，不建议自动生成报告 |
| 缺 metadata 或可用帧少于 3 | 阻断，失败返还 |
| ASR/OCR 任一缺失但视觉足够 | 可继续，标记降级 |
| 风险候选为高风险 | 进入人工复核或高强度风控，不直接进入原创输出 |

---

## 6. 数据结构 / API / Prompt / 流程 / 页面 / 状态

## 6.1 数据库 Schema 草案

> 说明：以下为 D08 草案，最终以 D16 数据库 Schema、D17 任务队列和文件生命周期文档为准。

### 6.1.1 video_processing_jobs

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | uuid | 是 | 视频处理任务 ID |
| video_analysis_task_id | uuid | 是 | 主任务 ID |
| user_id | uuid | 是 | 用户 ID |
| asset_id | uuid | 是 | 原视频资产 ID |
| status | enum | 是 | processing 状态 |
| processing_config | jsonb | 是 | 抽帧、ASR、OCR、镜头切分配置 |
| idempotency_key | string | 是 | 幂等键 |
| retry_count | int | 是 | 总重试次数 |
| degradation_flags | jsonb | 否 | 降级标记 |
| error_code | string | 否 | 错误码 |
| error_message | text | 否 | 错误摘要 |
| started_at | timestamp | 否 | 开始时间 |
| completed_at | timestamp | 否 | 完成时间 |
| created_at | timestamp | 是 | 创建时间 |
| updated_at | timestamp | 是 | 更新时间 |

### 6.1.2 video_processing_results

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | uuid | 是 | 处理结果 ID |
| job_id | uuid | 是 | 处理任务 ID |
| task_id | uuid | 是 | 主任务 ID |
| asset_id | uuid | 是 | 原视频资产 ID |
| schema_version | string | 是 | `video_processing_result.v1.0` |
| metadata | jsonb | 是 | VideoMetadata |
| audio_asset | jsonb | 否 | AudioAsset 摘要 |
| frames_summary | jsonb | 是 | 抽帧摘要 |
| transcript_summary | jsonb | 否 | ASR 摘要 |
| ocr_summary | jsonb | 否 | OCR 摘要 |
| shot_summary | jsonb | 是 | 镜头切分摘要 |
| evidence_pack | jsonb | 是 | MultimodalEvidencePack |
| processing_quality_report | jsonb | 是 | ProcessingQualityReport |
| ready_for_content_dna | boolean | 是 | 是否可进入 D07 |
| status | enum | 是 | completed / partial_completed / failed |
| created_at | timestamp | 是 | 创建时间 |
| updated_at | timestamp | 是 | 更新时间 |

### 6.1.3 frame_assets

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | uuid | 是 | 帧 ID |
| task_id | uuid | 是 | 主任务 ID |
| asset_id | uuid | 是 | 原资产 ID |
| timestamp_ms | int | 是 | 帧时间戳 |
| storage_key | string | 是 | 内部存储 key |
| extraction_reason | enum | 是 | fixed_interval / hook_window / shot_boundary / text_dense / representative |
| width | int | 是 | 帧宽 |
| height | int | 是 | 帧高 |
| phash | string | 否 | 感知哈希 |
| quality_metrics | jsonb | 否 | 模糊、亮度、文本密度等 |
| risk_candidates | jsonb | 否 | 风险候选 |
| created_at | timestamp | 是 | 创建时间 |

### 6.1.4 transcript_segments

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | uuid | 是 | ASR 段 ID |
| task_id | uuid | 是 | 主任务 ID |
| start_ms | int | 是 | 开始时间 |
| end_ms | int | 是 | 结束时间 |
| text | text | 是 | 原始识别文本，受限访问 |
| text_summary | text | 否 | 摘要文本 |
| confidence | decimal | 是 | 置信度 |
| language | string | 否 | 语言 |
| provider | string | 否 | ASR 供应商 |
| model | string | 否 | ASR 模型 |
| risk_terms | jsonb | 否 | 风险词 |
| created_at | timestamp | 是 | 创建时间 |

### 6.1.5 ocr_segments

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | uuid | 是 | OCR 段 ID |
| task_id | uuid | 是 | 主任务 ID |
| frame_id | uuid | 是 | 来源帧 |
| timestamp_ms | int | 是 | 时间戳 |
| text | text | 是 | OCR 原文，受限访问 |
| text_summary | text | 否 | 摘要 |
| bbox | jsonb | 否 | 位置框 |
| confidence | decimal | 是 | 置信度 |
| text_role | enum | 否 | subtitle / title_card / cta / price / address / logo / watermark / screen_text / unknown |
| risk_terms | jsonb | 否 | 风险词 |
| created_at | timestamp | 是 | 创建时间 |

### 6.1.6 shot_segments

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | uuid | 是 | 镜头段 ID |
| task_id | uuid | 是 | 主任务 ID |
| start_ms | int | 是 | 开始时间 |
| end_ms | int | 是 | 结束时间 |
| representative_frame_id | uuid | 否 | 代表帧 |
| segmentation_mode | enum | 是 | scene_detection / fixed_interval_fallback / manual / model_based |
| boundary_confidence | decimal | 是 | 切分置信度 |
| shot_type_candidate | enum | 否 | talking_head / product_closeup / store_scene / screen_recording / text_card / mixed / unknown |
| created_at | timestamp | 是 | 创建时间 |

### 6.1.7 video_understanding_results

| 字段 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| id | uuid | 是 | 视频理解结果 ID |
| task_id | uuid | 是 | 主任务 ID |
| evidence_pack_id | string | 是 | 证据包 ID |
| schema_version | string | 是 | `video_understanding.v1.0` |
| prompt_version | string | 是 | Prompt 版本 |
| model_call_log_id | uuid | 否 | 模型调用日志 |
| result_json | jsonb | 是 | VideoUnderstandingResult |
| confidence_score | decimal | 是 | 置信度 |
| status | enum | 是 | completed / partial_completed / failed |
| created_at | timestamp | 是 | 创建时间 |

## 6.2 API 契约草案

### 6.2.1 创建视频处理任务

`POST /api/internal/video-processing/jobs`

请求：

```json
{
  "task_id": "task_123",
  "asset_id": "ast_123",
  "user_id": "usr_123",
  "creator_profile_snapshot_id": "cps_123",
  "authorization_ack_id": "ack_123",
  "risk_precheck_id": "rp_123",
  "processing_config": {
    "max_duration_sec": 180,
    "frame_strategy": "hybrid_v1",
    "enable_asr": true,
    "enable_ocr": true,
    "enable_shot_detection": true,
    "enable_frame_description": true,
    "enable_video_understanding": true
  },
  "idempotency_key": "task_123:video_processing:v1"
}
```

响应：

```json
{
  "video_processing_job_id": "vpj_123",
  "status": "queued",
  "next_step": "metadata_parsing"
}
```

### 6.2.2 查询视频处理结果

`GET /api/internal/video-processing/results/{task_id}`

响应：

```json
{
  "task_id": "task_123",
  "status": "partial_completed",
  "video_processing_result_id": "vpr_123",
  "ready_for_content_dna": true,
  "degradation_flags": ["ocr_low_confidence"],
  "metadata": {},
  "evidence_pack": {},
  "processing_quality_report": {}
}
```

### 6.2.3 重试单个步骤

`POST /api/internal/video-processing/jobs/{job_id}/retry-step`

请求：

```json
{
  "step_code": "running_ocr",
  "reason": "ocr_provider_timeout",
  "requested_by": "system|admin",
  "idempotency_key": "vpj_123:running_ocr:retry_1"
}
```

### 6.2.4 取消处理任务

`POST /api/internal/video-processing/jobs/{job_id}/cancel`

请求：

```json
{
  "cancel_reason": "user_cancelled|timeout|admin_cancelled|risk_blocked",
  "requested_by": "user|system|admin"
}
```

### 6.2.5 后台诊断接口

`GET /api/admin/video-processing/jobs/{job_id}`

响应必须包含：任务基础信息、步骤日志、模型调用日志、成本、降级、错误、证据包摘要、权限脱敏后的帧/ASR/OCR/镜头诊断。

## 6.3 任务状态机

### 6.3.1 D08 状态枚举

```text
queued
metadata_parsing
metadata_parsed
normalizing
extracting_audio
running_asr
extracting_frames
running_ocr
detecting_shots
describing_frames
building_evidence_pack
understanding_video
quality_checking
partial_completed
completed
failed
cancelled
manual_review_pending
```

### 6.3.2 状态流转

```text
queued
→ metadata_parsing
→ metadata_parsed
→ normalizing? 
→ extracting_audio? 
→ running_asr? 
→ extracting_frames
→ running_ocr?
→ detecting_shots
→ describing_frames?
→ building_evidence_pack
→ understanding_video?
→ quality_checking
→ completed / partial_completed / failed / manual_review_pending
```

`?` 表示可配置或可降级步骤。

### 6.3.3 状态与前端展示

| 内部状态 | 用户可见文案 | 是否可取消 | 失败后动作 |
|---|---|---:|---|
| queued | 已进入队列 | 是 | 取消后释放冻结积分 |
| metadata_parsing | 正在读取视频信息 | 是 | 重新上传 |
| extracting_audio / running_asr | 正在识别语音内容 | 是 | 可降级继续 |
| extracting_frames | 正在提取关键画面 | 否 | 关键失败，重试/返还 |
| running_ocr | 正在识别画面文字 | 是 | 可降级继续 |
| detecting_shots | 正在分析镜头结构 | 是 | 固定切片降级 |
| building_evidence_pack | 正在整理多模态证据 | 否 | 重试 |
| understanding_video | 正在理解视频内容 | 是 | 降级到证据包或重试 |
| partial_completed | 已完成，但部分识别置信度较低 | 否 | 可查看低置信度提示 |
| completed | 视频处理完成 | 否 | 进入 Content DNA |
| failed | 视频处理失败 | 否 | 显示返还/客服入口 |
| manual_review_pending | 等待人工复核 | 否 | 后台处理 |

## 6.4 队列设计

### 6.4.1 推荐队列

| 队列 | 职责 | 并发建议 | 是否高成本 |
|---|---|---:|---:|
| `video.metadata.queue` | 元数据解析 | 高 | 否 |
| `video.normalize.queue` | 转码/代理文件 | 低-中 | 是 |
| `video.audio.queue` | 音频提取 | 中 | 否 |
| `ai.asr.queue` | ASR | 中 | 是 |
| `video.frames.queue` | 抽帧 | 中 | 否 |
| `ai.ocr.queue` | OCR | 中 | 是 |
| `video.shots.queue` | 镜头切分 | 中 | 否/中 |
| `ai.frame_description.queue` | 帧描述 | 低-中 | 是 |
| `video.evidence_pack.queue` | 证据包构建 | 高 | 否 |
| `ai.video_understanding.queue` | 视频理解 | 低-中 | 是 |
| `video.quality.queue` | 处理质量评估 | 高 | 否 |
| `billing.compensation.queue` | 失败返还/释放冻结 | 高 | 否 |

### 6.4.2 幂等规则

1. 每个步骤必须使用 `task_id + step_code + attempt_no` 生成幂等键。
2. 同一幂等键重复投递不得重复生成成本或重复写入结果。
3. 失败重试必须递增 attempt，并保留历史日志。
4. 任务取消后未开始的后续步骤不得继续执行。
5. 已完成步骤不得被普通重试覆盖，除非 Admin 明确发起 `force_rerun` 并写审计日志。

## 6.5 日志与可观测性

### 6.5.1 TaskStepLog 要求

每个步骤必须记录：

```json
{
  "task_id": "task_123",
  "step_code": "running_asr",
  "step_status": "started|completed|failed|skipped|degraded",
  "attempt_no": 1,
  "started_at": "2026-05-22T10:03:00Z",
  "ended_at": "2026-05-22T10:03:42Z",
  "duration_ms": 42000,
  "input_summary": {"audio_seconds": 72.4},
  "output_summary": {"segments": 18, "avg_confidence": 0.86},
  "error_code": null,
  "error_message": null,
  "cost_estimate": {"credits": 2, "usd": 0.08},
  "model_call_log_ids": ["mcl_123"]
}
```

### 6.5.2 ModelCallLog 要求

涉及 ASR、OCR、帧描述、视频理解的模型调用必须记录：

| 字段 | 说明 |
|---|---|
| provider | 模型供应商 |
| model | 模型名称 |
| prompt_code | Prompt code |
| prompt_version | Prompt 版本 |
| input_media_summary | 输入媒体摘要，不存敏感明文 |
| input_token_estimate | 输入 token 或媒体量估算 |
| output_token_estimate | 输出 token 估算 |
| media_units | 帧数、音频秒数、视频秒数 |
| latency_ms | 延迟 |
| cost_usd | 成本 |
| status | success / failed / timeout / retry |
| error_code | 错误码 |
| is_mock | 是否 mock |

### 6.5.3 监控指标

MVP1 至少监控：

1. 视频处理成功率。
2. 各步骤失败率。
3. 各步骤 P50/P95 延迟。
4. ASR/OCR/视频理解模型错误率。
5. 平均单任务处理成本。
6. 平均单任务冻结积分、扣费、返还。
7. 降级任务比例。
8. 人工复核触发率。
9. 对象存储读取/写入失败率。
10. 队列积压数量和最长等待时间。

## 6.6 Prompt 设计

### 6.6.1 Prompt 总览

| Prompt Code | 用途 | 所属模块 | 输出 |
|---|---|---|---|
| `P01_FRAME_DESCRIPTION` | 关键帧客观描述 | D08/M07-M08 | `FrameDescription` |
| `P01_ASR_CLEANUP` | ASR 文本清洗与摘要 | D08/M07 | 清洗后的 `TranscriptSegment` 摘要 |
| `P01_OCR_AGGREGATION` | OCR 聚合与角色识别 | D08/M07 | OCR 聚类与 text_role |
| `P01_VIDEO_UNDERSTANDING` | 多模态证据归纳 | D08/M08 前置 | `VideoUnderstandingResult` |
| `P01_EVIDENCE_PACK_REPAIR` | 证据包 JSON 修复 | D08/M08 前置 | 合法 JSON |

### 6.6.2 P01_FRAME_DESCRIPTION 系统 Prompt

```text
你是短视频关键帧客观描述助手。

你的任务：
基于单张视频帧图片和已知时间戳，输出客观、克制、可追溯的 FrameDescription JSON。

你必须遵守：
1. 只描述画面中可见的场景、主体、动作、文字、构图和风险候选。
2. 不识别具体自然人身份，不判断是否为某明星，不做人脸比对。
3. 不输出仿脸、仿声、复刻、搬运、去水印或规避平台审核建议。
4. 如果画面模糊或无法判断，降低 confidence，不要编造。
5. 只输出 JSON，不要输出 Markdown 或解释。
```

用户 Prompt：

```text
【帧时间戳】{{timestamp_ms}}
【帧图片】{{frame_image}}
【OCR 初筛文本】{{ocr_hint}}
【目标 JSON Schema】{{frame_description_schema}}

请输出 FrameDescription JSON。
```

### 6.6.3 P01_VIDEO_UNDERSTANDING 系统 Prompt

```text
你是短视频多模态证据归纳专家。

你的任务：
基于视频元数据、关键帧描述、ASR、OCR、镜头切分和处理质量报告，生成 VideoUnderstandingResult。

你必须遵守：
1. 你只做客观归纳，不做爆款原因最终判断，不生成原创脚本，不生成视频 Prompt。
2. 所有关键判断必须尽量引用 evidence_refs。
3. 你不能建议复用原视频素材、原台词、人物、声音、配乐、Logo、封面或独特剧情。
4. 你不能输出搬运、克隆、洗稿、仿脸、仿声、规避平台风控的方法。
5. 如证据不足，写入 uncertainty_notes 并降低 confidence。
6. 风险候选必须保留，不得为了让任务通过而删除。
7. 只输出严格 JSON，不要输出 Markdown、解释或额外文本。
```

用户 Prompt：

```text
请基于以下多模态证据生成 VideoUnderstandingResult。

【视频元数据】
{{metadata}}

【关键帧描述】
{{frame_descriptions}}

【ASR 片段】
{{transcript_segments}}

【OCR 片段】
{{ocr_segments}}

【镜头切分】
{{shot_segments}}

【处理质量报告】
{{processing_quality_report}}

【风险预检结果】
{{risk_precheck}}

【必须遵守的 JSON Schema】
{{video_understanding_schema}}

输出要求：
1. 只输出 JSON。
2. 必须包含 schema_version=video_understanding.v1.0。
3. 每个 timeline_summary 条目尽量引用 frame/asr/ocr/shot refs。
4. 不确定时写入 uncertainty_notes，不要编造。
5. 不得输出可复制原文案或复刻建议。
```

### 6.6.4 JSON Repair Prompt

```text
你是 JSON Schema 修复器。

以下输出未通过 D08 Schema 校验。请只修复 JSON 结构、字段缺失、枚举错误、类型错误，不要新增无证据内容。

【原始输出】
{{raw_model_output}}

【校验错误】
{{schema_errors}}

【目标 Schema】
{{target_schema}}

修复规则：
1. 只输出合法 JSON。
2. 不要输出解释。
3. 如果缺少证据，请使用 unknown、空数组、低置信度说明。
4. 不得删除 risk_candidates、uncertainty_notes、degradation_flags。
5. 不得新增搬运、复刻、仿脸、仿声、规避平台规则相关内容。
```

## 6.7 页面与 ViewModel

### 6.7.1 上传页 ViewModel

```json
{
  "asset_validation": {
    "accepted": true,
    "file_name": "demo.mp4",
    "duration_sec": 72.4,
    "file_size_mb": 100,
    "format": "mp4",
    "warnings": ["检测到水印候选，后续仅用于结构学习，不会复用素材"]
  },
  "credit_estimate": {
    "estimated_credits": 12,
    "freeze_required": true,
    "refund_policy_summary": "处理失败将按规则返还冻结积分"
  }
}
```

### 6.7.2 任务进度页 ViewModel

```json
{
  "task_id": "task_123",
  "current_stage": "running_ocr",
  "current_stage_label": "正在识别画面文字",
  "overall_progress_percent": 45,
  "steps": [
    {"code": "metadata_parsing", "label": "读取视频信息", "status": "completed"},
    {"code": "running_asr", "label": "识别语音内容", "status": "completed"},
    {"code": "running_ocr", "label": "识别画面文字", "status": "processing"}
  ],
  "can_cancel": true,
  "credit_status": "frozen",
  "degradation_notices": []
}
```

### 6.7.3 低置信度提示 ViewModel

```json
{
  "show_low_confidence_notice": true,
  "notice_level": "warning",
  "title": "部分识别结果置信度较低",
  "reasons": ["字幕较模糊，OCR 识别不完整"],
  "impact": "拆解报告仍可生成，但镜头文字分析可能不完整。",
  "user_actions": ["继续查看", "重新上传更清晰视频", "申请人工处理"]
}
```

## 6.8 权限与数据访问

| 数据 | 前台用户 | Admin | Ops/Support | Review | Developer |
|---|---|---|---|---|---|
| 原视频 | 仅本人可访问，短期签名 | 可访问，需审计 | 默认不可看原文件 | 需要复核时可访问摘要/必要片段 | 生产默认脱敏，访问需审批 |
| 抽帧图片 | 仅本人报告中受控展示或不展示 | 可访问，需审计 | 摘要可见 | 复核可见 | 脱敏或审批 |
| 音频文件 | 不直接展示 | 可访问，需审计 | 不可访问 | 必要时可访问摘要 | 脱敏或审批 |
| ASR/OCR 原文 | 受限展示，不输出大段原文 | 可见，需审计 | 摘要可见 | 可见风险相关段落 | 脱敏 |
| 证据包 | 不直接暴露完整 JSON | 可见 | 摘要可见 | 风险相关可见 | 非生产可见，生产脱敏 |
| 处理日志 | 自己任务的用户可见摘要 | 完整可见 | 可见必要字段 | 可见风险字段 | 可见技术字段 |

---

## 7. 异常与失败处理

## 7.1 失败分类

| 类型 | 示例 | 是否返还积分 | 是否可重试 | 是否进入人工复核 |
|---|---|---:|---:|---:|
| 输入失败 | 格式不支持、超时长、文件损坏 | 未冻结或全额返还 | 用户重新上传 | 否 |
| 授权失败 | 未确认授权、素材来源高风险 | 未冻结或全额返还 | 用户补充确认 | 可能 |
| 安全失败 | 文件安全扫描不通过 | 全额返还 | 否 | 是，后台记录 |
| 元数据失败 | 无法读取视频流/时长 | 全额返还 | 系统重试后失败 | 可选 |
| 抽帧失败 | 无法提取任何可用帧 | 全额或大部分返还 | 是 | 可选 |
| ASR 失败 | 服务超时、无音频、音质差 | 通常不全额返还，按输出质量 | 是 | 否 |
| OCR 失败 | 字幕模糊、OCR 超时 | 通常不全额返还，按输出质量 | 是 | 否 |
| 镜头切分失败 | 场景检测失败 | 不返还，使用 fallback | 是 | 否 |
| 视频理解失败 | 模型超时、JSON 无效 | 重试；最终失败返还 | 是 | 可选 |
| 高风险拦截 | 明星影视搬运、仿脸仿声意图 | 按任务阶段返还 | 否 | 是 |
| 用户取消 | 用户主动取消 | 按已消耗成本结算或释放冻结 | 否 | 否 |
| 系统异常 | 队列/存储/数据库故障 | 全额或大部分返还 | 是 | 运营告警 |

## 7.2 错误码

| 错误码 | 含义 | 用户提示 | 后台动作 |
|---|---|---|---|
| `VIDEO_METADATA_PARSE_FAILED` | 元数据解析失败 | 无法读取视频信息，请重新上传 | 记录 ffprobe 日志 |
| `VIDEO_UNSUPPORTED_CODEC` | 编码不支持 | 当前视频编码暂不支持，请转为 MP4/H.264 | 统计格式失败 |
| `VIDEO_FRAME_EXTRACTION_FAILED` | 抽帧失败 | 无法提取有效画面，请重新上传 | 重试后告警 |
| `VIDEO_ASR_FAILED` | ASR 失败 | 语音识别不完整，将以画面和字幕为主继续分析 | 降级 |
| `VIDEO_OCR_FAILED` | OCR 失败 | 画面文字识别不完整，将降低相关分析置信度 | 降级 |
| `VIDEO_SHOT_DETECTION_FAILED` | 镜头切分失败 | 镜头结构将使用时间片方式估算 | fallback |
| `VIDEO_UNDERSTANDING_SCHEMA_INVALID` | 视频理解 JSON 无效 | 系统正在修复分析结果 | JSON repair + retry |
| `VIDEO_PROCESSING_TIMEOUT` | 处理超时 | 任务处理超时，已为你处理积分返还/补偿 | 告警 + 补偿 |
| `VIDEO_RISK_BLOCKED` | 风险拦截 | 当前素材或需求存在合规风险，无法继续自动处理 | 转人工/记录 |
| `VIDEO_STORAGE_ACCESS_FAILED` | 存储访问失败 | 文件读取失败，请稍后重试 | 存储告警 |

## 7.3 积分返还规则草案

| 失败阶段 | 推荐处理 |
|---|---|
| 上传校验前失败 | 不冻结、不扣费 |
| 冻结后但未开始模型调用 | 全额释放冻结积分 |
| 元数据/抽帧关键失败 | 全额返还 |
| ASR/OCR 单项失败但报告可生成 | 不全额返还；可减免对应子项或给低置信度提示，具体由 D19 定价决定 |
| 视频理解最终失败 | 全额或大部分返还 |
| 用户主动取消 | 未消耗部分释放；已消耗高成本模型按规则结算 |
| 风控拦截 | 若任务前可识别则不扣费；若中途识别且未产出可用结果，返还大部分或全额 |
| 系统故障 | 默认全额返还并记录补偿事件 |

## 7.4 失败用户体验

任何用户可见失败都必须提供：

1. 失败发生在哪一步。
2. 通俗原因。
3. 积分状态：未扣费 / 已返还 / 部分结算 / 待人工处理。
4. 下一步操作：重新上传、继续降级、联系客服、申请人工处理。
5. 若涉及合规，不展示可绕过建议，只展示安全替代路径。

---

## 8. 风控与合规

## 8.1 合规原则

D08 必须贯彻以下合规原则：

1. **授权优先**：未完成素材授权确认，不得读取、处理、抽帧或调用模型。
2. **结构学习**：视频处理只服务结构理解，不服务素材复用、搬运、克隆、洗稿。
3. **不做人脸/声纹识别**：可以标记“有人脸/人物出现”作为风险候选，但不得识别身份或生成仿脸/仿声指令。
4. **不做去水印**：可以识别水印/Logo 候选作为风险信号，但不得提供去水印能力。
5. **不保留超期数据**：原视频、抽帧、音频、ASR/OCR 原文需按资产生命周期清理。
6. **AIGC 标识**：后续报告和生成内容必须说明由 AI 辅助拆解/生成；D08 输出需提供标识字段。
7. **风险保守处理**：影视、明星、综艺、未成年人、高风险医疗金融法律、擦边、暴力等内容应提升风险等级或转人工。

## 8.2 风险候选分类

| 风险类型 | D08 可识别信号 | 默认动作 |
|---|---|---|
| `copyright_media` | 影视/综艺/直播录屏/明显平台搬运素材候选 | 高风险，转人工或拦截 |
| `trademark_logo` | Logo、水印、品牌标识候选 | 标记不可复用，传 D13 |
| `face_identity` | 明显人脸或个人身份元素 | 标记，不识别身份，不仿脸 |
| `voice_identity` | 可听见人物声音 | 只做 ASR，不做声纹/仿声 |
| `minor_candidate` | 疑似未成年人画面 | 提升风险，必要时复核 |
| `medical_claim` | 疗效、治愈、诊断承诺文本/语音 | 高风险，传 D13/D21 |
| `financial_claim` | 收益承诺、稳赚、投资建议 | 高风险，传 D13/D21 |
| `legal_claim` | 法律结论性承诺 | 中高风险，传 D13/D21 |
| `misleading_promise` | 保证、一定、最快、唯一等夸大承诺 | 风险提示/重写 |
| `adult_sensitive` | 擦边/成人敏感画面 | 高风险复核 |
| `platform_abuse` | 搬运、规避审核、批量下载意图 | 拦截 |

## 8.3 人工复核触发

以下情况应创建 `manual_review_ticket`：

1. 安全扫描通过但模型/规则检测到高风险素材。
2. 视频疑似影视、综艺、明星、体育赛事、新闻版权素材。
3. 用户填写的意图包含“复刻同款、克隆、搬运、洗稿、仿声音、仿脸”等高风险词。
4. ASR/OCR 中出现高风险医疗疗效、金融收益、法律结论承诺。
5. D08 处理质量过低但用户请求继续生成。
6. 投诉或删除请求涉及该视频资产。

## 8.4 投诉删除机制

D08 必须支持资产级删除：

1. 用户可以申请删除原视频、抽帧、音频、ASR/OCR 文本、证据包。
2. 投诉成立时，相关资产需进入 `deletion_pending`，停止后续任务。
3. 删除动作必须写 `audit_logs`。
4. 删除后保留必要的合规审计摘要，不保留可复用原素材。
5. 删除影响已生成报告时，前端应显示“源资产已删除，报告证据不可回放”。

## 8.5 数据留存建议

| 数据 | MVP1 默认留存 | 删除策略 |
|---|---:|---|
| 原视频 | 7-30 天，可配置 | 用户删除/投诉成立/到期清理 |
| Proxy 文件 | 3-7 天 | 自动清理 |
| 抽帧图片 | 7-30 天 | 随原视频删除 |
| 音频文件 | 3-7 天 | 处理完成后可提前清理 |
| ASR/OCR 原文 | 30-90 天，受限访问 | 用户删除时清理或脱敏 |
| EvidencePack | 30-90 天 | 删除原素材后保留脱敏摘要或删除 |
| VideoUnderstandingResult | 随报告保留，可脱敏 | 用户删除报告时清理 |
| 日志/计费记录 | 按财务和审计周期 | 不含可复用素材 |

---

## 9. 测试与验收标准

## 9.1 测试类型

| 测试类型 | 覆盖内容 | 必做 |
|---|---|---:|
| 单元测试 | 文件校验、元数据解析、抽帧策略、ASR/OCR parser、shot fallback、Schema 校验 | 是 |
| API 测试 | 创建任务、查询结果、重试、取消、后台诊断 | 是 |
| 队列测试 | 幂等、重复投递、失败重试、取消、超时 | 是 |
| 集成测试 | 上传 → 处理 → 证据包 → 视频理解 → D07 输入 | 是 |
| E2E 测试 | 用户主流程：上传、授权、冻结、进度、完成、报告 | 是 |
| Prompt 回归 | 帧描述、视频理解、JSON repair | 是 |
| 风控测试 | 高风险素材候选、禁止意图、授权缺失、投诉删除 | 是 |
| 计费测试 | 冻结、释放、部分返还、幂等扣费 | 是 |
| 权限测试 | 跨用户访问、后台角色、签名 URL 过期 | 是 |
| 性能测试 | 15/60/90/180 秒视频处理耗时与成本 | 是 |
| 灾难恢复测试 | 存储失败、队列失败、模型供应商超时、数据库异常 | P1，但内测前建议做 |

## 9.2 Benchmark 样本建议

| 样本类型 | 最少数量 | 目的 |
|---|---:|---|
| 企业 IP 口播 | 10 | ASR、前 3 秒钩子、字幕识别 |
| 本地生活探店/门店 | 10 | 场景、产品、地址/价格 OCR、镜头切分 |
| 知识干货 | 5 | 长字幕、知识点时间线 |
| 无音频视频 | 5 | ASR 缺失降级 |
| 无字幕视频 | 5 | OCR 缺失降级 |
| 低清/模糊视频 | 5 | 低置信度与提示 |
| 屏幕录制 | 5 | OCR 密集文本处理 |
| 横屏/方形/竖屏 | 各 5 | 比例兼容 |
| 超短视频 5-10 秒 | 5 | 段落不足处理 |
| 90-180 秒边界视频 | 5 | 成本、性能、抽帧上限 |
| 高风险素材 | 10 | 风险候选与拦截 |
| 损坏/异常文件 | 5 | 校验和失败返还 |

## 9.3 AI 输出质量指标

| 指标 | MVP1 门槛 | 目标 |
|---|---:|---:|
| FrameDescription Schema 合法率 | ≥ 95% | ≥ 98% |
| VideoUnderstandingResult Schema 合法率 | ≥ 95% | ≥ 98% |
| JSON repair 成功率 | ≥ 80% | ≥ 90% |
| 时间线证据引用覆盖率 | ≥ 80% | ≥ 90% |
| 低置信度正确标记率 | ≥ 85% | ≥ 90% |
| 高风险候选召回率 | ≥ 85% | ≥ 90% |
| 不输出复刻/搬运建议 | 100% | 100% |

## 9.4 工程验收标准

D08 交付必须满足：

1. 视频处理主流程 E2E 可跑通。
2. `VideoProcessingResult`、`MultimodalEvidencePack`、`VideoUnderstandingResult` 均有 Schema 校验。
3. ASR/OCR/镜头切分失败不导致无意义失败，除非输入不可用。
4. 关键失败触发积分释放/返还，不重复扣费。
5. 每一步都有 TaskStepLog。
6. 每个模型调用都有 ModelCallLog。
7. 后台可以查看处理结果、步骤日志、模型成本、降级原因和错误码。
8. 前端能展示上传校验、进度、低置信度、失败和返还状态。
9. 高风险候选会传给 D13/D21，不被下游忽略。
10. Mock 能力生产默认关闭且显著标识。

## 9.5 上线前 Checklist

- [ ] P0 支持格式视频处理成功率达到门槛。
- [ ] 15-90 秒 Benchmark 视频 P95 耗时达标。
- [ ] 90-180 秒视频有成本控制和进度提示。
- [ ] 无音频/无字幕/低清/横屏/竖屏/屏幕录制均有测试。
- [ ] ASR/OCR/多模态模型失败可重试和降级。
- [ ] 固定时间片 fallback 可用。
- [ ] EvidencePack 可被 D07 Prompt 直接消费。
- [ ] 计费冻结/释放/返还幂等。
- [ ] 后台可定位失败任务。
- [ ] 权限、签名 URL、数据删除、审计日志通过测试。

---

## 10. AIcoding 研发任务拆分

## 10.1 任务总览

| 任务编号 | 任务名称 | 所属模块 | 优先级 | 适合 Agent | 主要交付物 |
|---|---|---|---:|---|---|
| D08-T01 | 视频处理 Schema、枚举、错误码与配置中心 | M07/M08 | P0 | 后端 Agent | 类型定义、Schema、配置、错误码 |
| D08-T02 | 视频资产校验与元数据解析 | M04/M07 | P0 | 后端/AI 工程 Agent | FFprobe 封装、校验、metadata |
| D08-T03 | 抽帧与 FrameAsset 管理 | M07 | P0 | 后端/AI 工程 Agent | 抽帧策略、frame_assets、质量指标 |
| D08-T04 | 音频提取与 ASR Adapter | M07/M10 | P0 | AI 工程 Agent | audio_asset、TranscriptSegment、ASR 降级 |
| D08-T05 | OCR Adapter 与文本聚合 | M07/M10 | P0 | AI 工程 Agent | OCRSegment、text_role、OCR 降级 |
| D08-T06 | 镜头切分与固定时间片 fallback | M07 | P0 | AI 工程/后端 Agent | ShotSegment、fallback、质量评估 |
| D08-T07 | 帧描述与风险候选识别 | M07/M08 | P0 | AI/Prompt Agent | FrameDescription Prompt、ModelCallLog |
| D08-T08 | MultimodalEvidencePack 构建器 | M07/M08 | P0 | 后端/AI Agent | EvidencePack Schema、timeline_evidence |
| D08-T09 | VideoUnderstandingResult Prompt 与服务 | M08/M10 | P0 | AI/Prompt/后端 Agent | P01_VIDEO_UNDERSTANDING、JSON repair |
| D08-T10 | ProcessingQualityReport 与 ready 判定 | M07/M08 | P0 | 后端/测试 Agent | 质量评分、降级、进入 D07 判定 |
| D08-T11 | 异步任务编排、重试、取消与幂等 | M06/M07 | P0 | 后端 Agent | 队列、状态机、TaskStepLog |
| D08-T12 | 积分冻结/返还与成本日志联动 | M05/M10/M07 | P0 | 后端 Agent | cost_usage、CreditTransaction 联动 |
| D08-T13 | 前端进度与低置信度 ViewModel | M06/M12 | P0 | 前端/全栈 Agent | 上传校验、进度、降级/失败展示 |
| D08-T14 | 后台诊断与人工复核入口 | M13/M14 | P0 | 后台/全栈 Agent | 后台详情、日志、成本、复核入口 |
| D08-T15 | 测试样本、Benchmark 与 E2E 验收 | M14/D12 | P0 | 测试 Agent | 样本集、自动化测试、验收报告 |

## 10.2 研发顺序

```text
D08-T01 Schema/配置/错误码
→ D08-T02 元数据解析
→ D08-T03 抽帧
→ D08-T04 ASR
→ D08-T05 OCR
→ D08-T06 镜头切分
→ D08-T11 任务编排并串联 T02-T06
→ D08-T07 帧描述
→ D08-T08 EvidencePack
→ D08-T09 VideoUnderstanding
→ D08-T10 ProcessingQualityReport
→ D08-T12 计费/成本联动
→ D08-T13 前端进度
→ D08-T14 后台诊断
→ D08-T15 测试验收
```

## 10.3 每个任务通用交付要求

每个 AIcoding Agent 完成任务后必须输出：

1. 读取了哪些文档和版本。
2. 本任务做什么、不做什么。
3. 新增/修改文件清单。
4. 数据库迁移说明。
5. API 变更说明。
6. 队列/状态机变更说明。
7. Prompt/Schema 变更说明。
8. 是否涉及积分扣费和返还。
9. 是否涉及风控合规。
10. 是否使用 mock，如何替换真实能力。
11. 测试命令和结果。
12. 已知问题、风险和后续建议。

---

## 11. AIcoding 执行提示词

### 11.1 D08 总执行提示词

```text
你是本项目的工程级 AIcoding 执行 Agent，负责 D08《视频处理与多模态理解工作流文档》的研发实现。

你必须先读取并遵守：
1. D00《AIcoding 研发总控文档 / 项目上下文总索引》
2. D03《MVP 范围定义与版本路线图文档》
3. D04《总 PRD 文档》
4. D05《功能模块拆分与独立研发边界文档》
5. D07《Content DNA Schema 与拆解报告标准文档》
6. D08《视频处理与多模态理解工作流文档》
7. 如涉及数据库/API/队列，还必须读取 D16/D17；如涉及计费，读取 D19；如涉及合规，读取 D21；如涉及测试，读取 D20/D12。

项目定位：
本项目是 AI 短视频爆款增长引擎，不是搬运、克隆、洗稿、仿脸、仿声工具。D08 只负责把用户上传的视频转化为可校验、可追踪、可风控、可计费、可供 Content DNA 使用的多模态证据包，不允许输出复刻、搬运、仿脸、仿声、去水印、规避平台审核能力。

本次任务：
{填写任务编号和任务名称}

本任务边界：
- 必须实现：{填写必须实现项}
- 明确不做：不生成原创脚本、不生成视频成片、不做账号抓取、不做仿脸仿声、不做去水印、不绕过风控。

执行要求：
1. 先复述你理解的任务边界。
2. 列出将新增/修改的文件。
3. 如涉及 DB，先给 migration 草案。
4. 如涉及 API，先给 request/response/error code。
5. 如涉及队列，先给状态流转和幂等策略。
6. 如涉及 AI，必须给 Prompt code、Prompt version、Input Schema、Output Schema、JSON repair、失败回退和最小评测样本。
7. 如涉及文件资产，必须处理格式、大小、时长、安全扫描、对象存储权限、签名 URL 和生命周期。
8. 如涉及计费，必须处理积分冻结、扣费、返还、成本日志和幂等。
9. 如涉及合规，必须处理授权、AIGC 标识、风险候选、人工复核、投诉删除。
10. 不允许只做 UI Demo；不允许用 mock 冒充真实能力；如使用 mock 必须显著标记并说明替换点。
11. 完成后必须运行测试，并输出测试命令和结果。

交付格式：
# AIcoding 任务交付说明
## 1. 读取文档
## 2. 任务边界
## 3. 新增/修改文件
## 4. 数据库/API/队列/Prompt/计费/风控影响
## 5. 实现说明
## 6. 测试命令与结果
## 7. 是否使用 mock
## 8. 已知问题与风险
## 9. 后续建议
```

### 11.2 D08-T02 元数据解析任务提示词

```text
任务编号：D08-T02
任务名称：视频资产校验与元数据解析
所属模块：M07 视频处理管线模块

目标：
实现从 asset_id 到 VideoMetadata 的稳定解析能力，支持文件格式、大小、时长、编码、音轨、分辨率、比例、fps、旋转信息校验，并输出可供后续抽帧、ASR、OCR、计费和前端进度使用的 metadata。

必须实现：
1. 读取 asset_id 对应对象存储文件，使用内部签名 URL 或服务端流，不暴露永久链接。
2. 校验格式、大小、时长、视频流、音频流、安全扫描状态和授权状态。
3. 调用 FFprobe 或等价工具解析 metadata。
4. 输出 VideoMetadata JSON 并写入 video_processing_results 或步骤临时结果。
5. 写入 TaskStepLog，包含输入摘要、输出摘要、耗时、错误码。
6. 元数据失败时重试一次，仍失败则标记关键失败并触发积分释放/返还事件。

不做：
不抽帧、不做 ASR/OCR、不生成 Content DNA、不生成报告。

测试：
标准 mp4、mov、webm；无音频；超时长；损坏文件；不支持编码；低分辨率；横屏/竖屏。
```

### 11.3 D08-T03 抽帧任务提示词

```text
任务编号：D08-T03
任务名称：抽帧与 FrameAsset 管理
所属模块：M07 视频处理管线模块

目标：
实现混合抽帧策略，把视频转换为多模态理解可用的关键帧证据，不用于素材复用。

必须实现：
1. 支持固定时间抽帧、前 3 秒密集抽帧、镜头边界代表帧、文字密集补抽预留。
2. 输出 FrameAsset，包含 timestamp_ms、storage_key、extraction_reason、质量指标、风险候选摘要。
3. 写入 frame_assets 表或等价数据结构。
4. 对象存储路径必须为内部路径，用户不可获得永久公开链接。
5. 至少 3 帧可用，否则关键失败或转人工。
6. 写入 TaskStepLog。

不做：
不生成报告，不输出可复制画面提示词，不做去水印。

测试：
15 秒、60 秒、90 秒、180 秒视频；黑屏帧；低清；横屏/竖屏；对象存储写入失败。
```

### 11.4 D08-T09 视频理解任务提示词

```text
任务编号：D08-T09
任务名称：VideoUnderstandingResult Prompt 与服务
所属模块：M08 多模态理解前置服务

目标：
基于 MultimodalEvidencePack 调用 P01_VIDEO_UNDERSTANDING，输出严格 JSON 的 VideoUnderstandingResult，供 D07 Content DNA 使用。

必须实现：
1. Prompt code = P01_VIDEO_UNDERSTANDING。
2. Prompt version = p01_video_understanding_v1.0.0。
3. 输入包含 metadata、frame_descriptions、transcript_segments、ocr_segments、shot_segments、processing_quality_report、risk_precheck。
4. 输出必须符合 video_understanding.v1.0 Schema。
5. 支持 JSON parse、Schema validate、JSON repair、最多 2 次重试。
6. 所有模型调用必须写 ModelCallLog。
7. 输出不得包含原创脚本、复刻建议、仿脸仿声、去水印、规避平台审核内容。
8. 高风险候选不得删除。

测试：
标准口播、本地生活、无 ASR、无 OCR、低清、高风险素材、模型输出非法 JSON。
```

### 11.5 D08-T15 测试验收任务提示词

```text
任务编号：D08-T15
任务名称：测试样本、Benchmark 与 E2E 验收
所属模块：M14 测试与监控基线

目标：
为 D08 建立可回归的视频处理与多模态理解测试体系，确保后续模型、Prompt、抽帧策略、ASR/OCR 供应商变更不会造成质量漂移。

必须实现：
1. 建立至少 50 条 MVP1 视频处理样本清单或测试夹具，覆盖企业 IP、本地生活、知识干货、无音频、无字幕、低清、横屏、竖屏、屏幕录制、高风险、损坏文件。
2. 建立自动化测试：Schema、API、队列、失败降级、计费幂等、权限、E2E。
3. 建立 Prompt 回归：FrameDescription、VideoUnderstandingResult、JSON repair。
4. 输出验收报告，包括成功率、失败率、P95 耗时、平均成本、降级比例、风险候选召回。
5. 所有测试数据不得包含未授权敏感素材；若使用真实用户素材，必须有授权和脱敏记录。

验收：
达到 D08 第 9 章上线门槛后，才允许进入 MVP1 联调。
```

---

## 12. 不做事项

D08 明确不做以下事项：

1. 不做对标账号自动抓取。
2. 不做自动下载账号全部作品。
3. 不做绕过平台限制的视频下载。
4. 不做视频搬运、克隆、洗稿能力。
5. 不做仿脸、仿声、声纹识别、人脸身份识别。
6. 不做去水印、去 Logo、规避平台审核。
7. 不把抽帧图片作为用户可复用素材输出。
8. 不把 ASR/OCR 原文作为可直接复用脚本输出。
9. 不生成原创脚本、标题、封面建议、视频 Prompt；这些属于 D09/D10 之后的输出链路。
10. 不做完整视频生成、分镜片段生成、拼接、字幕合成、MP4 导出。
11. 不做自动发布。
12. 不做账号级 Content DNA、批量模板库和数据诊断。
13. 不在生产环境使用未标记 mock。
14. 不绕过积分冻结、模型成本记录、任务日志和风控预检。
15. 不在后台无审计地查看、下载、删除用户原视频或抽帧。

---

## 13. 待确认问题

| 编号 | 问题 | 影响范围 | 当前建议 |
|---|---|---|---|
| Q01 | MVP1 视频最大时长最终是 90 秒还是 180 秒 | 抽帧、ASR/OCR 成本、积分、性能 | 工程支持 180 秒上限，Benchmark 重点覆盖 15-90 秒，90 秒以上提高积分并提示耗时 |
| Q02 | MVP1 文件大小上限是 200MB 还是 500MB | 上传、对象存储、处理成本 | 首发默认 200MB，后台配置可扩到 500MB |
| Q03 | ASR 首发供应商 | 成本、质量、语言支持 | 先封装 Adapter，不在业务层绑定供应商 |
| Q04 | OCR 首发供应商 | 字幕识别、价格/地址识别 | 先封装 Adapter，支持云 OCR 或多模态模型 OCR |
| Q05 | 帧描述是否调用独立视觉模型还是复用多模态模型 | 成本、质量、延迟 | MVP1 可先按成本选择；必须统一走 Model Gateway |
| Q06 | 是否保存原始 ASR/OCR 全文 | 隐私、合规、报告质量 | 受限保存，前端只展示摘要；用户删除时清理 |
| Q07 | 原视频和抽帧默认留存周期 | 存储成本、投诉处理 | 原视频 7-30 天可配置；抽帧随原视频清理 |
| Q08 | 低置信度任务是否扣全额积分 | 商业化、用户体验 | 建议按输出包和失败阶段部分结算，D19 定价最终确认 |
| Q09 | 高风险素材是否允许仅输出风险报告 | 合规与商业化 | 可作为后续“安全拆解”能力，MVP1 默认转人工或拦截 |
| Q10 | 是否需要人工上传视频授权证明 | 合规、转人工 | MVP1 先做用户勾选确认 + 审计，企业版再增强 |
| Q11 | 是否在报告页展示抽帧图片 | 合规、版权、用户体验 | MVP1 建议不默认展示大图，只展示时间线摘要；后台复核可查看 |
| Q12 | 多语言 ASR/OCR 是否首发支持 | 海外平台、成本 | P1；MVP1 优先中文，保留 language 字段 |

---

## 14. 最终 Checklist

### 14.1 文档完整性 Checklist

- [x] 已明确 D08 文档目标和边界。
- [x] 已对齐 D00 产品定位、MVP 边界、合规底线和 AIcoding 执行规则。
- [x] 已明确 D08 与 D01-D07、D09-D24 的关系。
- [x] 已明确 P0/P1/P2 范围。
- [x] 已明确输入、输出、流程、字段、状态、异常、风控、测试和验收。
- [x] 已定义数据结构、API、Prompt、任务队列、日志、权限和页面 ViewModel。
- [x] 已定义积分冻结、扣费、失败返还、成本和后台统计。
- [x] 已定义授权、AIGC 标识、风险分级、拦截、人工复核和投诉删除。
- [x] 已输出 AIcoding 可直接执行的任务拆分和提示词模板。
- [x] 已包含不做事项和待确认事项。

### 14.2 进入 D09 前 Checklist

- [ ] D08 已输出稳定 `MultimodalEvidencePack`。
- [ ] D08 已输出 `VideoUnderstandingResult`。
- [ ] D08 已输出 `ProcessingQualityReport`。
- [ ] D07 能消费 D08 输出并生成 Content DNA。
- [ ] ASR/OCR/镜头切分失败时，D07 能低置信度降级。
- [ ] 风险候选能传递到 D13/D21。
- [ ] 积分冻结、返还、成本统计链路可用。
- [ ] 后台可查看 D08 处理步骤日志和模型调用日志。

### 14.3 AIcoding 开发 Checklist

- [ ] D08-T01 Schema/配置/错误码已完成。
- [ ] D08-T02 元数据解析已完成。
- [ ] D08-T03 抽帧已完成。
- [ ] D08-T04 ASR Adapter 已完成。
- [ ] D08-T05 OCR Adapter 已完成。
- [ ] D08-T06 镜头切分已完成。
- [ ] D08-T07 帧描述已完成。
- [ ] D08-T08 EvidencePack 构建已完成。
- [ ] D08-T09 VideoUnderstanding 已完成。
- [ ] D08-T10 ProcessingQualityReport 已完成。
- [ ] D08-T11 任务编排和幂等已完成。
- [ ] D08-T12 积分/成本联动已完成。
- [ ] D08-T13 前端进度和低置信度展示已完成。
- [ ] D08-T14 后台诊断和复核入口已完成。
- [ ] D08-T15 测试样本和 E2E 验收已完成。

### 14.4 上线验收 Checklist

- [ ] 主流程：上传 → 授权 → 冻结积分 → 视频处理 → 证据包 → 视频理解 → Content DNA 可跑通。
- [ ] 处理失败可定位到具体步骤。
- [ ] 处理失败不会造成积分丢失。
- [ ] 重复任务消息不会重复扣费或重复写入核心结果。
- [ ] 所有模型调用有 ModelCallLog。
- [ ] 所有处理步骤有 TaskStepLog。
- [ ] 低置信度和降级提示前后台可见。
- [ ] 高风险候选会进入风控链路。
- [ ] 用户可申请删除源资产和处理产物。
- [ ] 生产环境无未标记 mock。
- [ ] D08 模块交接说明完整。

---

# 附录 A：D08 核心 Schema 汇总

## A.1 VideoProcessingResult 最小 JSON

```json
{
  "schema_version": "video_processing_result.v1.0",
  "task_id": "task_123",
  "asset_id": "ast_123",
  "status": "completed|partial_completed|failed",
  "metadata": {},
  "audio_asset": {},
  "frame_assets": [],
  "transcript_segments": [],
  "ocr_segments": [],
  "shot_segments": [],
  "frame_descriptions": [],
  "multimodal_evidence_pack": {},
  "video_understanding_result": {},
  "processing_quality_report": {},
  "degradation_flags": [],
  "ready_for_content_dna": true
}
```

## A.2 D08 到 D07 的最小输入包

```json
{
  "metadata": {},
  "frame_descriptions": [],
  "asr_segments": [],
  "ocr_segments": [],
  "shot_segments": [],
  "video_understanding_result": {},
  "processing_quality_report": {},
  "risk_candidates": [],
  "creator_profile_snapshot": {},
  "target_platform": "douyin|kuaishou|wechat_video|xiaohongshu|tiktok|other",
  "content_goal": "growth|lead_generation|conversion|brand|education|other",
  "authorization_ack": {},
  "risk_precheck": {}
}
```

# 附录 B：D08 版本更新记录模板

| 版本 | 日期 | 更新人 | 更新原因 | 主要变更 | 影响文档 | 影响模块 | 是否需要同步 Agent |
|---|---|---|---|---|---|---|---|
| V1.0 | 2026-05-22 | AI 产品总控 | 初版建立 | 建立视频处理与多模态理解工作流、Schema、API、Prompt、任务、测试与 AIcoding 任务 | D07/D09/D10/D11/D13/D15-D24 | M04/M05/M06/M07/M08/M10/M11/M13/M14 | 是 |
